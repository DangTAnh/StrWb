---
context: milestone
phase: 04-polish-deploy
task: lifecycle
total_tasks: 3
status: paused
last_updated: 2026-08-01T19:09:33.232Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Không có blocking constraint nào — không có failure gating resumption. Mọi state đã commit an toàn tới `398d437`.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Agent nền chết vì 429 rate limit (OPENCODE free tier) | UI researcher/auditor/integration-checker chết nhiều lần (0 tool uses); plan/execute/review agent sống qua retry | advisory | Dispatch agent nền với chỉ thị retry-on-429; việc nhỏ (UI-SPEC, UI-REVIEW, integration check) tự làm inline khi agent chết quá nhiều |
| Ghi file dài bị cắt (Write truncate / Edit disable) | Agent nền viết file dài bị cắt giữa chừng | advisory | Sau khi Write file dài, xác minh đầy đủ (đếm dòng / grep marker cuối); commit từng file cụ thể (không `git add .`) |
| Đọc transcript `.output` của agent nền | Gây tràn context vì transcript là JSONL full session | advisory | Chỉ nhận task-notification; không bao giờ Read file `.output` của agent |
| Context budget | Orchestrator ~75% tại pause; còn lifecycle (complete+cleanup) | advisory | Pause + `/clear` rồi `/gsd-resume-work`; handoff đã có sẵn |

<current_state>
**Milestone v1.0 StoreWeb đã HOÀN TẤT 100% — audit PASSED, đang giữa lifecycle (complete → cleanup → banner).**

- **Phase 1** — ĐÓNG: verified passed 5/5, code review 8/8 fixed.
- **Phase 2** — ĐÓNG: execute 9/9, verify human_needed (5/5 must-have + 3 UAT visual), UI 19/24 → 5 fixed.
- **Phase 3** — ĐÓNG: verified passed 5/5 (49 checks), code review 6/6 fixed, UI 20/24 → 5 fixed, 6 deferral → Phase 4.
- **Phase 4** — ĐÓNG: 3 plans, execute 13/13, verified passed 5/5, code review 7/7 fixed, UI 24/24 (0 findings). `94e97a9`.
- **Milestone audit** — PASSED: 28/28 reqs, 4/4 phases, 6/6 E2E flows. `.planning/v1.0-MILESTONE-AUDIT.md` committed `398d437`.
</current_state>

<completed_work>

Completed Tasks (this session):
- Phase 3 execute (13 commits) → verify passed → code review 6/6 fixed → UI 20/24 → 5 fixed → **Phase 3 ĐÓNG**
- Phase 4 discuss → 7 decisions (`d36f560`) → UI-SPEC inline (`a5aeb36`, verified 6/6) → 3 plans (`f51c5b7`+`80a4ce9`) → execute 13/13 (`f0d424d`) → verify passed 5/5 → code review 7 findings fixed (`a676902`..`af275bb`) → UI review 24/24 (`94e97a9`) → **Phase 4 ĐÓNG**
- STATE.md: completed_phases 4, completed_plans 12, percent 100
- REQUIREMENTS.md: all 28 v1 marked Complete (incl. 7 stale Phase 3 reconciliations)
- Milestone audit: `.planning/v1.0-MILESTONE-AUDIT.md` — status passed, committed `398d437`

</completed_work>

<remaining_work>

- **Lifecycle bước 1:** `/gsd:complete-milestone v1.0` — archive ROADMAP + tag
- **Lifecycle bước 2:** `/gsd-cleanup` — dọn scratch files (dry-run + xác nhận user)
- **Lifecycle bước 3:** Final completion banner
- Human UAT (non-blocking): 6 visual items Phase 2+3 + Phase 4 go-live (YOUR_DOMAIN placeholder, ₫ browser-render, phone responsive sweep)

</remaining_work>

<decisions_made>

- **Phase 4** (trong `04-CONTEXT.md`, 7 D): D-01 deploy cả hai (waitress Windows + gunicorn/systemd VPS); D-02 nginx + Let's Encrypt; D-03 có domain sẵn (YOUR_DOMAIN token); D-04 admin chỉ app login + nginx rate-limit /login; D-05 CAT-04 giữ hiện tại (opacity 0.45 + badge); D-06 audit responsive 480/768/1200 public+admin; D-07 fix 5 deferral UI.
- **Orchestrator tự viết UI-SPEC/UI-REVIEW/integration-check inline** — agent nền (UI researcher/auditor/integration-checker) chết vì 429 rate limit OPENCODE nhiều lần; orchestrator có đủ context nên tự làm.
- **Phase 3** (13 D): xem `03-CONTEXT.md` — grid card, responsive 2/3/4 cột, phân trang 12/trang, ảnh mờ+badge, gallery ảnh chính+thumb, Messenger CTA mọi trạng thái, search không dấu NFD+casefold, kết quả dùng pagination.total.

</decisions_made>

<blockers>
- Không có blocker kỹ thuật. Context budget ~75% — nên `/clear` + `/gsd-resume-work` trước lifecycle nếu cần.
- Agent nền bị 429 rate limit (OPENCODE free tier) — retry-on-429 trong prompt; việc nhỏ tự làm inline.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — trạng thái milestone (4/4 phases, 100%)
2. `.planning/v1.0-MILESTONE-AUDIT.md` — audit PASSED (28/28 reqs, 6/6 flows)
3. `.planning/ROADMAP.md` — 4 phase, success criteria
4. `.planning/phases/04-polish-deploy/04-VERIFICATION.md` — Phase 4 verify passed 5/5
5. `CLAUDE.md` — tech stack + GSD workflow enforcement

## Critical Anti-Patterns (do NOT repeat these)
- [AGENT 429]: Agent nền chết vì upstream 429 (OPENCODE free tier) → prompt agent nền kèm chỉ thị retry-on-429; UI-SPEC/UI-REVIEW/integration-check có thể làm inline.
- [WRITE TRUNCATE]: Write tool cắt nội dung dài / Edit disable trong agent nền → sau khi viết file dài xác minh đầy đủ; commit từng file cụ thể.
- [AGENT OUTPUT TRANSCRIPT]: Không bao giờ Read file `.output` của agent nền — transcript JSONL gây tràn context.
- [DB POLLUTION]: Harness test đụng `data/app.db` thật (Flask-SQLAlchemy eager engine) → dispose+rebuild `db._app_engines[app][None]` để cô lập temp DB; restore DB sau audit.

## Infrastructure State
- Windows 11 (win32), shell PowerShell/Bash. Không dùng gunicorn native — waitress cho Windows.
- Flask app hoàn chỉnh tại `app/`: 3 blueprint (public/admin/auth), Flask-Login, CSRF, SQLite WAL+busy_timeout, image_utils, format_price.
- Deploy configs: `docs/deploy/` (Windows.md, Linux.md, nginx.conf, storeweb.service, README.md), `.env.example` — tất cả đã qua code review 7/7 fix.
- Commit mới nhất: `398d437` (milestone audit passed). Working tree sạch, chỉ còn untracked `.planning/ui-reviews/` (scratch, gitignored).
- `requirements.txt` đã thêm `waitress==3.0.2`.

## Pre-Execution Critique Required
N/A — không pause giữa design và execution; đang ở giữa lifecycle (audit xong, complete+cleanup còn lại).

<context>
Chạy `/gsd-autonomous` interactive mode trên milestone v1.0 StoreWeb. Cả 4 phase đã ĐÓNG hoàn toàn (100%). Milestone audit PASSED. Chỉ còn: complete-milestone v1.0 → cleanup → banner. Human UAT items non-blocking. Agent nền bị 429 rate limit — retry hoặc làm inline.
</context>

<next_action>
Start with: chạy lifecycle tiếp — `Skill(gsd-complete-milestone, args="v1.0")` → sau đó `Skill(gsd-cleanup)` → banner hoàn thành. Audit đã passed (`398d437`), nên theo autonomous workflow: audit passed → auto-continue complete-milestone (không pause user). Đọc `.planning/STATE.md` + `v1.0-MILESTONE-AUDIT.md` trước.
</next_action>
