---
phase: 02-admin-crud-images
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/forms.py, app/__init__.py, app/admin.py, app/templates/admin/products/list.html, app/templates/admin/products/form.html, app/templates/admin/products/delete.html, app/templates/admin/dashboard.html, app/static/css/style.css]
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05, PROD-06, PROD-07]

must_haves:
  truths:
    - "Admin can open /admin/products and see a table of products (or the empty state 'Chưa có sản phẩm nào') with columns Ảnh / Tên / SKU / Giá / Tồn kho / Trạng thái / Hành động, sorted by sort_order then id (D-01, D-02)"
    - "The list paginates 20 products per page with Trước/Trang X/Y/Sau controls only when more than one page exists (D-03)"
    - "Admin can create a product (all PROD-01 fields), edit any field, and the price renders as integer VND like 1.200.000₫ (PROD-06)"
    - "Status derives from quantity/discontinued (Còn hàng / Hết hàng / Ngừng bán) and is shown as a badge (PROD-04, PROD-05)"
    - "Deleting a product requires a confirmation page showing the product name and an irreversible warning, performed via POST + CSRF — never a GET link (D-04, D-08, PROD-03, PROD-07)"
  artifacts:
    - path: "app/forms.py"
      provides: "ProductForm (Flask-WTF) with all product fields + CSRF"
      contains: "class ProductForm"
    - path: "app/admin.py"
      provides: "products/new/edit/delete routes behind @login_required"
      contains: "def products"
    - path: "app/templates/admin/products/list.html"
      provides: "Product table + pagination + empty state"
      contains: "Chưa có sản phẩm nào"
    - path: "app/templates/admin/products/form.html"
      provides: "Create/edit product form (fields only; gallery lands in 02-03)"
      contains: "Lưu sản phẩm"
    - path: "app/templates/admin/products/delete.html"
      provides: "Delete confirmation page (name + irreversible warning + POST form)"
      contains: "Hành động này không thể hoàn tác."
    - path: "app/__init__.py"
      provides: "format_price Jinja filter (integer VND → 1.200.000₫)"
      contains: "format_price"
    - path: "app/static/css/style.css"
      provides: "Admin table, buttons (primary/secondary/destructive), status badges, pagination, form fields, flash-warning styles"
      contains: "table"
  key_links:
    - from: "app/templates/admin/products/list.html"
      to: "app/admin.py"
      via: "url_for('admin.products', page=...) for pagination; url_for('admin.new_product') / admin.edit_product / admin.delete_product for row actions"
      pattern: "admin.products"
    - from: "app/admin.py"
      to: "app/models.py"
      via: "Product.query.order_by(Product.sort_order.asc(), Product.id.asc()).paginate(...)"
      pattern: "Product.sort_order"
    - from: "app/templates/admin/products/form.html"
      to: "app/forms.py"
      via: "{{ form.hidden_tag() }} and form field rendering"
      pattern: "form.hidden_tag"
    - from: "app/templates/admin/dashboard.html"
      to: "app/admin.py"
      via: "'Sản phẩm' nav link now targets url_for('admin.products')"
      pattern: "admin.products"
---

<objective>
Admin product CRUD core — list (table + thumbnail column, sort_order sort, 20/page pagination), create/edit forms with all product fields, integer VND price, derived status badge, CSRF-protected POST delete with a confirmation page.

Purpose: Deliver the full PROD-01..07 requirement set as a usable vertical slice before any image handling. All Phase 2 image work (02-02/02-03) builds on these routes and templates.
Output: ProductForm, products list/create/edit/delete routes, three admin templates, dashboard nav rewire, format_price filter, admin CSS base.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-admin-crud-images/02-CONTEXT.md (D-01..D-04, D-08)
@.planning/phases/02-admin-crud-images/02-UI-SPEC.md (Design System, Copywriting Contract, Product List Layout, Product Table, Pagination Bar, Product Create/Edit Form, Buttons, Color, Responsive Behavior)
@.planning/phases/01-scaffold-auth-data-model/01-CONTEXT.md (Product model D-05..D-09)
@.planning/research/PITFALLS.md (UX pitfalls: empty state, price format, POST delete; "Looks Done But Isn't")

<interfaces>
Consumed from Phase 1 (already on disk):
  - app/models.py: class Product with `price = db.Column(db.Integer)`, `status` property returning 'discontinued'|'available'|'out_of_stock', relationship `images` (lazy='dynamic')
  - app/admin.py: `admin_bp = Blueprint('admin', __name__, url_prefix='/admin')`, `@admin_bp.before_request @login_required` guard already protects every route
  - app/forms.py: LoginForm pattern (FlaskForm + Vietnamese labels + DataRequired messages)
  - app/templates/base.html: `{% block content %}` + flash zone rendering `get_flashed_messages(with_categories=true)` with `.flash {{ category }}`
  - app/__init__.py: `create_app()` factory, config `MAX_CONTENT_LENGTH=16*1024*1024` already set
  - app/static/css/style.css: CSS vars `--bg #F9FAFB`, `--card #FFFFFF`, `--accent #2563EB`, `--destructive #DC2626`, `--success #059669`, `--text #1F2937`, `--border #D1D5DB`; `.admin-card` max-width 720px

Template conventions: all copy Vietnamese (PLAT-01). Flash categories: 'success', 'error', 'warning' (warning color #D97706 added in this plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductForm + format_price filter</name>
  <files>app/forms.py, app/__init__.py</files>
  <read_first>
    - .planning/phases/02-admin-crud-images/02-CONTEXT.md (D-05..D-09 for the field set; D-06 sort_order/sku/admin_note)
    - .planning/phases/02-admin-crud-images/02-UI-SPEC.md (Product Create/Edit Form copy labels; Price formatting locked: `1.200.000₫`)
  </read_first>
  <action>
    Extend `app/forms.py` (keep LoginForm unchanged) with a `ProductForm` for create/edit:
    - Imports: `from flask_wtf import FlaskForm`, `from wtforms import StringField, IntegerField, TextAreaField, BooleanField, SubmitField`, `from wtforms.validators import DataRequired, Optional, Length, NumberRange`
    - `class ProductForm(FlaskForm):` with fields and labels EXACTLY:
      - `name = StringField('Tên sản phẩm', validators=[DataRequired(message='Vui lòng nhập tên sản phẩm'), Length(max=200)])` (PROD-01)
      - `price = IntegerField('Giá (VND)', validators=[DataRequired(message='Vui lòng nhập giá'), NumberRange(min=0, message='Giá không được âm')])` (PROD-06 — integer only, no decimals)
      - `brand = StringField('Thương hiệu', validators=[Optional(), Length(max=100)])`
      - `measurements = TextAreaField('Số đo', validators=[Optional()])` (free text)
      - `description = TextAreaField('Mô tả', validators=[Optional()])`
      - `quantity = IntegerField('Tồn kho', validators=[DataRequired(message='Vui lòng nhập tồn kho'), NumberRange(min=0, message='Tồn kho không được âm')], default=0)` (PROD-05)
      - `discontinued = BooleanField('Ngừng bán sản phẩm này')` (D-08 override)
      - `sku = StringField('Mã sản phẩm (SKU)', validators=[Optional(), Length(max=100)])`
      - `sort_order = IntegerField('Thứ tự hiển thị', validators=[Optional()], default=0)`
      - `admin_note = TextAreaField('Ghi chú nội bộ', validators=[Optional()])`
      - `submit = SubmitField('Lưu sản phẩm')`

    Register the `format_price` Jinja filter in `app/__init__.py` inside `create_app()`:
    - Add exactly:
      ```
      @app.template_filter('format_price')
      def format_price(value):
          return f'{int(value):,}'.replace(',', '.') + '₫'
      ```
    - This renders `1200000` → `1.200.000₫` (PROD-06, UI-SPEC price format). Do NOT use `:,.2f` anywhere.
  </action>
  <verify>
    <automated>python - <<'PY'
import os
os.environ['SECRET_KEY'] = 'x'*32
from wsgi import app
from app.forms import ProductForm
f = ProductForm()
assert 'Tên sản phẩm' in str(f.name.label)
assert 'Giá (VND)' in str(f.price.label)
assert 'Thứ tự hiển thị' in str(f.sort_order.label)
with app.app_context():
    r = app.jinja_env.filters['format_price'](1200000)
    assert r == '1.200.000₫', r
print('FORM_FILTER_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `app/forms.py` contains `class ProductForm(FlaskForm)` with all 11 fields (name, price, brand, measurements, description, quantity, discontinued, sku, sort_order, admin_note, submit)
    - `app/__init__.py` contains `@app.template_filter('format_price')` returning `...replace(',', '.') + '₫'`
    - Verify script exits 0 printing FORM_FILTER_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Product list route + table template + pagination + dashboard nav</name>
  <files>app/admin.py, app/templates/admin/products/list.html, app/templates/admin/dashboard.html</files>
  <read_first>
    - .planning/phases/02-admin-crud-images/02-UI-SPEC.md (Product List Layout, Product Table, Pagination Bar, Copywriting Contract — list page inventory, Empty state)
    - .planning/phases/02-admin-crud-images/02-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Add the product list route and template. All routes inherit `@login_required` from the existing `@admin_bp.before_request` guard — do NOT add per-route decorators.

    `app/admin.py`:
    - Add imports: `from flask import Blueprint, render_template, request, redirect, url_for, flash`, `from .models import Product`.
    - Add route `@admin_bp.route('/products', methods=['GET'])` → `def products():`
      - `page = request.args.get('page', 1, type=int)`
      - `pagination = Product.query.order_by(Product.sort_order.asc(), Product.id.asc()).paginate(page=page, per_page=20, error_out=False)` (D-02 sort, D-03 paginate)
      - `return render_template('admin/products/list.html', pagination=pagination, products=pagination.items)`
    - Update `dashboard()` route to pass product count: `products_count = Product.query.count()` and pass to the template.

    `app/templates/admin/products/list.html` (new, extends base):
    - Page heading `Sản phẩm` + primary button `Thêm sản phẩm` linking to `url_for('admin.new_product')`.
    - `{% if products %}` table else `{% else %}` empty state (PITFALLS "Looks Done But Isn't" — the else clause is required).
      - Empty state: heading `Chưa có sản phẩm nào`, body `Bấm “Thêm sản phẩm” để tạo sản phẩm đầu tiên.`, primary button `Thêm sản phẩm`.
    - Table (semantic `<table>`, `<thead><th scope="col">`, visually-hidden `<caption>Danh sách sản phẩm</caption>`):
      - Columns (D-01): `Ảnh` · `Tên` · `SKU` · `Giá` · `Tồn kho` · `Trạng thái` · `Hành động`
      - Ảnh cell: no image upload yet (02-03) — render the placeholder `<span class="thumb-placeholder">—</span>` (aria-label "Chưa có ảnh") for every row in this plan.
      - Tên: `{{ p.name }}` (600 weight), SKU: `{{ p.sku or '—' }}`, Giá: `{{ p.price | format_price }}` with `font-variant-numeric: tabular-nums`, Tồn kho: `{{ p.quantity }}` (render `0` muted).
      - Trạng thái: badge per `p.status`:
        - `available` → `Còn hàng` (green palette #059669/#ECFDF5/#A7F3D0)
        - `out_of_stock` → `Hết hàng` (warning #D97706/#FFFBEB/#FDE68A)
        - `discontinued` → `Ngừng bán` (neutral #6B7280/#F3F4F6/#E5E7EB)
        - Implement as a Jinja `if/elif` producing `<span class="badge badge-{status}">`.
      - Hành động: `Sửa` (accent link → `admin.edit_product`, product_id) · `Xóa` (#DC2626 link → `admin.delete_product`, product_id). Both keep 44px touch targets via cell padding.
    - Pagination bar (only when `pagination.pages > 1`): `Trước` link (disabled/`aria-disabled` at page 1), indicator `Trang {{ pagination.page }} / {{ pagination.pages }}`, `Sau` link (disabled at last). Links use `url_for('admin.products', page=pagination.prev_num)` / `page=pagination.next_num`.

    `app/templates/admin/dashboard.html`:
    - Change the `Sản phẩm` nav link from `url_for('admin.dashboard')` to `url_for('admin.products')`.
    - Replace the static badge `Chưa có sản phẩm` with: `{% if products_count > 0 %}{{ products_count }}{% else %}Chưa có sản phẩm{% endif %}`.
  </action>
  <verify>
    <automated>python - <<'PY'
import os
os.environ['SECRET_KEY'] = 'x'*32
os.environ['ADMIN_USERNAME'] = 'admin'
os.environ['ADMIN_PASSWORD'] = 'TestAdmin2026'
from wsgi import app
from app.db import db, init_db_command
from app.models import Product
with app.app_context():
    db.drop_all()
app.test_cli_runner().invoke(init_db_command)
with app.app_context():
    db.session.add_all([
        Product(name='Ao', price=100000, quantity=0, sort_order=2),
        Product(name='Quan', price=200000, quantity=5, sort_order=1),
    ])
    db.session.commit()
# login
c = app.test_client()
html = c.get('/login').get_data(as_text=True)
import re
tok = re.search(r'name="csrf_token"[^>]*value="([^"]+)"', html).group(1)
c.post('/login', data={'csrf_token': tok, 'username': 'admin', 'password': 'TestAdmin2026'})
r = c.get('/admin/products')
assert r.status_code == 200, r.status_code
body = r.get_data(as_text=True)
# sorted by sort_order: Quan (1) before Ao (2)
assert body.index('Quan') < body.index('Ao'), 'sort_order sort failed'
assert '1.200.000₫' in body  # wait: 200000 -> 200.000₫, 100000 -> 100.000₫
assert '200.000₫' in body and '100.000₫' in body
assert 'Còn hàng' in body and 'Hết hàng' in body
assert 'Thêm sản phẩm' in body and 'Sửa' in body and 'Xóa' in body
print('LIST_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin.py` contains `def products` with `.order_by(Product.sort_order.asc(), Product.id.asc()).paginate(page=page, per_page=20, error_out=False)`
    - `app/templates/admin/products/list.html` contains `Chưa có sản phẩm nào`, the 7 column headers, `format_price`, `url_for('admin.new_product')`, and a `Trang {{ pagination.page }}` indicator
    - `app/templates/admin/dashboard.html` links `Sản phẩm` to `url_for('admin.products')`
    - Verify script exits 0 printing LIST_OK (200, sort_order order, formatted VND, status badges, action links)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create/edit/delete routes + form/delete templates + admin CSS</name>
  <files>app/admin.py, app/templates/admin/products/form.html, app/templates/admin/products/delete.html, app/static/css/style.css</files>
  <read_first>
    - .planning/phases/02-admin-crud-images/02-UI-SPEC.md (Create/Edit Form layout + copy, Delete Confirmation Step, Buttons, Flash Message Copy, Color Usage Rules, Responsive Behavior)
    - .planning/phases/02-admin-crud-images/02-CONTEXT.md (D-04 confirmation shows name, D-08 irreversible, PROD-03 POST delete)
    - .planning/research/PITFALLS.md (UX: Delete via GET link → must be POST form + CSRF; no feedback after submit → flash)
  </read_first>
  <action>
    Add create/edit/delete routes and their templates, plus the admin CSS component base.

    `app/admin.py` — add three routes (imports already from Task 2; add `from .forms import ProductForm`, `from .db import db`):
    - `@admin_bp.route('/products/new', methods=['GET', 'POST'])` → `def new_product():`
      - `form = ProductForm()`
      - on `form.validate_on_submit()`: create `Product(name=form.name.data.strip(), price=form.price.data, brand=form.brand.data or None, measurements=form.measurements.data or None, description=form.description.data or None, quantity=form.quantity.data or 0, discontinued=form.discontinued.data, sku=form.sku.data or None, sort_order=form.sort_order.data or 0, admin_note=form.admin_note.data or None)`; `db.session.add(product); db.session.commit()`; `flash('Lưu sản phẩm thành công', 'success')`; `return redirect(url_for('admin.products'))`
      - else render `admin/products/form.html` with `form=form, product=None, is_new=True`
    - `@admin_bp.route('/products/<int:product_id>/edit', methods=['GET', 'POST'])` → `def edit_product(product_id):`
      - `product = db.session.get(Product, product_id)`; if `None`: `flash('Không tìm thấy sản phẩm.', 'error')`; `return redirect(url_for('admin.products'))`
      - `form = ProductForm(obj=product)`
      - on `form.validate_on_submit()`: `form.populate_obj(product)`; `db.session.commit()`; `flash(f'Đã cập nhật sản phẩm "{product.name}"', 'success')`; `return redirect(url_for('admin.products'))`
      - else render `form.html` with `form=form, product=product, is_new=False`
    - `@admin_bp.route('/products/<int:product_id>/delete', methods=['GET', 'POST'])` → `def delete_product(product_id):` (PROD-03 — POST only performs the delete)
      - `product = db.session.get(Product, product_id)`; if `None`: flash + redirect as above
      - GET: render `admin/products/delete.html` with `product=product`
      - POST: `name = product.name`; `image_count = product.images.count()`; `db.session.delete(product); db.session.commit()`; `flash(f'Đã xóa sản phẩm "{name}"', 'success')`; `return redirect(url_for('admin.products'))`
      - NOTE: file cleanup for deleted product images lands in 02-03 Task 3 (D-05/D-06/D-07/D-09); at this point no images can exist yet, so the count-based flash suffix is added there too.

    `app/templates/admin/products/form.html` (new, extends base):
    - Back link `Quay lại` (accent) → `url_for('admin.products')`.
    - Heading: `{% if is_new %}Thêm sản phẩm{% else %}Sửa sản phẩm{% endif %}`.
    - `<form method="post" action="" enctype="multipart/form-data">` (enctype needed now so the 02-03 gallery file input works unchanged):
      - `{{ form.hidden_tag() }}`
      - Fields with labels + required red `*` for name/price/quantity; field errors as `<span class="field-error">` under each input; inputs carry Body 16px/400, height 44px.
      - Layout rows per UI-SPEC: single-column Tên; 2-col row Giá | Thương hiệu; 2-col row SKU | Thứ tự hiển thị; single Số đo (placeholder `Ví dụ: 60×40×2cm hoặc M/L/XL`); single Mô tả (textarea); 2-col row Tồn kho | checkbox Ngừng bán; single Ghi chú nội bộ (textarea, help `Chỉ quản trị viên xem được`).
      - Derived-status hint under Tồn kho: `Trạng thái tự động: Còn hàng khi tồn kho > 0, Hết hàng khi tồn kho = 0. Bật “Ngừng bán” để ghi đè.`
      - (Gallery section `Ảnh sản phẩm` is added in 02-03 — do not add it here.)
      - Action row: primary submit `Lưu sản phẩm` + secondary link `Hủy` styled as button → `url_for('admin.products')`.

    `app/templates/admin/products/delete.html` (new, extends base):
    - Card max-width 480px centered: heading `Xóa sản phẩm`; body `Bạn có chắc muốn xóa sản phẩm “{{ product.name }}”?` (D-04); image note only if `product.images.count() > 0`: `Sản phẩm có {{ product.images.count() }} ảnh và sẽ bị xóa cùng.`; warning line `Hành động này không thể hoàn tác.` (#DC2626, D-08).
    - Action row: secondary link `Hủy` → `url_for('admin.products')`; destructive button `Xóa sản phẩm` inside `<form method="post" action="{{ url_for('admin.delete_product', product_id=product.id) }}">` with `<input type="hidden" name="csrf_token" value="{{ csrf_token() }}">` (PROD-03 POST + CSRF).

    `app/static/css/style.css` — append Phase 2 component styles (keep existing vars; target total <15KB):
    - Buttons: `.btn` base (height 44px, padding 0 24px, radius 8px, Label 14px/600), `.btn-primary` (bg #2563EB, white, hover #1D4ED8), `.btn-secondary` (bg #FFFFFF, border 1px #D1D5DB, text #1F2937, hover bg #F9FAFB), `.btn-danger` (bg #DC2626, white, hover #B91C1C); focus ring 2px #2563EB offset 2px.
    - Admin layout: `.admin-page` container max-width 1200px centered padding 16px; `.admin-card` keep 720px for form (add `.admin-card--wide` max-width 1200px for the list card), header row flex space-between.
    - Table: `.data-table` full width, thead bg #F9FAFB, `th` Label 14px/600 #6B7280 padding 8px 16px; `td` padding 8px 16px, bottom border 1px #E5E7EB, hover row bg #F9FAFB; `.thumb-placeholder` 48×48 box bg #F3F4F6 radius 4px centered muted `—` (#9CA3AF).
    - Badges: `.badge` pill (border-radius 999px, Label 14px/600, padding 2px 8px, 1px border) + `.badge-available` (#059669 / #ECFDF5 / #A7F3D0), `.badge-out_of_stock` (#D97706 / #FFFBEB / #FDE68A), `.badge-discontinued` (#6B7280 / #F3F4F6 / #E5E7EB).
    - Pagination: `.pagination` flex gap 8px, indicator color #6B7280; disabled links muted #9CA3AF.
    - Flash warning: `.flash-warning { color: #D97706; }` (D-09 warning color).
    - Form: `.form-field` margin-bottom 16px; `.form-row-2` 2-col flex gap 16px collapsing below 768px; `input/textarea` height 44px / textarea min-height 120px, padding 0 12px / 12px, border 1px #D1D5DB radius 8px, Body 16px; focus border #2563EB + ring; `.field-error` color #DC2626 font-size 14px margin-top 4px; `.required` color #DC2626; `.help-text` color #6B7280 font-size 14px.
    - Media queries at 768px (2-col → 1-col; header stacks) and 480px (table wrapped in `.table-scroll { overflow-x: auto; }`).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, re
os.environ['SECRET_KEY'] = 'x'*32
os.environ['ADMIN_USERNAME'] = 'admin'
os.environ['ADMIN_PASSWORD'] = 'TestAdmin2026'
from wsgi import app
from app.db import db, init_db_command
from app.models import Product
with app.app_context():
    db.drop_all()
app.test_cli_runner().invoke(init_db_command)
c = app.test_client()
def tok():
    h = c.get('/login').get_data(as_text=True)
    return re.search(r'name="csrf_token"[^>]*value="([^"]+)"', h).group(1)
c.post('/login', data={'csrf_token': tok(), 'username': 'admin', 'password': 'TestAdmin2026'})
# new form GET
r = c.get('/admin/products/new'); assert r.status_code == 200
# create POST
r = c.post('/admin/products/new', data={'csrf_token': tok(), 'name': 'Áo thun', 'price': 150000, 'quantity': 3, 'sort_order': 1})
assert r.status_code == 302, r.status_code
with app.app_context():
    p = Product.query.filter_by(name='Áo thun').first()
    assert p and p.price == 150000 and p.quantity == 3
    pid = p.id
# edit GET + POST
r = c.get(f'/admin/products/{pid}/edit'); assert r.status_code == 200
r = c.post(f'/admin/products/{pid}/edit', data={'csrf_token': tok(), 'name': 'Áo thun 2', 'price': 160000, 'quantity': 3})
assert r.status_code == 302
# delete confirm GET shows name + warning
r = c.get(f'/admin/products/{pid}/delete')
body = r.get_data(as_text=True)
assert 'Áo thun 2' in body and 'Hành động này không thể hoàn tác.' in body
# delete POST (CSRF) -> 302 redirect to list
r = c.post(f'/admin/products/{pid}/delete', data={'csrf_token': tok()})
assert r.status_code == 302, r.status_code
with app.app_context():
    assert Product.query.get(pid) is None
# GET delete link must NOT delete (405/redirect, no data change) -- verify via GET returning 200 confirm page
print('CRUD_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin.py` contains `def new_product`, `def edit_product`, `def delete_product`; delete is `methods=['GET','POST']` with the DB delete only inside the POST branch
    - `app/templates/admin/products/form.html` contains `form.hidden_tag()`, `Lưu sản phẩm`, `Trạng thái tự động`, `Quay lại`, `enctype="multipart/form-data"`
    - `app/templates/admin/products/delete.html` contains `Bạn có chắc muốn xóa sản phẩm`, `Hành động này không thể hoàn tác.`, `csrf_token`, `url_for('admin.delete_product'`
    - `app/static/css/style.css` contains `.btn-primary`, `.btn-danger`, `.data-table`, `.badge-available`, `.badge-out_of_stock`, `.badge-discontinued`, `.pagination`, `.flash-warning`, `.field-error`
    - Verify script exits 0 printing CRUD_OK (create 302 + row persists, edit 302, delete confirm shows name + warning, delete POST 302 + row gone)
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> admin CRUD routes | Untrusted form data + session cookie cross here; all routes behind `@login_required` |
| template -> rendered HTML | Product fields (name, description, admin_note) are user-controlled; Jinja autoescape is the only defense |
| admin POST -> DB | Create/edit/delete mutate products table; CSRF token required on every POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | CSRF on create/edit/delete POST | mitigate | Flask-WTF `form.hidden_tag()` in form.html; explicit `csrf_token()` hidden input in delete.html; CSRFProtect global (Phase 1) validates every POST |
| T-02-01-02 | Tampering | XSS via product fields (name/description/admin_note) | mitigate | Jinja2 autoescape (default ON); never `|safe` on user values; render with `{{ p.name }}` |
| T-02-01-03 | Tampering | GET-link delete (accidental/driven) | mitigate | delete route performs deletion only on POST (PROD-03); GET renders confirmation page (D-04); destructive button inside a POST form |
| T-02-01-04 | Information Disclosure | Unauthenticated access to admin CRUD | mitigate | `@admin_bp.before_request @login_required` (Phase 1) covers all new routes; login redirect to /login?next=... |
| T-02-01-05 | Spoofing | Integer overflow / negative price or quantity | mitigate | WTForms `NumberRange(min=0)` on price/quantity; price stored as `db.Integer` (PROD-06) |
| T-02-01-SC | Tampering | Compromised pip packages | mitigate | No new packages in this plan (Pillow already pinned in requirements.txt Phase 1); any NEW package requires a blocking human checkpoint before install |

## STRIDE Threat Register — Deferred to 02-02/02-03

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-06 | Tampering | Image path traversal / malicious upload | mitigate (02-03) | `app/image_utils.py` extension allowlist + magic bytes + Pillow verify + 2000×2000 + UUID filenames + re-encode (D-16); batch rejection (D-17) — implemented in 02-02/02-03 |
| T-02-01-07 | DoS | Decompression bomb / oversized upload | mitigate (02-02/03) | `MAX_CONTENT_LENGTH=16MB` already in config; dimension check 2000×2000 in image_utils; 413 handler in 02-03 |
</threat_model>

<verification>
- GET /admin/products (logged in) → 200 table or empty state; sorted sort_order asc then id asc; paginated 20/page
- Price renders as `1.200.000₫` via the format_price filter — never `:,.2f`
- Status badge derives from quantity/discontinued: available / out_of_stock / discontinued
- Create → 302 to list with flash `Lưu sản phẩm thành công`; Edit → 302 with `Đã cập nhật sản phẩm "{tên}"`; Delete POST → 302 with `Đã xóa sản phẩm "{tên}"`
- Delete confirmation page shows the product name and `Hành động này không thể hoàn tác.`; the delete POST requires a CSRF token
- `flask --app wsgi routes` lists admin.products, admin.new_product, admin.edit_product, admin.delete_product
</verification>

<success_criteria>
- PROD-01: create form accepts name, price, brand, measurements, description (all required fields validated)
- PROD-02: edit form pre-fills and updates every field
- PROD-03: delete only via POST + CSRF after a confirmation page
- PROD-04: status Còn hàng / Hết hàng / Ngừng bán shown as badges, derived from quantity + discontinued
- PROD-05: quantity field ≥ 0; quantity 0 → Hết hàng
- PROD-06: price integer VND, no precision loss, formatted `1.200.000₫`
- PROD-07: every admin form (create/edit/delete) carries CSRF protection and field validation
- D-01/D-02/D-03: table list with thumbnail column, sort_order sort, 20/page pagination
- D-04/D-08: confirmation page shows product name + irreversible warning; no undo/trash
</success_criteria>

<output>
Create `.planning/phases/02-admin-crud-images/02-01-SUMMARY.md` when done
</output>
