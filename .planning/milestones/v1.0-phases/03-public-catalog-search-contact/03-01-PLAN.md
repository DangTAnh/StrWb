---
phase: 03-public-catalog-search-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/templates/base.html, app/templates/public/base.html, app/templates/public/_nav.html, app/templates/public/_product_card.html, app/templates/public/index.html, app/templates/public/product_detail.html, app/templates/public/search.html, app/public.py, app/static/css/style.css]
autonomous: true
requirements: [CAT-01, CAT-03, CONT-01, CONT-02]

must_haves:
  truths:
    - "Khách mở trang chủ không cần đăng nhập và thấy grid thẻ sản phẩm (ảnh, tên, giá, trạng thái) — CAT-01, D-01"
    - "Grid responsive 2/3/4 cột theo breakpoint 768/1200 — D-02"
    - "Danh sách phân trang 12 sản phẩm/trang, sắp theo sort_order rồi id (như admin D-02) — D-03"
    - "Sản phẩm hết hàng / ngừng bán hiển thị ảnh mờ + nhãn overlay (Hết hàng / Ngừng bán), không lấn át hàng còn — D-04"
    - "Header trang chủ có ô tìm kiếm (GET form, D-10 submit) — D-09"
    - "Trang chủ có dải liên hệ Messenger dễ thấy, luôn hiển thị kể cả khi chưa có sản phẩm — D-13, CONT-01, CONT-02"
  artifacts:
    - path: "app/templates/base.html"
      provides: "Chèn {% block header %} giữa skip-link và flash zone; admin không override → không có header"
      contains: "block header"
    - path: "app/templates/public/base.html"
      provides: "Lớp public kế thừa base.html và điền header bằng public/_nav.html"
      contains: "public/_nav.html"
    - path: "app/templates/public/_nav.html"
      provides: "Header công khai: brand link + GET search form (role=search, input name=q, nút Tìm)"
      contains: "role=\"search\""
    - path: "app/templates/public/_product_card.html"
      provides: "Component thẻ sản phẩm dùng chung home + search (D-12); badge overlay + ảnh mờ khi hết hàng/ngừng bán"
      contains: "product-card"
    - path: "app/templates/public/index.html"
      provides: "Trang chủ: h1 Sản phẩm, grid sản phẩm (hoặc empty-state), phân trang, contact strip Messenger"
      contains: "contact-strip"
    - path: "app/templates/public/product_detail.html"
      provides: "Stub tối thiểu trang chi tiết (tên + giá + Quay lại) để card link render; 03-02 thay bằng bố cục đầy đủ"
      contains: "Quay lại"
    - path: "app/templates/public/search.html"
      provides: "Stub trang tìm kiếm (chỉ empty-query prompt); 03-03 thay bằng trang kết quả đầy đủ"
      contains: "Vui lòng nhập từ khóa để tìm kiếm."
    - path: "app/public.py"
      provides: "public_bp: home paginated (12/page) + route product_detail stub + route search stub"
      contains: "def home"
    - path: "app/static/css/style.css"
      provides: "site-header, search-form, product-grid, product-card, badge-overlay, contact-strip, pagination--center"
      contains: "product-grid"
  key_links:
    - from: "app/templates/public/_nav.html"
      to: "app/public.py"
      via: "url_for('public.search') — route phải tồn tại (stub) để form render; url_for('public.home') cho brand"
      pattern: "public.search"
    - from: "app/templates/public/_product_card.html"
      to: "app/public.py"
      via: "url_for('public.product_detail', product_id=p.id) — route phải tồn tại (stub) để GET / render 200"
      pattern: "public.product_detail"
    - from: "app/templates/public/index.html"
      to: "app/public.py"
      via: "pagination (pagination.has_prev/.page/.pages/.has_next) + url_for('public.home', page=...)"
      pattern: "pagination.pages"
    - from: "app/public.py"
      to: "app/models.py"
      via: "Product.query.order_by(Product.sort_order.asc(), Product.id.asc()).paginate(page=page, per_page=12, error_out=False)"
      pattern: "Product.sort_order"
    - from: "app/templates/public/index.html"
      to: "app/__init__.py"
      via: "config['MESSENGER_URL'] cho nút Mua qua Messenger (contact strip)"
      pattern: "MESSENGER_URL"
---

<objective>
Nền tảng public + trang chủ catalog: thay trang "đang chuẩn bị" (Phase 1) bằng grid thẻ sản phẩm thật — responsive 2/3/4 cột, phân trang 12/trang, badge overlay cho hết hàng/ngừng bán, dải liên hệ Messenger — cùng lớp header công khai chứa ô tìm kiếm (D-09) dùng chung cho mọi trang public.

Purpose: Giao vertical slice đầu tiên của Phase 3 — khách mở `/` thấy danh sách hàng rõ ràng (ảnh + giá + trạng thái) và nút Messenger, không cần đăng nhập (CAT-01, CONT-01/02). Lớp `public/base.html` + `_nav.html` là nền cho mọi trang public Phase 3 (detail, search).

## Coverage Map (toàn phase — không bỏ sót)

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| CAT-01 | Khách xem list công khai không đăng nhập | 03-01 |
| CAT-02 | Trang chi tiết: ảnh, giá, brand, số đo, mô tả, trạng thái | 03-02 |
| CAT-03 | Giá + trạng thái hiển thị rõ | 03-01, 03-02 |
| CAT-05 | Detail gallery nhiều ảnh | 03-02 |
| SRCH-01 | Tìm theo tên/mô tả | 03-03 |
| CONT-01 | Dải liên hệ Messenger | 03-01 |
| CONT-02 | Messenger dễ thấy trên trang chủ + chi tiết | 03-01, 03-02 |
| D-01 | Home grid thẻ (ảnh, tên, giá, trạng thái) | 03-01 |
| D-02 | Grid responsive 2/3/4 cột | 03-01 |
| D-03 | Phân trang 12/trang theo sort_order | 03-01 |
| D-04 | Hết hàng/ngừng bán: ảnh mờ + nhãn | 03-01 |
| D-05 | Detail ảnh trái — thông tin phải | 03-02 |
| D-06 | Gallery: ảnh chính + thumbnail click đổi | 03-02 |
| D-07 | Hết hàng trên detail: vẫn nút Messenger + dòng đỏ | 03-02 |
| D-08 | Thứ tự info: tên→giá→trạng thái→brand→số đo→mô tả | 03-02 |
| D-09 | Ô tìm kiếm trên header | 03-01 |
| D-10 | GET form submit → trang kết quả riêng | 03-03 |
| D-11 | Tìm không dấu tiếng Việt (NFD + strip + casefold) | 03-03 |
| D-12 | Kết quả dùng chung grid + "N sản phẩm cho" + no-results | 03-03 |
| D-13 | Nút Messenger trên home + detail | 03-01, 03-02 |

Output: base.html có block header, public/base.html, public/_nav.html, public/_product_card.html, index.html (grid mới), stub product_detail.html + search.html, public.py (home + 2 route stub), CSS Phase 3 base.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-public-catalog-search-contact/03-CONTEXT.md (D-01..D-04, D-09, D-13)
@.planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (Design System; Spacing; Color Usage Rules; Public Header/_nav; Product Card; Product Grid; Homepage Layout; Pagination public variant; Contact strip; Type Usage Map)
@.planning/phases/01-scaffold-auth-data-model/01-UI-SPEC.md (design system baseline: tokens, breakpoint 480/768/1200)
@.planning/phases/02-admin-crud-images/02-UI-SPEC.md (badge palette, btn component, .pagination base)
@.planning/research/PITFALLS.md ("Looks Done But Isn't" checklist; empty state; price format)
@CLAUDE.md (tech stack; "What NOT to Use")

<interfaces>
Consumed from existing code (already on disk):
  - app/models.py: `Product` (id, name, price Integer VND, brand, measurements, description, quantity, discontinued, sort_order; `status` property → 'available'|'out_of_stock'|'discontinued'; `primary_image` property → first ProductImage by sort_order). `ProductImage.thumb_filename` → `<uuid>_thumb.jpg` (400px thumb asset, IMG-04).
  - app/__init__.py: `config['MESSENGER_URL']` (default 'https://m.me/yourpage'); Jinja filter `format_price` (1200000 → `1.200.000₫`); global 404/500 handlers render errors/404.html + errors/500.html.
  - app/templates/base.html: `{% block title %}`, `{% block content %}`, skip-link, flash zone (`get_flashed_messages(with_categories=true)`), footer. Current public/index.html extends base.html directly.
  - app/static/css/style.css: tokens `--accent #2563EB`, `--destructive #DC2626`, `--success #059669`, `--text #1F2937`, `--border #D1D5DB`; `.btn`, `.btn-primary` (height 44px, radius 8px, 14px/600); `.badge` + `.badge-available/out_of_stock/discontinued` (Phase 2 palette); `.pagination` (flex-end, gap 8px); `.empty-state` (centered, h2 16px/600, p #6B7280); `.visually-hidden`; `.back-link`.
  - app/admin.py pagination precedent: `Product.query.order_by(Product.sort_order.asc(), Product.id.asc()).paginate(page=page, per_page=20, error_out=False)`; template uses `pagination.has_prev/.prev_num/.page/.pages/.has_next/.next_num`.

Blueprints/templates to add:
  - `public.base` extends base; fills `{% block header %}` with `{% include "public/_nav.html" %}`.
  - `public/_nav.html`: brand "Cửa hàng" → url_for('public.home'); GET form action url_for('public.search') with `<input type="search" name="q" id="search-input">` value `{{ request.args.get('q', '') }}` and submit button "Tìm".
  - `public/_product_card.html`: include receives loop var `p` (a Product); renders whole-card `<a>`.

Style contract note (UI-SPEC Dimension-4 flag, non-blocking): card-name clamp uses `line-height: 1.4` — DOCUMENT as an exception in the CSS task (not a new role line-height; it is a clamp-forcing value).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public layout chain + header/search form + search route stub</name>
  <files>app/templates/base.html, app/templates/public/base.html, app/templates/public/_nav.html, app/templates/public/search.html, app/public.py, app/static/css/style.css</files>
  <read_first>
    - .planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (Base Template structural change; Public Header / Nav; Search Results empty-query copy; Spacing exceptions: input 12px padding, 44px touch)
    - .planning/phases/03-public-catalog-search-contact/03-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
    Build the shared public chrome. Do NOT create the home grid here (Task 2).

    1. `app/templates/base.html` — insert `{% block header %}{% endblock %}` on its own line between the skip-link `<a>` (line ending `Bỏ qua đến nội dung chính</a>`) and the `{% with messages = get_flashed_messages(...) %}` flash block. Admin/auth templates never override it → they render no header (UI-SPEC "Decisions Flagged" 1).

    2. `app/templates/public/base.html` (NEW):
       ```
       {% extends "base.html" %}
       {% block header %}{% include "public/_nav.html" %}{% endblock %}
       ```

    3. `app/templates/public/_nav.html` (NEW) — exact copy per UI-SPEC Public Header / Nav:
       ```
       <header class="site-header">
         <div class="container site-header__inner">
           <a class="brand" href="{{ url_for('public.home') }}">Cửa hàng</a>
           <form class="search-form" role="search" method="get" action="{{ url_for('public.search') }}">
             <label class="visually-hidden" for="search-input">Tìm kiếm</label>
             <input type="search" id="search-input" name="q" placeholder="Tìm sản phẩm…" value="{{ request.args.get('q', '') }}">
             <button class="btn btn-primary" type="submit" aria-label="Tìm kiếm">Tìm</button>
           </form>
         </div>
       </header>
       ```
       `request.args.get('q', '')` pre-fills the input on the search results page (D-10 preserves the active query).

    4. `app/public.py` — add `from flask import Blueprint, render_template, request, abort` and `from .models import Product`. Keep the existing `/` home route (rewritten in Task 2). Add the search stub so `url_for('public.search')` resolves and the nav form never 500s:
       ```
       @public_bp.route('/search', methods=['GET'])
       def search():
           q = (request.args.get('q') or '').strip()
           return render_template('public/search.html', q=q, products=None, pagination=None)
       ```
       (03-03 replaces this with the full diacritic-insensitive search. The stub renders the empty-query prompt only.)

    5. `app/templates/public/search.html` (NEW, stub — 03-03 completes it) — extends `public/base.html`, block title `Tìm kiếm — Cửa hàng`, content:
       ```
       <div class="container">
         <h1 class="page-heading">Kết quả tìm kiếm</h1>
         <div class="empty-state">
           <h2>Tìm kiếm sản phẩm</h2>
           <p>Vui lòng nhập từ khóa để tìm kiếm.</p>
           <a class="btn btn-primary" href="{{ url_for('public.home') }}">Xem tất cả sản phẩm</a>
         </div>
       </div>
       ```

    6. `app/static/css/style.css` — APPEND this section (do not touch existing rules):
       ```
       /* ============ Phase 3: Public header + search ============ */
       .site-header { position: sticky; top: 0; z-index: 10; background: #FFFFFF; border-bottom: 1px solid #E5E7EB; }
       .site-header__inner { display: flex; flex-direction: column; gap: 8px; padding-top: 8px; padding-bottom: 8px; }
       .brand { font-size: 16px; font-weight: 600; color: #1F2937; }
       .brand:hover { color: #2563EB; text-decoration: none; }
       .search-form { display: flex; gap: 8px; flex: 1; }
       .search-form input[type="search"] {
         flex: 1; height: 44px; padding: 0 12px; /* 12px padding: UI-SPEC declared exception */
         border: 1px solid #D1D5DB; border-radius: 8px; font-size: 16px; font-family: inherit; color: #1F2937; background: #FFFFFF;
       }
       .search-form input[type="search"]:focus-visible { outline: 2px solid #2563EB; outline-offset: 0; border-color: #2563EB; }
       .search-form .btn { flex-shrink: 0; }
       .page-heading { margin-bottom: 16px; }
       @media (min-width: 768px) {
         .site-header__inner { flex-direction: row; align-items: center; gap: 16px; }
         .search-form { max-width: 480px; margin-left: auto; }
       }
       ```
       Keep total CSS under 20KB (Phase 3 budget).

    Commit boundaries (atomic, never `git add .`):
      - commit 1: `git add app/templates/base.html app/templates/public/base.html app/templates/public/_nav.html && git commit -m "feat(03-01): public header + search form layout chain"`
      - commit 2: `git add app/templates/public/search.html app/public.py && git commit -m "feat(03-01): search route stub + empty-query prompt"`
      - commit 3: `git add app/static/css/style.css && git commit -m "style(03-01): site header + search form css"`
  </action>
  <verify>
    <automated>export PYTHONIOENCODING=utf-8 && python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
from wsgi import app
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///' + tempfile.mktemp(suffix='.db')
from app.db import db
with app.app_context():
    db.create_all()
    c = app.test_client()
    # Task 1: `/` still renders the coming-soon page (index.html not yet on public/base.html — that is Task 2).
    # The public chrome (header + search form) is exercised via /search which extends public/base.html.
    assert c.get('/').status_code == 200
    r = c.get('/search')
    assert r.status_code == 200, r.status_code
    h = r.get_data(as_text=True)
    assert 'role="search"' in h and 'name="q"' in h and '>Tìm<' in h, 'search form missing'
    assert 'Cửa hàng' in h
    assert 'Vui lòng nhập từ khóa để tìm kiếm.' in h
    assert 'Tìm kiếm — Cửa hàng' in h
    base = open('app/templates/base.html', encoding='utf-8').read()
    assert 'block header' in base, 'base.html missing header block'
print('TASK1_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `base.html` has `{% block header %}{% endblock %}` between skip-link and flash zone; admin/auth templates render no header
    - `public/base.html` extends base and includes `public/_nav.html`
    - `public/_nav.html` contains `<form role="search" method="get" action=".../search">`, input `name="q"`, button "Tìm", brand link to `public.home`
    - `public.py` has a `search` route (stub) so `url_for('public.search')` never raises BuildError
    - `public/search.html` shows empty-query prompt (title "Tìm kiếm — Cửa hàng")
    - CSS: `.site-header` sticky, `.search-form input[type="search"]` 44px with 12px padding declared as exception, media ≥768px single-row header with search max-width 480px
    - Verify script exits 0 printing TASK1_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Home route (grid + pagination) + product card include + index.html rewrite + detail stub</name>
  <files>app/public.py, app/templates/public/_product_card.html, app/templates/public/index.html, app/templates/public/product_detail.html</files>
  <read_first>
    - .planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (Product Card; Product Grid; Homepage Layout; Pagination public variant; Contact strip; Empty state copy "Chưa có sản phẩm nào")
    - .planning/phases/03-public-catalog-search-contact/03-CONTEXT.md (D-01, D-02, D-03, D-04)
  </read_first>
  <action>
    Replace the coming-soon homepage with the real catalog grid and the shared card component.

    1. `app/public.py` — rewrite the `/` route:
       ```
       @public_bp.route('/', methods=['GET'])
       def home():
           page = request.args.get('page', 1, type=int)
           pagination = Product.query.order_by(Product.sort_order.asc(), Product.id.asc()).paginate(
               page=page, per_page=12, error_out=False
           )
           return render_template('public/index.html', pagination=pagination, products=pagination.items)
       ```
       (D-03: 12/page, sort_order ASC then id ASC — same sort as admin Phase 2 D-02.)

       Add the detail route STUB (required so `url_for('public.product_detail', ...)` in the card resolves and GET / renders 200):
       ```
       @public_bp.route('/products/<int:product_id>', methods=['GET'])
       def product_detail(product_id):
           product = db.session.get(Product, product_id)
           if product is None:
               abort(404)
           return render_template('public/product_detail.html', product=product)
       ```
       Import `db`: `from .db import db`. (03-02 replaces this with the full gallery/info page.)

    2. `app/templates/public/_product_card.html` (NEW) — shared card, per UI-SPEC. The include receives the loop var `p`:
       ```
       <a class="product-card{% if p.status != 'available' %} is-unavailable{% endif %}"
          href="{{ url_for('public.product_detail', product_id=p.id) }}">
         <div class="product-card-thumb">
           {% if p.primary_image %}
           <img src="{{ url_for('static', filename='uploads/' + p.primary_image.thumb_filename) }}" alt="{{ p.name }}" width="400" height="400" loading="lazy">
           {% else %}
           <span class="product-card-placeholder">—</span>
           {% endif %}
           {% if p.status == 'out_of_stock' %}
           <span class="badge badge-out_of_stock badge-overlay">Hết hàng</span>
           {% elif p.status == 'discontinued' %}
           <span class="badge badge-discontinued badge-overlay">Ngừng bán</span>
           {% endif %}
         </div>
         <div class="product-card-body">
           <h3 class="product-card-name">{{ p.name }}</h3>
           <p class="product-card-price">{{ p.price | format_price }}</p>
         </div>
       </a>
       ```
       Còn hàng has NO overlay (D-04 — don't overwhelm in-stock). Whole card is one link (single focus target). Grid conveys status in text (badge), not color alone.

    3. `app/templates/public/index.html` — rewrite (replaces coming-soon), extends `public/base.html`:
       ```
       {% extends "public/base.html" %}
       {% block content %}
       <div class="container">
         <h1 class="page-heading">Sản phẩm</h1>
         {% if products %}
         <div class="product-grid">
           {% for p in products %}{% include "public/_product_card.html" %}{% endfor %}
         </div>
         {% if pagination.pages > 1 %}
         <div class="pagination pagination--center">
           {% if pagination.has_prev %}<a href="{{ url_for('public.home', page=pagination.prev_num) }}">Trước</a>
           {% else %}<span class="disabled" aria-disabled="true">Trước</span>{% endif %}
           <span class="page-indicator">Trang {{ pagination.page }} / {{ pagination.pages }}</span>
           {% if pagination.has_next %}<a href="{{ url_for('public.home', page=pagination.next_num) }}">Sau</a>
           {% else %}<span class="disabled" aria-disabled="true">Sau</span>{% endif %}
         </div>
         {% endif %}
         {% else %}
         <div class="empty-state">
           <h2>Chưa có sản phẩm nào</h2>
           <p>Cửa hàng đang cập nhật sản phẩm mới. Vui lòng quay lại sau.</p>
         </div>
         {% endif %}
         <section class="contact-strip">
           <h2>Liên hệ mua hàng</h2>
           <p>Đặt câu hỏi hoặc đặt mua trực tiếp qua Messenger.</p>
           <a class="btn btn-primary" href="{{ config['MESSENGER_URL'] }}" target="_blank" rel="noopener">Mua qua Messenger</a>
         </section>
       </div>
       {% endblock %}
       ```
       The contact strip sits OUTSIDE the if/else — it renders even when the grid is empty (UI-SPEC: "the store is open for contact even with no listings"). Contact strip margin-top 64px (3xl — reserved for the catalog section break).

    4. `app/templates/public/product_detail.html` (NEW, minimal stub — 03-02 replaces) — extends `public/base.html`, block title `{{ product.name }} — Cửa hàng`, content: `.back-link` "Quay lại" → `url_for('public.home')`, `<h1>{{ product.name }}</h1>`, `<p class="product-price">{{ product.price | format_price }}</p>`. This makes the card click-through render a real (if minimal) page instead of a redirect.
  </action>
  <verify>
    <automated>export PYTHONIOENCODING=utf-8 && python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
from wsgi import app
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///' + tempfile.mktemp(suffix='.db')
from app.db import db
from app.models import Product, ProductImage
with app.app_context():
    db.create_all()
    c = app.test_client()
    # empty store -> empty state + contact strip
    r = c.get('/')
    h = r.get_data(as_text=True)
    assert 'Chưa có sản phẩm nào' in h, 'empty state missing'
    assert 'Liên hệ mua hàng' in h and 'Mua qua Messenger' in h, 'contact strip missing on empty store'
    # seed 13 products: #5 out_of_stock (qty 0), #7 discontinued, others in stock
    for i in range(1, 14):
        qty = 0 if i == 5 else 3
        disc = (i == 7)
        db.session.add(Product(name='Sản phẩm %02d' % i, price=100000*i, quantity=qty, discontinued=disc, sort_order=i))
    db.session.flush()
    p1 = Product.query.filter_by(sort_order=1).first()
    db.session.add(ProductImage(filename='seed.jpg', original_filename='seed.jpg', product_id=p1.id, sort_order=0, is_primary=True))
    db.session.commit()
    r = c.get('/')
    h = r.get_data(as_text=True)
    assert r.status_code == 200, r.status_code
    assert 'Sản phẩm' in h and 'Sản phẩm 01' in h, 'grid product missing'
    assert '100.000' in h and '₫' in h, 'price not formatted'
    assert 'Hết hàng' in h, 'out_of_stock overlay missing'
    assert 'Ngừng bán' in h, 'discontinued overlay missing'
    assert 'uploads/seed_thumb.jpg' in h, 'card thumb not rendered'
    assert 'Mua qua Messenger' in h, 'contact strip CTA missing'
    # pagination: page 2 shows 13th item, indicator
    r = c.get('/?page=2')
    h = r.get_data(as_text=True)
    assert r.status_code == 200, r.status_code
    assert 'Sản phẩm 13' in h, 'page 2 item missing'
    assert 'Trang 2 / 2' in h, 'pagination indicator missing'
    # detail stub renders (card link resolves, not 404)
    r = c.get('/products/%d' % p1.id)
    assert r.status_code == 200, r.status_code
    assert 'Sản phẩm 01' in r.get_data(as_text=True)
    # missing product -> 404
    assert c.get('/products/99999').status_code == 404
print('TASK2_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `/` renders grid of Product cards sorted by sort_order then id, 12/page (D-01, D-02, D-03)
    - `.product-grid` is 2-col mobile, 3-col ≥768px, 4-col ≥1200px
    - Out-of-stock/discontinued cards render `.is-unavailable` + overlay badge "Hết hàng"/"Ngừng bán"; in-stock cards have no overlay (D-04)
    - Empty store renders "Chưa có sản phẩm nào" + contact strip still present
    - Contact strip "Mua qua Messenger" links to `config['MESSENGER_URL']` with `target="_blank" rel="noopener"`
    - `/products/<id>` route exists (stub) so card `url_for` renders; unknown id → 404
    - Verify script exits 0 printing TASK2_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CSS — grid, card, badge-overlay, contact-strip, pagination--center</name>
  <files>app/static/css/style.css</files>
  <read_first>
    - .planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (Product Card; Product Grid; Homepage Layout; Pagination public variant; Color Usage Rules; Type Usage Map for card name/price/badge)
  </read_first>
  <action>
    APPEND the catalog CSS to `app/static/css/style.css` (keep existing rules untouched). Do not introduce new hex values beyond the Phase 1+2 token set.
    ```
    /* ============ Phase 3: Product grid + card ============ */
    .product-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }
    @media (min-width: 768px) { .product-grid { grid-template-columns: repeat(3, 1fr); gap: 24px; } }
    @media (min-width: 1200px) { .product-grid { grid-template-columns: repeat(4, 1fr); } }
    .product-card { display: block; background: #FFFFFF; border: 1px solid #E5E7EB; border-radius: 8px; overflow: hidden; color: #1F2937; }
    .product-card:hover { border-color: #2563EB; box-shadow: 0 2px 8px rgba(0,0,0,0.08); text-decoration: none; }
    .product-card:focus-visible { outline: 2px solid #2563EB; outline-offset: 2px; }
    .product-card-thumb { position: relative; display: block; aspect-ratio: 1 / 1; overflow: hidden; background: #F3F4F6; }
    .product-card-thumb img { width: 100%; height: 100%; object-fit: cover; display: block; }
    .product-card-placeholder { display: flex; align-items: center; justify-content: center; width: 100%; height: 100%; color: #9CA3AF; font-size: 24px; }
    .is-unavailable .product-card-thumb img { opacity: 0.45; }
    .badge-overlay { position: absolute; top: 8px; left: 8px; z-index: 1; }
    .product-card-body { padding: 16px; }
    .product-card-name {
      font-size: 16px; font-weight: 600; color: #1F2937;
      line-height: 1.4; /* EXCEPTION (UI-SPEC Dimension-4 flag, non-blocking): 1.4 is not a role line-height — it forces the 2-line clamp to close without clipping descenders. Documented, not a new token. */
      display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;
    }
    .product-card-price { margin-top: 4px; font-size: 16px; font-weight: 600; color: #2563EB; font-variant-numeric: tabular-nums; }
    .pagination--center { justify-content: center; }
    .pagination a { min-height: 44px; display: inline-flex; align-items: center; padding: 0 8px; }
    .contact-strip { margin-top: 64px; background: #FFFFFF; padding: 32px; border-radius: 8px; text-align: center; }
    .contact-strip h2 { font-size: 16px; font-weight: 600; margin-bottom: 4px; }
    .contact-strip p { color: #6B7280; margin-bottom: 16px; }
    .contact-strip .btn { min-width: 200px; }
    ```
    Notes: `.pagination` base already exists (flex-end, 14px, #6B7280); `.pagination--center` only overrides justify-content. `.pagination a` gains 44px touch targets. Confirm final style.css total size stays under 20KB.
  </action>
  <verify>
    <automated>python - <<'PY'
css = open('app/static/css/style.css', encoding='utf-8').read()
size = len(css.encode('utf-8'))
assert size < 20000, 'CSS over 20KB budget: %d' % size
for sel in ['.product-grid', '.product-card', '.badge-overlay', '.contact-strip', '.pagination--center',
            '.is-unavailable', '.product-card-name', '.product-card-price']:
    assert sel in css, 'missing ' + sel
assert 'opacity: 0.45' in css, 'dim missing'
assert 'grid-template-columns: repeat(2, 1fr)' in css
assert 'repeat(4, 1fr)' in css, '4-col breakpoint missing'
assert 'line-height: 1.4' in css, 'clamp line-height missing'
print('TASK3_OK size=%d' % size)
PY</automated>
  </verify>
  <acceptance_criteria>
    - `.product-grid` 2/3/4 cols with 16/24px gaps per UI-SPEC
    - `.product-card` white bg, 1px #E5E7EB border, radius 8px; hover accent border + soft shadow
    - `.is-unavailable img` opacity 0.45 (dimmed); `.badge-overlay` top/left 8px z-index 1
    - `.product-card-name` 2-line clamp, line-height 1.4 documented as exception
    - `.product-card-price` 16px/600 accent tabular-nums
    - `.pagination--center` centers; `.pagination a` min-height 44px
    - `.contact-strip` margin-top 64px, padding 32px, centered CTA
    - style.css total < 20KB
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> public API (/) | Untrusted query params (`page`), untrusted path (`/products/<id>`); no form POSTs on public pages (read-only, CAT-01) |
| app -> SQLite | Read-only Product queries; WAL + busy_timeout inherited (PLAT-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `/products/<int:product_id>` stub | mitigate | Unknown id → `abort(404)` → generic errors/404.html (no detail, no stack trace). Public templates render only Product public fields — never `admin_note`. |
| T-03-02 | Tampering | `page` query param on `/` | mitigate | `request.args.get('page', 1, type=int)` coerces to int; `.paginate(..., error_out=False)` never 500s on out-of-range page. |
| T-03-03 | Spoofing | None in this plan (no auth/session on public routes) | accept | Public catalog intentionally unauthenticated (CAT-01); admin surface still guarded by Phase 1 `@login_required`. |
| T-03-SC | Tampering | pip/npm/cargo installs | accept | Phase 3 adds ZERO new packages — `unicodedata` (03-03) and `types` are stdlib. No legitimacy checkpoint required. |

All Jinja interpolation of dynamic values (`p.name`, `p.price`, `request.args.get('q', '')`) is auto-escaped by Jinja2; never use `|safe` on user-derived values.
</threat_model>

<verification>
- `GET /` unauth → 200 with grid (or empty state), contact strip, header search form; no login redirect (CAT-01)
- `GET /?page=2` → second page of the 12/page sort_order-sorted grid; "Trang 2 / 2" when 13 items
- `GET /products/<id>` → 200 (stub page, name + price); `GET /products/99999` → 404
- `GET /search` → 200 empty-query prompt (stub; full search in 03-03)
- Out-of-stock/discontinued cards carry `.is-unavailable` + overlay badge; in-stock cards don't (D-04)
- Header search form present on every public page (index, stub detail, stub search) (D-09)
</verification>

<success_criteria>
- Coming-soon page is gone; `/` is a real catalog grid (D-01..D-04)
- Every public template extends `public/base.html` (admin/auth unchanged, no header) — verified by `flask --app wsgi routes` + rendering all admin/auth pages still 200
- Contact strip on homepage links `MESSENGER_URL` in a new tab (CONT-01, CONT-02, D-13)
- All public copy Vietnamese, `lang="vi"` + `utf-8` inherited from base (PLAT-01)
- style.css stays under 20KB
</success_criteria>

<output>
Create `.planning/phases/03-public-catalog-search-contact/03-01-SUMMARY.md` when done
</output>
