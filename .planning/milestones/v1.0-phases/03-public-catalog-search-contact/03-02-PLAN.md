---
phase: 03-public-catalog-search-contact
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [app/public.py, app/templates/public/product_detail.html, app/static/css/style.css]
autonomous: true
requirements: [CAT-02, CAT-03, CAT-05, CONT-02]

must_haves:
  truths:
    - "Khách mở /products/<id> thấy bố cục ảnh trái - thông tin phải (desktop ≥768px), mobile xếp ảnh trên, thông tin dưới — D-05, CAT-02"
    - "Gallery hiển thị ảnh chính lớn + dãy thumbnail nhỏ; click thumbnail đổi ảnh chính (inline vanilla JS ~10 dòng, không thư viện) — D-06, CAT-05"
    - "Thứ tự thông tin trên detail đúng: tên → giá → trạng thái → thương hiệu → số đo → mô tả — D-08, CAT-03"
    - "Nút 'Mua qua Messenger' hiển thị cho MỌI trạng thái; sản phẩm hết hàng kèm dòng đỏ 'Sản phẩm hiện đang hết hàng.' — D-07, D-13, CONT-02"
    - "Sản phẩm không tồn tại → 404 (errors/404.html) — CAT-02"
  artifacts:
    - path: "app/public.py"
      provides: "product_detail route đầy đủ: get Product, abort(404) nếu thiếu, truyền product + images (order sort_order)"
      contains: "def product_detail"
    - path: "app/templates/public/product_detail.html"
      provides: "Trang chi tiết: back-link, gallery (main image + thumb strip + JS swap), info (tên/giá/badge/CTA/dòng hết hàng/meta/mô tả)"
      contains: "gallery-thumb"
    - path: "app/static/css/style.css"
      provides: "product-detail, product-gallery, main-image-box, gallery-thumbs, product-info, product-price, messenger-cta, out-of-stock-note, product-meta, product-description"
      contains: "product-detail"
  key_links:
    - from: "app/templates/public/product_detail.html"
      to: "app/public.py"
      via: "url_for('public.product_detail', product_id=product.id) là nơi card trỏ tới; back-link dùng request.referrer cùng-origin hoặc url_for('public.home')"
      pattern: "public.home"
    - from: "app/templates/public/product_detail.html"
      to: "app/models.py"
      via: "product.primary_image.thumb_filename cho ảnh chính; product.images.order_by(ProductImage.sort_order.asc()).all() cho strip"
      pattern: "thumb_filename"
    - from: "app/templates/public/product_detail.html"
      to: "app/__init__.py"
      via: "config['MESSENGER_URL'] cho nút Mua qua Messenger; {{ product.price | format_price }}"
      pattern: "MESSENGER_URL"
    - from: "app/templates/public/product_detail.html"
      to: "app/templates/public/_nav.html"
      via: "extends public/base.html (header + search form hiện trên detail)"
      pattern: "public/base.html"
---

<objective>
Trang chi tiết sản phẩm đầy đủ: gallery nhiều ảnh (ảnh chính + thumbnail click-đổi), thứ tự thông tin tên → giá → trạng thái → thương hiệu → số đo → mô tả, nút "Mua qua Messenger" trên mọi trạng thái + dòng đỏ khi hết hàng.

Purpose: Giao CAT-02 (đủ ảnh/giá/brand/số đo/mô tả/trạng thái), CAT-05 (gallery), CAT-03, CONT-02 trên trang chi tiết. Đây là trang khách xem trước khi hỏi mua qua Messenger — CTA đặt gần giá/trạng thái (D-13).

## Coverage Map (toàn phase — không bỏ sót)

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| CAT-01 | Khách xem list công khai không đăng nhập | 03-01 |
| CAT-02 | Trang chi tiết: ảnh, giá, brand, số đo, mô tả, trạng thái | 03-02 |
| CAT-03 | Giá + trạng thái hiển thị rõ | 03-01, 03-02 |
| CAT-05 | Detail gallery nhiều ảnh | 03-02 |
| SRCH-01 | Tìm theo tên/mô tả | 03-03 |
| CONT-01 | Dải liên hệ Messenger | 03-01 |
| CONT-02 | Messenger dễ thấy trên trang chủ + chi tiết | 03-01, 03-02 |
| D-01 | Home grid thẻ | 03-01 |
| D-02 | Grid responsive 2/3/4 cột | 03-01 |
| D-03 | Phân trang 12/trang theo sort_order | 03-01 |
| D-04 | Hết hàng/ngừng bán: ảnh mờ + nhãn | 03-01 |
| D-05 | Detail ảnh trái — thông tin phải | 03-02 |
| D-06 | Gallery: ảnh chính + thumbnail click đổi | 03-02 |
| D-07 | Hết hàng trên detail: vẫn nút Messenger + dòng đỏ | 03-02 |
| D-08 | Thứ tự info: tên→giá→trạng thái→brand→số đo→mô tả | 03-02 |
| D-09 | Ô tìm kiếm trên header | 03-01 |
| D-10 | GET form submit → trang kết quả riêng | 03-03 |
| D-11 | Tìm không dấu tiếng Việt (NFD + strip + casefold) | 03-03 |
| D-12 | Kết quả dùng chung grid + "N sản phẩm cho" + no-results | 03-03 |
| D-13 | Nút Messenger trên home + detail | 03-01, 03-02 |

Output: public.py detail route đầy đủ, product_detail.html hoàn chỉnh (gallery + info + JS swap + back-link), CSS detail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-public-catalog-search-contact/03-CONTEXT.md (D-05, D-06, D-07, D-08, D-13)
@.planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (Product Detail Layout; Gallery thumbnail swap JS contract; Back link safety; Messenger interactions; Type Usage Map; Color Usage Rules; Spacing exceptions)
@.planning/phases/01-scaffold-auth-data-model/01-UI-SPEC.md (breakpoint baseline)
@.planning/phases/02-admin-crud-images/02-UI-SPEC.md (badge palette verbatim)
@.planning/research/PITFALLS.md (UX: missing back link; "Looks Done But Isn't")
@CLAUDE.md (tech stack; zero-JS baseline — one justified inline JS snippet)

<interfaces>
Consumed from 03-01 (already on disk):
  - app/templates/public/base.html: extends base, includes public/_nav.html (header + search form visible on detail)
  - app/templates/public/_nav.html: search form
  - app/templates/public/_product_card.html: cards link to `public.product_detail`
  - app/templates/base.html: `{% block title %}` / `{% block content %}`

Consumed from existing code:
  - app/models.py: `Product.status` → 'available'|'out_of_stock'|'discontinued'; `Product.primary_image` → first image; `ProductImage.thumb_filename` → `app/static/uploads/<uuid>_thumb.jpg` (400px asset, IMG-04 — reused for detail main image per UI-SPEC "Decisions Flagged" 4, no new full-size render)
  - app/__init__.py: `config['MESSENGER_URL']`; `format_price` filter; global 404 handler renders errors/404.html
  - app/static/css/style.css: `.badge` palette (available/out_of_stock/discontinued), `.btn .btn-primary`, `.back-link`, `.container`

Detail image asset decision (UI-SPEC "Decisions Flagged" 4): main image reuses the 400px thumbnail; at 2x DPR a 440px box ideally wants ~880px but MVP performance favors the existing asset. No new render variant — documented upgrade path (later phase) if sharpness is an issue.

Thumbnail strip: 72×72px buttons, real `<button type="button">` (keyboard accessible), `aria-label="Xem ảnh {n}"`, active thumb carries `aria-current="true"` + `.is-active`. Strip renders only when `images|length > 1` (D-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full product_detail route + product_detail.html (gallery + info + Messenger CTA + back link)</name>
  <files>app/public.py, app/templates/public/product_detail.html</files>
  <read_first>
    - .planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (Product Detail Layout; Gallery thumbnail swap JS; Back link safety; Messenger interactions; copy)
    - .planning/phases/03-public-catalog-search-contact/03-CONTEXT.md (D-05..D-08, D-13)
  </read_first>
  <action>
    Replace the 03-01 stub with the full detail page.

    1. `app/public.py` — rewrite `product_detail`:
       ```
       @public_bp.route('/products/<int:product_id>', methods=['GET'])
       def product_detail(product_id):
           product = db.session.get(Product, product_id)
           if product is None:
               abort(404)
           images = product.images.order_by(ProductImage.sort_order.asc()).all()
           return render_template('public/product_detail.html', product=product, images=images)
       ```
       Add `ProductImage` to the existing `from .models import Product` import (→ `from .models import Product, ProductImage`). Keep `from flask import Blueprint, render_template, request, abort` (request used only in template for back-link).

    2. `app/templates/public/product_detail.html` — full rewrite, extends `public/base.html`. Order per D-08 (enforced by markup order). Info block order: tên → giá → status badge → Messenger CTA → [dòng đỏ nếu hết hàng] → thương hiệu → số đo → mô tả.
       ```
       {% extends "public/base.html" %}
       {% block title %}{{ product.name }} — Cửa hàng{% endblock %}
       {% block content %}
       <div class="container">
         <a class="back-link" href="{% if request.referrer and request.referrer.startswith(request.host_url) %}{{ request.referrer }}{% else %}{{ url_for('public.home') }}{% endif %}">Quay lại</a>
         <div class="product-detail">
           <div class="product-gallery">
             <div class="main-image-box">
               {% if product.primary_image %}
               <img id="main-image" src="{{ url_for('static', filename='uploads/' + product.primary_image.thumb_filename) }}" alt="{{ product.name }}" width="400" height="400">
               {% else %}
               <span class="main-placeholder">—</span>
               {% endif %}
             </div>
             {% if images|length > 1 %}
             <div class="gallery-thumbs">
               {% for img in images %}
               <button type="button" class="gallery-thumb{% if loop.first %} is-active{% endif %}"{% if loop.first %} aria-current="true"{% endif %} aria-label="Xem ảnh {{ loop.index }}"
                       data-src="{{ url_for('static', filename='uploads/' + img.thumb_filename) }}" data-alt="{{ product.name }}">
                 <img src="{{ url_for('static', filename='uploads/' + img.thumb_filename) }}" alt="{{ product.name }}" width="72" height="72" loading="lazy">
               </button>
               {% endfor %}
             </div>
             {% endif %}
           </div>
           <div class="product-info">
             <h1>{{ product.name }}</h1>
             <p class="product-price">{{ product.price | format_price }}</p>
             <div class="product-status">
               {% if product.status == 'available' %}<span class="badge badge-available">Còn hàng</span>
               {% elif product.status == 'out_of_stock' %}<span class="badge badge-out_of_stock">Hết hàng</span>
               {% else %}<span class="badge badge-discontinued">Ngừng bán</span>{% endif %}
             </div>
             <a class="btn btn-primary messenger-cta" href="{{ config['MESSENGER_URL'] }}" target="_blank" rel="noopener">Mua qua Messenger</a>
             {% if product.status == 'out_of_stock' %}
             <p class="out-of-stock-note">Sản phẩm hiện đang hết hàng.</p>
             {% endif %}
             {% if product.brand or product.measurements %}
             <dl class="product-meta">
               {% if product.brand %}<div class="product-meta-row"><dt>Thương hiệu:</dt><dd>{{ product.brand }}</dd></div>{% endif %}
               {% if product.measurements %}<div class="product-meta-row"><dt>Số đo:</dt><dd>{{ product.measurements }}</dd></div>{% endif %}
             </dl>
             {% endif %}
             {% if product.description %}
             <section class="product-description">
               <h2>Mô tả</h2>
               <p>{{ product.description }}</p>
             </section>
             {% endif %}
           </div>
         </div>
       </div>
       <script>
       (function(){
         var main = document.getElementById('main-image');
         if (!main) return;
         var thumbs = document.querySelectorAll('.gallery-thumb');
         for (var i = 0; i < thumbs.length; i++) {
           thumbs[i].addEventListener('click', function(e) {
             e.preventDefault();
             main.src = this.getAttribute('data-src');
             main.alt = this.getAttribute('data-alt');
             var all = document.querySelectorAll('.gallery-thumb');
             for (var j = 0; j < all.length; j++) { all[j].classList.remove('is-active'); all[j].removeAttribute('aria-current'); }
             this.classList.add('is-active');
             this.setAttribute('aria-current', 'true');
           });
         }
       })();
       </script>
       {% endblock %}
       ```
       Contract notes:
       - Messenger CTA shown for ALL statuses (D-07 + UI-SPEC "Decisions Flagged" 6); red line ONLY for out_of_stock (fuller copy, not duplicating the badge).
       - Brand/measurements rows omitted entirely when value empty (no "—" clutter on a public detail page, UI-SPEC).
       - Description section omitted when empty.
       - Back-link: same-origin referrer else home (preserves search→detail→back, PITFALLS "missing back link").
       - Gallery main image `object-fit: contain` (CSS Task 2) on neutral white box — no tint.
       - JS is the single justified public-page exception to the zero-JS baseline (UI-SPEC "Decisions Flagged" 10); graceful when JS off (strip still shows all images, no swap).

    Commit boundaries (atomic, never `git add .`):
      - `git add app/public.py && git commit -m "feat(03-02): full product_detail route (404 + ordered images)"`
      - `git add app/templates/public/product_detail.html && git commit -m "feat(03-02): product detail page (gallery + info + messenger CTA + back link)"`
  </action>
  <verify>
    <automated>export PYTHONIOENCODING=utf-8 && python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
from wsgi import app
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///' + tempfile.mktemp(suffix='.db')
from app.db import db
from app.models import Product, ProductImage
with app.app_context():
    db.create_all()
    # out_of_stock product, 3 images, full fields
    p = Product(name='Áo sơ mi', price=1200000, brand='Nike', measurements='M / L / XL',
                description='Dòng 1\nDòng 2', quantity=0, sort_order=1)
    db.session.add(p); db.session.flush()
    for i, fn in enumerate(['a.jpg', 'b.jpg', 'c.jpg']):
        db.session.add(ProductImage(filename=fn, original_filename=fn, product_id=p.id, sort_order=i, is_primary=(i == 0)))
    db.session.commit()
    c = app.test_client()
    r = c.get('/products/%d' % p.id)
    assert r.status_code == 200, r.status_code
    h = r.get_data(as_text=True)
    # info + fields
    assert 'Áo sơ mi — Cửa hàng' in h
    assert '1.200.000' in h and '₫' in h
    assert 'Hết hàng' in h                     # status badge
    assert 'Sản phẩm hiện đang hết hàng.' in h  # red line only for out_of_stock
    assert 'Mua qua Messenger' in h
    assert 'Thương hiệu:' in h and 'Nike' in h
    assert 'Số đo:' in h and 'M / L / XL' in h
    assert 'Mô tả' in h
    assert 'https://m.me/yourpage' in h        # config MESSENGER_URL
    # gallery
    assert 'main-image' in h and 'uploads/a_thumb.jpg' in h
    assert h.count('gallery-thumb') >= 3       # 3 thumb buttons
    assert 'aria-current="true"' in h          # first thumb active
    assert 'Xem ảnh 1' in h and 'Xem ảnh 3' in h
    assert 'addEventListener' in h             # JS swap present
    # D-08 order: name < price < status < CTA < meta < description in html
    idx = lambda s: h.index(s)
    assert idx('Áo sơ mi') < idx('1.200.000') < idx('Hết hàng') < idx('Mua qua Messenger') < idx('Thương hiệu:') < idx('Mô tả')
    # back link
    assert 'Quay lại' in h
    # available product: no red line, badge Còn hàng, CTA still present
    pa = Product(name='Váy hoa', price=300000, quantity=5, sort_order=2)
    db.session.add(pa); db.session.commit()
    r = c.get('/products/%d' % pa.id)
    h = r.get_data(as_text=True)
    assert 'Còn hàng' in h and 'Sản phẩm hiện đang hết hàng.' not in h and 'Mua qua Messenger' in h
    # discontinued: badge Ngừng bán, no red line, CTA present
    pd = Product(name='Áo khoác', price=900000, quantity=0, discontinued=True, sort_order=3)
    db.session.add(pd); db.session.commit()
    r = c.get('/products/%d' % pd.id)
    h = r.get_data(as_text=True)
    assert 'Ngừng bán' in h and 'Sản phẩm hiện đang hết hàng.' not in h and 'Mua qua Messenger' in h
    # single-image product -> no thumb strip
    ps = Product(name='Mũ', price=100000, quantity=2, sort_order=4)
    db.session.add(ps); db.session.flush()
    db.session.add(ProductImage(filename='m.jpg', original_filename='m.jpg', product_id=ps.id, sort_order=0, is_primary=True))
    db.session.commit()
    r = c.get('/products/%d' % ps.id)
    assert 'gallery-thumbs' not in r.get_data(as_text=True)
    # 404 for missing
    assert c.get('/products/99999').status_code == 404
    # no admin_note leakage
    pa.admin_note = 'INTERNAL-ONLY-NOTE'
    db.session.commit()
    assert 'INTERNAL-ONLY-NOTE' not in c.get('/products/%d' % pa.id).get_data(as_text=True)
print('TASK1_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `public.py` detail route returns 200 for existing id, 404 for missing; passes `images` ordered by sort_order
    - `product_detail.html` info order: tên → giá → trạng thái → CTA → thương hiệu → số đo → mô tả (D-08)
    - Gallery main image + thumb strip (only when >1 image); first thumb active; JS swap updates #main-image src/alt + toggles active/aria-current (D-06)
    - Messenger CTA on all statuses (D-07, D-13); red line only when out_of_stock
    - Brand/measurements/description rows omitted when empty
    - Back-link same-origin referrer else home
    - Verify script exits 0 printing TASK1_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CSS — product-detail layout, gallery, meta, out-of-stock note</name>
  <files>app/static/css/style.css</files>
  <read_first>
    - .planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (Product Detail Layout; Responsive Behavior; Color Usage Rules; Type Usage Map detail rows)
  </read_first>
  <action>
    APPEND the detail CSS to `app/static/css/style.css` (existing rules untouched; no new hex values):
    ```
    /* ============ Phase 3: Product detail ============ */
    .product-detail { display: flex; flex-direction: column; gap: 32px; }
    .product-gallery { width: 100%; }
    .main-image-box {
      aspect-ratio: 1 / 1; background: #FFFFFF; border: 1px solid #E5E7EB; border-radius: 8px; overflow: hidden;
      display: flex; align-items: center; justify-content: center;
    }
    .main-image-box img { width: 100%; height: 100%; object-fit: contain; }
    .main-placeholder { color: #9CA3AF; font-size: 32px; }
    .gallery-thumbs { display: flex; gap: 8px; margin-top: 8px; flex-wrap: wrap; }
    .gallery-thumb {
      width: 72px; height: 72px; padding: 0; border: 1px solid #E5E7EB; border-radius: 4px;
      overflow: hidden; background: #FFFFFF; cursor: pointer;
    }
    .gallery-thumb img { width: 100%; height: 100%; object-fit: cover; display: block; }
    .gallery-thumb.is-active { border-color: #2563EB; }
    .gallery-thumb:focus-visible { outline: 2px solid #2563EB; outline-offset: 2px; }
    .product-info { flex: 1; min-width: 0; }
    .product-info h1 { margin-bottom: 8px; }
    .product-price { font-size: 32px; font-weight: 600; color: #2563EB; line-height: 1.1; font-variant-numeric: tabular-nums; margin-bottom: 8px; }
    .product-status { margin-bottom: 16px; }
    .messenger-cta { width: 100%; max-width: 320px; margin-bottom: 8px; }
    .out-of-stock-note { color: #DC2626; font-size: 14px; margin-bottom: 8px; }
    .product-meta { margin-bottom: 24px; }
    .product-meta-row { display: flex; gap: 8px; margin-bottom: 4px; }
    .product-meta dt { font-size: 14px; color: #6B7280; }
    .product-meta dd { font-size: 16px; color: #1F2937; }
    .product-description { border-top: 1px solid #E5E7EB; padding-top: 24px; margin-top: 24px; }
    .product-description h2 { font-size: 16px; font-weight: 600; margin-bottom: 4px; }
    .product-description p { white-space: pre-line; }
    @media (min-width: 768px) {
      .product-detail { flex-direction: row; gap: 32px; }
      .product-gallery { width: 360px; flex-shrink: 0; }
    }
    @media (min-width: 1200px) { .product-gallery { width: 440px; } }
    ```
    Contract notes: gallery column 360px at ≥768, 440px at ≥1200 (UI-SPEC); stacked full-width below 768 (D-05). Main image box neutral white (do NOT tint — product photos need neutral bg). `.product-price` uses Display 32px role (line-height 1.1) in accent — documented accent reservation for price. Confirm style.css total stays under 20KB.
  </action>
  <verify>
    <automated>python - <<'PY'
css = open('app/static/css/style.css', encoding='utf-8').read()
size = len(css.encode('utf-8'))
assert size < 20000, 'CSS over 20KB budget: %d' % size
for sel in ['.product-detail', '.main-image-box', '.gallery-thumb', '.product-price', '.messenger-cta',
            '.out-of-stock-note', '.product-meta', '.product-description']:
    assert sel in css, 'missing ' + sel
assert 'object-fit: contain' in css
assert 'width: 360px' in css and 'width: 440px' in css, 'gallery column widths missing'
assert 'white-space: pre-line' in css
assert 'font-size: 32px' in css and 'line-height: 1.1' in css, 'display role price missing'
print('TASK2_OK size=%d' % size)
PY</automated>
  </verify>
  <acceptance_criteria>
    - `.product-detail` flex column below 768, row at ≥768 (image left, info right — D-05)
    - Gallery column 360px (≥768) / 440px (≥1200); main image box white, contain-fit, 1px #E5E7EB border
    - `.gallery-thumb` 72×72px, radius 4px, active border accent
    - `.product-price` Display 32px accent tabular-nums; `.out-of-stock-note` #DC2626 14px
    - `.product-description p` white-space pre-line (preserves line breaks)
    - style.css total < 20KB
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> public API (/products/<id>) | Untrusted path id; read-only, no form POSTs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | `/products/<int:product_id>` | mitigate | Unknown id → `abort(404)` → generic errors/404.html (no stack trace). Template renders only public fields; verification asserts `admin_note` is never rendered. |
| T-03-05 | Tampering | `request.referrer` in back-link | mitigate | Back-link only used when `request.referrer.startswith(request.host_url)` — same-origin only; else falls back to `url_for('public.home')`. Never a raw reflected open redirect. Jinja auto-escapes `{{ request.referrer }}`. |
| T-03-06 | Tampering | `data-src`/`data-alt` attribute values | mitigate | Values come from `url_for('static', filename='uploads/' + img.thumb_filename)` — server-derived paths; thumb_filename is a controlled `<uuid>_thumb.jpg`. Auto-escaped. |
| T-03-SC | Tampering | pip/npm/cargo installs | accept | Phase 3 adds zero new packages; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `GET /products/<id>` for available / out_of_stock / discontinued products → 200, correct badge, CTA present on all, red line only for out_of_stock
- Gallery renders thumb strip only when >1 image; first thumb has `aria-current="true"`; JS `addEventListener` present
- `GET /products/99999` → 404 (errors/404.html)
- Info order verified by HTML index positions (name < price < status < CTA < brand < description) — D-08
- Back-link points to same-origin referrer when arriving from /search?q=..., else home
</verification>

<success_criteria>
- Trang chi tiết đủ mọi thông tin khách cần (CAT-02, CAT-03) + gallery (CAT-05)
- Nút "Mua qua Messenger" dễ thấy trên detail mọi trạng thái (D-13, CONT-02)
- Bố cục responsive: desktop ảnh trái - thông tin phải, mobile stack (D-05)
- zero-JS baseline giữ nguyên ngoại trừ 1 snippet inline ~10 dòng cho gallery swap (UI-SPEC exception)
- style.css < 20KB
</success_criteria>

<output>
Create `.planning/phases/03-public-catalog-search-contact/03-02-SUMMARY.md` when done
</output>
