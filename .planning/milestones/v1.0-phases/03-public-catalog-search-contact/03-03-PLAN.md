---
phase: 03-public-catalog-search-contact
plan: 03
type: execute
wave: 3
depends_on: [03-01]
files_modified: [app/public.py, app/templates/public/search.html, app/static/css/style.css]
autonomous: true
requirements: [SRCH-01]

must_haves:
  truths:
    - "Khách gõ từ khóa vào ô tìm kiếm header và submit → GET /search?q=... mở trang kết quả riêng — D-10, SRCH-01"
    - "Tìm không dấu tiếng Việt: 'ao' tìm ra 'Áo', 'quan' tìm ra 'quần'; so sánh cả name và description — D-11, SRCH-01"
    - "Kết quả hiển thị bằng chung grid card, dòng '{N} sản phẩm cho “{q}”', phân trang giữ nguyên q — D-12"
    - "Không có kết quả → 'Không tìm thấy sản phẩm' + CTA 'Xem tất cả sản phẩm'; từ khóa trống → nhắc nhập từ khóa — D-12"
    - "Ô tìm kiếm trên header giữ lại từ khóa đang tìm (pre-fill input) — D-09"
  artifacts:
    - path: "app/public.py"
      provides: "normalize_search_text (NFD + strip combining marks + casefold) + _manual_pagination + full /search route (Python filter + paginate)"
      contains: "def normalize_search_text"
    - path: "app/templates/public/search.html"
      provides: "3 trạng thái: empty-query prompt / kết quả (result-count + grid + pagination) / no-results"
      contains: "Không tìm thấy sản phẩm"
    - path: "app/static/css/style.css"
      provides: ".result-count"
      contains: "result-count"
  key_links:
    - from: "app/public.py"
      to: "app/models.py"
      via: "Product.query.order_by(Product.sort_order.asc(), Product.id.asc()).all() rồi lọc trong Python (không chạm SQL LIKE — tránh injection + áp chuẩn hóa Unicode) (D-11)"
      pattern: "Product.query"
    - from: "app/templates/public/search.html"
      to: "app/templates/public/_product_card.html"
      via: "{% include 'public/_product_card.html' %} trong vòng lặp sản phẩm (dùng chung grid, D-12)"
      pattern: "_product_card.html"
    - from: "app/templates/public/search.html"
      to: "app/public.py"
      via: "url_for('public.search', q=q, page=pagination.prev_num/next_num) — phân trang giữ q; CTA 'Xem tất cả sản phẩm' → url_for('public.home')"
      pattern: "public.search"
    - from: "app/public.py"
      to: "unicodedata (stdlib)"
      via: "normalize_search_text dùng unicodedata.normalize('NFD', ...) + unicodedata.category(ch) != 'Mn' + casefold()"
      pattern: "unicodedata"
---

<objective>
Tìm kiếm hoàn chỉnh: chuẩn hóa tiếng Việt không dấu (NFD + strip combining marks + casefold) áp lên cả từ khóa lẫn tên/mô tả sản phẩm, GET form submit → trang kết quả riêng dùng chung grid card, kèm các trạng thái empty-query / no-results và phân trang giữ nguyên q.

Purpose: Giao SRCH-01 + D-10/D-11/D-12. Cơ chế lọc trong Python (không thêm cột chuẩn hóa vào DB, không SQL LIKE) — đúng khuyến nghị UI-SPEC "Decisions Flagged" 7 cho catalog nhỏ một admin.

## Coverage Map (toàn phase — không bỏ sót)

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| CAT-01 | Khách xem list công khai không đăng nhập | 03-01 |
| CAT-02 | Trang chi tiết: ảnh, giá, brand, số đo, mô tả, trạng thái | 03-02 |
| CAT-03 | Giá + trạng thái hiển thị rõ | 03-01, 03-02 |
| CAT-05 | Detail gallery nhiều ảnh | 03-02 |
| SRCH-01 | Tìm theo tên/mô tả | 03-03 |
| CONT-01 | Dải liên hệ Messenger | 03-01 |
| CONT-02 | Messenger dễ thấy trên trang chủ + chi tiết | 03-01, 03-02 |
| D-01 | Home grid thẻ | 03-01 |
| D-02 | Grid responsive 2/3/4 cột | 03-01 |
| D-03 | Phân trang 12/trang theo sort_order | 03-01 |
| D-04 | Hết hàng/ngừng bán: ảnh mờ + nhãn | 03-01 |
| D-05 | Detail ảnh trái — thông tin phải | 03-02 |
| D-06 | Gallery: ảnh chính + thumbnail click đổi | 03-02 |
| D-07 | Hết hàng trên detail: vẫn nút Messenger + dòng đỏ | 03-02 |
| D-08 | Thứ tự info: tên→giá→trạng thái→brand→số đo→mô tả | 03-02 |
| D-09 | Ô tìm kiếm trên header | 03-01 |
| D-10 | GET form submit → trang kết quả riêng | 03-03 |
| D-11 | Tìm không dấu tiếng Việt (NFD + strip + casefold) | 03-03 |
| D-12 | Kết quả dùng chung grid + "N sản phẩm cho" + no-results | 03-03 |
| D-13 | Nút Messenger trên home + detail | 03-01, 03-02 |

Output: normalize_search_text helper + _manual_pagination helper + full /search route, search.html 3-trạng thái, .result-count CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-public-catalog-search-contact/03-CONTEXT.md (D-09, D-10, D-11, D-12)
@.planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (Search flow; Search Results Layout; Empty-query / No-results copy; Route map; "Decisions Flagged" 7 — in-Python filter)
@.planning/research/PITFALLS.md ("Looks Done But Isn't": empty state; no silent blank)
@CLAUDE.md (tech stack)

<interfaces>
Consumed from 03-01 (already on disk):
  - app/templates/public/base.html + _nav.html: header + GET search form (input name="q", pre-fill `request.args.get('q', '')`)
  - app/templates/public/_product_card.html: shared card include (loop var `p`)
  - app/public.py: existing stub `search` route (this plan replaces it) and `home` route

Consumed from existing code:
  - app/models.py: `Product.name`, `Product.description` (nullable), `Product.sort_order`, `Product.id`
  - app/__init__.py: `format_price` filter

Search contract (UI-SPEC Search flow):
  1. GET /search receives `q`; strip whitespace.
  2. Empty after strip → empty-query prompt (no error, just a prompt).
  3. Diacritic-insensitive match (D-11): normalize query AND each product's name + description with `unicodedata.normalize('NFD', s)` → strip combining marks (Unicode category Mn) → `casefold()`, then substring match. Applied to both sides. "ao" matches "áo".
  4. Match on name OR description (SRCH-01). Handle None description as ''.
  5. Results rendered through the shared product-card include (D-12), 12/page, pagination links carry `q`.
  6. Zero matches → no-results state.
  Search page omits the contact strip (UI-SPEC "Decisions Flagged" 8 — it is a navigational page; Messenger CTA already on home strip + detail button).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: normalize_search_text + _manual_pagination + full /search route</name>
  <files>app/public.py</files>
  <read_first>
    - .planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (Search flow; "Decisions Flagged" 7)
    - .planning/phases/03-public-catalog-search-contact/03-CONTEXT.md (D-11)
  </read_first>
  <action>
    Replace the 03-01 search stub in `app/public.py` with the full mechanism. Add `import unicodedata` at the top; add `from types import SimpleNamespace`.

    1. Diacritic-insensitive normalizer (D-11) — module-level helper:
       ```
       def normalize_search_text(text):
           """D-11: NFD -> strip combining marks -> casefold. 'áo'->'ao', 'Áo'->'ao'."""
           if not text:
               return ''
           decomposed = unicodedata.normalize('NFD', text)
           stripped = ''.join(ch for ch in decomposed if unicodedata.category(ch) != 'Mn')
           return stripped.casefold()
       ```

    2. Manual pagination helper — the search filter runs in Python, so it cannot use `Query.paginate()`. Expose the SAME template-facing fields the home grid uses so `search.html` reuses the identical pagination markup:
       ```
       def _manual_pagination(page, per_page, total):
           pages = max(1, -(-total // per_page))
           page = max(1, min(page, pages))
           return SimpleNamespace(
               page=page, pages=pages, per_page=per_page, total=total,
               has_prev=page > 1, has_next=page < pages,
               prev_num=page - 1 if page > 1 else 1,
               next_num=page + 1 if page < pages else pages,
           )
       ```

    3. Full route:
       ```
       @public_bp.route('/search', methods=['GET'])
       def search():
           q = (request.args.get('q') or '').strip()
           nq = normalize_search_text(q)
           page = request.args.get('page', 1, type=int)
           per_page = 12
           if not nq:
               return render_template('public/search.html', q=q, products=None, pagination=None)
           all_products = Product.query.order_by(Product.sort_order.asc(), Product.id.asc()).all()
           matched = [
               p for p in all_products
               if nq in normalize_search_text(p.name or '')
               or nq in normalize_search_text(p.description or '')
           ]
           pagination = _manual_pagination(page, per_page, len(matched))
           start = (pagination.page - 1) * per_page
           pagination.items = matched[start:start + per_page]
           return render_template('public/search.html', q=q, products=pagination.items, pagination=pagination)
       ```
       Contract notes:
       - In-Python filter over the whole catalog (single admin, low volume — UI-SPEC "Decisions Flagged" 7). No new DB column, no SQL LIKE (avoids pattern-escaping pitfalls and keeps the Unicode normalization in one place). Upgrade path if the catalog ever grows: store a `normalized_name/normalized_description` column at write time and `LIKE` against it — NOT in this phase.
       - `p.name or ''` / `p.description or ''` guard None (description is nullable).
       - `pagination.items` assigned after construction (SimpleNamespace is mutable).
       - Query too long / pathological: no limit needed; Python substring scan over a small catalog is bounded. `page` is int-coerced and clamped by `_manual_pagination`.

    Commit boundaries (atomic, never `git add .`):
      - `git add app/public.py && git commit -m "feat(03-03): diacritic-insensitive search (normalize + python filter + paginate)"`
  </action>
  <verify>
    <automated>export PYTHONIOENCODING=utf-8 && python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
from wsgi import app
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///' + tempfile.mktemp(suffix='.db')
from app.db import db
from app.models import Product
from app.public import normalize_search_text, _manual_pagination
# unit-check normalizer (D-11)
assert normalize_search_text('áo') == 'ao'
assert normalize_search_text('Áo') == 'ao'
assert normalize_search_text('QUẦN') == 'quan'
assert normalize_search_text('Thùng') == 'thung'
assert normalize_search_text('') == ''
with app.app_context():
    db.create_all()
    for nm, desc, so in [
        ('Áo sơ mi trắng', 'áo cotton mềm', 1),
        ('Quần jean xanh', 'quần jean nam', 2),
        ('Váy hoa', 'váy nữ mùa hè', 3),
        ('Áo khoác', None, 4),
        ('Áo len', 'len nhập khẩu', 5),
        ('Áo thun', 'thun cotton', 6),
        ('Áo polo', 'polo nam', 7),
        ('Áo hoodie', 'hoodie nỉ', 8),
        ('Áo vest', 'vest công sở', 9),
        ('Áo blazer', 'blazer nữ', 10),
        ('Áo bomber', 'bomber nam', 11),
        ('Áo cardigan', 'cardigan nữ', 12),
        ('Áo măng tô', 'măng tô dài', 13),
        ('Áo gile', 'gile len', 14),
        ('Áo lửng', 'lửng tay ngắn', 15),
    ]:
        db.session.add(Product(name=nm, price=200000, quantity=1, description=desc, sort_order=so))
    db.session.commit()
    c = app.test_client()
    # diacritic-insensitive: 'ao' matches 13 of 15 seeded products (all Ao-*); 12/page -> 2 pages
    r = c.get('/search?q=ao')
    h = r.get_data(as_text=True)
    assert r.status_code == 200, r.status_code
    assert '13 sản phẩm cho “ao”' in h, 'result count missing'
    assert 'Áo sơ mi trắng' in h and 'Áo thun' in h
    # page 2 keeps q
    r = c.get('/search?q=ao&page=2')
    h = r.get_data(as_text=True)
    assert r.status_code == 200, r.status_code
    assert 'Áo măng tô' in h and 'Trang 2 / 2' in h
    assert 'q=ao&page=1' in h or 'q=ao&amp;page=1' in h, 'pagination link loses q'
    # pre-fill input
    assert 'value="ao"' in h, 'nav input not pre-filled'
    # description-only match, no diacritic, uppercase query
    r = c.get('/search?q=quan')
    h = r.get_data(as_text=True)
    assert '1 sản phẩm cho “quan”' in h and 'Quần jean xanh' in h
    # name match via description of product 4 (description None) must not crash and not match
    r = c.get('/search?q=khoac')
    assert 'Áo khoác' in r.get_data(as_text=True) and '1 sản phẩm' in r.get_data(as_text=True)
    # no results
    r = c.get('/search?q=zzzz')
    h = r.get_data(as_text=True)
    assert 'Không tìm thấy sản phẩm' in h and 'Không có kết quả cho “zzzz”' in h
    assert 'Xem tất cả sản phẩm' in h
    # empty query prompt
    r = c.get('/search')
    assert 'Vui lòng nhập từ khóa để tìm kiếm.' in r.get_data(as_text=True)
    r = c.get('/search?q=%20%20')
    assert 'Vui lòng nhập từ khóa để tìm kiếm.' in r.get_data(as_text=True)
print('TASK1_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `normalize_search_text('áo') == 'ao'` and `normalize_search_text('Áo') == 'ao'`; strips combining marks + casefold (D-11)
    - `/search?q=ao` matches products whose name OR description contains "áo"/"Áo" (diacritic-insensitive, SRCH-01)
    - Empty query (blank or whitespace) → empty-query prompt, not an error
    - Zero matches → no-results state with "Không tìm thấy sản phẩm" + CTA
    - Pagination is 12/page, clamps page bounds, preserves `q` in all page links
    - Products with None description do not crash the filter
    - Verify script exits 0 printing TASK1_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: search.html 3-trạng thái + .result-count CSS</name>
  <files>app/templates/public/search.html, app/static/css/style.css</files>
  <read_first>
    - .planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (Search Results Layout; Empty-query state; No-results state; copy)
  </read_first>
  <action>
    Replace the 03-01 stub template with the full 3-state page. The page heading stays "Kết quả tìm kiếm" for all three states; the states differ below it.

    `app/templates/public/search.html` (full rewrite, extends `public/base.html`):
    ```
    {% extends "public/base.html" %}
    {% block title %}Tìm kiếm — Cửa hàng{% endblock %}
    {% block content %}
    <div class="container">
      <h1 class="page-heading">Kết quả tìm kiếm</h1>
      {% if not q %}
      <div class="empty-state">
        <h2>Tìm kiếm sản phẩm</h2>
        <p>Vui lòng nhập từ khóa để tìm kiếm.</p>
        <a class="btn btn-primary" href="{{ url_for('public.home') }}">Xem tất cả sản phẩm</a>
      </div>
      {% elif products %}
      <p class="result-count">{{ products|length }} sản phẩm cho “{{ q }}”</p>
      <div class="product-grid">
        {% for p in products %}{% include "public/_product_card.html" %}{% endfor %}
      </div>
      {% if pagination.pages > 1 %}
      <div class="pagination pagination--center">
        {% if pagination.has_prev %}<a href="{{ url_for('public.search', q=q, page=pagination.prev_num) }}">Trước</a>
        {% else %}<span class="disabled" aria-disabled="true">Trước</span>{% endif %}
        <span class="page-indicator">Trang {{ pagination.page }} / {{ pagination.pages }}</span>
        {% if pagination.has_next %}<a href="{{ url_for('public.search', q=q, page=pagination.next_num) }}">Sau</a>
        {% else %}<span class="disabled" aria-disabled="true">Sau</span>{% endif %}
      </div>
      {% endif %}
      {% else %}
      <div class="empty-state">
        <h2>Không tìm thấy sản phẩm</h2>
        <p>Không có kết quả cho “{{ q }}”. Kiểm tra lại từ khóa hoặc duyệt tất cả sản phẩm.</p>
        <a class="btn btn-primary" href="{{ url_for('public.home') }}">Xem tất cả sản phẩm</a>
      </div>
      {% endif %}
    </div>
    {% endblock %}
    ```
    Contract notes:
    - Result count uses `products|length` (the current page count) — for a single-page result this equals total. The UI-SPEC string is "{N} sản phẩm cho “{q}”" and "1 sản phẩm" is grammatically correct in VN (no plural inflection). If you prefer the true total across pages, the view's `pagination.total` is also available (`{{ pagination.total }}`), but `products|length` matches the single-page case and avoids showing a misleading total on page 2+; use `pagination.total` if you want the phase-wide total. The verify script asserts the single-page count, so either is consistent for the tested cases.
    - No contact strip on this page (UI-SPEC "Decisions Flagged" 8).
    - Pagination links always carry `q` so page 2 keeps the query.
    - Nav input is pre-filled with `q` automatically via `_nav.html` (`request.args.get('q', '')`).

    `app/static/css/style.css` — APPEND:
    ```
    /* ============ Phase 3: Search results ============ */
    .result-count { font-size: 14px; color: #6B7280; margin-bottom: 48px; }
    ```
    Confirm style.css total stays under 20KB.

    Commit boundaries (atomic, never `git add .`):
      - `git add app/templates/public/search.html && git commit -m "feat(03-03): search results page (3 states + pagination keeps q)"`
      - `git add app/static/css/style.css && git commit -m "style(03-03): search result-count"`
  </action>
  <verify>
    <automated>export PYTHONIOENCODING=utf-8 && python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
from wsgi import app
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///' + tempfile.mktemp(suffix='.db')
from app.db import db
from app.models import Product
with app.app_context():
    db.create_all()
    db.session.add(Product(name='Áo sơ mi', price=200000, quantity=1, description='áo cotton', sort_order=1))
    db.session.add(Product(name='Quần jean', price=300000, quantity=1, description='quần nam', sort_order=2))
    db.session.commit()
    c = app.test_client()
    # results state
    h = c.get('/search?q=ao').get_data(as_text=True)
    assert 'Kết quả tìm kiếm' in h
    assert '1 sản phẩm cho “ao”' in h
    assert 'Áo sơ mi' in h and 'product-card' in h
    # no-results state
    h = c.get('/search?q=zzzz').get_data(as_text=True)
    assert 'Không tìm thấy sản phẩm' in h
    assert 'Xem tất cả sản phẩm' in h
    # empty-query state
    h = c.get('/search').get_data(as_text=True)
    assert 'Tìm kiếm sản phẩm' in h and 'Vui lòng nhập từ khóa để tìm kiếm.' in h
    # nav pre-fill
    assert 'value="ao"' in c.get('/search?q=ao').get_data(as_text=True)
# CSS
css = open('app/static/css/style.css', encoding='utf-8').read()
assert '.result-count' in css
assert len(css.encode('utf-8')) < 20000, 'CSS over 20KB budget'
print('TASK2_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `search.html` renders the correct state for: empty query (prompt), results (count + grid + pagination), no-results (heading + body + CTA)
    - Result-count copy "{N} sản phẩm cho “{q}”" present; pagination links carry `q`
    - Nav search input pre-filled with the active query (D-09)
    - `.result-count` 14px #6B7280 margin-bottom 48px; style.css < 20KB
    - Verify script exits 0 printing TASK2_OK
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> public API (/search) | Untrusted `q` and `page` query params; reflected in copy and input value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | Reflected XSS via `q` in result-count / no-results / nav input value | mitigate | All `{{ q }}` interpolations are Jinja2 auto-escaped (HTML + attribute contexts); NEVER `|safe`. The nav input value is attribute-context escaped. |
| T-03-08 | Denial of Service | Python filter scans whole catalog per request | accept | Single admin, low volume, small catalog (UI-SPEC "Decisions Flagged" 7). `page` int-coerced + clamped. Upgrade path (documented, not this phase): store normalized columns at write time + SQL LIKE. |
| T-03-09 | Spoofing | None — search is public, read-only, no session involvement | accept | Public search is intentionally unauthenticated (CAT-01). |
| T-03-SC | Tampering | pip/npm/cargo installs | accept | Phase 3 adds zero new packages (`unicodedata`, `types` are stdlib); no legitimacy checkpoint required. |
</threat_model>

<verification>
- `GET /search?q=ao` → 200, results use the shared card grid, "N sản phẩm cho “ao”" present (D-12)
- `GET /search?q=quan` matches "Quần jean xanh" (name) and description matches (SRCH-01, D-11)
- `GET /search?q=zzzz` → no-results state + CTA; `GET /search` → empty-query prompt
- `GET /search?q=ao&page=2` → page 2 of results, pagination links preserve `q`
- No contact strip on the search page
</verification>

<success_criteria>
- Khách tìm không dấu ra sản phẩm có dấu (D-11), kết quả hiển thị đẹp trên grid chung (D-12)
- Trạng thái rỗng đầy đủ: empty-query + no-results đều có CTA dẫn về danh sách đầy đủ
- Phân trang kết quả giữ nguyên từ khóa (D-10)
- Không thêm dependency, không đổi schema DB
- style.css < 20KB
</success_criteria>

<output>
Create `.planning/phases/03-public-catalog-search-contact/03-03-SUMMARY.md` when done
</output>
