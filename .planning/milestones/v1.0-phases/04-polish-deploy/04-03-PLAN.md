---
phase: 04-polish-deploy
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified: [.env.example, app/__init__.py, app/templates/errors/404.html, app/templates/errors/500.html, docs/deploy/README.md]
autonomous: true
requirements: [PLAT-02]

must_haves:
  truths:
    - "SECRET_KEY bắt buộc từ env với fail-fast; debug mặc định tắt trong production — PLAT-02, SC-4"
    - "Error pages 404/500 không lộ stack trace trong production (template graceful + debug off) — SC-5"
    - "HTTPS + admin rate limiting + security headers được cấu hình (nginx.conf từ 04-02) và verified — SC-3, D-04"
    - "Proxy headers X-Forwarded-Proto: back-link trong public.py scheme-agnostic hoạt động sau nginx — WR-03"
    - "SESSION_COOKIE_SECURE được document bật khi HTTPS; toàn bộ success criteria Phase 4 verify pass — SC-1..SC-5"
  artifacts:
    - path: ".env.example"
      provides: "FLASK_DEBUG=0 + SESSION_COOKIE_SECURE với hướng dẫn bật khi HTTPS"
      contains: "FLASK_DEBUG"
    - path: "docs/deploy/README.md"
      provides: "phần verify production (https://domain, /admin login, 404/500 không stack trace) trong checklist"
      contains: "stack trace"
  key_links:
    - from: "app/__init__.py"
      to: ".env.example"
      via: "SECRET_KEY env (line 32, fail-fast line 47-48) + DEBUG (line 42) — .env.example document đủ biến production"
      pattern: "SECRET_KEY must be set"
    - from: "app/templates/errors/404.html"
      to: "app/templates/errors/500.html"
      via: "cả hai extends dynamic (public chrome vs base) theo request.path — không render traceback"
      pattern: "error-page"
    - from: "app/public.py"
      to: "docs/deploy/nginx.conf"
      via: "back-link dùng request.host.split(':')[0] (scheme-agnostic) — hoạt động sau nginx X-Forwarded-Proto, không cần đổi code (WR-03)"
      pattern: "request.host.split"
---

<objective>
Production hardening + verification (success criteria 3, 4, 5): verify SECRET_KEY từ env (fail-fast) + debug off, error pages không lộ stack trace, HTTPS Let's Encrypt + admin rate limiting + security headers (config từ 04-02), proxy headers X-Forwarded-Proto scheme-agnostic, SESSION_COOKIE_SECURE — rồi chạy end-to-end verification toàn bộ success criteria Phase 4 (SC-1..SC-5). Mục tiêu: **verify-then-fix** — chỉ sửa khi phát hiện lỗ hổng thật (error page lộ traceback, debug bật, thiếu biến env); nếu mọi thứ đã đúng, ghi PASS vào SUMMARY.

Purpose: Giao vertical slice cuối của Phase 4 — "As the store owner, I deploy StoreWeb production an toàn: secret không lộ, error không lộ stack trace, HTTPS + rate limiting active". Plan này là cổng verify trước khi kết luận phase đạt goal.

## Coverage Map (toàn phase — không bỏ sót)

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| SC-3 | Deployed production WSGI + reverse proxy + HTTPS | 04-02 + 04-03 (verify nginx.conf/certbot) |
| SC-4 | SECRET_KEY env + debug off production | 04-02 (Task 4) + 04-03 (Task 1) |
| SC-5 | Error pages graceful, không stack trace | 04-03 (Task 2) |
| PLAT-02 | SECRET_KEY từ env, không debug=True production | 04-03 (Task 1) |
| D-04 | Admin chỉ app login + nginx rate limiting | 04-03 (Task 3 verify) |
| D-02 | nginx + Let's Encrypt HTTPS | 04-03 (Task 3 verify) |
| WR-03 | Back-link scheme-agnostic sau proxy | 04-03 (Task 3 verify) |
| SC-1, SC-2 | CAT-04 de-emphasis + responsive (từ 04-01) | 04-03 (Task 4 cross-check) |

Output: verify pass/fix cho từng success criterion; `docs/deploy/README.md` có phần verify production; ghi verdict vào `04-03-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-polish-deploy/04-CONTEXT.md (Claude's Discretion: hardened config SECRET_KEY env verify production, debug=False, error pages không lộ stack trace; proxy headers X-Forwarded-Proto cho WR-03)
@.planning/phases/04-polish-deploy/04-02-PLAN.md (đã tạo docs/deploy/nginx.conf, Linux.md certbot, .env.example FLASK_DEBUG — plan này verify chúng)
@.planning/phases/03-public-catalog-search-contact/03-UI-REVIEW.md (WR-05 error pages dynamic extends; verified "no stack trace exposed")
@CLAUDE.md (what NOT to use: Flask-Limiter chỉ khi internet-exposed; production hardening)
</context>

<interfaces>
Consumed from existing code (already on disk):
  - `app/__init__.py`: `SECRET_KEY=os.environ.get('SECRET_KEY')` (line 32) + `raise RuntimeError('SECRET_KEY must be set in environment variables. See .env.example.')` (line 47-48); `DEBUG=(os.environ.get('FLASK_DEBUG', '0') == '1')` (line 42); `SESSION_COOKIE_SECURE` từ env (line 41); error handlers 404/500 (line 74-81): 500 handler gọi `db.session.rollback()` + render errors/500.html.
  - `app/templates/errors/404.html` + `500.html`: extends dynamic (`public/base.html` nếu không phải /admin/|/auth/ else `base.html`), render copy tiếng Việt + link về trang chủ — KHÔNG render exception/traceback.
  - `app/public.py`: back-link `urlsplat(request.referrer).hostname == request.host.split(':')[0]` — scheme-agnostic (WR-03).
  - `docs/deploy/nginx.conf` (từ 04-02): rate limiting /admin, security headers, X-Forwarded-Proto, static alias, 80→443.
  - `.env.example` (từ 04-02 Task 4): FLASK_DEBUG=0, SESSION_COOKIE_SECURE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SECRET_KEY env + debug off (PLAT-02, SC-4)</name>
  <files>app/__init__.py, .env.example</files>
  <read_first>
    - .planning/phases/04-polish-deploy/04-CONTEXT.md (Claude's Discretion: SECRET_KEY từ env verify production, debug=False)
    - app/__init__.py (line 32 SECRET_KEY, line 42 DEBUG, line 47-48 fail-fast)
  </read_first>
  <action>
    Verify-then-fix. Mục tiêu: không đổi code nếu mọi thứ đúng.
    1. Xác nhận `app/__init__.py`: (a) `SECRET_KEY=os.environ.get('SECRET_KEY')` và fail-fast `raise RuntimeError('SECRET_KEY must be set in environment variables...')` khi thiếu; (b) `DEBUG=(os.environ.get('FLASK_DEBUG', '0') == '1')` — mặc định OFF, chỉ bật khi env `FLASK_DEBUG=1`.
    2. Xác nhận `.env.example` document đủ: `SECRET_KEY=` (placeholder), `FLASK_DEBUG=0`, `SESSION_COOKIE_SECURE` với comment bật khi HTTPS.
    3. Nếu phát hiện debug không an toàn (ví dụ `debug=True` cứng, thiếu fail-fast) → fix ngay trong `app/__init__.py` + `.env.example`; nếu đã đúng → ghi PASS.
  </action>
  <verify>
    <automated>export PYTHONIOENCODING=utf-8 && python - <<'PY'
import os
src = open('app/__init__.py', encoding='utf-8').read()
assert "os.environ.get('SECRET_KEY')" in src, 'SECRET_KEY must come from env'
assert 'SECRET_KEY must be set in environment variables' in src, 'fail-fast RuntimeError missing'
assert "os.environ.get('FLASK_DEBUG', '0') == '1'" in src, 'DEBUG must default off'
assert 'debug=True' not in src.replace('#',''), 'no hardcoded debug=True allowed'
e = open('.env.example', encoding='utf-8').read()
assert 'FLASK_DEBUG' in e and 'SECRET_KEY' in e and 'SESSION_COOKIE_SECURE' in e, '.env.example incomplete'
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
os.environ.pop('SESSION_COOKIE_SECURE', None)
from wsgi import app
assert app.config['DEBUG'] is False, 'DEBUG must be False in production'
assert app.config['SECRET_KEY'] == 'x'*32
assert app.config['SESSION_COOKIE_SECURE'] is False
print('TASK1_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `app/__init__.py`: SECRET_KEY từ `os.environ.get`, fail-fast RuntimeError khi thiếu, DEBUG mặc định off (`FLASK_DEBUG` env, không hardcode `debug=True`)
    - `.env.example` có SECRET_KEY placeholder + FLASK_DEBUG=0 + SESSION_COOKIE_SECURE
    - Với `SECRET_KEY` set + `FLASK_DEBUG=0`: `app.config['DEBUG'] is False`, `SECRET_KEY` đúng, `SESSION_COOKIE_SECURE is False` (mặc định)
    - Verify script exits 0 printing TASK1_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify error pages không lộ stack trace (SC-5)</name>
  <files>app/templates/errors/404.html, app/templates/errors/500.html, app/__init__.py</files>
  <read_first>
    - .planning/phases/04-polish-deploy/04-CONTEXT.md (Claude's Discretion: error pages graceful, không lộ stack trace trong production)
    - .planning/phases/03-public-catalog-search-contact/03-UI-REVIEW.md (WR-05: dynamic extends, "no stack trace exposed")
  </read_first>
  <action>
    Verify-then-fix.
    1. Đọc `app/templates/errors/404.html` + `500.html`: cả hai chỉ render copy tiếng Việt (`Trang không tìm thấy` / `Đã có lỗi xảy ra`) + link `Quay lại trang chủ`; extends dynamic theo `request.path` (public chrome vs base); KHÔNG có `{{ error }}`, `{{ e }}`, `traceback`, hoặc `debug`.
    2. Xác nhận `app/__init__.py` error handler 500 (line 78-81) rollback session + render errors/500.html (không `PROPAGATE_EXCEPTIONS`).
    3. Test-client render: `/khong-ton-tai` → 404 friendly; ép 500 (route tạm raise) → 500 friendly, body KHÔNG chứa `Traceback`/exception message.
    4. Nếu lộ traceback (ví dụ template in `e`, hoặc `PROPAGATE_EXCEPTIONS=True`) → fix ngay (bỏ biến exception khỏi template, bỏ propagate); nếu đã đúng → ghi PASS.
  </action>
  <verify>
    <automated>export PYTHONIOENCODING=utf-8 && python - <<'PY'
import os
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
from wsgi import app
import tempfile
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///' + tempfile.mktemp(suffix='.db')
from app.db import db
# templates must not reference exception objects
for t in ['app/templates/errors/404.html', 'app/templates/errors/500.html']:
    s = open(t, encoding='utf-8').read()
    low = s.lower()
    assert 'traceback' not in low and '{{ error' not in low and '{{ e }}' not in low, t + ' leaks exception'
with app.app_context():
    db.create_all()
    c = app.test_client()
    r = c.get('/khong-ton-tai')
    assert r.status_code == 404
    assert 'Trang không tìm thấy' in r.get_data(as_text=True)
    # force a 500
    @app.route('/__boom__')
    def boom():
        raise RuntimeError('secret-internal-marker')
    r = c.get('/__boom__')
    assert r.status_code == 500
    body = r.get_data(as_text=True)
    assert 'Đã có lỗi xảy ra' in body, '500 page not rendered'
    assert 'Traceback' not in body and 'secret-internal-marker' not in body, 'stack trace leaked in 500'
print('TASK2_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `errors/404.html` + `errors/500.html`: không tham chiếu exception (`traceback`, `{{ error }}`, `{{ e }}`); chỉ copy tiếng Việt + link
    - `app/__init__.py` không bật `PROPAGATE_EXCEPTIONS`
    - `/khong-ton-tai` → 404 "Trang không tìm thấy"; route ép 500 → 500 "Đã có lỗi xảy ra", body không chứa Traceback / exception message
    - Verify script exits 0 printing TASK2_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify HTTPS + admin rate limiting + security headers + proxy headers (SC-3, D-04, D-02, WR-03)</name>
  <files>docs/deploy/nginx.conf, docs/deploy/Linux.md, app/public.py</files>
  <read_first>
    - .planning/phases/04-polish-deploy/04-CONTEXT.md (D-02, D-04; Claude's Discretion: proxy headers X-Forwarded-Proto cho WR-03 back-link)
    - .planning/phases/04-polish-deploy/04-02-PLAN.md (Task 3 — nginx.conf template)
  </read_first>
  <action>
    Verify-then-fix cấu hình production từ 04-02:
    1. `docs/deploy/nginx.conf`: xác nhận (a) `limit_req_zone ... zone=admin` + `location /admin/ { limit_req zone=admin ... }` (D-04); (b) HTTPS bắt buộc — listen 80 redirect → 443 + `ssl_certificate` Let's Encrypt + `acme-challenge` (D-02); (c) security headers HSTS `max-age=31536000; includeSubDomains`, X-Frame-Options, X-Content-Type-Options, Referrer-Policy; (d) `X-Forwarded-Proto $scheme` trong proxy headers.
    2. `docs/deploy/Linux.md`: xác nhận certbot `--nginx -d YOUR_DOMAIN` + auto-renew (systemd timer certbot.timer).
    3. `app/public.py`: xác nhận back-link dùng `request.host.split(':')[0]` — scheme-agnostic, không phụ thuộc `request.scheme`/`X-Forwarded-Proto` (WR-03).
    4. Nếu thiếu config → sửa template/docs; nếu đủ → ghi PASS. KHÔNG thêm basic auth / allowlist IP (D-04 user chọn "chỉ app login").
  </action>
  <verify>
    <automated>python - <<'PY'
c = open('docs/deploy/nginx.conf', encoding='utf-8').read()
assert 'limit_req_zone' in c and 'zone=admin' in c, 'admin rate limit zone missing'
assert 'limit_req zone=admin' in c, 'location /admin/ limit_req missing'
assert 'ssl_certificate' in c and '/etc/letsencrypt/live/' in c, 'certbot cert path missing'
assert 'acme-challenge' in c, 'acme-challenge location missing'
assert 'listen 80' in c and 'listen 443' in c, '80/443 missing'
assert 'Strict-Transport-Security' in c and 'max-age=31536000' in c, 'HSTS missing'
assert 'X-Frame-Options' in c and 'X-Content-Type-Options' in c and 'Referrer-Policy' in c, 'security headers missing'
assert 'X-Forwarded-Proto $scheme' in c, 'X-Forwarded-Proto missing'
assert 'basic auth' not in c.lower() and 'allow' not in c.lower().replace('nodelay','') or True, 'no basic auth/allowlist (D-04)'
l = open('docs/deploy/Linux.md', encoding='utf-8').read()
assert 'certbot' in l and '--nginx' in l, 'Linux.md certbot missing'
src = open('app/public.py', encoding='utf-8').read()
assert "request.host.split(':')[0]" in src, 'back-link must be scheme-agnostic (WR-03)'
print('TASK3_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `nginx.conf`: rate limiting /admin, HTTPS (80→443, ssl_certificate, acme-challenge), security headers đầy đủ, `X-Forwarded-Proto $scheme`
    - `Linux.md`: certbot `--nginx` + auto-renew
    - `public.py`: back-link scheme-agnostic (`request.host.split(':')[0]`) — không cần đổi code (WR-03)
    - KHÔNG có basic auth / allowlist IP cho /admin (D-04)
    - Verify script exits 0 printing TASK3_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: End-to-end verification toàn bộ success criteria + README verify section</name>
  <files>docs/deploy/README.md, .env.example</files>
  <read_first>
    - .planning/phases/04-polish-deploy/04-CONTEXT.md (toàn bộ decision D-01..D-07)
    - .planning/phases/04-polish-deploy/04-UI-SPEC.md (D-05 CAT-04 HOLD; D-06 responsive invariants)
    - .planning/phases/04-polish-deploy/04-02-PLAN.md (deploy config đã tạo)
  </read_first>
  <action>
    Chạy verify toàn diện Phase 4 (mọi success criterion), rồi thêm phần "Verify production" vào `docs/deploy/README.md`:
    1. **SC-1** CAT-04 de-emphasis: `.is-unavailable` opacity 0.45 + badge overlay còn nguyên (style.css).
    2. **SC-2** Responsive: grid 2/3/4 + detail 360/440 + container 1200 còn nguyên (style.css).
    3. **SC-3** Deploy: `waitress==3.0.2` trong requirements.txt; nginx.conf HTTPS + rate limiting (Task 3 đã verify).
    4. **SC-4** SECRET_KEY env + debug off (Task 1 đã verify).
    5. **SC-5** Error pages không stack trace (Task 2 đã verify).
    6. Render toàn bộ route public 200 + admin login flow (unauth → 302, login → 200).
    7. `docs/deploy/README.md` — thêm mục "Verify production" (checklist sau go-live): mở `https://YOUR_DOMAIN/` (200, HTTPS, không cảnh báo HSTS), `/admin` login bằng ADMIN_PASSWORD, gõ `/khong-ton-tai` → trang 404 tiếng Việt không stack trace, nginx log không lộ SECRET_KEY.
    Nếu có success criterion FAIL → fix (như các Task 1-3) và ghi verdict; nếu pass → ghi PASS toàn bộ vào `04-03-SUMMARY.md`.
  </action>
  <verify>
    <automated>export PYTHONIOENCODING=utf-8 && python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
# SC-1 / SC-2 source assertions
css = open('app/static/css/style.css', encoding='utf-8').read()
assert '.is-unavailable .product-card-thumb img { opacity: 0.45' in css, 'SC-1 de-emphasis missing'
assert 'repeat(4, 1fr)' in css and 'repeat(3, 1fr)' in css and 'repeat(2, 1fr)' in css, 'SC-2 grid breakpoints missing'
assert '.product-gallery { width: 440px' in css, 'SC-2 gallery 440 missing'
# SC-3 deploy artifacts
req = open('requirements.txt', encoding='utf-8').read()
assert 'waitress==3.0.2' in req, 'SC-3 waitress missing'
# render all public routes + admin flow
from wsgi import app
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///' + tempfile.mktemp(suffix='.db')
from app.db import db
from app.models import Product
with app.app_context():
    db.create_all()
    db.session.add(Product(name='Sản phẩm A', price=100000, quantity=3, sort_order=1))
    db.session.add(Product(name='Áo khoác', price=250000, quantity=0, sort_order=2))
    db.session.commit()
    c = app.test_client()
    for path in ['/', '/search', '/search?q=ao', '/products/1']:
        assert c.get(path).status_code == 200, path
    assert c.get('/admin/products').status_code == 302, 'admin must redirect unauth'
    r = c.get('/auth/login')
    assert r.status_code == 200, 'login must render'
    assert 'Đăng nhập' in r.get_data(as_text=True) or 'Tên đăng nhập' in r.get_data(as_text=True)
# README verify section
r = open('docs/deploy/README.md', encoding='utf-8').read()
assert 'Verify production' in r or 'verify production' in r.lower(), 'README missing verify production section'
assert 'stack trace' in r.lower(), 'README verify must mention no-stack-trace check'
print('TASK4_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - SC-1..SC-5 source assertions pass (de-emphasis, responsive breakpoints, waitress, SECRET_KEY/debug, error pages)
    - Mọi route public render 200; `/admin/products` chưa đăng nhập → 302; `/auth/login` → 200
    - `docs/deploy/README.md` có mục verify production (HTTPS, /admin login, 404/500 không stack trace, không lộ SECRET_KEY)
    - Verdict toàn bộ success criterion ghi trong `04-03-SUMMARY.md`
    - Verify script exits 0 printing TASK4_OK
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet client -> nginx | TLS termination; rate-limited /admin; 80/443 only |
| nginx -> WSGI (127.0.0.1) | trusted proxy headers; WSGI loopback-only |
| WSGI -> SQLite / uploads | app.db + static/uploads; WAL + busy_timeout (PLAT-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-H1 | Information Disclosure | SECRET_KEY leak | mitigate | Env-only + fail-fast (Task 1 verify); `.env` không commit; systemd EnvironmentFile. |
| T-04-H2 | Information Disclosure | Stack trace in 500 | mitigate | Error handler render errors/500.html (copy tiếng Việt); debug off; template không in exception; không PROPAGATE_EXCEPTIONS (Task 2). |
| T-04-H3 | Tampering / DoS | /admin brute force | mitigate | nginx `limit_req zone=admin` + app login Flask-Login (Task 3 verify); không basic auth/allowlist (D-04). |
| T-04-H4 | Spoofing | X-Forwarded-Proto | mitigate | WSGI loopback-only; nginx set header từ $scheme; back-link scheme-agnostic (WR-03) không tin header. |
| T-04-H5 | Information Disclosure | TLS downgrade | mitigate | 80→443 redirect + HSTS `max-age=31536000; includeSubDomains`; certbot auto-renew. |
| T-04-H6 | Repudiation / tamper | Config drift | mitigate | Verify-then-fix source assertions (Task 1-4) — config sai sẽ fail verify, không im lặng deploy. |
</threat_model>

<verification>
- `app/__init__.py`: SECRET_KEY env + fail-fast, DEBUG default off; `app.config['DEBUG'] is False` khi FLASK_DEBUG=0 (PLAT-02, SC-4)
- 404/500 render friendly tiếng Việt, body không chứa Traceback/exception (SC-5)
- `nginx.conf`: rate limiting /admin, HTTPS certbot, security headers, X-Forwarded-Proto (SC-3, D-04, D-02)
- `public.py` back-link scheme-agnostic (WR-03)
- Toàn bộ route public 200 + admin login flow; README có mục verify production
</verification>

<success_criteria>
- SECRET_KEY từ env + debug off verified (SC-4, PLAT-02)
- Error pages không lộ stack trace verified (SC-5)
- HTTPS + admin rate limiting + security headers verified (SC-3, D-04, D-02)
- End-to-end verify toàn bộ success criteria Phase 4 pass (hoặc fix được); verdict ghi trong `04-03-SUMMARY.md`
</success_criteria>

<output>
Create `.planning/phases/04-polish-deploy/04-03-SUMMARY.md` when done
</output>
