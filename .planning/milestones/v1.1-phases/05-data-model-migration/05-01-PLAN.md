---
phase: 05-data-model-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/models.py]
autonomous: true
requirements: [ORD-04, COST-01]

must_haves:
  truths:
    - "Product có cột cost_price Integer nullable (VND, không Float — D-05); NULL = chưa nhập (COST-01)"
    - "Order lưu snapshot đầy đủ tên sản phẩm + giá bán + giá nhập + số lượng tại thời điểm đặt; xóa sản phẩm không mất đơn (ORD-04)"
    - "Order lưu thông tin khách (tên, SĐT, địa chỉ, ghi chú) + trạng thái String nhãn VN + timestamp utcnow (ORD-04)"
    - "db.create_all() trên DB mới tạo bảng orders đúng định nghĩa model (nửa của PLAT-05 — bảng được định nghĩa đúng)"
  artifacts:
    - path: "app/models.py"
      provides: "Product.cost_price column (Integer nullable, VND)"
      contains: "cost_price = db.Column(db.Integer, nullable=True)"
    - path: "app/models.py"
      provides: "Order model — snapshot pricing + customer info + status + timestamps"
      contains: "class Order"
  key_links:
    - from: "Order.product_id"
      to: "products.id"
      via: "ForeignKey ondelete='SET NULL' — xóa sản phẩm NULL hóa FK, đơn giữ nguyên nhờ snapshot (ORD-04)"
      pattern: "ondelete='SET NULL'"
    - from: "Order.created_at/updated_at"
      to: "app/models.py utcnow()"
      via: "default=utcnow — pattern codebase (Product.updated_at)"
      pattern: "default=utcnow"
---

<objective>
Thêm cột `cost_price` nullable (VND Integer) vào `Product` và model `Order` với snapshot giá bán + giá nhập + tên sản phẩm tại thời điểm đặt (ORD-04, COST-01). Đây là slice thuần data-model — không đổi route, không đổi template, không đổi hành vi runtime.

Purpose: Tạo nền cho toàn bộ v1.1 Buy System. Phase 6 (order form) ghi vào Order, Phase 7 (admin tracking) đọc Order + snapshot, Phase 8 (stats) tính profit từ `product_cost_price`. Snapshot đảm bảo xóa sản phẩm không làm mất đơn và admin luôn xem được sản phẩm gốc tại thời điểm đặt (ORD-04). Cột cost_price là nền cho field admin form (05-03) và profit stats (Phase 8).

## Coverage Map (đúng phạm vi Phase 5)

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| ORD-04 | Order giữ product_id FK nullable + snapshot đầy đủ (tên, giá bán, giá nhập, số lượng) | 05-01 (Task 1) |
| COST-01 | Cột cost_price Integer nullable, VND, không Float (D-05) | 05-01 (Task 1) |
| D-05 | Tiền = Integer VND, không Float | 05-01 (Task 1 — cost_price, product_price) |
| — | Trạng thái String nhãn VN, ID đơn = id tăng dần (decisions) | 05-01 (Task 1) |
| Deferred | Giỏ hàng nhiều sản phẩm (ORD-10), tự trừ tồn kho (ORD-12) | KHÔNG cài đặt |

Output: `app/models.py` có `Product.cost_price` + class `Order`. Không tạo file mới, không thêm dependency (05-UI-SPEC: zero new dependencies).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-data-model-migration/05-CONTEXT.md (decisions ORD-04/COST-01: snapshot, FK nullable, status String VN, ID đơn = id, cost_price Integer nullable)
@.planning/phases/05-data-model-migration/05-UI-SPEC.md (Interaction Contract — model/column contract cho Phase 5)
@CLAUDE.md (tech stack; D-05 Integer VND never Float)
</context>

<interfaces>
Consumed from existing code (already on disk):
  - `app/models.py` patterns: `__tablename__ = 'snake_case'`; `db.Column(db.Integer, ...)` cho tiền kèm comment `# VND, Integer only (D-05; never Float)` (xem `Product.price` line 27); timestamps `created_at = db.Column(db.DateTime, default=utcnow)` + `updated_at = db.Column(db.DateTime, default=utcnow, onupdate=utcnow)` (Product lines 36-37); relationship `images = db.relationship('ProductImage', backref='product', lazy='dynamic', cascade='all, delete-orphan')` (Product line 38)
  - `app/models.py` helper: `def utcnow(): return datetime.now(timezone.utc)` (lines 8-10) — đã định nghĩa, tái sử dụng
  - `app/models.py` import header: `from datetime import datetime, timezone`, `from .db import db` (đã có)
  - `db.create_all()` register mọi model vì `app/__init__.py` chạy `from . import models` (line 51)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Thêm Product.cost_price + Order model (snapshot pricing)</name>
  <files>app/models.py</files>
  <read_first>
    - .planning/phases/05-data-model-migration/05-CONTEXT.md (ORD-04: product_id FK nullable + snapshot đầy đủ; status String VN; ID đơn = id tăng dần; COST-01: cost_price Integer nullable VND)
    - app/models.py (Product/ProductImage pattern: snake_case tablename, utcnow(), Integer VND comment D-05)
    - CLAUDE.md (D-05: money as Integer VND, never Float)
  </read_first>
  <action>
    Sửa duy nhất `app/models.py`. Hai thay đổi:

    1. Trong class Product, thêm cột cost_price NGAY SAU dòng `price = db.Column(db.Integer, nullable=False)` (line 27), gom hai field giá cạnh nhau:
       - `cost_price = db.Column(db.Integer, nullable=True)  # VND, Integer only (D-05; never Float); NULL = not entered (COST-01)`
       - nullable=True → form bỏ trống persist NULL; Integer → VND integer, không Float (khớp comment pattern của `price`).

    2. Append class Order ở CUỐI `app/models.py` (sau ProductImage), theo convention codebase:
       - `class Order(db.Model):` + `__tablename__ = 'orders'` (snake_case)
       - `id = db.Column(db.Integer, primary_key=True)` — số hiệu đơn = ID tăng dần, KHÔNG mã định dạng (decision)
       - `product_id = db.Column(db.Integer, db.ForeignKey('products.id', ondelete='SET NULL'), nullable=True)` — FK nullable; xóa sản phẩm → NULL hóa FK, đơn không mất (ORD-04); `ondelete='SET NULL'` đúng cả khi bật PRAGMA foreign_keys về sau
       - Snapshot columns (bản sao tại thời điểm đặt — ORD-04):
         - `product_name = db.Column(db.String(200), nullable=False)`
         - `product_price = db.Column(db.Integer, nullable=False)` — giá bán VND, Integer only (D-05)
         - `product_cost_price = db.Column(db.Integer, nullable=True)` — giá nhập VND; NULL nếu sản phẩm không có giá nhập
         - `quantity = db.Column(db.Integer, nullable=False)` — số lượng đặt; >= 1 được enforce ở Phase 6 form
       - Customer PII columns:
         - `customer_name = db.Column(db.String(100), nullable=False)`
         - `customer_phone = db.Column(db.String(20), nullable=False)`
         - `customer_address = db.Column(db.Text, nullable=False)`
         - `customer_note = db.Column(db.Text, nullable=True)`
       - `status = db.Column(db.String(20), default='Chờ xác nhận', nullable=False)` — nhãn VN trực tiếp (decision); forward-only transitions được enforce ở Phase 7, Phase 5 chỉ định nghĩa column (05-CONTEXT specifics)
       - `created_at = db.Column(db.DateTime, default=utcnow)` và `updated_at = db.Column(db.DateTime, default=utcnow, onupdate=utcnow)` — khớp pattern Product
       - `product = db.relationship('Product', backref=db.backref('orders', passive_deletes=True))` — passive_deletes: SQLAlchemy không load/nullify orders khi xóa Product, dựa vào ON DELETE SET NULL; đơn tồn tại sau khi xóa sản phẩm (ORD-04)

    KHÔNG thêm: `__repr__` (model hiện có không dùng), server_default (pattern dùng Python-side default), index trên status (defer — Phase 7 xử lý filter nếu cần), cascade delete (đơn phải sống sót qua xóa sản phẩm).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
with app.app_context():
    from sqlalchemy import inspect
    from app.db import db
    from app.models import Product, Order
    db.create_all()
    insp = inspect(db.engine)
    pcols = {c['name']: c for c in insp.get_columns('products')}
    assert 'cost_price' in pcols, 'cost_price column missing'
    assert pcols['cost_price']['nullable'] is True, 'cost_price must be nullable'
    assert pcols['cost_price']['type'].python_type is int, 'cost_price must be Integer, not Float (D-05)'
    ocols = {c['name']: c for c in insp.get_columns('orders')}
    for col in ['id','product_id','product_name','product_price','product_cost_price','quantity','customer_name','customer_phone','customer_address','customer_note','status','created_at','updated_at']:
        assert col in ocols, 'missing orders column: ' + col
    fks = insp.get_foreign_keys('orders')
    assert fks and fks[0]['referred_table'] == 'products', 'orders must FK to products'
    assert fks[0]['options'].get('ondelete') == 'SET NULL', 'FK must be ON DELETE SET NULL (ORD-04)'
    o = Order(product_name='X', product_price=1000, quantity=1, customer_name='A', customer_phone='1', customer_address='2')
    db.session.add(o); db.session.flush()
    assert o.id is not None and o.status == 'Chờ xác nhận', 'status default must be Chờ xác nhận'
    db.session.rollback()
print('TASK_OK')
PY</automated>
  </verify>
  <done>Product.cost_price (Integer nullable VND) và class Order (13 columns, FK product_id ondelete SET NULL, status default 'Chờ xác nhận', timestamps utcnow) tồn tại trong app/models.py; không route/template/dependency mới</done>
  <acceptance_criteria>
    - `app/models.py` class Product chứa `cost_price = db.Column(db.Integer, nullable=True)` ngay sau `price`; inspect xác nhận Integer (python_type is int), nullable True
    - class Order với `__tablename__ = 'orders'` và đủ 13 column: id, product_id, product_name, product_price, product_cost_price, quantity, customer_name, customer_phone, customer_address, customer_note, status, created_at, updated_at
    - FK product_id → products.id với ondelete='SET NULL'; snapshot gồm product_name/product_price/product_cost_price/quantity
    - status default 'Chờ xác nhận'; created_at/updated_at default=utcnow
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → SQLite (schema) | Phase 5 là model-only slice: không route, không input mới. `db.create_all()` sinh bảng `orders` từ định nghĩa model; cột `cost_price` là schema artifact, chưa có luồng input nào chạm vào ở Phase 5. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `Product.cost_price` column | mitigate | Cột nullable Integer VND; không route/template nào đọc nó ở Phase 5. Rủi ro lộ chỉ hình thành nếu một template render nó — bị chặn bởi 05-03 (COST-02) grep toàn bộ `app/templates/public/**` không chứa `cost_price`/`Giá nhập`. |
| T-05-02 | Tampering | `Order.product_id` FK + delete semantics | mitigate | FK nullable + `ondelete='SET NULL'` + `passive_deletes=True` trên backref: xóa Product NULL hóa FK thay vì cascade-delete hoặc lỗi — snapshot đơn giữ nguyên (ORD-04). Không cascade trên relationship. |
| T-05-03 | Information Disclosure | Order customer PII (customer_name/phone/address) data-at-rest | accept | Dữ liệu trong SQLite server-local, chỉ admin truy cập qua auth Phase 1. Phase 5 không có route đọc orders (route đến Phase 7 sau `@login_required`). Bổ sung admin-only access ở Phase 7; không đưa PII vào public. |
</threat_model>

<verification>
- Task 1 verify script exits 0 printing TASK_OK: Product.cost_price Integer nullable; bảng orders đủ 13 column; FK ondelete SET NULL; status default 'Chờ xác nhận'
- Không đổi route/template/CSS; không thêm dependency; không cài đặt deferred (ORD-10/ORD-12)
</verification>

<success_criteria>
- Product có cột `cost_price` Integer nullable VND (D-05, không Float) — COST-01
- Order model đầy đủ: snapshot (tên + giá bán + giá nhập + số lượng), customer info (tên/SĐT/địa chỉ/ghi chú), status String VN default 'Chờ xác nhận', created_at/updated_at utcnow — ORD-04
- `db.create_all()` tạo bảng `orders` trên DB mới (nửa bảng của PLAT-05)
- Không có task cài đặt ORD-10 hoặc ORD-12 (deferred)
</success_criteria>

<output>
Create `.planning/phases/05-data-model-migration/05-01-SUMMARY.md` when done
</output>
