---
phase: 05-data-model-migration
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [app/db.py]
autonomous: true
requirements: [PLAT-05]

must_haves:
  truths:
    - "init-db chạy trên bản sao DB v1.0 (products không cost_price, không bảng orders): thêm cost_price + tạo orders, exit 0, không mất dữ liệu (PLAT-05)"
    - "init-db chạy trên DB mới (fresh): create_all tạo products ĐÃ có cost_price + bảng orders"
    - "init-db idempotent — chạy lần 2 exit 0, không lặp column, dữ liệu không đổi"
  artifacts:
    - path: "app/db.py"
      provides: "init-db: create_all + PRAGMA table_info guard + ALTER TABLE ADD COLUMN cost_price"
      contains: "ALTER TABLE products ADD COLUMN cost_price INTEGER"
  key_links:
    - from: "app/db.py init_db_command"
      to: "products table"
      via: "PRAGMA table_info(products) guard trước ALTER — idempotent (PLAT-05)"
      pattern: "PRAGMA table_info"
    - from: "app/db.py init_db_command"
      to: "orders table"
      via: "db.create_all() tạo bảng thiếu (orders) — model Order register bởi `from . import models` trong app/__init__.py"
      pattern: "db.create_all"
---

<objective>
Mở rộng CLI `init-db` để migration an toàn, idempotent cho cả DB mới lẫn DB v1.0 cũ (PLAT-05): `db.create_all()` tạo bảng `orders` còn thiếu; guard `PRAGMA table_info(products)` chặn trước `ALTER TABLE products ADD COLUMN cost_price INTEGER`. Một đường code duy nhất phục vụ cả hai trường hợp, không mất dữ liệu.

Purpose: DB `data/app.db` hiện tại là schema v1.0 (products KHÔNG có cost_price, không có bảng orders — đã xác nhận bằng PRAGMA). Khi 05-01 thêm cột vào model, `create_all` KHÔNG thể thêm cột vào bảng đã tồn tại — ALTER guard là bước bắt buộc. Verify chạy trên bản SAO của DB v1.0 thật để chứng minh không mất dữ liệu, không đụng DB thật trong lúc execute.

## Coverage Map (đúng phạm vi Phase 5)

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| PLAT-05 | Migration idempotent: thêm cost_price + bảng orders, không mất dữ liệu | 05-02 (Task 1) |
| — | Mở rộng CLI init-db, guard PRAGMA trước ALTER (05-CONTEXT decisions) | 05-02 (Task 1) |
| Deferred | ORD-10 / ORD-12 | KHÔNG cài đặt |

Output: `app/db.py` init-db thêm migration guard. Không tạo file mới, không dependency mới.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-data-model-migration/05-CONTEXT.md (PLAT-05 decision: guard PRAGMA table_info trước ALTER; create_all tạo bảng orders; 1 đường code cho fresh + DB cũ)
@.planning/phases/05-data-model-migration/05-01-PLAN.md (đã execute: Order model + Product.cost_price trên disk)
@.planning/phases/05-data-model-migration/05-UI-SPEC.md (Migration section: PRAGMA guard + ALTER; idempotent)
</context>

<interfaces>
Consumed from existing code (already on disk):
  - `app/db.py` `init_db_command` hiện tại (PLAT-04): `@click.command('init-db')` + `@with_appcontext`; check ADMIN_PASSWORD (empty/change-me/<8 chars → ClickException); `db.create_all()` (line 29); admin upsert `AdminUser.query.filter_by(...)` + `generate_password_hash` + `db.session.commit()` (lines 31-38)
  - `app/db.py` header: `import click`, `from flask.cli import with_appcontext`, `from flask_sqlalchemy import SQLAlchemy`, `from werkzeug.security import generate_password_hash`; `db = SQLAlchemy()` (line 8)
  - `app/__init__.py`: `from . import models` (line 51) register Order/Product → `db.create_all()` trong init-db thấy mọi model
  - SQLite PRAGMA: `PRAGMA table_info(products)` trả rows `(cid, name, type, notnull, dflt_value, pk)` → `row[1]` = tên cột
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: init-db idempotent — PRAGMA guard + ALTER cost_price</name>
  <files>app/db.py</files>
  <read_first>
    - .planning/phases/05-data-model-migration/05-CONTEXT.md (PLAT-05: guard PRAGMA table_info trước ALTER; create_all tạo orders; 1 đường code fresh + DB cũ)
    - app/db.py (init_db_command hiện tại: password check → db.create_all → admin upsert)
    - app/models.py (Order model + Product.cost_price — đã tồn tại trên disk sau 05-01)
  </read_first>
  <action>
    Sửa `app/db.py` — chỉ trong `init_db_command`, giữ nguyên toàn bộ password checks và admin upsert:

    1. Thêm import `from sqlalchemy import text` (đầu file, cạnh các import hiện có).

    2. Giữ `db.create_all()` nguyên vị trí (sau password checks, line 29) — idempotent (CREATE TABLE IF NOT EXISTS), tạo bảng `orders` còn thiếu. create_all KHÔNG bao giờ đổi/ALTER bảng đã tồn tại nên an toàn với DB cũ.

    3. Ngay sau `db.create_all()`, thêm migration guard (PLAT-05):
       - with db.engine.begin() as conn:
           rows = conn.execute(text('PRAGMA table_info(products)')).fetchall()
           if not any(row[1] == 'cost_price' for row in rows):
               conn.execute(text('ALTER TABLE products ADD COLUMN cost_price INTEGER'))
               click.echo('Migrated: added products.cost_price (v1.0 -> v1.1).')
       - `row[1]` là tên cột (PRAGMA trả cid, name, type, notnull, dflt_value, pk)
       - `engine.begin()` mở transaction: commit khi thành công, rollback khi lỗi — ALTER không thể dở chừng
       - Chạy SAU create_all → bảng products chắc chắn tồn tại trước PRAGMA

    4. Admin upsert block (lines 31-38) giữ nguyên.

    Idempotent semantics (đúng theo 05-CONTEXT):
    - Fresh DB: create_all tạo products ĐÃ có cost_price (từ model 05-01) → guard thấy cột → bỏ qua ALTER
    - DB v1.0: create_all tạo orders; guard thấy thiếu cost_price → ALTER thêm (nullable, không chạm dòng dữ liệu nào)
    - Chạy lần 2: cost_price đã có → bỏ qua; create_all không tạo lại orders → exit 0, dữ liệu không đổi

    KHÔNG thêm lệnh DROP/RENAME/backup; KHÔNG thay đổi URI hoặc WAL pragma (app/__init__.py đã xử lý); không thêm dependency.
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile, shutil, sqlite3
os.environ['SECRET_KEY'] = 'x'*32
os.environ['ADMIN_PASSWORD'] = 'testpass1234'
os.environ['ADMIN_USERNAME'] = 'admin'
os.environ['FLASK_DEBUG'] = '0'
import app as app_module

def db_tables(path):
    con = sqlite3.connect(path); cur = con.cursor()
    tables = {r[0] for r in cur.execute("SELECT name FROM sqlite_master WHERE type='table'")}
    cols = {t: [c[1] for c in cur.execute('PRAGMA table_info(%s)' % t)] for t in tables}
    counts = {t: cur.execute('SELECT COUNT(*) FROM "%s"' % t).fetchone()[0] for t in tables}
    con.close(); return tables, cols, counts

def run_init(base_dir):
    app_module.BASE_DIR = base_dir
    app = app_module.create_app()
    r = app.test_cli_runner().invoke(args=['init-db'])
    assert r.exit_code == 0, r.output
    return r

# CASE A: bản sao DB v1.0 thật -> migration không mất dữ liệu
d = tempfile.mkdtemp(); os.makedirs(os.path.join(d, 'data'))
cp = os.path.join(d, 'data', 'app.db')
shutil.copy('data/app.db', cp)
t0, c0, n0 = db_tables(cp)
assert 'orders' not in t0 and 'cost_price' not in c0['products'], 'precondition: bản sao phải là v1.0'
run_init(d)
t1, c1, n1 = db_tables(cp)
assert 'orders' in t1, 'orders table missing sau migration'
assert 'cost_price' in c1['products'], 'cost_price column missing sau migration'
for k in n0:
    assert n1[k] == n0[k], 'data loss table %s: %s -> %s' % (k, n0[k], n1[k])

# CASE B: idempotent — chạy lần 2 không lỗi, không lặp, không đổi dữ liệu
run_init(d)
t2, c2, n2 = db_tables(cp)
assert 'cost_price' in c2['products'] and 'orders' in t2
for k in n1:
    assert n2[k] == n1[k], 'lần 2 làm đổi dữ liệu table %s' % k

# CASE C: DB mới (fresh) -> products có cost_price + bảng orders
d2 = tempfile.mkdtemp(); os.makedirs(os.path.join(d2, 'data'))
run_init(d2)
t3, c3, n3 = db_tables(os.path.join(d2, 'data', 'app.db'))
assert 'orders' in t3 and 'cost_price' in c3['products'], 'fresh DB thiếu orders/cost_price'
assert n3['orders'] == 0 and n3['products'] == 0
print('TASK_OK')
PY</automated>
  </verify>
  <done>init-db thêm cost_price + bảng orders trên bản sao DB v1.0 (không mất dữ liệu), idempotent khi chạy lần 2, đúng trên DB mới; DB thật không bị đổi trong verify</done>
  <acceptance_criteria>
    - `app/db.py` chứa guard `PRAGMA table_info(products)` + `ALTER TABLE products ADD COLUMN cost_price INTEGER` ngay sau `db.create_all()`; import `from sqlalchemy import text` tồn tại
    - Case A (bản sao v1.0): `orders` + `cost_price` xuất hiện; số dòng mọi bảng cũ KHÔNG đổi (không mất dữ liệu)
    - Case B (idempotent): chạy lần 2 exit 0, không lặp column, dữ liệu không đổi
    - Case C (fresh): products có cost_price + bảng orders
    - Verify script exits 0 printing TASK_OK
    - DB thật `data/app.db` không bị đổi trong lúc verify (chỉ thao tác temp copy)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → SQLite (data/app.db) | `init-db` chạy DDL (CREATE TABLE + ALTER TABLE) lên file DB. Idempotency là thuộc tính an toàn — chạy lại không được hỏng DB. |
| env → AdminUser upsert | ADMIN_PASSWORD/ADMIN_USERNAME từ môi trường được dùng để upsert admin (PLAT-04, giữ nguyên). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `ALTER TABLE products ADD COLUMN cost_price` | mitigate | Guard `PRAGMA table_info(products)` chạy trước ALTER — chỉ ALTER khi cột chưa tồn tại (skip nếu đã có). `engine.begin()` bọc transaction: commit on success / rollback on error — không có ALTER dở chừng. ADD COLUMN nullable không chạm dòng dữ liệu nào (no data loss). |
| T-05-02 | Tampering | `db.create_all()` trên DB đã tồn tại | mitigate | create_all chỉ tạo bảng THIẾU (IF NOT EXISTS semantics) — không bao giờ DROP/ALTER bảng tồn tại; dữ liệu products/product_images/admin_users giữ nguyên (verify Case A đếm dòng trước/sau). |
| T-05-03 | Tampering | SQL injection vào PRAGMA/ALTER | mitigate | Tên bảng/cột là literal cứng trong code (`'products'`, `'cost_price'`), không bao giờ lấy từ input người dùng; `text()` chỉ thực thi SQL hằng số. |
</threat_model>

<verification>
- Task 1 verify script exits 0 printing TASK_OK (Case A v1.0-copy, Case B idempotent, Case C fresh)
- Trong lúc verify, DB thật `data/app.db` KHÔNG bị đổi — chỉ dùng bản sao temp (đã chứng minh bằng harness: patched BASE_DIR trước create_app)
</verification>

<success_criteria>
- `init-db` idempotent trên cả 3 trường hợp: DB v1.0 cũ (thêm cost_price + orders), DB mới (create_all tạo đủ), chạy lặp (không đổi gì) — PLAT-05
- Không mất dữ liệu: số dòng mọi bảng cũ không đổi sau migration (verify trên bản sao DB thật)
- Operator note ghi trong SUMMARY: migrate DB thật bằng `flask --app app init-db` (cần ADMIN_PASSWORD hợp lệ trong .env) trước Phase 6 — vì init-db upsert password admin từ env
</success_criteria>

<output>
Create `.planning/phases/05-data-model-migration/05-02-SUMMARY.md` when done
</output>
