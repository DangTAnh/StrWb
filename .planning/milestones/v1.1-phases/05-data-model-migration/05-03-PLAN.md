---
phase: 05-data-model-migration
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified: [app/forms.py, app/admin.py, app/templates/admin/products/form.html]
autonomous: true
requirements: [COST-01, COST-02]

must_haves:
  truths:
    - "Admin form create/edit có field tùy chọn 'Giá nhập (VND)' ngay sau row Giá/Thương hiệu, kèm help-text 'Chỉ quản trị viên xem được', không dấu sao required (05-UI-SPEC 6/6 PASS)"
    - "Giá nhập lưu Integer VND (NULL khi bỏ trống); âm bị chặn bởi NumberRange(min=0) với message 'Giá nhập không được âm' (COST-01)"
    - "Create lẫn Edit đều persist cost_price (05-UI-SPEC Interaction Contract)"
    - "cost_price không xuất hiện trên bất kỳ template public nào — app/templates/public/** (COST-02)"
  artifacts:
    - path: "app/forms.py"
      provides: "ProductForm.cost_price IntegerField Optional + NumberRange"
      contains: "Giá nhập (VND)"
    - path: "app/admin.py"
      provides: "new_product constructor persist cost_price"
      contains: "cost_price=form.cost_price.data"
    - path: "app/templates/admin/products/form.html"
      provides: "cost_price field markup sau row giá/thương hiệu"
      contains: "Giá nhập (VND)"
  key_links:
    - from: "app/templates/admin/products/form.html"
      to: "app/forms.py"
      via: "{{ form.cost_price(class='input', min='0', step='1') }} render field mới"
      pattern: "form.cost_price"
    - from: "app/admin.py new_product"
      to: "app/forms.py"
      via: "form.cost_price.data or None truyền vào Product(cost_price=...) — create persist"
      pattern: "cost_price=form.cost_price.data"
    - from: "COST-02 guard"
      to: "app/templates/public/**"
      via: "không template public nào chứa 'cost_price' hoặc 'Giá nhập'"
      pattern: "cost_price"
---

<objective>
Thêm field tùy chọn "Giá nhập (VND)" lên admin product create/edit form (COST-01) đúng 05-UI-SPEC (contract 6/6 PASS): field mới duy nhất, đặt ngay sau row "Giá (VND) | Thương hiệu", full-width `.form-field`, không CSS/component/token mới. Đồng thời enforce COST-02: giá nhập không bao giờ xuất hiện trên template public.

Purpose: Cho admin nhập giá nhập (chỉ admin thấy) để Phase 8 tính lợi nhuận = doanh thu − giá nhập. Field nằm cạnh giá bán để admin thấy lợi nhuận từng sản phẩm khi nhập. UI change duy nhất của Phase 5 (theo 05-UI-SPEC).

## Coverage Map (đúng phạm vi Phase 5)

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| COST-01 | Field giá nhập tùy chọn trên admin form, chỉ admin thấy | 05-03 (Task 1 + Task 2) |
| COST-02 | Giá nhập không bao giờ hiển thị cho khách (public) | 05-03 (Task 2 — grep guard) |
| D-05 | Integer VND, không Float — input type=number min=0 step=1 | 05-03 (Task 2) |
| Deferred | ORD-10 / ORD-12 | KHÔNG cài đặt |

Output: `app/forms.py` + `app/admin.py` + `app/templates/admin/products/form.html`. Không thêm CSS, không dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-data-model-migration/05-UI-SPEC.md (BINDING contract — Copywriting: label 'Giá nhập (VND)', help-text 'Chỉ quản trị viên xem được', error 'Giá nhập không được âm'; Layout & Component Contract: markup chính xác, placement sau row Giá/Thương hiệu; Interaction Contract: WTForms + Model; COST-02 non-goal)
@.planning/phases/05-data-model-migration/05-CONTEXT.md (COST-01/COST-02; patterns: Optional() + NumberRange(min=0) cho field tùy chọn như admin_note/sku/sort_order)
@.planning/phases/05-data-model-migration/05-01-PLAN.md (đã execute: Product.cost_price column trên disk)
@CLAUDE.md (Flask-WTF + CSRF trên admin forms; D-05 Integer VND)
</context>

<interfaces>
Consumed from existing code (already on disk):
  - `app/forms.py` imports (line 4): `from wtforms import IntegerField, TextAreaField, BooleanField` + `from wtforms.validators import DataRequired, InputRequired, Optional, Length, NumberRange` — ĐÃ có IntegerField/Optional/NumberRange, không cần thêm import
  - `app/forms.py` `ProductForm` hiện tại (lines 13-24): `price = IntegerField('Giá (VND)', validators=[InputRequired(...), NumberRange(min=0, message='Giá không được âm')])` (line 15); pattern field tùy chọn: `sort_order = IntegerField('Thứ tự hiển thị', validators=[Optional()], default=0)` (line 22), `admin_note = TextAreaField('Ghi chú nội bộ', validators=[Optional()])` (line 23)
  - `app/admin.py` `new_product()` (lines 103-131): constructor dùng explicit kwargs `Product(name=..., price=form.price.data, brand=form.brand.data or None, ...)` (lines 107-118) — KHÔNG dùng populate_obj, phải thêm cost_price bằng tay
  - `app/admin.py` `edit_product()` (lines 134-155): dùng `form.populate_obj(product)` (line 142) — tự bind mọi field có trong form, KHÔNG cần sửa
  - `app/templates/admin/products/form.html` (lines 18-29): form-row-2 "Giá (VND) | Thương hiệu" — field cost_price chèn ngay sau `</div>` đóng của row này; pattern help-text của admin_note (lines 70-75): `<p class="help-text">Chỉ quản trị viên xem được</p>`
  - Login URL là `/login` (auth blueprint KHÔNG có url_prefix — verify dùng đường này, không phải /auth/login)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ProductForm.cost_price + new_product persistence</name>
  <files>app/forms.py, app/admin.py</files>
  <read_first>
    - .planning/phases/05-data-model-migration/05-UI-SPEC.md (Interaction Contract — field WTForms đúng như spec)
    - app/forms.py (ProductForm pattern: IntegerField + Optional + NumberRange cho sort_order/sku)
    - app/admin.py (new_product dùng explicit kwargs — cần thêm cost_price; edit_product dùng populate_obj tự bind)
  </read_first>
  <action>
    1. `app/forms.py` — trong class ProductForm, thêm field NGAY SAU `price` (line 15), đúng 05-UI-SPEC:
       - `cost_price = IntegerField('Giá nhập (VND)', validators=[Optional(), NumberRange(min=0, message='Giá nhập không được âm')])`
       - IntegerField ép int (D-05, không Float); `Optional()` → input trống lưu None/NULL (khớp pattern sku/sort_order/admin_note); `NumberRange(min=0)` chặn âm, message EXACT `'Giá nhập không được âm'` (05-UI-SPEC Copywriting)
       - Không cần thêm import (IntegerField/Optional/NumberRange đã có ở line 4)

    2. `app/admin.py` — `new_product()` (lines 106-118): constructor dùng explicit kwargs, KHÔNG dùng populate_obj, nên BẮT BUỘC thêm:
       - `cost_price=form.cost_price.data or None,`
       - Đặt ngay sau dòng `price=form.price.data,` (line 109). Thiếu dòng này → create âm thầm bỏ cost_price.
       - `edit_product()` KHÔNG cần sửa: `form.populate_obj(product)` tự bind field mới (UI-SPEC xác nhận). Không đổi route/route khác.

    Chú ý: 05-UI-SPEC ghi "no route change needed" — đúng cho edit (populate_obj), nhưng SAI cho create (new_product dùng constructor explicit kwargs). Task này sửa create.
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile, re
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
from app.db import db
from app.models import AdminUser, Product
from werkzeug.security import generate_password_hash
with app.app_context():
    db.create_all()
    db.session.add(AdminUser(username='admin', password_hash=generate_password_hash('secret123')))
    db.session.commit()
c = app.test_client()
def csrf(path):
    html = c.get(path).get_data(as_text=True)
    m = re.search(r'name="csrf_token"[^>]*value="([^"]+)"', html)
    assert m, 'csrf token not found in ' + path
    return m.group(1)
tok = csrf('/login')
r = c.post('/login', data={'csrf_token': tok, 'username': 'admin', 'password': 'secret123'}, follow_redirects=False)
assert r.status_code == 302, 'login failed'
# create persists cost_price
tok2 = csrf('/admin/products/new')
r2 = c.post('/admin/products/new', data={'csrf_token': tok2, 'name': 'Áo khoác', 'price': 250000, 'brand': '', 'measurements': '', 'description': '', 'quantity': 5, 'discontinued': '', 'sku': '', 'sort_order': '', 'admin_note': '', 'cost_price': '150000', 'image_order': '', 'delete_images': ''}, follow_redirects=False)
assert r2.status_code == 302, 'create failed'
with app.app_context():
    p = Product.query.filter_by(name='Áo khoác').first()
    assert p is not None and p.cost_price == 150000, 'create must persist cost_price=150000'
    pid = p.id
# edit persists updated cost_price
tok3 = csrf('/admin/products/%d/edit' % pid)
r3 = c.post('/admin/products/%d/edit' % pid, data={'csrf_token': tok3, 'name': 'Áo khoác', 'price': 260000, 'brand': '', 'measurements': '', 'description': '', 'quantity': 5, 'discontinued': '', 'sku': '', 'sort_order': '', 'admin_note': '', 'cost_price': '200000', 'image_order': '', 'delete_images': ''}, follow_redirects=False)
assert r3.status_code == 302, 'edit failed'
with app.app_context():
    p2 = db.session.get(Product, pid)
    assert p2.cost_price == 200000, 'edit must persist cost_price'
# negative rejected with exact message
tok4 = csrf('/admin/products/new')
r4 = c.post('/admin/products/new', data={'csrf_token': tok4, 'name': 'Xấu', 'price': 1000, 'brand': '', 'measurements': '', 'description': '', 'quantity': 1, 'discontinued': '', 'sku': '', 'sort_order': '', 'admin_note': '', 'cost_price': '-5', 'image_order': '', 'delete_images': ''})
body = r4.get_data(as_text=True)
assert r4.status_code == 200 and 'Giá nhập không được âm' in body, 'negative cost_price must be rejected'
# empty -> NULL
tok5 = csrf('/admin/products/new')
r5 = c.post('/admin/products/new', data={'csrf_token': tok5, 'name': 'Bỏ trống', 'price': 1000, 'brand': '', 'measurements': '', 'description': '', 'quantity': 1, 'discontinued': '', 'sku': '', 'sort_order': '', 'admin_note': '', 'cost_price': '', 'image_order': '', 'delete_images': ''}, follow_redirects=False)
assert r5.status_code == 302, 'empty cost_price create failed'
with app.app_context():
    p3 = Product.query.filter_by(name='Bỏ trống').first()
    assert p3 is not None and p3.cost_price is None, 'empty cost_price must persist NULL'
print('TASK_OK')
PY</automated>
  </verify>
  <done>ProductForm.cost_price + new_product persist: create/edit lưu giá trị cost_price, âm bị chặn ('Giá nhập không được âm'), bỏ trống → NULL; verify exits 0 printing TASK_OK</done>
  <acceptance_criteria>
    - `app/forms.py` chứa `cost_price = IntegerField('Giá nhập (VND)', validators=[Optional(), NumberRange(min=0, message='Giá nhập không được âm')])`
    - `app/admin.py` `new_product()` chứa `cost_price=form.cost_price.data or None,`
    - POST create cost_price=150000 → Product.cost_price == 150000; POST edit cost_price=200000 → cập nhật
    - POST cost_price=-5 → form render lại status 200 kèm chuỗi 'Giá nhập không được âm'; POST cost_price rỗng → persist NULL
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: cost_price field markup form.html + COST-02 public guard</name>
  <files>app/templates/admin/products/form.html</files>
  <read_first>
    - .planning/phases/05-data-model-migration/05-UI-SPEC.md (Layout & Component Contract — markup chính xác; Copywriting — help-text 'Chỉ quản trị viên xem được'; COST-02 non-goal)
    - app/templates/admin/products/form.html (vị trí chèn sau form-row-2 giá/thương hiệu lines 18-29; pattern .form-field/.help-text/.field-error của admin_note lines 70-75)
  </read_first>
  <action>
    Chèn field cost_price ngay SAU `</div>` đóng của "Giá (VND) | Thương hiệu" form-row-2 (hiện là lines 18-29), thành `.form-field` full-width riêng — KHÔNG thêm vào trong form-row-2:
    - <div class="form-field">
        {{ form.cost_price.label }}
        {{ form.cost_price(class="input", min="0", step="1") }}
        <p class="help-text">Chỉ quản trị viên xem được</p>
        {% for error in form.cost_price.errors %}<span class="field-error">{{ error }}</span>{% endfor %}
      </div>
    - KHÔNG `required` attribute, KHÔNG `.required` span, KHÔNG `aria-required` (field tùy chọn — khớp xử lý sku/sort_order/admin_note)
    - WTForms IntegerField render `type="number"`; `min="0"` `step="1"` (integer VND, D-05)
    - help-text EXACT `'Chỉ quản trị viên xem được'` (tái sử dụng nguyên văn từ field admin_note — 05-UI-SPEC)
    - Chỉ dùng class có sẵn (.form-field/.input/.help-text/.field-error) — KHÔNG thêm CSS mới, KHÔNG token mới, KHÔNG media query
    - COST-02: KHÔNG thêm 'Giá nhập' hoặc cost_price vào bất kỳ template public nào (`app/templates/public/**`); không đổi copy public
  </action>
  <verify>
    <automated>python - <<'PY'
import glob, re
t = open('app/templates/admin/products/form.html', encoding='utf-8').read()
assert 'Giá nhập (VND)' in t, 'label missing'
assert 'Chỉ quản trị viên xem được' in t, 'help-text missing'
assert re.search(r'form\.cost_price\(class="input", min="0", step="1"\)', t) or re.search(r"form\.cost_price\(class='input', min='0', step='1'\)", t), 'input attrs missing'
block = t[t.find('Giá nhập (VND)'):]
block = block[:block.find('{% endfor %}')+12]
assert 'required' not in block, 'cost_price must not be required'
for f in glob.glob('app/templates/public/**/*.html', recursive=True):
    s = open(f, encoding='utf-8').read()
    assert 'cost_price' not in s and 'Giá nhập' not in s, f + ' leaks cost price (COST-02)'
print('TASK_OK')
PY</automated>
  </verify>
  <done>form.html có block cost_price đúng markup (label + input min/step + help-text + error loop) sau row Giá/Thương hiệu; không template public chứa 'Giá nhập'/'cost_price' (COST-02); verify exits 0 printing TASK_OK</done>
  <acceptance_criteria>
    - `form.html` chứa block cost_price đúng markup (label + input class="input" min="0" step="1" + help-text + error loop) ngay sau row Giá/Thương hiệu
    - Block không có `required`/`aria-required`; không CSS/token/media query mới
    - Grep toàn bộ `app/templates/public/**/*.html`: không chứa 'cost_price' hoặc 'Giá nhập' (COST-02)
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → product form (cost_price) | Input số không tin cậy từ admin browser vượt qua Flask-WTF validation; CSRFProtect bảo vệ POST (Phase 1). |
| Server → public templates | cost_price (dữ liệu admin-only) phải không bao giờ render ở `app/templates/public/**` (COST-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `cost_price` IntegerField input | mitigate | WTForms IntegerField ép int — giá trị phi-số bị validation error; `NumberRange(min=0)` chặn âm với message 'Giá nhập không được âm'; `Optional()` lưu rỗng thành None/NULL. Flask-WTF CSRFProtect đã bọc mọi POST admin. |
| T-05-02 | Information Disclosure | cost_price trên public pages | mitigate | Field chỉ render trong `app/templates/admin/products/form.html`; plan-level grep verify toàn bộ `app/templates/public/**` không chứa 'cost_price' hay 'Giá nhập' (COST-02). Không có template public nào bị sửa trong Phase 5. |
| T-05-03 | Tampering | `new_product` constructor drop cost_price | mitigate | Thêm explicit `cost_price=form.cost_price.data or None` vào constructor (edit dùng populate_obj tự bind); behavioral verify khẳng định create persist giá trị (không drop âm thầm). |
</threat_model>

<verification>
- Task 1 verify (login `/login` + CSRF + create + edit + negative + empty) exits 0 printing TASK_OK
- Task 2 verify (template markup + COST-02 grep public) exits 0 printing TASK_OK
- Public pages không bị đổi: không sửa `app/templates/public/**`, không đổi copy public
</verification>

<success_criteria>
- Admin product create/edit form có field tùy chọn "Giá nhập (VND)" đúng 05-UI-SPEC: vị trí sau row Giá/Thương hiệu, help-text 'Chỉ quản trị viên xem được', không required — COST-01
- Create lẫn Edit persist cost_price (Integer VND); âm bị chặn 'Giá nhập không được âm'; bỏ trống → NULL
- Không template public nào lộ cost_price — COST-02
- Không CSS/component/token/dependency mới; tuân thủ 05-UI-SPEC (contract 6/6 PASS)
</success_criteria>

<output>
Create `.planning/phases/05-data-model-migration/05-03-SUMMARY.md` when done
</output>
