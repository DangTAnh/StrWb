---
phase: 06-public-order-form
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/models.py, app/db.py]
autonomous: true
requirements: [ORD-10a]

must_haves:
  truths:
    - "orders bảng chỉ còn thông tin khách + status (id, customer_name, customer_phone, customer_address, customer_note, status, created_at, updated_at) — KHÔNG còn snapshot trực tiếp (ORD-10a)"
    - "order_items lưu snapshot từng sản phẩm (product_name/product_price/product_cost_price/quantity) + FK order_id CASCADE + product_id nullable SET NULL + CheckConstraint quantity >= 1 (ORD-10a, IN-03 Phase 5 review)"
    - "init-db idempotent: trên DB v1.0 tạo orders (schema mới) + order_items; trên DB có orders legacy (có cột product_name) rebuild an toàn; không mất dữ liệu (count guard trước DROP)"
  artifacts:
    - path: "app/models.py"
      provides: "Order refactored (customer + status) + OrderItem model mới"
      contains: "class OrderItem"
    - path: "app/db.py"
      provides: "orders-table guard (PRAGMA table_info) + create_all tạo order_items"
      contains: "PRAGMA table_info(orders)"
  key_links:
    - from: "OrderItem.order_id"
      to: "orders.id"
      via: "ForeignKey ondelete='CASCADE' + ORM cascade delete-orphan — xóa đơn xóa items"
      pattern: "ondelete='CASCADE'"
    - from: "OrderItem.product_id"
      to: "products.id"
      via: "ForeignKey ondelete='SET NULL' nullable + passive_deletes — xóa sản phẩm giữ snapshot, không mất đơn"
      pattern: "ondelete='SET NULL'"
---

<objective>
Refactor `Order` → `Order` + `OrderItem` (ORD-10a): `orders` giữ thông tin khách + status; `order_items` lưu snapshot từng sản phẩm (name/price/cost_price/quantity) tại thời điểm đặt. Mở rộng `init-db` migration idempotent cho schema mới, không mất dữ liệu.

Purpose: Phase 6 chuyển từ "form đặt hàng 1 sản phẩm" sang "giỏ hàng nhiều sản phẩm". Model `Order` hiện tại (Phase 5) chứa snapshot sản phẩm trực tiếp — phải tách sang `OrderItem` để 1 Order có nhiều sản phẩm (ORD-10a). CheckConstraint `quantity >= 1` rẻ khi bảng mới (IN-03 Phase 5 review deferred → đưa vào đây). Migration xử lý cả DB v1.0 (chưa có bảng orders) lẫn DB đã migrate Phase 5 (orders legacy có cột snapshot) — `data/app.db` thật CHƯA migrate, verify chỉ trên temp copy.

## Coverage Map

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| ORD-10a | Refactor Order → Order + OrderItem; orders giữ customer+status; order_items snapshot từng sản phẩm + FK order_id CASCADE + product_id nullable SET NULL | 06-01 (Task 1) |
| IN-03 | CheckConstraint `quantity >= 1` trên bảng mới order_items (Phase 5 review deferred) | 06-01 (Task 1) |
| 06-CONTEXT | Migration idempotent: guard PRAGMA table_info(orders) + create_all tạo order_items; không mất dữ liệu | 06-01 (Task 2) |
| Deferred | ORD-11 (thông báo đơn mới), ORD-12 (trừ tồn kho), lưu giỏ DB | KHÔNG cài đặt |

Output: `app/models.py` Order refactored + OrderItem; `app/db.py` migration guard. Không file mới ngoài model, không dependency mới (06-UI-SPEC: zero new dependencies).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-public-order-form/06-CONTEXT.md (decisions ORD-10a: orders giữ customer+status; order_items: id, order_id FK CASCADE, product_id FK nullable SET NULL, snapshot product_name/product_price/product_cost_price/quantity; CheckConstraint quantity >= 1; migration guard PRAGMA table_info(orders) + create_all tạo order_items; data/app.db thật CHƯA migrate)
@.planning/phases/05-data-model-migration/05-REVIEW.md (IN-03 deferred: CheckConstraint `quantity >= 1` rẻ khi bảng mới; WR-01 fixed: PRAGMA foreign_keys=ON bật → SET NULL/CASCADE hoạt động)
@.planning/phases/05-data-model-migration/05-CONTEXT.md (Phase 5 Order model gốc — snapshot trực tiếp, status String VN, ID đơn = id)
@CLAUDE.md (D-05: tiền = Integer VND, never Float; SQLite WAL + foreign_keys=ON)
</context>

<interfaces>
Consumed from existing code (already on disk):
  - `app/models.py` class Order hiện tại (lines 73-90): `__tablename__ = 'orders'`; 13 columns gồm `product_id` (FK products.id ondelete SET NULL nullable), snapshot columns `product_name`/`product_price`/`product_cost_price`/`quantity`, customer PII, `status` (String(20) default 'Chờ xác nhận'), `created_at`/`updated_at` = utcnow; relationship `product = db.relationship('Product', backref=db.backref('orders', passive_deletes=True))`
  - `app/models.py` helper `def utcnow(): return datetime.now(timezone.utc)` (lines 8-10) — tái sử dụng
  - `app/db.py` `init_db_command` (lines 12-47): `@click.command('init-db')` + `@with_appcontext`; password checks → `db.create_all()` (line 30) → cost_price guard `PRAGMA table_info(products)` + `ALTER TABLE ... ADD COLUMN cost_price` trong `with db.engine.begin()` → admin upsert
  - `app/__init__.py`: `from . import models` (line 52) register Order/OrderItem → `db.create_all()` thấy mọi model
  - SQLite: `PRAGMA table_info(t)` trả rows `(cid, name, type, notnull, dflt_value, pk)` → row[1] = tên cột; `PRAGMA foreign_keys=ON` đã bật (app/__init__.py line 26, WR-01 fix) → ON DELETE CASCADE / SET NULL cấp DB hoạt động
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Order model + thêm OrderItem model (snapshot từng sản phẩm)</name>
  <files>app/models.py</files>
  <read_first>
    - .planning/phases/06-public-order-form/06-CONTEXT.md (ORD-10a: orders giữ customer+status; order_items: id, order_id FK CASCADE, product_id FK nullable SET NULL, snapshot product_name/product_price/product_cost_price/quantity; CheckConstraint quantity >= 1)
    - app/models.py (class Order hiện tại lines 73-90; Product pattern: Integer VND comment D-05, utcnow() timestamps, relationship cascade)
    - .planning/phases/05-data-model-migration/05-REVIEW.md (IN-03: CheckConstraint quantity >= 1)
    - CLAUDE.md (D-05: money as Integer VND, never Float)
  </read_first>
  <action>
    Sửa duy nhất `app/models.py`. Hai thay đổi:

    1. Refactor class Order (lines 73-90):
       - XÓA các columns snapshot trực tiếp: `product_id`, `product_name`, `product_price`, `product_cost_price`, `quantity`.
       - XÓA relationship `product = db.relationship('Product', backref=db.backref('orders', passive_deletes=True))`.
       - GIỮ NGUYÊN: `id`, `customer_name = db.Column(db.String(100), nullable=False)`, `customer_phone = db.Column(db.String(20), nullable=False)`, `customer_address = db.Column(db.Text, nullable=False)`, `customer_note = db.Column(db.Text, nullable=True)`, `status = db.Column(db.String(20), default='Chờ xác nhận', nullable=False)`, `created_at = db.Column(db.DateTime, default=utcnow)`, `updated_at = db.Column(db.DateTime, default=utcnow, onupdate=utcnow)`.
       - THÊM relationship items: `items = db.relationship('OrderItem', backref='order', lazy='dynamic', cascade='all, delete-orphan')` — xóa Order xóa items qua ORM (khớp ondelete='CASCADE' cấp DB).
       - Giữ comment `# order number = incrementing id (no formatted code)` trên id.

    2. Append class OrderItem ở CUỐI file (sau Order), theo convention codebase:
       - `class OrderItem(db.Model):` + `__tablename__ = 'order_items'`
       - `__table_args__ = (db.CheckConstraint('quantity >= 1', name='ck_order_items_quantity_positive'),)` — IN-03 Phase 5 review deferred, rẻ khi bảng mới
       - Columns:
         - `id = db.Column(db.Integer, primary_key=True)`
         - `order_id = db.Column(db.Integer, db.ForeignKey('orders.id', ondelete='CASCADE'), nullable=False)` — FK CASCADE: xóa đơn xóa items
         - `product_id = db.Column(db.Integer, db.ForeignKey('products.id', ondelete='SET NULL'), nullable=True)` — nullable; xóa sản phẩm → NULL hóa, snapshot giữ nguyên (ORD-10a)
         - `product_name = db.Column(db.String(200), nullable=False)` — snapshot tại thời điểm đặt
         - `product_price = db.Column(db.Integer, nullable=False)` — giá bán VND, Integer only (D-05)
         - `product_cost_price = db.Column(db.Integer, nullable=True)` — giá nhập VND; NULL nếu sản phẩm không có giá nhập
         - `quantity = db.Column(db.Integer, nullable=False)` — số lượng ≥ 1 (enforce CheckConstraint)
         - `created_at = db.Column(db.DateTime, default=utcnow)` — khớp pattern Product/Order
       - Relationship: `product = db.relationship('Product', backref=db.backref('order_items', passive_deletes=True))` — passive_deletes: SQLAlchemy không load/nullify order_items khi xóa Product, dựa vào DB SET NULL (PRAGMA foreign_keys=ON đã bật).

    KHÔNG thêm: `__repr__` (model hiện có không dùng), server_default (pattern Python-side default), index trên order_id (Phase 7 xử lý nếu cần), `cascade='all, delete-orphan'` trên OrderItem.product (SET NULL phải giữ snapshot, không xóa items khi xóa sản phẩm).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
with app.app_context():
    from sqlalchemy import inspect, text
    from app.db import db
    from app.models import Order, OrderItem, Product
    db.create_all()
    insp = inspect(db.engine)
    ocols = {c['name'] for c in insp.get_columns('orders')}
    assert ocols == {'id','customer_name','customer_phone','customer_address','customer_note','status','created_at','updated_at'}, ocols
    icols = {c['name'] for c in insp.get_columns('order_items')}
    for col in ['id','order_id','product_id','product_name','product_price','product_cost_price','quantity','created_at']:
        assert col in icols, 'missing order_items column: ' + col
    fks = {fk['referred_table']: fk for fk in insp.get_foreign_keys('order_items')}
    assert fks['orders']['options'].get('ondelete') == 'CASCADE', 'order_id FK must be CASCADE'
    assert fks['products']['options'].get('ondelete') == 'SET NULL', 'product_id FK must be SET NULL'
    create_sql = db.engine.execute(text("SELECT sql FROM sqlite_master WHERE type='table' AND name='order_items'")).scalar()
    assert 'quantity >= 1' in (create_sql or ''), 'CheckConstraint quantity >= 1 missing'
    # behavior: 1 Order + 2 OrderItems; delete product -> SET NULL; delete order -> CASCADE
    p = Product(name='Ao', price=100000, quantity=5, cost_price=60000)
    db.session.add(p); db.session.flush()
    o = Order(customer_name='A', customer_phone='1', customer_address='2')
    db.session.add(o); db.session.flush()
    db.session.add(OrderItem(order_id=o.id, product_id=p.id, product_name='Ao', product_price=100000, product_cost_price=60000, quantity=2))
    db.session.commit()
    assert o.items.count() == 1
    db.session.delete(p); db.session.commit()  # PRAGMA FK ON -> product_id SET NULL
    assert db.session.get(OrderItem, 1).product_id is None
    db.session.delete(db.session.get(Order, o.id)); db.session.commit()  # cascade delete-orphan
    assert OrderItem.query.count() == 0
print('TASK_OK')
PY</automated>
  </verify>
  <done>Order chỉ còn 8 cột customer+status; OrderItem đủ 8 cột với FK CASCADE/SET NULL + CheckConstraint quantity >= 1; xóa sản phẩm → SET NULL; xóa đơn → CASCADE items</done>
  <acceptance_criteria>
    - `app/models.py` class Order: 8 column (id, customer_name, customer_phone, customer_address, customer_note, status, created_at, updated_at); KHÔNG còn product_id/product_name/product_price/product_cost_price/quantity; relationship `items` với `cascade='all, delete-orphan'`
    - class OrderItem: `__tablename__ = 'order_items'`, `__table_args__` chứa `db.CheckConstraint('quantity >= 1', name='ck_order_items_quantity_positive')`; columns id/order_id/product_id/product_name/product_price/product_cost_price/quantity/created_at
    - FK order_id → orders.id ondelete='CASCADE'; FK product_id → products.id ondelete='SET NULL' nullable; relationship `product` backref `order_items` passive_deletes=True
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: init-db idempotent — orders legacy guard + tạo order_items</name>
  <files>app/db.py</files>
  <read_first>
    - .planning/phases/06-public-order-form/06-CONTEXT.md (migration: guard PRAGMA table_info(orders); orders bảng cũ xử lý cột thừa + create_all tạo order_items; không mất dữ liệu — orders chưa có data live)
    - app/db.py (init_db_command: password checks → db.create_all → cost_price guard trong engine.begin() → admin upsert)
    - app/models.py (Order/OrderItem đã refactor trên disk sau Task 1)
  </read_first>
  <action>
    Sửa `app/db.py` — chỉ trong `init_db_command`, giữ nguyên password checks và admin upsert:

    1. Giữ `db.create_all()` nguyên vị trí (line 30) — idempotent; tạo bảng THIẾU: `order_items` (mới). Nếu `orders` đã tồn tại (legacy Phase 5) create_all skip.

    2. Trong `with db.engine.begin() as conn:` hiện có, SAU block cost_price guard, thêm orders guard:
       - `orders_exist = conn.execute(text("SELECT name FROM sqlite_master WHERE type='table' AND name='orders'")).fetchone()`
       - Nếu orders tồn tại:
         - `ocols = [row[1] for row in conn.execute(text('PRAGMA table_info(orders)'))]`
         - Nếu `'product_name' in ocols` (schema legacy Phase 5 — snapshot trực tiếp trên orders):
           - `order_count = conn.execute(text('SELECT COUNT(*) FROM orders')).scalar()`
           - Nếu `order_count > 0`: `raise click.ClickException(...)` — KHÔNG BAO GIỜ tự DROP khi có dữ liệu (guard chống mất dữ liệu); message rõ ràng bảo migrate tay.
           - Nếu 0 rows: `conn.execute(text('DROP TABLE orders'))` + `click.echo('Migrated: rebuilt orders table (legacy snapshot schema -> customer-only schema).')`
       - `engine.begin()` bọc transaction: commit on success / rollback on error — DROP không dở chừng.

    3. SAU `with db.engine.begin()` block (trước admin upsert), thêm dòng `db.create_all()` — tái tạo bảng `orders` (schema mới) sau khi DROP legacy; với DB không cần rebuild thì no-op (idempotent).

    Semantics:
    - DB v1.0 (data/app.db hiện tại, chưa có orders): create_all tạo orders (mới) + order_items; guard thấy không có orders → skip; create_all lần 2 no-op.
    - DB đã migrate Phase 5 (orders legacy có product_name, 0 rows): create_all tạo order_items; guard DROP orders; create_all lần 2 tạo orders schema mới.
    - DB có orders legacy CÓ dữ liệu: raise ClickException — không mất dữ liệu (thực tế không xảy ra vì chưa có route ghi orders).

    KHÔNG thêm lệnh DROP/RENAME/backup khác; KHÔNG đổi URI/WAL pragma (app/__init__.py xử lý); không thêm dependency.
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile, sqlite3
os.environ['SECRET_KEY'] = 'x'*32
os.environ['ADMIN_PASSWORD'] = 'testpass1234'
os.environ['ADMIN_USERNAME'] = 'admin'
os.environ['FLASK_DEBUG'] = '0'
import app as app_module

def db_tables(path):
    con = sqlite3.connect(path); cur = con.cursor()
    tables = {r[0] for r in cur.execute("SELECT name FROM sqlite_master WHERE type='table'")}
    cols = {t: [c[1] for c in cur.execute('PRAGMA table_info("%s")' % t)] for t in tables}
    counts = {t: cur.execute('SELECT COUNT(*) FROM "%s"' % t).fetchone()[0] for t in tables}
    con.close(); return tables, cols, counts

def run_init(base_dir):
    app_module.BASE_DIR = base_dir
    app = app_module.create_app()
    r = app.test_cli_runner().invoke(args=['init-db'])
    assert r.exit_code == 0, r.output
    return r

def create_v10_db(path):
    con = sqlite3.connect(path); cur = con.cursor()
    cur.execute('CREATE TABLE products (id INTEGER PRIMARY KEY, name VARCHAR(200) NOT NULL, price INTEGER NOT NULL, brand VARCHAR(100), measurements TEXT, description TEXT, quantity INTEGER DEFAULT 0 NOT NULL, discontinued BOOLEAN DEFAULT 0 NOT NULL, sku VARCHAR(100), sort_order INTEGER DEFAULT 0 NOT NULL, admin_note TEXT, created_at DATETIME, updated_at DATETIME)')
    cur.execute("INSERT INTO products (name, price, quantity) VALUES ('Ao', 100000, 5)")
    con.commit(); con.close()

def create_legacy_db(path):
    create_v10_db(path)
    con = sqlite3.connect(path); cur = con.cursor()
    cur.execute('ALTER TABLE products ADD COLUMN cost_price INTEGER')
    cur.execute("CREATE TABLE orders (id INTEGER PRIMARY KEY, product_id INTEGER, product_name VARCHAR(200) NOT NULL, product_price INTEGER NOT NULL, product_cost_price INTEGER, quantity INTEGER NOT NULL, customer_name VARCHAR(100) NOT NULL, customer_phone VARCHAR(20) NOT NULL, customer_address TEXT NOT NULL, customer_note TEXT, status VARCHAR(20) DEFAULT 'Chờ xác nhận' NOT NULL, created_at DATETIME, updated_at DATETIME)")
    con.commit(); con.close()

# CASE A: DB v1.0 (no orders) -> init-db tạo orders (schema mới) + order_items, không mất dữ liệu
d1 = tempfile.mkdtemp(); os.makedirs(os.path.join(d1, 'data'))
db1 = os.path.join(d1, 'data', 'app.db'); create_v10_db(db1)
t0, c0, n0 = db_tables(db1); assert 'orders' not in t0
run_init(d1)
t1, c1, n1 = db_tables(db1)
assert 'orders' in t1 and 'order_items' in t1
assert 'product_name' not in c1['orders'] and 'cost_price' in c1['products']
assert n1['products'] == n0['products'] == 1

# CASE B: DB legacy orders (0 rows) -> rebuild orders + order_items, không mất dữ liệu
d2 = tempfile.mkdtemp(); os.makedirs(os.path.join(d2, 'data'))
db2 = os.path.join(d2, 'data', 'app.db'); create_legacy_db(db2)
t0b, c0b, n0b = db_tables(db2); assert 'product_name' in c0b['orders']
run_init(d2)
t2, c2, n2 = db_tables(db2)
assert 'orders' in t2 and 'order_items' in t2 and 'product_name' not in c2['orders']
assert n2['products'] == n0b['products']

# CASE C: idempotent — chạy lần 2 không đổi
run_init(d2)
t3, c3, n3 = db_tables(db2)
assert 'product_name' not in c3['orders'] and n3['orders'] == 0 and n3['order_items'] == 0
assert n3['products'] == n2['products']

# CASE D: DB legacy orders CÓ dữ liệu -> init-db RAISE (không DROP, không mất dữ liệu)
d4 = tempfile.mkdtemp(); os.makedirs(os.path.join(d4, 'data'))
db4 = os.path.join(d4, 'data', 'app.db'); create_legacy_db(db4)
con = sqlite3.connect(db4); con.execute("INSERT INTO orders (product_name, product_price, quantity, customer_name, customer_phone, customer_address) VALUES ('Ao', 100000, 1, 'A', '1', '2')"); con.commit(); con.close()
app_module.BASE_DIR = d4
app = app_module.create_app()
r4 = app.test_cli_runner().invoke(args=['init-db'])
assert r4.exit_code != 0 and 'Manual migration' in r4.output
t4, c4, n4 = db_tables(db4)
assert 'orders' in t4 and n4['orders'] == 1  # dữ liệu còn nguyên

# CASE E: fresh DB -> orders + order_items đúng
d5 = tempfile.mkdtemp(); os.makedirs(os.path.join(d5, 'data'))
run_init(d5)
t5, c5, n5 = db_tables(os.path.join(d5, 'data', 'app.db'))
assert 'orders' in t5 and 'order_items' in t5 and 'product_name' not in c5['orders']
print('TASK_OK')
PY</automated>
  </verify>
  <done>init-db idempotent: Case A v1.0 → orders mới + order_items; Case B legacy rebuild; Case C chạy lặp không đổi; Case D legacy có dữ liệu → raise giữ dữ liệu; Case E fresh; DB thật không bị đổi trong verify</done>
  <acceptance_criteria>
    - `app/db.py` chứa guard `PRAGMA table_info(orders)` + nhánh `DROP TABLE orders` (chỉ khi 0 rows) + `click.ClickException` khi có dữ liệu; `db.create_all()` gọi lại sau block guard
    - Case A/B/C/E exit 0; Case D exit != 0 với 'Manual migration', orders giữ 1 dòng
    - Số dòng products/admin_users không đổi sau mọi migration (không mất dữ liệu)
    - Verify script exits 0 printing TASK_OK
    - DB thật `data/app.db` KHÔNG bị đổi trong lúc verify (chỉ thao tác temp DB)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → SQLite (schema) | `init-db` chạy DDL (CREATE TABLE + DROP TABLE) lên file DB. Idempotency + no-data-loss là thuộc tính an toàn bắt buộc. |
| app → SQLite (data layer) | Model refactor đổi schema orders; OrderItem là bảng mới — chưa có route ghi (route đến Phase 6/7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `DROP TABLE orders` trong migration | mitigate | Guard count trước DROP: chỉ DROP khi `SELECT COUNT(*) == 0`; nếu > 0 → `raise ClickException('Manual migration')` — KHÔNG BAO GIỜ tự DROP dữ liệu. `engine.begin()` bọc transaction: rollback khi lỗi. |
| T-06-02 | Tampering | `db.create_all()` trên DB đã tồn tại | mitigate | create_all chỉ tạo bảng THIẾU (IF NOT EXISTS semantics) — không DROP/ALTER bảng tồn tại; verify Case A/B/C đếm dòng products trước/sau. |
| T-06-03 | Information Disclosure | `OrderItem.product_cost_price` (giá nhập snapshot) | mitigate | Column Integer nullable VND; không route/template public nào render nó ở Phase 6 (grep gate ở 06-02/06-03: `product_cost_price` chỉ xuất hiện trong models/db + route checkout ghi, không trong template public). Admin đọc qua Phase 7 sau `@login_required`. |
| T-06-04 | Tampering | SQL injection vào PRAGMA/ALTER/DROP | mitigate | Tên bảng/cột là literal cứng trong code (`'orders'`, `'product_name'`, `'cost_price'`); `text()` chỉ thực thi SQL hằng số — không input người dùng. |
| T-06-05 | Information Disclosure | Customer PII (customer_name/phone/address) data-at-rest | accept | SQLite server-local, admin-only qua auth Phase 1; route đọc orders đến Phase 7 sau `@login_required`. Không đưa PII vào public. |
</threat_model>

<verification>
- Task 1 verify exits 0 printing TASK_OK: Order 8 cột; OrderItem 8 cột + FK CASCADE/SET NULL + CheckConstraint quantity >= 1; delete product → SET NULL; delete order → CASCADE
- Task 2 verify exits 0 printing TASK_OK (5 cases A-E); DB thật không bị đổi
</verification>

<success_criteria>
- `orders` chỉ giữ thông tin khách + status (8 cột); `order_items` giữ snapshot từng sản phẩm + FK order_id CASCADE + product_id nullable SET NULL + CheckConstraint `quantity >= 1` (IN-03) — ORD-10a
- Migration idempotent không mất dữ liệu trên: DB v1.0, DB legacy orders (0 rows), DB legacy có dữ liệu (raise giữ dữ liệu), fresh DB — PLAT-05 pattern giữ nguyên
- Không cài đặt deferred (ORD-11/ORD-12); không dependency mới
</success_criteria>

<output>
Create `.planning/phases/06-public-order-form/06-01-SUMMARY.md` when done
</output>
