---
phase: 06-public-order-form
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [app/forms.py, app/public.py, app/templates/public/product_detail.html, app/templates/public/_nav.html, app/templates/public/cart.html, app/static/css/style.css]
autonomous: true
requirements: [ORD-10, ORD-10b, ORD-03]

must_haves:
  truths:
    - "Trang chi tiết có block 'Thêm vào giỏ hàng' (số lượng 1..tồn kho, POST có CSRF); nút 'Mua qua Messenger' biến mất khỏi trang chi tiết; block ẩn khi hết hàng/ngừng bán (ORD-10b, ORD-03)"
    - "Giỏ lưu trong session `{product_id: quantity}` (key chuỗi, JSON); thêm thay thế qty (không cộng dồn) (ORD-10)"
    - "Khách sửa số lượng/xóa món trong giỏ — server validate 1 ≤ qty ≤ tồn kho (ORD-10)"
    - "Trang giỏ hàng liệt kê món (thumb + tên + đơn giá + số lượng + thành tiền), tổng tiền dùng format_price; giỏ trống hiện empty state + CTA về trang chủ (ORD-10)"
    - "Nav hiển thị link 'Giỏ hàng' + badge số món (len(session['cart'])) khi giỏ không trống (ORD-10)"
    - "Món hết hàng/ngừng bán/xóa bị lọc khỏi giỏ khi render kèm flash info (ORD-03, ORD-10)"
  artifacts:
    - path: "app/public.py"
      provides: "Routes cart_add / cart / cart_update / cart_remove + helper đọc/ghi session cart"
      exports: ["cart_add", "cart", "cart_update", "cart_remove"]
    - path: "app/forms.py"
      provides: "CartForm (quantity IntegerField)"
      contains: "class CartForm"
    - path: "app/templates/public/cart.html"
      provides: "Trang giỏ hàng (bảng + empty state + tổng tiền + include checkout section)"
      contains: "public/cart"
    - path: "app/templates/public/product_detail.html"
      provides: "Add-to-cart block thay Messenger CTA (status-gated)"
      contains: "cart_add"
    - path: "app/templates/public/_nav.html"
      provides: "Cart-link + cart-badge trong header"
      contains: "cart-badge"
  key_links:
    - from: "product_detail.html add-to-cart form"
      to: "public.cart_add"
      via: "POST url_for('public.cart_add', product_id=product.id) + csrf_token()"
      pattern: "cart_add"
    - from: "cart.html cart-qty-form"
      to: "public.cart_update"
      via: "POST url_for('public.cart_update', product_id=...) + csrf_token()"
      pattern: "cart_update"
    - from: "cart.html remove form"
      to: "public.cart_remove"
      via: "POST url_for('public.cart_remove', product_id=...) + csrf_token()"
      pattern: "cart_remove"
    - from: "public.cart route"
      to: "session['cart']"
      via: "đọc + filter món hết hàng/ngừng bán (reassign toàn dict để mark modified)"
      pattern: "session\\['cart'\\] = "
---

<objective>
Giỏ hàng nhiều sản phẩm lưu session (ORD-10): routes thêm/sửa/xóa món, trang giỏ hàng hiển thị danh sách + tổng tiền, badge số món trong nav, và block "Thêm vào giỏ hàng" trên trang chi tiết thay nút "Mua qua Messenger" (ORD-10b, ORD-03). Checkout form và route đặt hàng thuộc 06-03 — plan này tạo trang giỏ hàng với include placeholder `_checkout_form.html` (sẽ tồn tại sau 06-03).

Purpose: Là lớp tương tác khách hàng — nền cho checkout (06-03). Mọi thay đổi giỏ chỉ trên session (không bảng DB, không tài khoản khách — giữ scope 06-CONTEXT). Validate số lượng phía server (1 ≤ qty ≤ tồn kho) — min/max attribute trên input chỉ là client hint, không tin client.

## Coverage Map

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| ORD-10 | Giỏ session `{product_id: quantity}`; thêm/sửa số lượng/xóa; xem tổng tiền | 06-02 (Task 1, Task 2) |
| ORD-10b | Trang chi tiết có nút "Thêm vào giỏ hàng" (chọn số lượng, ≤ tồn kho); form đặt hàng đơn bị bỏ; dải Messenger giữ nguyên (chỉ bỏ CTA trang chi tiết) | 06-02 (Task 2) |
| ORD-03 | Form không hiện khi hết hàng/ngừng bán (add-to-cart block status-gated); món stale bị lọc + flash info | 06-02 (Task 1, Task 2) |
| 06-CONTEXT | Session cart JSON key string; server-side validate; flash messages tiếng Việt | 06-02 (Task 1, Task 2) |
| 06-UI-SPEC | Add-to-cart block markup, cart-link badge, cart table, empty state, flash copy | 06-02 (Task 2) |
| Deferred | ORD-11/ORD-12, lưu giỏ DB | KHÔNG cài đặt |

Output: CartForm + 4 routes public + cart.html + nav badge + add-to-cart block + CSS. Không dependency mới.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-public-order-form/06-CONTEXT.md (decisions cart: session {product_id: quantity}; add/update/remove; tổng tiền format_price; ẩn khi hết hàng/ngừng bán; server-side validate qty; flash VN)
@.planning/phases/06-public-order-form/06-UI-SPEC.md (Add-to-Cart Block section — markup verbatim; Cart-link Badge; Cart Page; Cart Session Shape; Add-to-Cart/Update/Remove flow; Flash Messages; CSS classes)
@.planning/phases/06-public-order-form/06-01-PLAN.md (đã execute: Order/OrderItem refactor — cart không cần model mới, chỉ session)
@CLAUDE.md (Flask session; Jinja2; D-05 Integer VND; không JS framework)
</context>

<interfaces>
Consumed from existing code (already on disk):
  - `app/public.py`: `public_bp = Blueprint('public', __name__)`; imports hiện có: `from flask import Blueprint, render_template, request, abort, redirect, url_for`; routes home/product_detail/search; `product_detail(product_id)` render `public/product_detail.html` (line 44-58). CẦN THÊM imports: `flash`, `session`.
  - `app/forms.py`: pattern `from flask_wtf import FlaskForm`; `from wtforms import IntegerField, SubmitField`; validators `InputRequired, NumberRange` (đã import); `ProductForm.quantity` dùng `InputRequired` (giá trị 0 hợp lệ — nhưng cart qty dùng `NumberRange(min=1)`).
  - `app/templates/public/product_detail.html` line 34: `<a class="btn btn-primary messenger-cta" href="{{ config['MESSENGER_URL'] }}" ...>Mua qua Messenger</a>` — thay bằng add-to-cart block.
  - `app/templates/public/_nav.html`: header `.site-header` > `.container.site-header__inner` (flex-column mobile / flex-row ≥768px) chứa `a.brand` + `form.search-form`. Thêm `a.cart-link` sau search form.
  - `app/static/css/style.css`: `.data-table` (lines 184-198), `.form-field` (252-275), `.btn`/`.btn-primary`/`.btn-secondary` (154-180), `.empty-state` (239-248), `.thumb`/`.thumb-placeholder` (210-210), `.link-danger` (199), `.messenger-cta` (409), `.site-header__inner` responsive (356-359).
  - `app/__init__.py`: template filter `format_price` (line 64-66) → `1.200.000₫`; CSRFProtect app-wide (line 54).
  - Flask session gotcha: `session['cart'][key] = v` (mutate dict con) KHÔNG tự mark modified — phải gán lại cả dict (`session['cart'] = cart`) hoặc `session.modified = True`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CartForm + routes cart_add / cart / cart_update / cart_remove</name>
  <files>app/forms.py, app/public.py</files>
  <read_first>
    - .planning/phases/06-public-order-form/06-CONTEXT.md (cart session shape `{product_id: quantity}`; add/update/remove; server-side validate 1 ≤ qty ≤ tồn kho; flash VN)
    - .planning/phases/06-public-order-form/06-UI-SPEC.md (Cart Session Shape; Add-to-Cart Flow; Update Quantity; Remove Item; Flash Messages)
    - app/public.py (blueprint + product_detail route pattern; imports)
    - app/forms.py (FlaskForm pattern; validators đã import)
    - app/models.py (Product.status property: available/out_of_stock/discontinued; Product.quantity)
  </read_first>
  <action>
    Hai file — `app/forms.py` và `app/public.py`:

    **forms.py** — thêm class `CartForm(FlaskForm)` ở cuối file:
    - `quantity = IntegerField('Số lượng', validators=[InputRequired(message='Vui lòng nhập số lượng'), NumberRange(min=1, message='Số lượng phải từ 1 trở lên')])`
    - Không cần SubmitField (template hand-rolled button).

    **public.py**:
    1. Bổ sung imports: `flash`, `session` vào dòng `from flask import ...`. Import `CartForm`: `from .forms import CartForm`.
    2. Thêm 4 routes ở cuối file (sau `search`):
       - `@public_bp.route('/cart/add/<int:product_id>', methods=['POST'])` `def cart_add(product_id):`
         - `product = db.session.get(Product, product_id)`; None → `abort(404)`.
         - Nếu `product.status != 'available'` → `flash('Số lượng không hợp lệ hoặc sản phẩm đã hết hàng.', 'error')`; `return redirect(url_for('public.product_detail', product_id=product.id))`.
         - `form = CartForm()`; parse qty: `qty = form.quantity.data`; validate `form.validate()` VÀ `1 <= qty <= product.quantity`. Nếu sai → flash error như trên + redirect về detail.
         - Hợp lệ: `cart = session.get('cart', {})`; `cart[str(product_id)] = qty`; `session['cart'] = cart` (reassign toàn dict — mark modified); `flash(f'Đã thêm {qty} sản phẩm vào giỏ.', 'success')`; `return redirect(url_for('public.cart'))`.
       - `@public_bp.route('/cart', methods=['GET'])` `def cart():`
         - `cart = session.get('cart', {})`; build `items = []`; `total = 0`.
         - Với mỗi `pid_str, qty in cart.items()`: bỏ qua key không phải chữ số (`if not pid_str.isdigit(): continue`); `product = db.session.get(Product, int(pid_str))`.
         - Filter stale: nếu `product is None` hoặc `product.status != 'available'` → `flash(f"Sản phẩm '{product.name if product else pid_str}' đã ngừng bán hoặc hết hàng và được xóa khỏi giỏ.", 'info')`; xóa khỏi session (`cart.pop(pid_str, None)`, reassign toàn dict). (Chỉ flash khi `product` còn tồn tại — tránh lộ thông tin id.)
         - Nếu hợp lệ: `items.append(SimpleNamespace(product=product, quantity=qty))`; `total += product.price * qty` (Integer VND, D-05).
         - `session['cart'] = cart`; `return render_template('public/cart.html', items=items, total=total)`.
       - `@public_bp.route('/cart/update/<int:product_id>', methods=['POST'])` `def cart_update(product_id):`
         - `product = db.session.get(Product, product_id)`; None → abort(404).
         - `form = CartForm()`; `qty = form.quantity.data`; nếu `not form.validate() or not (1 <= qty <= product.quantity)` → `flash('Số lượng vượt quá tồn kho.', 'error')`; `return redirect(url_for('public.cart'))`.
         - Hợp lệ: `cart = session.get('cart', {})`; `cart[str(product_id)] = qty`; `session['cart'] = cart`; `flash('Giỏ hàng đã cập nhật.', 'success')`; redirect `/cart`.
       - `@public_bp.route('/cart/remove/<int:product_id>', methods=['POST'])` `def cart_remove(product_id):`
         - `cart = session.get('cart', {})`; `cart.pop(str(product_id), None)`; `session['cart'] = cart`; `flash('Đã xóa sản phẩm khỏi giỏ.', 'success')`; redirect `/cart`.
    3. Giữ nguyên home/product_detail/search — không sửa hành vi cũ.

    KHÔNG thêm: session cart trong DB, JSON endpoint, API trả về giỏ, tự cộng dồn qty khi add (thay thế, UI-SPEC "replace, not increment").
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False  # functional tests (CSRF riêng check cuối)
with app.app_context():
    from app.db import db
    from app.models import Product
    db.create_all()
    p = Product(name='Ao', price=100000, quantity=5)
    db.session.add(p); db.session.commit(); pid = p.id
client = app.test_client()
# add hợp lệ -> 302 /cart + session cart
r = client.post(f'/cart/add/{pid}', data={'quantity': '2'})
assert r.status_code == 302 and r.headers['Location'].endswith('/cart')
with client.session_transaction() as s:
    assert s['cart'] == {str(pid): 2}
# add qty > tồn kho -> giữ nguyên, flash error
r = client.post(f'/cart/add/{pid}', data={'quantity': '99'})
assert r.status_code == 302
with client.session_transaction() as s:
    assert s['cart'][str(pid)] == 2
# add qty 0 -> giữ nguyên
r = client.post(f'/cart/add/{pid}', data={'quantity': '0'})
with client.session_transaction() as s:
    assert s['cart'][str(pid)] == 2
# update qty -> đổi
r = client.post(f'/cart/update/{pid}', data={'quantity': '3'})
with client.session_transaction() as s:
    assert s['cart'][str(pid)] == 3
# update quá tồn kho -> giữ nguyên
r = client.post(f'/cart/update/{pid}', data={'quantity': '50'})
with client.session_transaction() as s:
    assert s['cart'][str(pid)] == 3
# remove -> giỏ trống
r = client.post(f'/cart/remove/{pid}')
with client.session_transaction() as s:
    assert s['cart'] == {}
# add sản phẩm không tồn tại -> 404
r = client.post('/cart/add/999', data={'quantity': '1'})
assert r.status_code == 404
# CSRF: instance riêng (CSRF enabled default) -> POST thiếu token -> 400
app2 = app_module.create_app()
r2 = app2.test_client().post(f'/cart/add/{pid}', data={'quantity': '1'})
assert r2.status_code == 400
print('TASK_OK')
PY</automated>
  </verify>
  <done>4 routes public hoạt động (add/update/remove/session); validate qty server-side; CSRF chặn POST thiếu token</done>
  <acceptance_criteria>
    - `app/forms.py` có class CartForm với `quantity` IntegerField InputRequired + NumberRange(min=1)
    - `app/public.py` có 4 hàm: `cart_add` (/cart/add/<int:product_id> POST), `cart` (/cart GET), `cart_update` (/cart/update/<int:product_id> POST), `cart_remove` (/cart/remove/<int:product_id> POST); imports `flash`, `session`, `CartForm`
    - add/update validate `1 <= qty <= product.quantity` và `product.status == 'available'`; add thay thế qty (không cộng dồn); remove dùng `pop(str(id), None)`
    - Mọi ghi session gán lại cả dict `session['cart'] = cart` (tránh gotcha nested mutation)
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Trang chi tiết add-to-cart + nav badge + cart.html + CSS</name>
  <files>app/templates/public/product_detail.html, app/templates/public/_nav.html, app/templates/public/cart.html, app/static/css/style.css</files>
  <read_first>
    - .planning/phases/06-public-order-form/06-UI-SPEC.md (Add-to-Cart Block section — markup verbatim lines 163-182; Cart-link Badge markup + CSS; Cart Page layout; Copywriting Contract)
    - app/templates/public/product_detail.html (line 34 Messenger CTA; out-of-stock-note block lines 35-37)
    - app/templates/public/_nav.html (header structure)
    - app/templates/public/base.html (extends target cho cart.html)
    - app/static/css/style.css (classes cần tái sử dụng: .data-table, .form-field, .btn, .empty-state, .link-danger, .messenger-cta line 409)
  </read_first>
  <action>
    Bốn file template/CSS:

    **1. `app/templates/public/product_detail.html`** — thay dòng 34 (`<a class="btn btn-primary messenger-cta" ...>Mua qua Messenger</a>`) bằng block status-gated, giữ `.out-of-stock-note` hiện có (lines 35-37) chuyển vào nhánh elif. Dùng markup UI-SPEC "Add-to-Cart Block" verbatim:
    - `{% if product.status == 'available' %}` → `<form class="add-to-cart-form" method="post" action="{{ url_for('public.cart_add', product_id=product.id) }}">` chứa `<input type="hidden" name="csrf_token" value="{{ csrf_token() }}">`, `.form-field` với `<label for="cart-quantity">Số lượng</label>`, `<input type="number" id="cart-quantity" name="quantity" min="1" max="{{ product.quantity }}" value="1" required>`, `<p class="help-text">Còn {{ product.quantity }} sản phẩm trong kho</p>`, và `<button type="submit" class="btn btn-primary cart-cta">Thêm vào giỏ hàng</button>`.
    - `{% elif product.status == 'out_of_stock' %}` → giữ `<p class="out-of-stock-note">Sản phẩm hiện đang hết hàng.</p>`.
    - discontinued: không render gì (badge "Ngừng bán" ở product-status đã có).
    - KHÔNG đổi `.contact-strip` ở homepage (giữ Messenger CTA toàn site ở index.html + footer — chỉ bỏ CTA trang chi tiết).

    **2. `app/templates/public/_nav.html`** — thêm `a.cart-link` SAU `form.search-form` trong `.site-header__inner`:
    - `<a class="cart-link" href="{{ url_for('public.cart') }}">Giỏ hàng` + `{% with cart_count = session.get('cart', {}) | length %}{% if cart_count > 0 %}<span class="cart-badge">{{ cart_count }}</span>{% endif %}{% endwith %}</a>`
    - Badge hiển thị số món riêng biệt (len session cart), ẩn hẳn khi giỏ trống (link vẫn hiện). `session` có sẵn trong Jinja2 context (Flask inject).

    **3. `app/templates/public/cart.html`** — file MỚI, `{% extends "public/base.html" %}`:
    - `<h1 class="page-heading">Giỏ hàng</h1>`.
    - `{% if items %}`:
      - `<div class="table-scroll">` bọc `<table class="data-table">` với `<caption class="visually-hidden">Giỏ hàng của bạn</caption>`; thead 5 cột: `Sản phẩm`, `Đơn giá`, `Số lượng`, `Thành tiền`, `Xóa` (th scope="col").
      - Mỗi dòng item: td sản phẩm = thumb 80×80 (`{% if item.product.primary_image %}<img ... src="...thumb..." width="80" height="80">{% else %}<span class="thumb-placeholder">—</span>{% endif %}` + tên); td đơn giá `{{ item.product.price | format_price }}`; td qty = `<form class="cart-qty-form" method="post" action="{{ url_for('public.cart_update', product_id=item.product.id) }}">` + csrf_token + `<input type="number" name="quantity" min="1" max="{{ item.product.quantity }}" value="{{ item.quantity }}" class="cart-qty-input" aria-label="Số lượng {{ item.product.name }}">` + `<button type="submit" class="btn btn-secondary">Cập nhật</button>`; td thành tiền `{{ (item.product.price * item.quantity) | format_price }}`; td Xóa = `<form method="post" action="{{ url_for('public.cart_remove', product_id=item.product.id) }}">` + csrf_token + `<button type="submit" class="link-danger">Xóa</button>`.
      - Sau table: `.cart-total` (label "Tổng cộng:" + `{{ total | format_price }}`), `.cart-actions` (`<a class="btn btn-secondary" href="{{ url_for('public.home') }}">Tiếp tục mua sắm</a>` + `<a class="btn btn-primary" href="#checkout">Đặt hàng</a>`), và `.checkout-section` id="checkout" với `<h2>Thông tin đặt hàng</h2>` + `{% include "public/_checkout_form.html" ignore missing %}` (checkout form đến 06-03; `ignore missing` để cart render được khi file chưa tồn tại).
    - `{% else %}`: `.empty-state` với `<h2>Giỏ hàng trống</h2>`, `<p>Bạn chưa thêm sản phẩm nào vào giỏ.</p>`, `<a class="btn btn-primary" href="{{ url_for('public.home') }}">Tiếp tục mua sắm</a>`.

    **4. `app/static/css/style.css`** — thêm block Phase 6 ở cuối file (trước/sau search section đều được, giữ nguyên CSS cũ):
    - `.cart-link { font-size: 14px; font-weight: 600; color: var(--text); }` + `.cart-link:hover { color: var(--accent); text-decoration: none; }`
    - `.cart-badge { display: inline-block; margin-left: 4px; padding: 0 8px; height: 20px; background: var(--accent); color: #fff; font-size: 14px; font-weight: 600; border-radius: 999px; line-height: 1.5; }`
    - `.add-to-cart-form { max-width: 320px; margin-bottom: 8px; }` (mirror `.messenger-cta` sizing — thay thế); `.cart-cta { width: 100%; }`
    - XÓA rule `.messenger-cta` cũ (line 409) vì class không còn dùng (chỉ trong product_detail.html đã thay).
    - `.cart-qty-form { display: flex; gap: 8px; align-items: center; }`; `.cart-qty-input { width: 72px; height: 44px; padding: 0 8px; border: 1px solid var(--border); border-radius: 8px; font-size: 16px; font-family: inherit; }`
    - `.honeypot { display: none; }` (checkout form 06-03 dùng)
    - `.cart-total { display: flex; justify-content: space-between; align-items: baseline; padding: 16px 0; border-top: 1px solid #E5E7EB; margin-top: 24px; }`; `.cart-total-label { font-size: 14px; font-weight: 600; color: #6B7280; }`; `.cart-total-value { font-size: 32px; font-weight: 600; color: var(--accent); line-height: 1.1; }`
    - `.cart-actions { display: flex; gap: 16px; margin-top: 16px; }`
    - `.checkout-section { margin-top: 32px; padding-top: 32px; border-top: 1px solid #E5E7EB; }` + `.checkout-section h2 { margin-bottom: 16px; }`
    - `button.link-danger { background: none; border: none; padding: 0; font-size: 14px; font-weight: 600; font-family: inherit; cursor: pointer; }` + `button.link-danger:hover { text-decoration: underline; }` (reset button khi dùng class link-danger — `.link-danger` gốc vẫn dùng cho `<a>` admin).

    Không thêm dependency; không JS mới cho cart (form POST thuần).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
with app.app_context():
    from app.db import db
    from app.models import Product
    db.create_all()
    p1 = Product(name='Ao Thun', price=100000, quantity=5)
    p2 = Product(name='Quan Jean', price=200000, quantity=0)  # hết hàng
    db.session.add_all([p1, p2]); db.session.commit()
    pid, oos = p1.id, p2.id
client = app.test_client()
# detail available -> add-to-cart, không còn Messenger CTA
html = client.get(f'/products/{pid}').get_data(as_text=True)
assert 'Thêm vào giỏ hàng' in html and 'Mua qua Messenger' not in html
assert 'Còn 5 sản phẩm trong kho' in html
# detail hết hàng -> không add-to-cart, có out-of-stock note
html = client.get(f'/products/{oos}').get_data(as_text=True)
assert 'Thêm vào giỏ hàng' not in html and 'Sản phẩm hiện đang hết hàng.' in html
# cart có items -> bảng + tổng tiền
with client.session_transaction() as s:
    s['cart'] = {str(pid): 2}
html = client.get('/cart').get_data(as_text=True)
assert 'Ao Thun' in html and 'Đơn giá' in html and 'Thành tiền' in html and 'Xóa' in html
assert '200.000₫' in html  # 2 x 100000
# nav badge trên home: 1 món riêng biệt -> badge "1"
html = client.get('/').get_data(as_text=True)
assert 'Giỏ hàng' in html and 'cart-badge' in html and '>1<' in html
# cart trống -> empty state
with client.session_transaction() as s:
    s['cart'] = {}
html = client.get('/cart').get_data(as_text=True)
assert 'Giỏ hàng trống' in html and 'Tiếp tục mua sắm' in html
# món stale (out-of-stock trong giỏ) -> bị lọc + flash info
with client.session_transaction() as s:
    s['cart'] = {str(oos): 1}
r = client.get('/cart')
html = r.get_data(as_text=True)
assert 'Giỏ hàng trống' in html  # bị lọc hết -> empty state
with client.session_transaction() as s:
    assert s['cart'] == {}  # món stale đã bị xóa khỏi session
print('TASK_OK')
PY</automated>
  </verify>
  <done>Detail page có add-to-cart (ẩn khi hết hàng), không còn Messenger CTA; nav có badge; cart.html hiển thị bảng + tổng tiền + empty state; món stale bị lọc</done>
  <acceptance_criteria>
    - `product_detail.html` không còn chứa `Mua qua Messenger` / `messenger-cta`; chứa `url_for('public.cart_add'`, `csrf_token()`, `name="quantity"`, `max="{{ product.quantity }}"`; block trong `{% if product.status == 'available' %}`; `.out-of-stock-note` trong nhánh elif out_of_stock
    - `_nav.html` chứa `a.cart-link` href `public.cart` + `span.cart-badge` khi `cart_count > 0` (dùng `session.get('cart', {}) | length`)
    - `cart.html` mới: extends public/base.html; `caption.visually-hidden`; 5 th scope="col"; qty form POST `public.cart_update`; Xóa POST `public.cart_remove`; `.cart-total` dùng `format_price`; `{% include "public/_checkout_form.html" ignore missing %}`; empty state
    - `style.css` có `.cart-badge`, `.cart-link`, `.cart-qty-form`, `.cart-qty-input`, `.honeypot`, `.cart-total`, `.cart-cta`, `button.link-danger`; không còn `.messenger-cta`
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → public routes (cart_add/cart_update/cart_remove) | Input `quantity` từ client POST — untrusted; product_id trong URL path. |
| client → session cookie | Cart lưu trong session cookie (client-readable JSON) — untrusted state, phải re-validate mọi thứ server-side. |
| client → public cart page (GET) | Rendering phải filter món hết hàng/ngừng bán, không leak cost_price. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `quantity` trong POST add/update | mitigate | Server re-validate `1 <= qty <= product.quantity` + `product.status == 'available'` (min/max attribute chỉ là client hint); qty tamper → flash error, không ghi session. |
| T-06-02 | Tampering | session cart cookie bị sửa (product_id không tồn tại / key không phải số) | mitigate | `cart()` lọc key không phải chữ số (`isdigit()`), `db.session.get` None → xóa khỏi session; món discontinued/out_of_stock bị lọc + flash info. Checkout (06-03) re-validate lại toàn bộ. |
| T-06-03 | CSRF | POST cart_add/cart_update/cart_remove | mitigate | CSRFProtect app-wide (app/__init__.py line 54) + `csrf_token()` trong MỌI form POST (add-to-cart, qty, remove). Verify: POST thiếu token → 400. |
| T-06-04 | Information Disclosure | `product_cost_price` / `Product.cost_price` render ra public | mitigate | Grep gate: `app/templates/public/**` không được chứa `cost_price` / `Giá nhập`; cart.html chỉ render `product.price` (format_price). |
| T-06-05 | Repudiation / Spam | Bot tự add giỏ hàng | accept | Giỏ session không tạo đơn; spam chỉ tốn session cookie. Checkout (06-03) có honeypot + CSRF chống spam thật. |
</threat_model>

<verification>
- Task 1 verify exits 0 printing TASK_OK (routes + validate + CSRF 400)
- Task 2 verify exits 0 printing TASK_OK (render detail/cart/nav + filter stale + no Messenger CTA)
- Grep gate: `grep -rn "cost_price\|Giá nhập" app/templates/public/` → 0 hits
</verification>

<success_criteria>
- Khách thêm/sửa/xóa sản phẩm trong giỏ session; xem tổng tiền format_price; giỏ trống hiện empty state — ORD-10
- Trang chi tiết có "Thêm vào giỏ hàng" (qty 1..tồn kho), không còn Messenger CTA; dải Messenger homepage giữ nguyên — ORD-10b
- Add-to-cart ẩn khi hết hàng/ngừng bán; món stale bị lọc kèm flash info — ORD-03
- Mọi POST có CSRF; server validate qty; không leak cost_price — security gate Phase 6
</success_criteria>

<output>
Create `.planning/phases/06-public-order-form/06-02-SUMMARY.md` when done
</output>
