---
phase: 07-admin-order-tracking
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [app/admin.py, app/templates/admin/orders/detail.html, app/static/css/style.css]
autonomous: true
requirements: [ORD-08, ORD-09]

must_haves:
  truths:
    - "Admin chuyển trạng thái đơn forward-only qua POST /admin/orders/<id>/status: Chờ xác nhận → Đã gói → Đã gửi → Đã nhận (+ Đã hủy chỉ từ Chờ xác nhận/Đã gói) — ORD-08"
    - "Trạng thái chỉ tiến về trước, không lùi; Đã nhận/Đã hủy là terminal (không chuyển tiếp được) — ORD-09"
    - "Transition không hợp lệ (bao gồm lùi, hủy từ Đã gửi/Đã nhận, chuyển từ terminal) → flash error + redirect, KHÔNG đổi DB, không 500"
    - "Mọi POST status có CSRF token (Flask-WTF app-wide); không token → 400"
    - "Detail page render stepper + nút transition đúng theo transition map; terminal hiện note thay vì nút"
  artifacts:
    - path: "app/admin.py"
      provides: "TRANSITION_MAP constant + update_order_status POST route (server-side validate)"
      contains: "def update_order_status"
    - path: "app/templates/admin/orders/detail.html"
      provides: "section 'Cập nhật trạng thái': .order-progress stepper + nút transition (hidden next_status + csrf_token) + hủy confirm + terminal note"
      contains: "order-progress"
    - path: "app/static/css/style.css"
      provides: ".order-progress (+ .is-done/.is-current/.dot/connector) + .order-terminal"
      contains: "order-progress"
  key_links:
    - from: "update_order_status"
      to: "TRANSITION_MAP"
      via: "next_status not in TRANSITION_MAP.get(order.status, set()) -> flash error + redirect, no DB change"
      pattern: "TRANSITION_MAP"
    - from: "detail.html"
      to: "admin.update_order_status"
      via: "url_for('admin.update_order_status', order_id=order.id) — mỗi nút là form POST riêng"
      pattern: "update_order_status"
    - from: "detail.html"
      to: "csrf_token"
      via: "<input type=hidden name=csrf_token value={{ csrf_token() }}> trong mỗi form transition"
      pattern: "csrf_token"
    - from: "update_order_status"
      to: "Order.status"
      via: "order.status = next_status + db.session.commit() (chỉ khi hợp lệ)"
      pattern: "order.status"
---

<objective>
Forward-only status transitions (ORD-08 + ORD-09): route `POST /admin/orders/<int:order_id>/status` chuyển trạng thái đơn theo transition map server-side (single source of truth, không tin client). Detail page render stepper 4 bước + nút "Chuyển sang: X" / "Hủy đơn" (chỉ các transition hợp lệ).

Purpose: hoàn thiện admin order tracking — admin thao tác trạng thái đơn từ trang chi tiết; forward-only + terminal enforce nghiêm ngặt phía server.

**Decisions:**
- **CSRF-hidden-form (KHÔNG dùng OrderStatusForm)** — theo 07-UI-SPEC section 3 markup: mỗi nút là form POST riêng với `<input type="hidden" name="csrf_token" value="{{ csrf_token() }}">` + `<input type="hidden" name="next_status">`. CSRFProtect app-wide (`app/__init__.py:54`) validate token trên mọi POST — pattern y hệt form logout trong dashboard.html. `OrderStatusForm` (FlaskForm) chỉ thêm boilerplate cho 1 hidden field → KHÔNG tạo → `app/forms.py` KHÔNG bị đụng (lệch so với mô tả ban đầu, có lý do).
- Route đọc `request.form.get('next_status')` và validate `next_status in TRANSITION_MAP[order.status]`.
- Stepper: 4 bước forward (Chờ xác nhận → Đã gói → Đã gửi → Đã nhận); Đã nhận = all-done + note "Đơn đã hoàn thành."; Đã hủy = NO stepper, chỉ note "Đơn đã bị hủy." (07-UI-SPEC open question #1).

## Coverage Map

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| ORD-08 | Admin cập nhật trạng thái: Chờ xác nhận → Đã gói → Đã gửi → Đã nhận (+ Đã hủy, chỉ admin) | 07-03 (Task 1 route + map, Task 2 stepper/buttons, Task 3 CSS) |
| ORD-09 | Forward-only, không lùi; Đã nhận/Đã hủy terminal | 07-03 (Task 1 TRANSITION_MAP enforce) |
| 07-CONTEXT | Transition map 5 status; invalid → flash error + redirect, không đổi DB, không 500; hủy chỉ từ Chờ xác nhận/Đã gói; Đã gửi/Đã nhận không cho hủy | 07-03 |
| 07-UI-SPEC | Stepper `.order-progress` 4 bước (is-done/is-current/aria-current); nút "Chuyển sang: {next}" `.btn-primary`; "Hủy đơn" `.btn-danger` + confirm; terminal note; flash messages | 07-03 |
| Deferred | Bulk status, export CSV, thông báo đơn mới | KHÔNG cài đặt |

Output: route `admin.update_order_status()`, `TRANSITION_MAP`, section status trong detail.html, CSS stepper. Không dependency mới.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-order-tracking/07-CONTEXT.md (decisions ORD-08/ORD-09: transition map server-side duy nhất; forward-only không lùi; Đã nhận terminal, Đã hủy absorbing; hủy chỉ từ Chờ xác nhận/Đã gói; Đã gửi/Đã nhận không cho hủy; invalid → flash error + redirect, không đổi DB, không 500; route POST /admin/orders/<id>/status; CSRF app-wide)
@.planning/phases/07-admin-order-tracking/07-01-SUMMARY.md (07-01: ORDER_STATUSES + _order_total/order_badge_class Jinja global + admin.orders — tái sử dụng)
@.planning/phases/07-admin-order-tracking/07-02-SUMMARY.md (07-02: admin.order_detail route + detail.html bản nền (customer/items/total/timestamps) + strftime filter — plan này SỬA detail.html thêm section status)
@.planning/phases/07-admin-order-tracking/07-UI-SPEC.md (section 3 Status Flow UI: stepper markup semantic ol 4 bước; nút transition form POST hidden next_status + csrf_token; hủy .btn-danger onsubmit confirm; terminal note; bảng mapping 5 status → buttons; flash messages table)
@app/admin.py (sau 07-02: import Order, ORDER_STATUSES, _order_total/order_badge_class, orders(), order_detail())
@app/templates/admin/orders/detail.html (sau 07-02: bản nền — thêm section "Cập nhật trạng thái" ở cuối .admin-card.order-detail)
@app/forms.py (KHÔNG đụng — dùng CSRF-hidden-form, không OrderStatusForm)
@app/templates/admin/dashboard.html (pattern form POST + csrf_token hidden field — logout form)
@app/__init__.py (line 54: csrf.init_app(app) — CSRFProtect app-wide validate mọi POST)
@CLAUDE.md (D-05: tiền Integer VND; ngôn ngữ tiếng Việt; CSRF bật)
</context>

<interfaces>
Consumed từ 07-01/07-02: `_order_total(order)` + `order_badge_class(status)` Jinja global, `ORDER_STATUSES`, `admin.orders`, `admin.order_detail`, `strftime` filter, detail.html bản nền.

Plan này TẠO:
- `TRANSITION_MAP` module constant trong `app/admin.py` (single source of truth — verbatim từ 07-CONTEXT):
  ```python
  TRANSITION_MAP = {
      'Chờ xác nhận': {'Đã gói', 'Đã hủy'},
      'Đã gói':       {'Đã gửi', 'Đã hủy'},
      'Đã gửi':       {'Đã nhận'},
      'Đã nhận':      set(),  # terminal
      'Đã hủy':       set(),  # terminal, absorbing
  }
  ```
- `admin.update_order_status(order_id)` view: `POST /admin/orders/<int:order_id>/status` — đọc `request.form.get('next_status', '')`; validate; set+commit; flash; redirect detail.
- Trong detail.html: section `.order-section` "Cập nhật trạng thái" với stepper `.order-progress` + nút transition.

Order model: `.status` (String VN, default 'Chờ xác nhận'); `.id` dùng trong flash messages.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TRANSITION_MAP + update_order_status POST route (server-side forward-only enforce)</name>
  <files>app/admin.py</files>
  <read_first>
    - .planning/phases/07-admin-order-tracking/07-CONTEXT.md (transition map + invalid handling; hủy chỉ từ Chờ xác nhận/Đã gói; Đã gửi/Đã nhận không hủy)
    - app/admin.py (sau 07-02: import Order; orders()/order_detail() — thêm route status cạnh order_detail)
    - app/models.py (Order.status String(20) default 'Chờ xác nhận'; items relationship)
  </read_first>
  <action>
    Sửa `app/admin.py`. Hai thay đổi:

    1. Thêm module constant `TRANSITION_MAP` (sau `ORDER_STATUSES` — dùng làm single source of truth, giữ y hệt map trong 07-CONTEXT):
    ```python
    TRANSITION_MAP = {
        'Chờ xác nhận': {'Đã gói', 'Đã hủy'},
        'Đã gói':       {'Đã gửi', 'Đã hủy'},
        'Đã gửi':       {'Đã nhận'},
        'Đã nhận':      set(),  # terminal — hết chuỗi
        'Đã hủy':       set(),  # terminal — hấp thụ
    }
    ```

    2. Thêm route (cạnh `order_detail()`):
    ```python
    @admin_bp.route('/orders/<int:order_id>/status', methods=['POST'])
    def update_order_status(order_id):
        order = db.session.get(Order, order_id)
        if order is None:
            flash('Không tìm thấy đơn.', 'error')
            return redirect(url_for('admin.orders'))
        next_status = request.form.get('next_status', '')
        valid = TRANSITION_MAP.get(order.status, set())
        if next_status not in valid:
            flash(f'Không thể chuyển trạng thái đơn #{order.id}.', 'error')
            return redirect(url_for('admin.order_detail', order_id=order.id))
        order.status = next_status
        db.session.commit()
        if next_status == 'Đã hủy':
            flash(f'Đã hủy đơn #{order.id}.', 'success')
        else:
            flash(f'Đã chuyển đơn #{order.id} sang trạng thái “{next_status}”.', 'success')
        return redirect(url_for('admin.order_detail', order_id=order.id))
    ```
    - `TRANSITION_MAP.get(order.status, set())` — nếu status bất thường (không trong map) → empty set → mọi transition bị reject (fail-safe).
    - Invalid (lùi, hủy từ Đã gửi/Đã nhận, chuyển từ terminal, next_status rỗng/không hợp lệ) → flash error + redirect, KHÔNG đổi DB, không 500.
    - CSRF: KHÔNG validate thủ công — CSRFProtect app-wide chặn trước route (không token → 400). Hidden csrf_token do template render (Task 2).

    KHÔNG: thêm form (dùng request.form trực tiếp); không đổi TRANSITION_MAP trạng thái khác; không cho chuyển sang trạng thái hiện tại (tự loại vì không nằm trong map).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from werkzeug.security import generate_password_hash
from app.db import db
from app.models import AdminUser, Order
from app.admin import TRANSITION_MAP
assert TRANSITION_MAP['Chờ xác nhận'] == {'Đã gói', 'Đã hủy'}
assert TRANSITION_MAP['Đã gói'] == {'Đã gửi', 'Đã hủy'}
assert TRANSITION_MAP['Đã gửi'] == {'Đã nhận'}
assert TRANSITION_MAP['Đã nhận'] == set()
assert TRANSITION_MAP['Đã hủy'] == set()
assert 'admin.update_order_status' in app.view_functions

def seed(status):
    with app.app_context():
        o = Order(customer_name='A', customer_phone='1', customer_address='2', status=status)
        db.session.add(o); db.session.commit(); return o.id

def st(oid):
    with app.app_context(): return db.session.get(Order, oid).status

with app.app_context():
    db.create_all()
    db.session.add(AdminUser(username='admin', password_hash=generate_password_hash('testpass1234')))
    db.session.commit()
with app.test_client() as c:
    c.post('/login', data={'username':'admin','password':'testpass1234'})
    # happy path forward
    oid = seed('Chờ xác nhận')
    assert c.post(f'/admin/orders/{oid}/status', data={'next_status': 'Đã gói'}).status_code == 302
    assert st(oid) == 'Đã gói'
    # invalid: chuyển sang cùng trạng thái -> reject, không đổi
    c.post(f'/admin/orders/{oid}/status', data={'next_status': 'Đã gói'})
    assert st(oid) == 'Đã gói'
    # backward (không lùi) -> reject
    c.post(f'/admin/orders/{oid}/status', data={'next_status': 'Chờ xác nhận'})
    assert st(oid) == 'Đã gói'
    # forward tới terminal Đã nhận
    c.post(f'/admin/orders/{oid}/status', data={'next_status': 'Đã gửi'})
    c.post(f'/admin/orders/{oid}/status', data={'next_status': 'Đã nhận'})
    assert st(oid) == 'Đã nhận'
    # terminal: không chuyển tiếp được
    c.post(f'/admin/orders/{oid}/status', data={'next_status': 'Đã gói'})
    assert st(oid) == 'Đã nhận'
    # hủy từ Chờ xác nhận -> Đã hủy, absorbing
    oid2 = seed('Chờ xác nhận')
    rb = c.post(f'/admin/orders/{oid2}/status', data={'next_status': 'Đã hủy'}, follow_redirects=True)
    assert 'Đã hủy đơn' in rb.get_data(as_text=True) and st(oid2) == 'Đã hủy'
    c.post(f'/admin/orders/{oid2}/status', data={'next_status': 'Đã gói'})
    assert st(oid2) == 'Đã hủy'
    # KHÔNG hủy được từ Đã gửi (forward-only)
    oid3 = seed('Đã gửi')
    c.post(f'/admin/orders/{oid3}/status', data={'next_status': 'Đã hủy'})
    assert st(oid3) == 'Đã gửi'
    # flash error cho invalid transition
    rb = c.post(f'/admin/orders/{oid3}/status', data={'next_status': 'Chờ xác nhận'}, follow_redirects=True)
    assert 'Không thể chuyển trạng thái đơn' in rb.get_data(as_text=True)
    # missing order
    rb = c.post('/admin/orders/99999/status', data={'next_status': 'Đã gói'}, follow_redirects=True)
    assert 'Không tìm thấy đơn.' in rb.get_data(as_text=True)
    # CSRF: bật lại, POST không token -> 400
    app.config['WTF_CSRF_ENABLED'] = True
    assert c.post(f'/admin/orders/{oid}/status', data={'next_status': 'Đã gói'}).status_code == 400
print('TASK_OK')
PY</automated>
  </verify>
  <done>TRANSITION_MAP đúng 5 status; forward-only enforce: advance/cancel hợp lệ đổi DB, invalid/backward/hủy từ Đã gửi/terminal đều reject không đổi DB, flash error đúng, missing → flash, CSRF không token → 400</done>
  <acceptance_criteria>
    - `app/admin.py` có `TRANSITION_MAP` (y hệt map 07-CONTEXT) + `@admin_bp.route('/orders/<int:order_id>/status', methods=['POST'])` `def update_order_status(order_id)` validate `next_status not in TRANSITION_MAP.get(order.status, set())` → flash + redirect (không commit); hợp lệ → `order.status = next_status` + commit + flash success + redirect detail
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Section "Cập nhật trạng thái" trong detail.html — stepper + transition buttons + hủy confirm + terminal note</name>
  <files>app/templates/admin/orders/detail.html</files>
  <read_first>
    - .planning/phases/07-admin-order-tracking/07-UI-SPEC.md (section 3: stepper markup ol.order-progress; buttons mapping table 5 status; nút transition form POST hidden next_status + csrf_token; hủy .btn-danger onsubmit confirm; terminal notes; a11y aria-current)
    - app/templates/admin/orders/detail.html (sau 07-02: bản nền — thêm section status cuối .admin-card.order-detail)
    - app/templates/admin/dashboard.html (pattern hidden csrf_token field trong form POST)
  </read_first>
  <action>
    SỬA `app/templates/admin/orders/detail.html` — thêm section "Cập nhật trạng thái" ở CUỐI `.admin-card.order-detail` (sau section "Thời gian"), trước `</div>` đóng card:

    ```html
    <section class="order-section">
      <h2>Cập nhật trạng thái</h2>
      {% if order.status == 'Đã hủy' %}
        <p class="order-terminal">Đơn đã bị hủy.</p>
      {% elif order.status == 'Đã nhận' %}
        <ol class="order-progress">
          <li class="is-done"><span class="dot"></span>Chờ xác nhận</li>
          <li class="is-done"><span class="dot"></span>Đã gói</li>
          <li class="is-done"><span class="dot"></span>Đã gửi</li>
          <li class="is-done"><span class="dot"></span>Đã nhận</li>
        </ol>
        <p class="order-terminal">Đơn đã hoàn thành.</p>
      {% else %}
        <ol class="order-progress">
          {% set flow = ['Chờ xác nhận', 'Đã gói', 'Đã gửi', 'Đã nhận'] %}
          {% for s in flow %}
          <li {% if s == order.status %}class="is-current" aria-current="step"{% elif loop.index0 < flow.index(order.status) %}class="is-done"{% endif %}><span class="dot"></span>{{ s }}</li>
          {% endfor %}
        </ol>
        <div class="action-row">
          {% if order.status == 'Chờ xác nhận' %}
            <form method="post" action="{{ url_for('admin.update_order_status', order_id=order.id) }}" class="inline-form">
              <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
              <input type="hidden" name="next_status" value="Đã gói">
              <button type="submit" class="btn btn-primary">Chuyển sang: Đã gói</button>
            </form>
            <form method="post" action="{{ url_for('admin.update_order_status', order_id=order.id) }}" class="inline-form"
                  onsubmit="return confirm('Hủy đơn #{{ order.id }}? Hành động này không thể hoàn tác.');">
              <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
              <input type="hidden" name="next_status" value="Đã hủy">
              <button type="submit" class="btn btn-danger">Hủy đơn</button>
            </form>
          {% elif order.status == 'Đã gói' %}
            ... form POST hidden next_status="Đã gửi" button .btn-primary "Chuyển sang: Đã gửi" ...
            ... form POST hidden next_status="Đã hủy" button .btn-danger onsubmit confirm "Hủy đơn" ...
          {% elif order.status == 'Đã gửi' %}
            ... form POST hidden next_status="Đã nhận" button .btn-primary "Chuyển sang: Đã nhận" ...
          {% endif %}
        </div>
      {% endif %}
    </section>
    ```

    Bắt buộc (đúng 07-UI-SPEC):
    - **Đã hủy**: KHÔNG stepper — chỉ `<p class="order-terminal">Đơn đã bị hủy.</p>` + badge header (07-02).
    - **Đã nhận**: stepper all 4 `.is-done` + note "Đơn đã hoàn thành." — KHÔNG nút.
    - **Khác**: stepper 4 bước; bước trước current `.is-done`, current `.is-current` + `aria-current="step"`, bước sau idle. `flow.index(order.status)` cho vị trí current.
    - Nút transition: chỉ render transition hợp lệ theo map — Chờ xác nhận: Đã gói + Hủy; Đã gói: Đã gửi + Hủy; Đã gửi: Đã nhận. Đã nhận/Đã hủy: không nút.
    - Mỗi form: hidden `csrf_token` + hidden `next_status` + button class `.btn`/`.btn-primary`/`.btn-danger`. Label nút là "Chuyển sang: {next_status}" (verb+target, không "Cập nhật").
    - Form hủy có `onsubmit="return confirm('Hủy đơn #{{ order.id }}? Hành động này không thể hoàn tác.');"`.
    - Server là nguồn truth — nút chỉ hiện option hợp lệ; `next_status` không trong map vẫn bị reject (Task 1).

    KHÔNG dùng OrderStatusForm/dropdown (UI-SPEC chốt 1 nút primary mỗi transition hợp lệ).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from werkzeug.security import generate_password_hash
from app.db import db
from app.models import AdminUser, Order
def seed(status):
    with app.app_context():
        o = Order(customer_name='A', customer_phone='1', customer_address='2', status=status)
        db.session.add(o); db.session.commit(); return o.id
with app.app_context():
    db.create_all()
    db.session.add(AdminUser(username='admin', password_hash=generate_password_hash('testpass1234')))
    db.session.commit()
with app.test_client() as c:
    c.post('/login', data={'username':'admin','password':'testpass1234'})
    def body(oid): return c.get(f'/admin/orders/{oid}').get_data(as_text=True)
    # Chờ xác nhận: stepper + advance + cancel
    b = body(seed('Chờ xác nhận'))
    assert 'order-progress' in b and 'Chuyển sang: Đã gói' in b and 'Hủy đơn' in b
    assert 'name="next_status" value="Đã gói"' in b and 'name="next_status" value="Đã hủy"' in b
    assert 'csrf_token' in b and 'admin.update_order_status' in b or '/status' in b
    assert 'aria-current="step"' in b and 'is-current' in b
    # Đã gói: Đã gửi + Hủy
    b = body(seed('Đã gói'))
    assert 'Chuyển sang: Đã gửi' in b and 'Hủy đơn' in b
    # Đã gửi: Đã nhận, KHÔNG Hủy (forward-only)
    b = body(seed('Đã gửi'))
    assert 'Chuyển sang: Đã nhận' in b and 'Hủy đơn' not in b
    # Đã nhận: terminal, không nút, all-done stepper + note
    b = body(seed('Đã nhận'))
    assert 'Đơn đã hoàn thành.' in b and 'Chuyển sang:' not in b
    assert b.count('class="is-done"') == 4
    # Đã hủy: terminal, KHÔNG stepper, chỉ note
    b = body(seed('Đã hủy'))
    assert 'Đơn đã bị hủy.' in b and 'order-progress' not in b and 'Chuyển sang:' not in b
print('TASK_OK')
PY</automated>
  </verify>
  <done>Detail page render đúng theo status: stepper + nút transition đúng map, aria-current trên bước current, hủy confirm chỉ từ Chờ xác nhận/Đã gói, terminal note cho Đã nhận/Đã hủy, mọi form có csrf_token + next_status</done>
  <acceptance_criteria>
    - `app/templates/admin/orders/detail.html` chứa section "Cập nhật trạng thái": `.order-progress` (4 bước, is-done/is-current/aria-current), nút `.btn-primary` "Chuyển sang: {next}" + `.btn-danger` "Hủy đơn" (onsubmit confirm) cho từng status theo TRANSITION_MAP, `.order-terminal` cho Đã nhận/Đã hủy
    - Mỗi form transition có hidden csrf_token + hidden next_status + action `url_for('admin.update_order_status', order_id=order.id)`
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CSS stepper — .order-progress (+ state variants) + .order-terminal</name>
  <files>app/static/css/style.css</files>
  <read_first>
    - app/static/css/style.css (sau 07-02: .order-section/.order-meta/.unit-price; .action-row line 285; .btn/.btn-primary/.btn-danger; media queries)
    - .planning/phases/07-admin-order-tracking/07-UI-SPEC.md (section 3 + Layout: stepper dots 12px, connector 2px border-top, done #059669, current #2563EB + ring #DBEAFE, idle #D1D5DB; terminal #6B7280; mobile .order-progress overflow-x auto)
  </read_first>
  <action>
    APPEND vào `app/static/css/style.css`:
    ```css
    /* Order status stepper (Phase 7) */
    .order-progress { list-style: none; display: flex; align-items: center; gap: 8px; margin: 0 0 32px; padding: 0; overflow-x: auto; }
    .order-progress li { display: flex; align-items: center; gap: 8px; position: relative; font-size: 14px; color: #6B7280; white-space: nowrap; }
    .order-progress li + li { margin-left: 32px; }
    .order-progress li + li::before { content: ''; position: absolute; left: -32px; top: 50%; width: 32px; height: 2px; background: #D1D5DB; transform: translateY(-50%); }
    .order-progress li.is-done + li::before { background: #059669; }
    .order-progress .dot { width: 12px; height: 12px; border-radius: 50%; background: #D1D5DB; flex: 0 0 auto; }
    .order-progress li.is-done .dot { background: #059669; }
    .order-progress li.is-current .dot { background: #2563EB; box-shadow: 0 0 0 3px #DBEAFE; }
    .order-progress li.is-current { color: #1F2937; font-weight: 600; }
    .order-terminal { color: #6B7280; }
    ```
    - Connector: `li + li::before` (border-top 2px solid semantics) — done segment `#059669`, idle `#D1D5DB`.
    - Dot 12px (exception đã khai trong UI-SPEC, không phải spacing token); current dot ring `box-shadow: 0 0 0 3px #DBEAFE`.
    - Mobile (trong `@media (max-width: 480px)`): `.order-progress { overflow-x: auto; }` đã có ở base — nếu cần, chỉ thêm rule để 4 bước scroll không wrap.

    KHÔNG đụng `.action-row`/`.btn` hiện có; không thêm icon glyph (CSS dots thuần).
  </action>
  <verify>
    <automated>python - <<'PY'
import os
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
css = open(os.path.join(app_module.BASE_DIR, 'app', 'static', 'css', 'style.css'), encoding='utf-8').read()
assert '.order-progress' in css and '.order-progress .dot' in css
assert '.order-progress li.is-done .dot' in css and '.order-progress li.is-current .dot' in css
assert '.order-terminal' in css
assert '#DBEAFE' in css and '#2563EB' in css  # ring + current dot
print('TASK_OK')
PY</automated>
  </verify>
  <done>style.css chứa .order-progress + .dot + is-done/is-current + connector + .order-terminal; màu đúng UI-SPEC</done>
  <acceptance_criteria>
    - `app/static/css/style.css` chứa `.order-progress` (+ `li`, `.dot`, `li.is-done .dot`, `li.is-current .dot`, `li + li::before` connector, `li.is-done + li::before`) và `.order-terminal`
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → POST /admin/orders/<id>/status | Untrusted `next_status` + CSRF token. Đây là boundary thay đổi trạng thái DB — phải validate. |
| Admin browser → GET /admin/orders/<id> (stepper render) | Read-only; quyết định nút render dựa trên status hiện tại. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `next_status` form field | mitigate | Server-side whitelist `next_status in TRANSITION_MAP.get(order.status, set())` — không tin client. Invalid (lùi, hủy từ Đã gửi/Đã nhận, terminal, rỗng) → flash error + redirect, KHÔNG commit. `TRANSITION_MAP.get(order.status, set())` fail-safe khi status bất thường. |
| T-07-02 | Tampering | POST không có CSRF token | mitigate | Flask-WTF CSRFProtect app-wide (`csrf.init_app(app)`); mọi form transition render hidden `csrf_token`; verify: không token → 400. |
| T-07-03 | Tampering | `order_id` path param | mitigate | `db.session.get(Order, order_id)` + missing → flash 'Không tìm thấy đơn.' + redirect; không đổi DB. |
| T-07-04 | Spoofing | Truy cập route status | mitigate | Route trong admin_bp + `@login_required` (`_protect_admin` before_request) — chỉ admin. Hủy là admin-only (ORD-08). |
| T-07-05 | Tampering | Stepper render (client-side) | mitigate | Server render stepper/buttons theo `order.status` hiện tại; nút chỉ hiện option hợp lệ — nhưng validation độc lập server (T-07-01) là nguồn truth cuối. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Không có package install trong plan này. |
</threat_model>

<verification>
- Task 1 verify exits 0 printing TASK_OK: TRANSITION_MAP + toàn bộ forward-only/terminal/cancel/CSRF logic
- Task 2 verify exits 0 printing TASK_OK: detail render stepper/buttons/terminal theo 5 status
- Task 3 verify exits 0 printing TASK_OK: CSS stepper + terminal
- Verify chạy trên temp DB (patch BASE_DIR) — KHÔNG chạm data/app.db thật; không dependency mới
</verification>

<success_criteria>
- POST /admin/orders/<id>/status forward-only theo TRANSITION_MAP: hợp lệ đổi status + commit + flash success + redirect; invalid → flash error + redirect, không đổi DB, không 500 — ORD-08, ORD-09
- Đã hủy chỉ từ Chờ xác nhận/Đã gói; Đã nhận/Đã hủy terminal; hủy là admin-only (toàn route admin đã @login_required)
- Detail page: stepper 4 bước (is-done/is-current/aria-current), nút "Chuyển sang: X" / "Hủy đơn" (confirm), terminal note; CSRF token trong mọi form POST
- `app/forms.py` KHÔNG bị đụng (CSRF-hidden-form theo UI-SPEC); không dependency mới; DB thật không bị đổi
</success_criteria>

<output>
Create `.planning/phases/07-admin-order-tracking/07-03-SUMMARY.md` when done
</output>
