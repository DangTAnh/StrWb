---
phase: 08-admin-stats
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [app/admin.py, app/templates/admin/stats.html, app/static/css/style.css]
autonomous: true
requirements: [STAT-03]

must_haves:
  truths:
    - "Admin thấy thẻ 'Sản phẩm đã bán' = sum(quantity) trên item của đơn Đã gửi + Đã nhận (nhất quán với revenue) — STAT-03"
    - "Admin thấy section 'Đơn hàng': 'Tổng số đơn' + breakdown 5 trạng thái + dòng 'Tất cả' — STAT-03"
    - "Mỗi dòng breakdown là link sang /admin/orders?status=<VN label> (dòng 'Tất cả' → /admin/orders không filter) — STAT-03"
    - "Trạng thái không có đơn hiển thị 0 (template dùng status_counts.get(s, 0) — group_by omits zero-count statuses), không KeyError, không 500 — STAT-03"
    - "Tổng đơn = sum mọi trạng thái (gồm cả Đã hủy) — STAT-03"
    - "Page vẫn render 200 khi có status bằng 0 và khi empty DB (0 đơn, 0₫) — STAT-03"
  artifacts:
    - path: "app/admin.py"
      provides: "Q4 status_counts (group_by Order.status, Phase 7 pattern) + total_orders = sum(status_counts.values())"
      contains: "status_counts"
    - path: "app/templates/admin/stats.html"
      provides: "units_sold card (3rd trong money section) + section 'Đơn hàng' với ul.status-breakdown 6 links"
      contains: "status_counts.get"
    - path: "app/static/css/style.css"
      provides: ".status-breakdown + .badge-neutral"
      contains: "status-breakdown"
  key_links:
    - from: "admin.stats() Q4"
      to: "Order.status"
      via: "db.session.query(Order.status, db.func.count(Order.id)).group_by(Order.status).all() — exact Phase 7 admin.orders() expression (admin.py:133-135)"
      pattern: "group_by"
    - from: "stats.html breakdown row"
      to: "admin.orders filter"
      via: "url_for('admin.orders', status='Đã gửi') — Phase 7 route handles ?status= whitelist"
      pattern: "admin.orders"
    - from: "stats.html status badge"
      to: "order_badge_class global"
      via: "{{ order_badge_class('Chờ xác nhận') }} → badge-order-pending"
      pattern: "order_badge_class"
---

<objective>
Mở rộng stats dashboard — orders-by-status + units sold, phục vụ STAT-03. Plan 02 của Phase 8 theo split ROADMAP (08-02 Orders-by-status + units sold).

Purpose: Thêm Q4 (status_counts + total_orders) vào `admin.stats()` theo ĐÚNG pattern Phase 7 `admin.orders()` (`admin.py:133-135` — `dict(db.session.query(Order.status, db.func.count(Order.id)).group_by(Order.status).all())`), render card "Sản phẩm đã bán" (units_sold — đã được 08-01 pass trong context) và section "Đơn hàng" với breakdown 5 trạng thái + "Tất cả", mỗi dòng link sang `/admin/orders?status=<label>`. Thêm CSS `.status-breakdown` + `.badge-neutral`.

**BẮT BUỘC — verified pitfall (research Pattern 2):** `GROUP BY` omit status có 0 đơn (VD không có đơn Đã gói → dict không có key 'Đã gói'). Template PHẢI dùng `status_counts.get(s, 0)` — tuyệt đối không subscript `status_counts['Đã gói']` (KeyError → 500).

**Self-consistent (Pitfall 4):** plan này thêm var `status_counts` + `total_orders` vào route VÀ template cùng lúc. KHÔNG render section "Kho" / var `total_products`/`in_stock`/`out_of_stock`/`discontinued` (thuộc 08-03).

**Lưu ý units_sold:** card "Sản phẩm đã bán" là thẻ thứ 3 trong money section "Doanh thu & Lợi nhuận" (UI-SPEC layout — revenue, profit, units cùng 1 grid). Var `units_sold` đã có từ 08-01 Task 1 — plan này chỉ render nó, không sửa query.

Output: Q4 trong `admin.stats()` + 1 card units + section "Đơn hàng" (breakdown) + CSS. Không dependency mới, GET-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-admin-stats/08-CONTEXT.md (STAT-03: breakdown cả 5 trạng thái + tổng (gồm Đã hủy); mỗi status click được → /admin/orders?status=<label>; units sold = sum(quantity) trên đơn Đã gửi+Đã nhận)
@.planning/phases/08-admin-stats/08-RESEARCH.md (Pattern 2: group_by omit zero-count statuses → template .get(s,0); Q4 code; Pattern 1 coalesce vẫn giữ từ 08-01; Pitfall 2 KeyError)
@.planning/phases/08-admin-stats/08-UI-SPEC.md (section 1: money section 3 cards — thêm card Sản phẩm đã bán; section "Đơn hàng" markup: .stat-card "Tổng số đơn" + ul.status-breakdown 6 li/links với .badge-order-* + .badge-neutral cho "Tất cả"; copy contract: Tổng số đơn / Sản phẩm đã bán / hints)
@.planning/phases/08-admin-stats/08-01-SUMMARY.md (nếu đã có: xác nhận stats() đã pass revenue/profit/profit_note/units_sold)
@app/admin.py (sau 08-01: `stats()` đã có Q1/Q2/Q3 + REVENUE_STATUSES; orders() line 125-144 pattern group_by status_counts; order_badge_class line 31-39)
@app/templates/admin/orders/list.html (pattern breakdown/badge: `{{ status_counts.get(s, 0) }}` line 10; `order_badge_class(order.status)` line 32)
@app/templates/admin/stats.html (sau 08-01: money section 2 cards — thêm card thứ 3 + section mới sau section money, trong cùng .admin-card--wide)
</context>

<interfaces>
Context contract plan này MỞ RỘNG cho `admin.stats()` (08-03 sẽ mở rộng thêm):

- Bổ sung vào `render_template('admin/stats.html', ...)` (giữ nguyên 4 var 08-01):
  - `status_counts` (dict VN label → int; chỉ các status CÓ đơn — zero-count bị omit)
  - `total_orders` (int = sum(status_counts.values()) — gồm cả Đã hủy)
- `admin/stats.html` sau plan này render: section "Doanh thu & Lợi nhuận" 3 cards (revenue, profit, **units_sold**) + section "Đơn hàng" (total_orders + breakdown 6 links dùng status_counts.get/order_badge_class).

Reused (KHÔNG tạo mới):
- `order_badge_class(status)` Jinja global (`app/admin.py:31-39`) → `badge-order-pending/packed/shipped/delivered/cancelled`.
- `.badge` CSS (`style.css:214`), `.badge-order-*` (`style.css:461-465`), `.badge-discontinued` trio gray (`style.css:224`) — nguồn cho `.badge-neutral` (cùng bộ màu).
- Phase 7 route `admin.orders` đã xử lý `?status=` (whitelist) — breakdown link chỉ cần url_for.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend — Q4 status_counts + total_orders trong admin.stats()</name>
  <files>app/admin.py</files>
  <read_first>
    - app/admin.py (sau 08-01: `stats()` với Q1/Q2/Q3 + render_template; orders() line 125-144 — pattern group_by status_counts `dict(db.session.query(Order.status, db.func.count(Order.id)).group_by(Order.status).all())`)
    - .planning/phases/08-admin-stats/08-RESEARCH.md (Q4 code + Pattern 2 — group_by omit zero-count statuses)
    - .planning/phases/08-admin-stats/08-CONTEXT.md (STAT-03: breakdown 5 status + tổng gồm Đã hủy)
  </read_first>
  <action>
    Sửa duy nhất `app/admin.py` — trong `stats()`, SAU khối Q3/derive profit_note và TRƯỚC `return render_template(...)`:

    1. Thêm Q4 (Sao chép ĐÚNG pattern Phase 7 `admin.orders()` line 133-135 — research Pattern 2):
       `status_counts = dict(db.session.query(Order.status, db.func.count(Order.id)).group_by(Order.status).all())`
    2. `total_orders = sum(status_counts.values())` (UI-SPEC: sum của 5 counts; tương đương Order.query.count() nhưng không cần query thêm — research Open Question 2)
    3. Thêm vào `render_template('admin/stats.html', ...)` hai var mới: `status_counts=status_counts, total_orders=total_orders`. GIỮ NGUYÊN 4 var 08-01 (revenue, profit, profit_note, units_sold).

    KHÔNG sửa Q1/Q2/Q3, không thêm query nào khác, không đọc request.args, không mutation DB.
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'test'
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from app.db import db
from app.models import Product, Order, OrderItem
with app.app_context():
    db.create_all()
    p1 = Product(name='Áo', price=100000, cost_price=60000, quantity=5)
    db.session.add(p1); db.session.flush()
    a = Order(customer_name='A', customer_phone='1', customer_address='x', status='Đã nhận')
    b = Order(customer_name='B', customer_phone='1', customer_address='x', status='Đã gửi')
    c = Order(customer_name='C', customer_phone='1', customer_address='x', status='Đã hủy')
    d = Order(customer_name='D', customer_phone='1', customer_address='x', status='Chờ xác nhận')
    db.session.add_all([a, b, c, d]); db.session.flush()
    db.session.add_all([
        OrderItem(order_id=a.id, product_id=p1.id, product_name='Áo', product_price=100000, product_cost_price=60000, quantity=2),
        OrderItem(order_id=b.id, product_id=p1.id, product_name='Áo', product_price=100000, product_cost_price=60000, quantity=1),
    ])
    db.session.commit()
    status_counts = dict(db.session.query(Order.status, db.func.count(Order.id)).group_by(Order.status).all())
    total_orders = sum(status_counts.values())
    # group_by omits zero-count status: Đã gói không có đơn -> absent
    assert status_counts == {'Chờ xác nhận': 1, 'Đã gửi': 1, 'Đã nhận': 1, 'Đã hủy': 1}, status_counts
    assert total_orders == 4, total_orders
    assert 'Đã gói' not in status_counts  # verified pitfall: omitted, template must .get(s, 0)
print('TASK_OK')
PY</automated>
  </verify>
  <done>Q4 status_counts + total_orders tính đúng (4 status có đơn, Đã gói bị omit), render_template pass status_counts + total_orders</done>
  <acceptance_criteria>
    - `app/admin.py` `stats()` chứa `status_counts = dict(db.session.query(Order.status, db.func.count(Order.id)).group_by(Order.status).all())` + `total_orders = sum(status_counts.values())`
    - `render_template('admin/stats.html', ...)` chứa `status_counts=status_counts, total_orders=total_orders` và vẫn giữ revenue/profit/profit_note/units_sold
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Template stats.html — card "Sản phẩm đã bán" + section "Đơn hàng" (breakdown 6 links)</name>
  <files>app/templates/admin/stats.html</files>
  <read_first>
    - app/templates/admin/stats.html (sau 08-01: money section 2 cards trong .stats-grid; thêm card thứ 3 + section "Đơn hàng" SAU section money, TRONG cùng .admin-card--wide)
    - .planning/phases/08-admin-stats/08-UI-SPEC.md (section 1 markup Đơn hàng: `div.stat-card` chứa p.stat-label "Tổng số đơn" + p.stat-value total_orders + p.stat-hint + ul.status-breakdown; 6 li: "Tất cả" dùng .badge-neutral, 5 status dùng .badge-order-*; copy contract)
    - .planning/phases/08-admin-stats/08-RESEARCH.md (stats.html snippet section Đơn hàng — mọi status dùng `status_counts.get('X', 0)`)
    - app/templates/admin/orders/list.html (pattern `.get(s, 0)` line 10; `order_badge_class` line 32)
  </read_first>
  <action>
    Sửa `app/templates/admin/stats.html`. Hai thay đổi:

    1. TRONG section "Doanh thu & Lợi nhuận" (section đầu), thêm card thứ 3 vào `.stats-grid` SAU profit card:
       `<article class="stat-card"><p class="stat-label">Sản phẩm đã bán</p><p class="stat-value">{{ units_sold }}</p><p class="stat-hint">Từ các đơn Đã gửi và Đã nhận.</p></article>`
       (units_sold là số nguyên — KHÔNG format_price; tabular-nums từ .stat-value.)

    2. THÊM section mới "Đơn hàng" SAU section money, TRONG cùng `.admin-card--wide` (đóng section money trước):
       ```
       <section class="stats-group">
         <h2 class="stats-group-title">Đơn hàng</h2>
         <div class="stat-card">
           <p class="stat-label">Tổng số đơn</p>
           <p class="stat-value">{{ total_orders }}</p>
           <p class="stat-hint">Gồm cả đơn đã hủy.</p>
           <ul class="status-breakdown">
             <li><a href="{{ url_for('admin.orders') }}">Tất cả <span class="badge badge-neutral">{{ total_orders }}</span></a></li>
             <li><a href="{{ url_for('admin.orders', status='Chờ xác nhận') }}">Chờ xác nhận <span class="badge {{ order_badge_class('Chờ xác nhận') }}">{{ status_counts.get('Chờ xác nhận', 0) }}</span></a></li>
             <li><a href="{{ url_for('admin.orders', status='Đã gói') }}">Đã gói <span class="badge {{ order_badge_class('Đã gói') }}">{{ status_counts.get('Đã gói', 0) }}</span></a></li>
             <li><a href="{{ url_for('admin.orders', status='Đã gửi') }}">Đã gửi <span class="badge {{ order_badge_class('Đã gửi') }}">{{ status_counts.get('Đã gửi', 0) }}</span></a></li>
             <li><a href="{{ url_for('admin.orders', status='Đã nhận') }}">Đã nhận <span class="badge {{ order_badge_class('Đã nhận') }}">{{ status_counts.get('Đã nhận', 0) }}</span></a></li>
             <li><a href="{{ url_for('admin.orders', status='Đã hủy') }}">Đã hủy <span class="badge {{ order_badge_class('Đã hủy') }}">{{ status_counts.get('Đã hủy', 0) }}</span></a></li>
           </ul>
         </div>
       </section>
       ```

    BẮT BUỘC:
    - MỌI status dùng `status_counts.get('<label>', 0)` — KHÔNG subscript (Pitfall 2: group_by omit zero-count → KeyError → 500). Label trong chuỗi là hằng số VN khớp ORDER_STATUSES.
    - Dòng "Tất cả" → `url_for('admin.orders')` (không status param) + `.badge-neutral`; 5 status → `url_for('admin.orders', status='<label>')` + `.badge {{ order_badge_class('<label>') }}`.
    - KHÔNG render section "Kho" / total_products / in_stock / out_of_stock / discontinued (thuộc 08-03).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile, re
os.environ['SECRET_KEY'] = 'test'
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from werkzeug.security import generate_password_hash
from app.db import db
from app.models import AdminUser, Product, Order, OrderItem
with app.app_context():
    db.create_all()
    db.session.add(AdminUser(username='admin', password_hash=generate_password_hash('testpass1234')))
    p1 = Product(name='Áo', price=100000, cost_price=60000, quantity=5)
    db.session.add(p1); db.session.flush()
    a = Order(customer_name='A', customer_phone='1', customer_address='x', status='Đã nhận')
    b = Order(customer_name='B', customer_phone='1', customer_address='x', status='Đã gửi')
    c = Order(customer_name='C', customer_phone='1', customer_address='x', status='Đã hủy')
    d = Order(customer_name='D', customer_phone='1', customer_address='x', status='Chờ xác nhận')
    db.session.add_all([a, b, c, d]); db.session.flush()
    db.session.add_all([
        OrderItem(order_id=a.id, product_id=p1.id, product_name='Áo', product_price=100000, product_cost_price=60000, quantity=2),
        OrderItem(order_id=b.id, product_id=p1.id, product_name='Áo', product_price=100000, product_cost_price=60000, quantity=1),
    ])
    db.session.commit()
with app.test_client() as tc:
    tc.post('/login', data={'username':'admin','password':'testpass1234'})
    resp = tc.get('/admin/stats')
    assert resp.status_code == 200
    html = resp.get_data(as_text=True)
    assert 'Sản phẩm đã bán' in html and 'Từ các đơn Đã gửi và Đã nhận.' in html
    m = re.search(r'<p class="stat-label">Sản phẩm đã bán</p>\s*<p class="stat-value">(\d+)</p>', html)
    assert m and m.group(1) == '3', m.group(1) if m else None   # units = 2+1 = 3 (chỉ a+b)
    assert 'Đơn hàng' in html and 'Tổng số đơn' in html and 'Gồm cả đơn đã hủy.' in html
    assert 'Tất cả' in html and 'badge-neutral' in html
    # zero-count status render 0 (Đã gói omitted -> .get returns 0)
    assert re.search(r'Đã gói.*badge-order-packed">0<', html), 'Đã gói phải hiển thị 0'
    # 5 status links + Tất cả link sang admin.orders
    assert html.count('href="/admin/orders') >= 6, html.count('href="/admin/orders')
    assert 'href="/admin/orders?status=' in html
    assert 'badge-order-pending' in html and 'badge-order-shipped' in html and 'badge-order-delivered' in html and 'badge-order-cancelled' in html
    assert 'total_products' not in html and 'in_stock' not in html   # 08-03 chưa tới
print('TASK_OK')
PY</automated>
  </verify>
  <done>GET /admin/stats render card "Sản phẩm đã bán" (units 3) + section "Đơn hàng" với breakdown 6 links, status 0 hiển thị 0, không KeyError</done>
  <acceptance_criteria>
    - `app/templates/admin/stats.html` chứa `Sản phẩm đã bán` card với `{{ units_sold }}`; section `Đơn hàng` với `{{ total_orders }}` + `ul.status-breakdown` đủ 6 li (Tất cả + 5 status)
    - Mọi status dùng `status_counts.get('<label>', 0)`; dòng "Tất cả" dùng `badge-neutral` + `url_for('admin.orders')`; 5 status dùng `order_badge_class` + `url_for('admin.orders', status='<label>')`
    - KHÔNG chứa `total_products`/`in_stock`/`out_of_stock`/`discontinued`
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CSS .status-breakdown + .badge-neutral trong style.css</name>
  <files>app/static/css/style.css</files>
  <read_first>
    - app/static/css/style.css (cuối file Phase 8 banner từ 08-01 Task 3; .badge-discontinued line 224 — bộ màu gray cho .badge-neutral; .badge-order-* line 461-465)
    - .planning/phases/08-admin-stats/08-RESEARCH.md (New CSS block: .status-breakdown + .badge-neutral — giá trị chính xác)
    - .planning/phases/08-admin-stats/08-UI-SPEC.md (interaction: row min-height 44px touch target; hover #F9FAFB; .badge-neutral same trio as .badge-discontinued)
  </read_first>
  <action>
    Sửa `app/static/css/style.css` — APPEND TRONG Phase 8 banner (sau các class stats từ 08-01) các rule sau (giá trị chính xác từ RESEARCH New CSS block):
    - `.status-breakdown { list-style: none; margin: 16px 0 0; padding: 0; }`
    - `.status-breakdown li + li { border-top: 1px solid #E5E7EB; }`
    - `.status-breakdown a { display: flex; align-items: center; justify-content: space-between; gap: 8px; min-height: 44px; padding: 12px 8px; color: #1F2937; }`
    - `.status-breakdown a:hover { background: #F9FAFB; text-decoration: none; }`
    - `.badge-neutral { color: #6B7280; background: #F3F4F6; border-color: #E5E7EB; }` (cùng bộ 3 gray như .badge-discontinued — UI-SPEC)

    Không sửa `.badge`/`.badge-order-*` hiện có, không thêm class khác.
  </action>
  <verify>
    <automated>python - <<'PY'
import os
from pathlib import Path
css = Path('app/static/css/style.css').read_text(encoding='utf-8')
for sel in ['.status-breakdown {', '.status-breakdown li + li', '.status-breakdown a', '.status-breakdown a:hover', '.badge-neutral {']:
    assert sel in css, sel
assert '#6B7280' in css and '#F3F4F6' in css and '#E5E7EB' in css  # badge-neutral trio
print('TASK_OK')
PY</automated>
  </verify>
  <done>style.css chứa .status-breakdown (list/li/a/hover) + .badge-neutral gray trio</done>
  <acceptance_criteria>
    - `app/static/css/style.css` chứa `.status-breakdown`, `.status-breakdown li + li`, `.status-breakdown a` (min-height 44px), `.status-breakdown a:hover`, `.badge-neutral` (color #6B7280 / bg #F3F4F6 / border #E5E7EB)
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → /admin/stats | Route admin bảo vệ `@login_required` qua `_protect_admin` before_request. |
| Stats breakdown link → /admin/orders?status= | Link từ template tới route Phase 7 (đã whitelist filter). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | status_counts/total_orders — tần suất đơn theo trạng thái | mitigate | Route trong admin_bp + `_protect_admin` @login_required; template admin-only. |
| T-08-06 | Tampering | `?status=` qua breakdown link | accept | Label status là hằng số VN hardcoded trong template (không từ user); Phase 7 `admin.orders()` whitelist `if status in ORDER_STATUSES` — link sai chỉ show tất cả, không 500, không injection. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Không có package install trong plan này (0 dependency mới). |
</threat_model>

<verification>
- Task 1 verify exits 0 printing TASK_OK: Q4 status_counts omit zero-count (Đã gói absent), total_orders sum đúng
- Task 2 verify exits 0 printing TASK_OK: GET /admin/stats render 200, units_sold 3, breakdown 6 links, Đã gói hiển thị 0, badge-order-* đủ 4 status, không render 08-03 var
- Task 3 verify exits 0 printing TASK_OK: CSS .status-breakdown + .badge-neutral
- Toàn bộ verify chạy trên temp DB (patch BASE_DIR, create_all + seed, SECRET_KEY=test) — không chạm data/app.db
</verification>

<success_criteria>
- Admin thấy card "Sản phẩm đã bán" = sum(quantity) trên đơn Đã gửi+Đã nhận — STAT-03
- Admin thấy "Tổng số đơn" (gồm Đã hủy) + breakdown 5 status, mỗi row link sang /admin/orders?status= — STAT-03
- Status 0 đơn hiển thị 0 (status_counts.get), không KeyError/500 — STAT-03
- Không dependency mới; DB thật không đổi; GET-only
</success_criteria>

<output>
Create `.planning/phases/08-admin-stats/08-02-SUMMARY.md` when done
</output>
