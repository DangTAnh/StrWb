---
phase: 09-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/static/css/style.css
  - app/templates/public/cart.html
  - app/templates/public/_checkout_form.html
  - README.md
  - app/templates/admin/products/form.html
autonomous: true
requirements: [ORD-01, ORD-02, ORD-05, ORD-10]
user_setup: []
must_haves:
  truths:
    - "Cart actions wrap at a 320px viewport instead of overflowing (F-01)"
    - "Cart thumbnails render via a .cart-thumb class — no inline style (F-02)"
    - "Cart badge pill centers 2-digit counts without clipping (F-03)"
    - "Cart and order-detail line totals share the identical accent treatment (F-04, no code change)"
    - "Both v1.1 money totals read exactly 'Tổng cộng' (F-05)"
    - "Checkout button is class-styled width 100%, max-width 320px (F-06)"
    - "README.md line 1 is exactly '# StoreWeb' (R-01)"
    - "Admin product form shows the auto-status help-text (R-02)"
  artifacts:
    - path: "app/static/css/style.css"
      provides: "Phase 9 polish CSS rules"
      contains: ".cart-thumb, .checkout-form .btn, .cart-actions flex-wrap: wrap, .cart-badge line-height: 1"
    - path: "app/templates/public/cart.html"
      provides: "Cart page polished markup"
      contains: "class=\"cart-thumb\" and 'Tổng cộng' (no trailing colon)"
    - path: "app/templates/public/_checkout_form.html"
      provides: "Checkout submit button without inline style"
      contains: "class=\"btn btn-primary\" with no style attribute"
    - path: "README.md"
      provides: "Restored H1"
      contains: "# StoreWeb"
    - path: "app/templates/admin/products/form.html"
      provides: "Restored quantity help-text"
      contains: "Trạng thái tự động"
  key_links:
    - from: "cart.html line 24 <img>"
      to: "style.css .cart-thumb rule"
      via: "class=\"cart-thumb\" attribute"
    - from: "_checkout_form.html line 22 <button>"
      to: "style.css .checkout-form .btn rule"
      via: "class=\"btn btn-primary\" + CSS descendant selector"
---

<objective>
Apply the locked Phase 9 UI polish contract (09-UI-SPEC.md F-01..F-06, R-01, R-02) to the v1.1 surfaces.

Purpose: Close the six confirmed UI-review findings on the new cart/checkout surfaces and restore the two stray uncommitted edits (repo README heading, product-form help-text). No new features, no redesign, no new CSS tokens — only the fixes locked by 09-UI-SPEC.md, each token-bound to the inherited design system.

Output: polished `app/static/css/style.css`, `cart.html`, `_checkout_form.html`; restored `README.md` H1 and `app/templates/admin/products/form.html` help-text. F-04 requires no code change (documented here); V-01/V-02 are executed by plan 09-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-deploy/09-CONTEXT.md
@.planning/phases/09-polish-deploy/09-UI-SPEC.md

<interfaces>
<!-- Exact current state + target state for each locked fix. Cite these directly; do not invent alternatives. -->

From 09-UI-SPEC.md Polish Contract (source of truth):

- F-01 — `style.css:453` `.cart-actions { display: flex; gap: 16px; margin-top: 16px; }` (no wrap) → add `flex-wrap: wrap;` (base rule, NO new media query).
- F-02 — `cart.html:24` `<img ... style="object-fit: cover; border-radius: 4px; vertical-align: middle;" width="80" height="80">` inline style → new `.cart-thumb` class in the Phase 6 CSS section: `{ width: 80px; height: 80px; object-fit: cover; border-radius: 4px; vertical-align: middle; }`. Existing `.thumb` (`style.css:211`, 48px) is too small — do NOT reuse it. Preserve `alt="{{ item.product.name }}"` and `width="80" height="80"`.
- F-03 — `style.css:432-443` `.cart-badge` has `line-height: 1.5;` → change to `line-height: 1;`. No other `.cart-badge` property changes.
- F-04 — NO code change. The shared `.data-table .line-total` rule (`style.css:452`, `font-weight: 600; color: var(--accent); font-variant-numeric: tabular-nums;`) is the intended treatment on BOTH `cart.html:38` and `detail.html:32`. Verify it is untouched.
- F-05 — `cart.html:51` `<span class="cart-total-label">Tổng cộng:</span>` (trailing colon) → drop the colon: `<span class="cart-total-label">Tổng cộng</span>`, matching `detail.html:38`.
- F-06 — `_checkout_form.html:22` `<button type="submit" class="btn btn-primary" style="width: 100%; max-width: 320px;">Đặt hàng</button>` → remove the inline style; add `.checkout-form .btn { width: 100%; max-width: 320px; }` to the Phase 6 CSS section (mirrors product-detail `.cart-cta` max-width 320px).
- R-01 — `README.md:1` currently ` StoreWeb` (leading space, marker lost) → restore `# StoreWeb`. The committed baseline was `# StoreWeb` (git diff confirms the heading is the only uncommitted README change).

Git baseline for the two reverts: `git diff README.md` currently shows only line 1 (`-# StoreWeb` / `+ StoreWeb`); `git diff app/templates/admin/products/form.html` shows only the deleted help-text line. After each revert that diff must be empty.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply CSS polish fixes F-01, F-02, F-03, F-06 to style.css</name>
  <files>app/static/css/style.css</files>
  <read_first>
    - app/static/css/style.css (Phase 6 section, lines 429-458 — read the actual current state before editing)
    - .planning/phases/09-polish-deploy/09-UI-SPEC.md (F-01..F-06 contract — the exact target rules)
  </read_first>
  <action>
    Edit app/static/css/style.css inside the existing Phase 6 section (lines 429-458), using the file's existing 4-space indent style and existing tokens only. Four changes:
    1. F-01: in the `.cart-actions` rule (currently line 453) add `flex-wrap: wrap;` so the two cart buttons wrap at a 320px viewport instead of overflowing. Target rule: `.cart-actions { display: flex; flex-wrap: wrap; gap: 16px; margin-top: 16px; }`. No media query, no other property changes.
    2. F-02: add a new `.cart-thumb` rule in the Phase 6 section near `.cart-qty-input`: `width: 80px; height: 80px; object-fit: cover; border-radius: 4px; vertical-align: middle;`. Do NOT reuse the existing `.thumb` rule at line 211 (48px — too small for the cart's 80px thumbnail). Do not duplicate the selector.
    3. F-03: in the `.cart-badge` rule (lines 432-443) change `line-height: 1.5;` to `line-height: 1;` so 14px badge text centers within the 20px pill at any digit count. No other `.cart-badge` property changes.
    4. F-06: add `.checkout-form .btn { width: 100%; max-width: 320px; }` in the Phase 6 section.
    F-04 is verify-only — confirm the `.data-table .line-total` rule at line 452 is unchanged (`font-weight: 600; color: var(--accent); font-variant-numeric: tabular-nums;`) and shared by cart and order-detail; do not edit it.
  </action>
  <acceptance_criteria>
    - `grep -n "flex-wrap" app/static/css/style.css` → `flex-wrap: wrap;` appears in the `.cart-actions` rule only (one match).
    - `grep -n "\.cart-thumb" app/static/css/style.css` → exactly 1 match; `grep -A 1 "\.cart-thumb" app/static/css/style.css` shows `width: 80px; height: 80px;`.
    - `grep -A 10 "\.cart-badge {" app/static/css/style.css` → contains `line-height: 1;` and NOT `line-height: 1.5`.
    - `grep -n "\.checkout-form \.btn" app/static/css/style.css` → 1 match; the rule body is `width: 100%; max-width: 320px;`.
    - `.data-table .line-total` rule at line 452 is byte-identical to the current state (accent 600 + tabular-nums).
    - No new hex values added anywhere in style.css (diff shows only the 4 rule changes).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "flex-wrap: wrap" app/static/css/style.css &amp;&amp; grep -c "\.cart-thumb" app/static/css/style.css &amp;&amp; grep -n "line-height: 1;" app/static/css/style.css | head -1 &amp;&amp; grep -n "\.checkout-form \.btn" app/static/css/style.css</automated>
  </verify>
  <done>
    All four CSS rules are present exactly as specified, .cart-thumb and .checkout-form .btn appear once each, no new hex values, `.data-table .line-total` untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply template polish fixes F-02, F-05, F-06</name>
  <files>app/templates/public/cart.html, app/templates/public/_checkout_form.html</files>
  <read_first>
    - app/templates/public/cart.html (lines 20-60 — read current markup before editing)
    - app/templates/public/_checkout_form.html (line 22)
    - .planning/phases/09-polish-deploy/09-UI-SPEC.md (F-02, F-05, F-06 contracts)
  </read_first>
  <action>
    Edit the two public templates. Three changes, no other markup edits:
    1. F-02 — cart.html line 24: replace the inline `style="object-fit: cover; border-radius: 4px; vertical-align: middle;"` on the product thumbnail `<img>` with `class="cart-thumb"`. Preserve `src="{{ url_for('static', filename='uploads/' + item.product.primary_image.thumb_filename) }}"`, `alt="{{ item.product.name }}"`, and the `width="80" height="80"` attributes exactly.
    2. F-05 — cart.html line 51: change `<span class="cart-total-label">Tổng cộng:</span>` to `<span class="cart-total-label">Tổng cộng</span>` — drop the trailing colon so it matches detail.html line 38.
    3. F-06 — _checkout_form.html line 22: remove `style="width: 100%; max-width: 320px;"` from the submit button, leaving `<button type="submit" class="btn btn-primary">Đặt hàng</button>`. The `.checkout-form .btn` rule from Task 1 supplies the width.
    Do not touch the empty-state branch of cart.html (lines 62-68) — V-02 reuses the existing cart-empty screenshots, so it must remain byte-identical.
  </action>
  <acceptance_criteria>
    - `grep -n "cart-thumb" app/templates/public/cart.html` → line 24 contains `class="cart-thumb"`; that same line has NO `style=` attribute.
    - `grep -n "cart-total-label" app/templates/public/cart.html` → the label line is `<span class="cart-total-label">Tổng cộng</span>` (no colon, no `:` inside).
    - `grep -n "btn-primary" app/templates/public/_checkout_form.html` → the submit button has NO `style=` attribute.
    - `grep -n "style=" app/templates/public/cart.html app/templates/public/_checkout_form.html` → zero matches (both files inline-style-free).
    - cart.html empty-state branch (lines 62-68) unchanged from the current committed state.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "style=" app/templates/public/cart.html app/templates/public/_checkout_form.html 2>/dev/null; grep -n "class=\"cart-thumb\"" app/templates/public/cart.html; grep -n "Tổng cộng</span>" app/templates/public/cart.html</automated>
  </verify>
  <done>
    Both templates contain zero inline `style=` attributes; the cart thumbnail uses class="cart-thumb"; both money totals read "Tổng cộng"; the checkout button is `class="btn btn-primary"` only.
  </done>
</task>

<task type="auto">
  <name>Task 3: Restore reverted edits R-01 (README heading) and R-02 (form help-text)</name>
  <files>README.md, app/templates/admin/products/form.html</files>
  <read_first>
    - README.md (line 1 — current stray ` StoreWeb`)
    - app/templates/admin/products/form.html (quantity block, lines 63-68)
    - 09-UI-SPEC.md (R-01 and R-02 contracts — the exact baseline strings)
  </read_first>
  <action>
    Restore the two stray uncommitted edits to their committed baseline:
    1. R-01 — README.md line 1: change the current ` StoreWeb` (leading space, heading marker lost) to `# StoreWeb`. After this edit `git diff README.md` must show NO diff (the heading was the only uncommitted README change). Do not add or remove any other README content — the v1.1 description line belongs to plan 09-03.
    2. R-02 — app/templates/admin/products/form.html: inside the quantity `.form-field` block, immediately after the `{% for error in form.quantity.errors %}` loop (line 67), re-add the deleted help-text line verbatim:
    Use the HTML entity `&gt;` (not a raw `>`) and the curly quotes `“…”` exactly as shown. After this edit `git diff app/templates/admin/products/form.html` must show NO diff (this file returns to the committed baseline).
  </action>
  <acceptance_criteria>
    - `sed -n '1p' README.md` outputs exactly `# StoreWeb`.
    - `git diff README.md` is empty.
    - `git diff app/templates/admin/products/form.html` is empty.
    - `grep -n "Trạng thái tự động" app/templates/admin/products/form.html` matches one line containing `&gt;` and `“Ngừng bán”`.
  </acceptance_criteria>
  <verify>
    <automated>sed -n '1p' README.md; git diff --quiet README.md app/templates/admin/products/form.html &amp;&amp; echo "REVERTS_CLEAN"</automated>
  </verify>
  <done>
    Both files return to their committed baselines — `git diff` is empty for README.md and form.html; README.md line 1 is `# StoreWeb`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template markup → browser | Cart/checkout Jinja2 renders product-supplied data (name, price) into HTML; polish edits alter static class/markup only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | cart.html / _checkout_form.html markup | accept | Edits are static class/label changes; Jinja2 autoescaping and the render paths are untouched — no new user-controlled injection surface |
| T-09-02 | Tampering | style.css | accept | CSS-only rule changes using inherited tokens; no script content; no new hex values |
| T-09-03 | Tampering | form.html help-text (R-02) | mitigate | Restore the committed baseline verbatim with the HTML entity `&gt;` and curly quotes — never a raw `>` — so the restored string cannot break HTML structure |

No package installs in this plan — package-legitimacy gate not applicable (T-{phase}-SC not required).
</threat_model>

<verification>
1. Source assertions from Tasks 1-3 pass (grep outputs match the specified target state).
2. App still boots after the edits: `SECRET_KEY=test python -c "from wsgi import app; print(app.test_client().get('/').status_code)"` → `200` (does not touch `data/app.db` writes; home page renders).
3. `git diff --stat` shows only the intended files changed and `git diff README.md app/templates/admin/products/form.html` is empty (both reverts clean).
4. F-04 documented (shared `.line-total` accent) — verified by plan 09-02 V-02 screenshots.
</verification>

<success_criteria>
- style.css contains exactly the four new rules from the contract (`.cart-actions` flex-wrap, `.cart-thumb`, `.cart-badge` line-height 1, `.checkout-form .btn`) and no other changes.
- cart.html and _checkout_form.html contain zero inline styles; cart total label is "Tổng cộng"; thumbnail uses class="cart-thumb".
- README.md line 1 = `# StoreWeb`; form.html help-text restored; both `git diff` empty.
- No v1.0 surface markup altered (home/search/product_detail untouched); empty-state cart branch byte-identical.
</success_criteria>

<output>
Create `.planning/phases/09-polish-deploy/09-01-SUMMARY.md` when done
</output>
