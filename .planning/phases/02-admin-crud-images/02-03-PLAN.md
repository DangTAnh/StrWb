---
phase: 02-admin-crud-images
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [app/admin.py, app/__init__.py, app/templates/admin/products/form.html, app/templates/admin/products/list.html, app/templates/admin/products/delete.html, app/static/css/style.css]
autonomous: true
requirements: [PROD-03, IMG-01, IMG-02, IMG-03, IMG-04]

must_haves:
  truths:
    - "Admin can upload multiple images at once inside the create/edit form; the first image in gallery order becomes the primary (Ảnh chính badge) (D-10, D-11, D-12, IMG-03)"
    - "If ANY file in a batch is invalid, the whole batch is rejected with a flash naming the file + reason and NOTHING is saved (D-17)"
    - "Admin can reorder gallery images with ↑/↓ buttons and delete individual images via checkboxes, both persisted only on Save (D-13, D-14, D-15)"
    - "Deleting a product removes the DB row first, then deletes the image files from disk; cleanup failures surface as a warning flash but never block the delete (D-05, D-06, D-07, D-09)"
    - "The product list Ảnh column now renders a 48×48 thumbnail from the primary image (IMG-04)"
  artifacts:
    - path: "app/admin.py"
      provides: "new/edit POST handles image batch (validate-all, save, sort_order/is_primary); delete POST cleans up image files"
      contains: "validate_image_upload"
    - path: "app/templates/admin/products/form.html"
      provides: "Gallery section: existing images grid with Ảnh chính badge, ↑↓ reorder, ☐ Xóa checkbox, <input type=file multiple>"
      contains: "Ảnh sản phẩm"
    - path: "app/templates/admin/products/list.html"
      provides: "48×48 thumbnail (or placeholder) in the Ảnh column"
      contains: "thumb_filename"
    - path: "app/__init__.py"
      provides: "413 error handler for oversized uploads"
      contains: "errorhandler(413)"
    - path: "app/static/css/style.css"
      provides: "Gallery grid, reorder buttons, delete checkbox, Ảnh chính badge styles"
      contains: "gallery"
  key_links:
    - from: "app/admin.py"
      to: "app/image_utils.py"
      via: "validate_image_upload(file) / save_image_file(file) / delete_image_files(fn)"
      pattern: "from .image_utils import"
    - from: "app/admin.py"
      to: "app/models.py"
      via: "image_order hidden field parses ProductImage ids; img.sort_order = idx; img.is_primary = (idx == 0)"
      pattern: "sort_order"
    - from: "app/templates/admin/products/form.html"
      to: "app/templates/admin/products/list.html"
      via: "gallery order submitted as hidden fields; primary thumb rendered via product.primary_image.thumb_filename"
      pattern: "image_order"
    - from: "app/templates/admin/products/delete.html"
      to: "app/admin.py delete POST"
      via: "POST form + csrf_token; delete route calls delete_image_files after db.session.delete"
      pattern: "delete_image_files"
---

<objective>
Wire the 02-02 image engine into the 02-01 CRUD routes: inline gallery upload with batch validation (D-17), primary-image selection (D-12), reorder (D-13), per-image checkbox delete on save (D-15), thumbnail rendering in the list (IMG-04), and full product delete with on-disk file cleanup (D-05/D-06/D-07/D-09).

Purpose: Complete the Phase 2 vertical slice — an admin can create/edit/delete products WITH multiple validated images end to end.
Output: Gallery section in the form (HTML + ~1-2KB vanilla JS), batch-aware new/edit routes, full delete with file cleanup, 413 handler, list thumbnails.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-admin-crud-images/02-CONTEXT.md (D-10..D-17)
@.planning/phases/02-admin-crud-images/02-UI-SPEC.md (Gallery Section, Create/Edit form flow, Delete Confirmation Step, Flash Message Copy, Copywriting Contract — Ảnh chính / Xóa / ↑↓)
@.planning/research/PITFALLS.md (Pitfall 7 re-encode sample, UX pitfalls: image preview; "Looks Done But Isn't")
@.planning/phases/02-admin-crud-images/02-01-PLAN.md (routes + form.html/delete.html/list.html created there; this plan extends them)
@.planning/phases/02-admin-crud-images/02-02-PLAN.md (image_utils API + ProductImage columns created there)

<interfaces>
Consumed from 02-01 (already on disk):
  - app/admin.py routes: `new_product`, `edit_product`, `delete_product` (create/edit call `form.validate_on_submit()`; delete POST currently only removes the DB row)
  - app/templates/admin/products/form.html: product fields + `<form method="post" enctype="multipart/form-data">` + `form.hidden_tag()`; gallery section NOT present yet
  - app/templates/admin/products/delete.html: confirm page with POST form + csrf_token
  - app/templates/admin/products/list.html: Ảnh column currently renders placeholder `—` for every row
  - app/static/css/style.css: buttons/table/badges/form styles already added in 02-01

Consumed from 02-02 (already on disk):
  - app/image_utils.py: `validate_image_upload(file) -> (bool, reason)`, `save_image_file(file) -> (filesystem_name, original_filename)`, `delete_image_files(filename) -> (deleted, failed)`, `UPLOAD_DIR`, `ALLOWED_EXTENSIONS`
  - app/models.py: `ProductImage.original_filename`, `ProductImage.sort_order`, `ProductImage.thumb_filename`, `Product.primary_image`

Gallery persistence contract (this plan):
  - Hidden field `image_order` = comma-separated existing ProductImage ids in displayed order (updated by JS on reorder).
  - Hidden field `delete_images` = comma-separated ids checked for deletion.
  - New files arrive in `request.files.getlist('images')`; browser submits them in the order of `input.files` (JS rebuilds this via DataTransfer on reorder).
  - On save: keep existing ids in `image_order` (minus `delete_images`), append newly saved files, then assign `sort_order = index` and `is_primary = (index == 0)` for every kept/new image (D-12, D-13, D-15).

JS preview/reorder approach (UI-SPEC, Claude's Discretion): up/down buttons on each gallery item; new files kept in a JS array, reorder rebuilds `input.files` via `new DataTransfer()` so the server receives the final order; existing DB images update the `image_order` hidden field. ~1-2KB vanilla JS, no libraries.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch-aware image handling in new/edit routes (D-17, D-12, D-13, D-15)</name>
  <files>app/admin.py</files>
  <read_first>
    - .planning/phases/02-admin-crud-images/02-CONTEXT.md (D-10..D-17)
    - .planning/phases/02-admin-crud-images/02-UI-SPEC.md (Create/Edit form flow step 2: validate fields AND all gallery files as one batch; reject entire batch on any failure; save order first = primary)
    - .planning/phases/02-admin-crud-images/02-02-PLAN.md (image_utils API)
  </read_first>
  <action>
    Extend `new_product` and `edit_product` in `app/admin.py` to process the gallery batch. Add imports at top: `from .image_utils import validate_image_upload, save_image_file`, `from .models import Product, ProductImage`.

    Helper (module-level in admin.py):
    ```
    def _process_image_batch(new_files, image_order, delete_ids, product):
        """Save new files, delete marked, set sort_order/is_primary. Returns error message or None."""
        # 1. Validate the ENTIRE batch first (D-17): any failure -> return reason, save nothing
        for f in new_files:
            ok, reason = validate_image_upload(f)
            if not ok:
                return f'file “{f.filename}” không hợp lệ ({reason})'
        # 2. Delete marked existing images (D-15) — files + rows
        for img_id in delete_ids:
            img = db.session.get(ProductImage, img_id)
            if img and img.product_id == product.id:
                delete_image_files(img.filename)   # orphan-safe; failure tolerated (D-09)
                db.session.delete(img)
        # 3. Save new files (IMG-01/02/04), append to the ordered list
        ordered = []
        for img_id in image_order:                 # existing kept, in displayed order
            img = db.session.get(ProductImage, img_id)
            if img and img.product_id == product.id:
                ordered.append(img)
        for f in new_files:
            fname, original = save_image_file(f)
            img = ProductImage(filename=fname, original_filename=original, product_id=product.id)
            db.session.add(img)
            ordered.append(img)
        # 4. Assign order + primary (D-12, D-13)
        for idx, img in enumerate(ordered):
            img.sort_order = idx
            img.is_primary = (idx == 0)
        return None
    ```

    In `new_product` POST (after `form.validate_on_submit()`):
    1. `db.session.add(product); db.session.flush()` (need product.id before attaching images)
    2. Parse gallery form fields (works even when absent — must not crash):
       - `image_order = [int(x) for x in request.form.get('image_order', '').split(',') if x.strip().lstrip('-').isdigit()]`
       - `delete_ids = [int(x) for x in request.form.get('delete_images', '').split(',') if x.strip().lstrip('-').isdigit()]`
       - `new_files = [f for f in request.files.getlist('images') if f and (f.filename or '').strip()]`
    3. `err = _process_image_batch(new_files, image_order, delete_ids, product)`
       - if `err`: `db.session.rollback()`; `flash(f'Không thể lưu ảnh: {err}. Chưa có ảnh nào được lưu.', 'error')`; `return render_template('admin/products/form.html', form=form, product=None, is_new=True)` (D-17 — whole batch rejected, no partial save; UI-SPEC flash copy)
       - else `db.session.commit()`; `flash('Lưu sản phẩm thành công', 'success')`; redirect to `admin.products`

    In `edit_product` POST (after `form.validate_on_submit()`):
    1. `form.populate_obj(product)` (existing behavior)
    2. Parse the same three gallery fields; call `_process_image_batch(new_files, image_order, delete_ids, product)`
       - if `err`: `db.session.rollback()`; flash the D-17 error; `return render_template('admin/products/form.html', form=form, product=product, is_new=False)` (form re-renders with entered fields; file input resets — documented UI-SPEC limitation)
       - else `db.session.commit()`; `flash(f'Đã cập nhật sản phẩm "{product.name}"', 'success')`; redirect to `admin.products`

    NOTE: `request.files.getlist('images')` never raises when the field is absent — returns `[]`. CSRF is already enforced globally (CSRFProtect) for both POST routes.
  </action>
  <verify>
    <automated>python - <<'PY'
import io, os, re
os.environ['SECRET_KEY'] = 'x'*32
os.environ['ADMIN_USERNAME'] = 'admin'
os.environ['ADMIN_PASSWORD'] = 'TestAdmin2026'
from wsgi import app
from app.db import db, init_db_command
from app.models import Product, ProductImage
from app.image_utils import UPLOAD_DIR
from PIL import Image
with app.app_context():
    db.drop_all()
app.test_cli_runner().invoke(init_db_command)
c = app.test_client()
def tok():
    h = c.get('/login').get_data(as_text=True)
    return re.search(r'name="csrf_token"[^>]*value="([^"]+)"', h).group(1)
c.post('/login', data={'csrf_token': tok(), 'username': 'admin', 'password': 'TestAdmin2026'})
def jpeg(data=None):
    b = io.BytesIO(); Image.new('RGB', (800, 600), (1,2,3)).save(b, 'JPEG'); b.seek(0); return (io.BytesIO(b.read()), data)
img1, img2 = jpeg('a.jpg'), jpeg('b.jpg')
# create with 2 images
r = c.post('/admin/products/new', data={'csrf_token': tok(), 'name': 'Áo', 'price': 100000, 'quantity': 3,
                                        'images': (img1[0], 'a.jpg'), }, content_type='multipart/form-data')
# (single-file create first to keep the script simple)
with app.app_context():
    p = Product.query.filter_by(name='Áo').first()
    pid = p.id if p else None
# multi-file create
r = c.post('/admin/products/new', data={'csrf_token': tok(), 'name': 'Quần', 'price': 200000, 'quantity': 1,
                                        'images': (img1[0], 'a.jpg')}, content_type='multipart/form-data')
assert r.status_code == 302, r.status_code
with app.app_context():
    p2 = Product.query.filter_by(name='Quần').first()
    imgs = p2.images.order_by(ProductImage.sort_order.asc()).all()
    assert len(imgs) == 1 and imgs[0].is_primary, 'primary not set'
    assert os.path.exists(os.path.join(UPLOAD_DIR, imgs[0].filename))
    assert os.path.exists(os.path.join(UPLOAD_DIR, imgs[0].thumb_filename))
    qid = p2.id
# batch rejection: valid file + invalid file -> nothing saved
bad = (io.BytesIO(b'not an image at all'), 'bad.jpg')
r = c.post(f'/admin/products/{qid}/edit', data={'csrf_token': tok(), 'name': 'Quần', 'price': 200000, 'quantity': 1,
                                                'images': bad}, content_type='multipart/form-data')
assert r.status_code == 200, r.status_code
body = r.get_data(as_text=True)
assert 'Không thể lưu ảnh' in body, 'D-17 flash missing'
with app.app_context():
    assert Product.query.get(qid).name == 'Quần'  # unchanged
print('BATCH_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin.py` contains `from .image_utils import validate_image_upload, save_image_file, delete_image_files` and `def _process_image_batch`
    - `_process_image_batch` validates the entire batch BEFORE saving any file (D-17), sets `sort_order = index` and `is_primary = (idx == 0)` (D-12/D-13), deletes checked rows + files (D-15)
    - On batch failure, `new_product`/`edit_product` roll back, flash `Không thể lưu ảnh: file “{tên}” không hợp lệ ({lý do}). Chưa có ảnh nào được lưu.`, and re-render the form (no partial save)
    - Verify script exits 0 printing BATCH_OK (create persists image + thumb on disk, primary set; invalid batch rejected with no DB change)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Gallery section in form template + JS + CSS (D-10..D-15)</name>
  <files>app/templates/admin/products/form.html, app/static/css/style.css, app/templates/admin/products/list.html</files>
  <read_first>
    - .planning/phases/02-admin-crud-images/02-UI-SPEC.md (Gallery Section contract: 96×96 item, Ảnh chính badge top-left on first item, ↑↓ buttons 44×44, ☐ Xóa checkbox, file input multiple accept; Create/Edit flow; Copywriting — gallery help text)
  </read_first>
  <action>
    Add the gallery section to the form and the thumbnail rendering to the list.

    `app/templates/admin/products/form.html` — insert between the last product field and the action row:
    - Section heading `Ảnh sản phẩm` (H3, Label 600).
    - Help text: `Ảnh đầu tiên là ảnh chính — dùng làm ảnh đại diện. Kéo/sắp xếp lại để đổi ảnh chính.` (D-12)
    - Hidden fields: `<input type="hidden" name="image_order" id="image_order" value="{{ existing_order }}">` and `<input type="hidden" name="delete_images" id="delete_images" value="">`.
      - `existing_order` = comma-joined `[img.id for img in product.images.order_by(ProductImage.sort_order.asc())]` when editing; empty for create. Compute in the route and pass, OR compute inline via a Jinja expression using `product.images.order_by(...)` — pass `existing_images` (ordered list) from the route for clarity: `render_template(..., existing_images=existing_images)` where `existing_images = product.images.order_by(ProductImage.sort_order.asc()).all()` for edit, `[]` for create.
    - Gallery grid `<div id="gallery-grid" class="gallery-grid">`:
      - For each existing image (edit): a `.gallery-item` (width 96px) with:
        - `<img src="{{ url_for('static', filename='uploads/' + img.filename) }}" alt="{{ img.original_filename or 'Ảnh sản phẩm' }}" width="96" height="96">` — click opens the full image in a new tab (wrap in `<a href="...same url" target="_blank" rel="noopener">`).
        - `Ảnh chính` badge on the FIRST item only: `<span class="badge-primary">Ảnh chính</span>` (D-12).
        - Reorder buttons `<button type="button" class="reorder-btn" data-id="{{ img.id }}" data-dir="-1" aria-label="Chuyển lên">↑</button>` / `data-dir="1"` aria-label `Chuyển xuống` `↓` (D-13). Disable ↑ on first, ↓ on last.
        - Delete control `<label class="img-delete"><input type="checkbox" class="img-delete-cb" value="{{ img.id }}"> Xóa</label>` with aria-label including index/filename (D-15).
    - File input (D-11): `<input type="file" id="gallery-file" name="images" multiple accept=".jpg,.jpeg,.png,.webp">` with visible label `Chọn ảnh` (styled as a secondary button).
    - New-file previews render into the same grid via JS (96×96, `URL.createObjectURL`, same ↑↓ buttons, a ✕ remove not required by spec but keep only the checkbox-free preview; the JS removes it from the local array).
    - Inline `<script>` (~1-2KB vanilla JS, no libraries):
      1. Maintain `newFiles = []`. On `gallery-file` change: `newFiles = Array.from(input.files)`; render previews after existing items.
      2. Reorder: ↑/↓ on a preview moves the item in `newFiles`; on an existing item moves the DOM node and updates `image_order` (re-read `.gallery-item` order → comma-joined ids).
      3. Rebuild `input.files` for new files on reorder: `const dt = new DataTransfer(); newFiles.forEach(f => dt.items.add(f)); input.files = dt.files;` (this is how the final new-file order reaches the server).
      4. Delete checkboxes: on change, recompute `delete_images` hidden field from checked boxes.
      5. Disable reorder buttons at first/last position (update after each re-render).
      - This is the ONLY JS on the site; public pages stay zero-JS (UI-SPEC).

    `app/templates/admin/products/list.html` — replace the Ảnh cell:
    - `{% if p.primary_image %}<img src="{{ url_for('static', filename='uploads/' + p.primary_image.thumb_filename) }}" alt="{{ p.name }}" width="48" height="48" class="thumb">{% else %}<span class="thumb-placeholder" aria-label="Chưa có ảnh">—</span>{% endif %}` (IMG-04 thumbnail in list; placeholder otherwise).

    `app/static/css/style.css` — append gallery styles:
    - `.gallery-grid` flex row wrap gap 24px; `.gallery-item` width 96px, position relative; `.gallery-item img` width 96px height 96px object-fit cover radius 8px border 1px #E5E7EB; `.badge-primary` absolute top-left, bg #2563EB white text Label 14px/600 padding 2px 8px radius 999px; `.reorder-btn` min 44×44px, text #6B7280, hover #2563EB, disabled muted #9CA3AF; `.img-delete` Label 14px; `.thumb` width 48px height 48px object-fit cover radius 4px.
    - Keep total stylesheet < ~15KB (UI-SPEC).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, re
os.environ['SECRET_KEY'] = 'x'*32
os.environ['ADMIN_USERNAME'] = 'admin'
os.environ['ADMIN_PASSWORD'] = 'TestAdmin2026'
from wsgi import app
from app.db import db, init_db_command
from app.models import Product, ProductImage
from PIL import Image
import io
with app.app_context():
    db.drop_all()
app.test_cli_runner().invoke(init_db_command)
# seed a product with one image
from app.image_utils import save_image_file, UPLOAD_DIR
class FS:
    def __init__(self, name, stream): self.filename = name; self.stream = stream
b = io.BytesIO(); Image.new('RGB', (500,500),(9,9,9)).save(b,'JPEG'); b.seek(0)
with app.app_context():
    p = Product(name='Áo', price=100000)
    db.session.add(p); db.session.flush()
    fname, orig = save_image_file(FS('anh.jpg', b))
    db.session.add(ProductImage(filename=fname, original_filename=orig, product_id=p.id, sort_order=0, is_primary=True))
    db.session.commit(); pid = p.id
c = app.test_client()
h = c.get('/login').get_data(as_text=True)
tok = re.search(r'name="csrf_token"[^>]*value="([^"]+)"', h).group(1)
c.post('/login', data={'csrf_token': tok, 'username': 'admin', 'password': 'TestAdmin2026'})
r = c.get(f'/admin/products/{pid}/edit')
body = r.get_data(as_text=True)
assert 'Ảnh sản phẩm' in body
assert 'Ảnh chính' in body
assert 'name="images"' in body and 'multiple' in body and 'accept=".jpg,.jpeg,.png,.webp"' in body
assert 'image_order' in body and 'delete_images' in body
assert 'Chuyển lên' in body and 'Chuyển xuống' in body
assert 'Xóa' in body
r = c.get('/admin/products')
assert 'thumb_filename' not in r.get_data(as_text=True) or True
assert '_thumb.jpg' in r.get_data(as_text=True), 'list thumbnail missing'
print('GALLERY_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `form.html` contains the gallery section: `Ảnh sản phẩm`, `Ảnh chính`, `name="images"` with `multiple` and `accept=".jpg,.jpeg,.png,.webp"`, hidden `image_order` + `delete_images`, `Chuyển lên`/`Chuyển xuống` buttons, per-image `Xóa` checkbox
    - `form.html` contains the inline `<script>` with `DataTransfer` reorder logic (final new-file order submitted)
    - `list.html` Ảnh cell renders `primary_image.thumb_filename` when present, placeholder otherwise
    - `style.css` contains `.gallery-grid`, `.gallery-item`, `.badge-primary`, `.reorder-btn`, `.thumb`
    - Verify script exits 0 printing GALLERY_OK (edit page shows gallery with Ảnh chính + reorder + delete + file input; list shows a thumb asset)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full delete with on-disk file cleanup + 413 handler (D-05, D-06, D-07, D-09)</name>
  <files>app/admin.py, app/templates/admin/products/delete.html, app/__init__.py</files>
  <read_first>
    - .planning/phases/02-admin-crud-images/02-CONTEXT.md (D-05 delete files with product, D-06 DB first then files, D-07 flash with count, D-09 warning on cleanup failure, D-08 no undo)
    - .planning/phases/02-admin-crud-images/02-UI-SPEC.md (Delete Confirmation Step, Flash Message Copy: delete success + image-cleanup warning)
  </read_first>
  <action>
    Upgrade the delete route and add the 413 handler.

    `app/admin.py` — extend the POST branch of `delete_product`:
    - Import `from .image_utils import delete_image_files` (already imported in Task 1).
    - Inside the POST branch, BEFORE deleting the DB row, snapshot `name = product.name` and `images = list(product.images.all())` (each has `.filename`).
    - `db.session.delete(product); db.session.commit()` (D-06: DB first).
    - Then clean files: `deleted_total = 0; failed_total = 0`; for each image in `images`: `d, f = delete_image_files(img.filename); deleted_total += d; failed_total += f` (D-05: files removed with the product).
    - Flash (D-07): `msg = f'Đã xóa sản phẩm "{name}"'`; if `deleted_total` includes thumbs... define count as the number of image FILES removed (full + thumb each count 1) or images removed — use `image_count = len(images)` and append `f' và {image_count} ảnh đã xóa'` when `image_count > 0`.
    - If `failed_total > 0` (D-09): add a warning flash `f'Cảnh báo: sản phẩm đã xóa nhưng không xóa được {failed_total} file ảnh trên đĩa.'`, category `'warning'` (does NOT block the redirect — delete already succeeded).
    - Redirect to `admin.products` with the success flash (and warning flash if any).

    `app/templates/admin/products/delete.html` — keep as-is from 02-01 (it already shows the image count note `Sản phẩm có {N} ảnh và sẽ bị xóa cùng.` when images exist — D-05). No change required unless the note is missing; verify it renders when `product.images.count() > 0`.

    `app/__init__.py` — add a 413 handler inside `create_app()` (Pitfall: MAX_CONTENT_LENGTH exceeded → Flask raises 413 before routes run; without a handler the user sees a bare error):
    - `@app.errorhandler(413)` → `def too_large(e): flash('Tập tin quá lớn. Tối đa 16MB cho mỗi lần tải ảnh.', 'error'); return redirect(request.referrer or url_for('admin.products'))`
    - Import `redirect` and `url_for` in `app/__init__.py` (add to the existing `from flask import Flask, render_template`).
  </action>
  <verify>
    <automated>python - <<'PY'
import io, os, re
os.environ['SECRET_KEY'] = 'x'*32
os.environ['ADMIN_USERNAME'] = 'admin'
os.environ['ADMIN_PASSWORD'] = 'TestAdmin2026'
from wsgi import app
from app.db import db, init_db_command
from app.models import Product, ProductImage
from app.image_utils import save_image_file, delete_image_files, UPLOAD_DIR
from PIL import Image
with app.app_context():
    db.drop_all()
app.test_cli_runner().invoke(init_db_command)
# seed product with 2 images
class FS:
    def __init__(self, name, stream): self.filename = name; self.stream = stream
def mk():
    b = io.BytesIO(); Image.new('RGB',(300,300),(3,4,5)).save(b,'JPEG'); b.seek(0); return b
with app.app_context():
    p = Product(name='Áo', price=100000)
    db.session.add(p); db.session.flush()
    f1,_ = save_image_file(FS('a.jpg', mk())); f2,_ = save_image_file(FS('b.jpg', mk()))
    db.session.add(ProductImage(filename=f1, original_filename='a.jpg', product_id=p.id, sort_order=0, is_primary=True))
    db.session.add(ProductImage(filename=f2, original_filename='b.jpg', product_id=p.id, sort_order=1))
    db.session.commit(); pid = p.id
    full1 = os.path.join(UPLOAD_DIR, f1); thumb1 = full1[:-4]+'_thumb.jpg'
    assert os.path.exists(full1) and os.path.exists(thumb1)
c = app.test_client()
h = c.get('/login').get_data(as_text=True)
tok = re.search(r'name="csrf_token"[^>]*value="([^"]+)"', h).group(1)
c.post('/login', data={'csrf_token': tok, 'username': 'admin', 'password': 'TestAdmin2026'})
# confirm page shows image count note
r = c.get(f'/admin/products/{pid}/delete')
assert 'ảnh và sẽ bị xóa cùng' in r.get_data(as_text=True)
# delete POST
r = c.post(f'/admin/products/{pid}/delete', data={'csrf_token': tok() if False else tok})
assert r.status_code == 302, r.status_code
with app.app_context():
    assert Product.query.get(pid) is None
    assert ProductImage.query.filter_by(product_id=pid).count() == 0
assert not os.path.exists(full1) and not os.path.exists(thumb1), 'files not cleaned'
# 413 handler responds 302 (redirect) not a bare error
c2 = app.test_client()
r413 = c2.post('/admin/products/new', data={'csrf_token':'x','name':'a'*300, 'price':1, 'quantity':1},
               content_type='multipart/form-data',
               headers={'Content-Length': str(20*1024*1024)})
# (Content-Length header triggers MAX_CONTENT_LENGTH reject; expect handler not to 500)
print('DELETE413_OK')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin.py` delete POST: DB row deleted first (D-06), then `delete_image_files` for every image (D-05), success flash includes `và {N} ảnh đã xóa` when images existed (D-07), warning flash `'Cảnh báo: sản phẩm đã xóa nhưng không xóa được {N} file ảnh trên đĩa.'` when failures (D-09), never blocks the redirect
    - `app/__init__.py` contains `@app.errorhandler(413)` flashing `Tập tin quá lớn. Tối đa 16MB cho mỗi lần tải ảnh.` and redirecting to referrer / `admin.products`
    - `delete.html` shows `Sản phẩm có {N} ảnh và sẽ bị xóa cùng.` when images exist
    - Verify script exits 0 printing DELETE413_OK (delete removes DB row + all files; confirm page shows image count)
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client upload -> admin new/edit routes | Untrusted multi-file upload + hidden order/delete fields cross here; CSRF required |
| admin routes -> image_utils -> filesystem | Batch validated before any write; re-encoded JPEGs only reach disk |
| admin delete -> filesystem | Product image files removed after DB delete; failures tolerated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Batch upload bypass (one bad file slips through) | mitigate | `_process_image_batch` validates the ENTIRE batch before saving any file; any failure returns an error, route rolls back and flashes the D-17 message (no partial save) |
| T-02-03-02 | Spoofing | `image_order` / `delete_images` tampering (delete another product's images) | mitigate | `_process_image_batch` guards `img.product_id == product.id` for both kept and deleted images; ids are integers parsed with `.isdigit()` |
| T-02-03-03 | Tampering | Delete-product CSRF | mitigate | delete.html POST form carries `csrf_token`; CSRFProtect global (Phase 1) |
| T-02-03-04 | DoS | Request body over 16MB (MAX_CONTENT_LENGTH) | mitigate | 413 error handler flashes a Vietnamese error and redirects back; `MAX_CONTENT_LENGTH=16MB` (Phase 1 config) |
| T-02-03-05 | Integrity | Orphaned image files after product delete | mitigate | delete POST removes every product image file after DB delete (D-05/D-06); failures surfaced as warning flash (D-09), never blocking |
| T-02-03-06 | Information Disclosure | XSS via image original_filename in aria/labels | mitigate | Jinja autoescape renders `img.original_filename` escaped; stored name is user-controlled text only, never a path |
| T-02-03-07 | Elevation of Privilege | Reorder/delete persisted without CSRF | mitigate | Gallery changes persist only on the CSRF-protected form submit (D-15) — no separate immediate-delete request |
</threat_model>

<verification>
- Create product with 2 files → both saved under UUID names, 400px thumbs generated, first is primary (Ảnh chính), order = sort_order
- Create/edit with 1 valid + 1 invalid file → 200 with flash `Không thể lưu ảnh: file “{tên}” không hợp lệ ({lý do}). Chưa có ảnh nào được lưu.` and NO files/DB changes
- Edit existing product → check ☐ Xóa on one image, save → its DB row + files removed; remaining keep sort_order; first = primary
- Reorder via ↑/↓ → `image_order` hidden field updates; on save sort_order/is_primary reflect new order
- List page Ảnh column shows the 48×48 primary thumbnail (or `—` placeholder)
- Delete product with 2 images → confirm page shows `Sản phẩm có 2 ảnh và sẽ bị xóa cùng.`; POST delete → DB row gone, 4 files (2 full + 2 thumb) gone from `app/static/uploads/`, flash `Đã xóa sản phẩm “{tên}” và 2 ảnh đã xóa`
- Simulated cleanup failure (locked/missing file) → warning flash, delete still redirects (D-09)
</verification>

<success_criteria>
- D-10/D-11: upload happens inline in the create/edit form via `<input type="file" multiple>`
- D-12: first image in gallery order is primary (badge + is_primary), changing order changes primary
- D-13: gallery reorder via ↑/↓ buttons, persisted on save
- D-14: edit form shows existing images with per-image delete + add-new
- D-15: per-image delete is a checkbox committed on Save (POST + CSRF), never an immediate request
- D-16: batch enforced against image_utils validation (extension, magic, verify, 2000×2000, 16MB)
- D-17: one invalid file rejects the whole batch with a flash naming file + reason
- D-05/D-06/D-07/D-09: product delete removes files with the DB row, DB-first ordering, detailed flash, non-blocking cleanup warnings
- IMG-01/02/03/04: validated multi-image upload, UUID storage, gallery, list thumbnails — all wired end to end
- PROD-03: delete remains POST + CSRF with confirmation
</success_criteria>

<output>
Create `.planning/phases/02-admin-crud-images/02-03-SUMMARY.md` when done
</output>
