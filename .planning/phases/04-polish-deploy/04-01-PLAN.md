---
phase: 04-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/public.py, app/templates/public/product_detail.html, app/static/css/style.css]
autonomous: true
requirements: [CAT-04, CAT-06]

must_haves:
  truths:
    - "CAT-04 giữ nguyên hiện trạng (D-05): sản phẩm hết hàng/ngừng bán vẫn hiển thị ảnh mờ opacity 0.45 + badge overlay 'Hết hàng'/'Ngừng bán', không thêm grayscale, không đổi thứ tự sort_order — chỉ verify"
    - "Search out-of-range page redirect 302 về trang cuối cùng (giống home public.py:39-40) thay vì clamp âm thầm — D-07 #2"
    - "Dòng 'Sản phẩm hiện đang hết hàng.' dùng màu #B91C1C (≈5.5:1 trên #F9FAFB, đạt AA ≥4.5:1) — D-07 #3"
    - "Gallery detail: ảnh chính + data-src của thumbnail dùng img.filename (bản đầy đủ, JPEG q85, max 2000px); thumb 72×72 vẫn dùng thumb_filename — D-07 #4"
    - "Responsive audit public + admin tại 480/768/1200 không còn vi phạm; mọi chỗ vỡ được fix — D-06, CAT-06"
  artifacts:
    - path: "app/public.py"
      provides: "search route redirect out-of-range page về trang cuối (mirror home)"
      contains: "pagination.page > pagination.pages"
    - path: "app/templates/public/product_detail.html"
      provides: "main-image src + gallery data-src dùng img.filename (bản đầy đủ); width/height 440"
      contains: "img.filename"
    - path: "app/static/css/style.css"
      provides: ".out-of-stock-note màu #B91C1C + comment 'declared spec addition' cho .contact-strip .btn { min-width: 200px }"
      contains: "out-of-stock-note"
  key_links:
    - from: "app/public.py"
      to: "app/public.py"
      via: "search route redirect url_for('public.search', q=q, page=pagination.pages) — mirror home pattern (public.py:39-40)"
      pattern: "url_for('public.search'"
    - from: "app/templates/public/product_detail.html"
      to: "app/models.py"
      via: "ProductImage.filename / thumb_filename — ảnh chính + data-src dùng filename, thumb dùng thumb_filename"
      pattern: "thumb_filename"
    - from: "app/static/css/style.css"
      to: "app/templates/public/product_detail.html"
      via: ".out-of-stock-note (màu mới #B91C1C) render cho dòng hết hàng trên detail"
      pattern: "out-of-stock-note"
---

<objective>
Polish giao diện production cho Phase 4: (1) verify CAT-04 giữ nguyên hiện trạng de-emphasis (D-05), (2) fix cả 5 deferral UI từ Phase 3 (D-07), (3) audit responsive toàn bộ public + admin tại 480/768/1200 và fix mọi chỗ vỡ (D-06/CAT-06). Phase 4 KHÔNG thêm trang hay component mới — mọi thay đổi là comment/CSS, Jinja, hoặc hành vi route hiện có (04-UI-SPEC: "verifies the existing system, audits responsiveness, fixes the 5 deferral items").

Purpose: Giao vertical slice polish — khách mobile mở store không thấy layout vỡ (CAT-06), khách thấy dòng hết hàng rõ ràng và đạt AA contrast (D-07 #3), khách xem gallery ảnh chính nét trên màn hình 2x (D-07 #4), admin không thấy bảng tràn màn hình khi dùng điện thoại (D-06 admin).

## Coverage Map (toàn phase — không bỏ sót)

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| CAT-04 | Hết hàng/ngừng bán hiển thị khác đi, không lấn át hàng còn | 04-01 (Task 5 verify) |
| CAT-06 | Giao diện responsive trên mobile | 04-01 (Task 5) |
| D-05 | CAT-04 giữ nguyên hiện trạng (opacity 0.45 + badge, không grayscale) | 04-01 (Task 5) |
| D-06 | Audit responsive 480/768/1200 toàn bộ public + admin | 04-01 (Task 5) |
| D-07 #1 | Spec-sync `.contact-strip .btn { min-width: 200px }` (document) | 04-01 (Task 1) |
| D-07 #2 | Search out-of-range page → redirect (khớp home) | 04-01 (Task 2) |
| D-07 #3 | Contrast `.out-of-stock-note` → `#B91C1C` (≥4.5:1) | 04-01 (Task 3) |
| D-07 #4 | Gallery ảnh chính dùng bản đầy đủ (filename) | 04-01 (Task 4) |
| D-07 #5 | Kiểm tra render ký tự `₫` trong font stack | 04-01 (Task 1) |

Output: 04-01 xử lý trọn vẹn D-07 + verify D-05/D-06. Không tạo file mới ngoài comment trong `app/static/css/style.css` và thay đổi template/route hiện có.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-polish-deploy/04-UI-SPEC.md (design contract — D-07 deferral fixes 1..5; D-05 CAT-04 HOLD; D-06 responsive invariants 480/768/1200; Copywriting contract unchanged; Registry Safety)
@.planning/phases/04-polish-deploy/04-CONTEXT.md (D-05 giữ nguyên hiện trạng; D-06 audit toàn bộ; D-07 fix 5 deferral)
@.planning/phases/03-public-catalog-search-contact/03-UI-REVIEW.md (Deferred to Phase 4 items; style.css:384, public.py:75 vs 39-40, .out-of-stock-note style.css:408)
@.planning/phases/03-public-catalog-search-contact/03-UI-SPEC.md (design system extensions Phase 3 — grid, detail gallery, contact strip)
@.planning/phases/01-scaffold-auth-data-model/01-UI-SPEC.md (design system baseline: tokens, breakpoint 480/768/1200, font 'Noto Sans VN')
@CLAUDE.md (tech stack; "What NOT to Use": no new libs, plain CSS)
</context>

<interfaces>
Consumed from existing code (already on disk):
  - `app/public.py`: `home()` redirects out-of-range page → last page (`public.py:39-40`); `search()` uses `_manual_pagination` which CLAMPS `page` to `pages` (`public.py:22-30`, `public.py:75`). `normalize_search_text` D-11. Route `search` renders `public/search.html` with `q`, `products`, `pagination`.
  - `app/templates/public/product_detail.html`: main image `<img id="main-image" src="...thumb_filename" width="400" height="400">` (line 10); gallery-thumb buttons `data-src="...thumb_filename"` + inner `<img ... thumb_filename width=72 height=72>` (lines 18-20). Inline gallery-swap JS reads `data-src` (lines 53-70).
  - `app/models.py`: `ProductImage.filename` (bản đầy đủ re-encoded JPEG q85, max 2000px — `image_utils.py`), `thumb_filename` property → `<uuid>_thumb.jpg` (400px, q82).
  - `app/static/css/style.css`: `--destructive: #DC2626` token (line 5); `.out-of-stock-note { color: #DC2626; font-size: 14px; margin-bottom: 8px; }` (line 409); `.contact-strip .btn { min-width: 200px; }` (line 385); `body { font-family: 'Noto Sans VN', Roboto, system-ui, -apple-system, sans-serif; }` (line 14); admin table wrapper `.table-scroll { overflow-x: auto; }` (line 183); breakpoints `@media (max-width: 768px)` (line 329), `@media (min-width: 768px)` (line 356), `@media (min-width: 1200px)` (line 364/421); `.product-gallery` width 360px ≥768 (line 419), 440px ≥1200 (line 421).
  - `app/__init__.py`: Jinja filter `format_price` → `150.000₫` (line 63-65).
  - `app/static/css/style.css` budget: total must stay < 20KB (Phase 3 constraint).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-07 #1 spec-sync comment + #5 ₫ glyph check</name>
  <files>app/static/css/style.css</files>
  <read_first>
    - .planning/phases/04-polish-deploy/04-UI-SPEC.md (D-07 items 1 và 5: contract chi tiết)
    - .planning/phases/03-public-catalog-search-contact/03-UI-REVIEW.md (deferral: "Document or drop .contact-strip .btn { min-width: 200px }"; "Confirm ₫ renders in the Noto Sans VN fallback chain")
  </read_first>
  <action>
    1. `app/static/css/style.css` — thêm comment ngay trên rule `.contact-strip .btn { min-width: 200px }` (line 385) ghi rõ đây là **declared spec addition** (04-UI-SPEC D-07 #1) — rule giữ CTA là touch target ≥44px, không phải token mới:
       ```
       /* Declared spec addition (04-UI-SPEC D-07 #1): keeps the Messenger CTA a >=44px touch target on mobile. Harmless, documented, no token change. */
       .contact-strip .btn { min-width: 200px; }
       ```
       KHÔNG đổi giá trị rule — chỉ thêm comment.

    2. Kiểm tra ký tự `₫` (U+20AB): xác nhận `body { font-family: 'Noto Sans VN', Roboto, system-ui, -apple-system, sans-serif; }` (line 14) — Noto Sans VN carry U+20AB (Dong sign), đứng đầu chuỗi fallback. Nếu glyph thiếu trong browser (kiểm tra thủ công 1 trang chi tiết có giá), **document fallback** bằng cách thêm font carry U+20AB vào cuối chuỗi (ví dụ `'Segoe UI'`) — KHÔNG thay ký tự `₫` bằng chuỗi khác. Ghi verdict (PASS / fallback appended) vào `04-01-SUMMARY.md`.
  </action>
  <verify>
    <automated>python - <<'PY'
import io, re
css = open('app/static/css/style.css', encoding='utf-8').read()
# D-07 #1: comment documents the declared spec addition right above the rule
m = re.search(r'/\*\s*[^\n]*Declared spec addition[^\n]*\*/\s*\.contact-strip \.btn \{ min-width: 200px; \}', css)
assert m, 'spec-sync comment missing above .contact-strip .btn'
# D-07 #5: font stack leads with Noto Sans VN
assert "font-family: 'Noto Sans VN', Roboto, system-ui, -apple-system, sans-serif" in css, 'Noto Sans VN not first in font stack'
size = len(css.encode('utf-8'))
assert size < 20000, 'CSS over 20KB budget: %d' % size
print('TASK1_OK size=%d' % size)
PY</automated>
  </verify>
  <acceptance_criteria>
    - `style.css` có comment chứa chuỗi `Declared spec addition` ngay trên `.contact-strip .btn { min-width: 200px; }`; giá trị rule không đổi
    - `body` font-family bắt đầu bằng `'Noto Sans VN'` (U+20AB có trong font)
    - Verdict kiểm tra `₫` (PASS hoặc fallback được append) được ghi trong `04-01-SUMMARY.md`
    - style.css tổng < 20KB
    - Verify script exits 0 printing TASK1_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: D-07 #3 contrast `.out-of-stock-note` → `#B91C1C`</name>
  <files>app/static/css/style.css</files>
  <read_first>
    - .planning/phases/04-polish-deploy/04-UI-SPEC.md (D-07 item 3: #DC2626 ≈4.3:1 dưới AA; chốt `#B91C1C` red-700 ≈5.5:1, cùng red ramp, giữ destructive semantics)
    - app/static/css/style.css (line 409 `.out-of-stock-note`)
  </read_first>
  <action>
    `app/static/css/style.css` — đổi rule `.out-of-stock-note` (line 409) từ `color: #DC2626` sang `color: #B91C1C`:
    ```
    .out-of-stock-note { color: #B91C1C; font-size: 14px; margin-bottom: 8px; }
    ```
    KHÔNG đổi nội dung copy ("Sản phẩm hiện đang hết hàng." — Copywriting contract 04-UI-SPEC) và không đổi token `--destructive` (vẫn #DC2626 cho button/error). Chỉ 1 token thay đổi, không thêm hue family mới.
  </action>
  <verify>
    <automated>python - <<'PY'
css = open('app/static/css/style.css', encoding='utf-8').read()
assert '.out-of-stock-note { color: #B91C1C' in css, '.out-of-stock-note must be #B91C1C'
# token untouched
assert '--destructive: #DC2626' in css, 'destructive token must stay #DC2626'
# relative luminance contrast check (#B91C1C vs #F9FAFB)
def lum(h):
    rgb = [int(h[i:i+2], 16)/255 for i in (1,3,5)]
    def lin(c): return c/12.92 if c <= 0.03928 else ((c+0.055)/1.055)**2.4
    r,g,b = [lin(c) for c in rgb]
    return 0.2126*r + 0.7152*g + 0.0722*b
L1, L2 = lum('#B91C1C'), lum('#F9FAFB')
ratio = (max(L1,L2)+0.05)/(min(L1,L2)+0.05)
assert ratio >= 4.5, 'contrast ratio %.2f:1 below AA' % ratio
print('TASK3_OK ratio=%.2f:1' % ratio)
PY</automated>
  </verify>
  <acceptance_criteria>
    - `.out-of-stock-note` dùng `#B91C1C` (không còn `#DC2626` trong rule này)
    - Contrast `#B91C1C` trên `#F9FAFB` ≥ 4.5:1 (tính bằng công thức WCAG trong verify)
    - Token `--destructive` giữ nguyên `#DC2626`; copy dòng hết hàng không đổi
    - Verify script exits 0 printing TASK3_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: D-05 verify CAT-04 + D-06 responsive audit public/admin (480/768/1200)</name>
  <files>app/static/css/style.css, app/templates/admin/products/list.html, app/templates/admin/products/form.html, app/templates/auth/login.html</files>
  <read_first>
    - .planning/phases/04-polish-deploy/04-UI-SPEC.md (D-05 CAT-04 de-emphasis HOLD; D-06 responsive invariants table — public <480/480-767/768-1199/≥1200, admin <768/≥768)
    - .planning/phases/03-public-catalog-search-contact/03-UI-REVIEW.md (Pillar 3 Responsive 4/4 verified baseline; admin table wrapper `.table-scroll`)
    - app/static/css/style.css (grid 2/3/4 breakpoints lines 361-364, detail 360/440 lines 417-421, `.table-scroll` line 183, admin-card media line 329-331, contact-strip line 382-385)
  </read_first>
  <action>
    **Verify-then-fix** CAT-04 và responsive theo invariant 04-UI-SPEC. Mục tiêu: KHÔNG đổi gì trừ khi phát hiện vi phạm — nếu có, fix tối thiểu trong `app/static/css/style.css` (hoặc template admin/auth nếu cần).

    1. **CAT-04 de-emphasis (D-05 — HOLD, chỉ verify):** xác nhận `.is-unavailable .product-card-thumb img { opacity: 0.45 }` (line 371) + badge overlay "Hết hàng"/"Ngừng bán" vẫn hoạt động; KHÔNG thêm grayscale, KHÔNG đổi thứ tự grid (sort_order). Grid order giữ `sort_order ASC, id ASC`.

    2. **Responsive audit public** (mọi invariant phải HOLD, fix nếu vỡ):
       - `< 480` / `480–767`: không horizontal scroll; grid 2 cột (repeat(2, 1fr), gap 16); card ≥ ~140px với price + clamp; header stack (column, gap 8); detail stack (gallery trên, info dưới); `.contact-strip .btn { min-width: 200px }` khớp trong ≥288px available ở viewport 320; touch target ≥ 44px.
       - `768–1199`: grid 3 cột gap 24; header 1 hàng với search `max-width: 480px; margin-left: auto`; detail 2 cột gallery 360px; contact-strip padding 32.
       - `≥ 1200`: grid 4 cột; gallery 440px; container max-width 1200.
       - Kiểm tra `.container` max-width, `.product-gallery` width 360/440, header media query hiện diện trong CSS (source assertions ở verify).
    3. **Responsive audit admin**:
       - `< 768`: table không ép horizontal scroll toàn trang — `.table-scroll { overflow-x: auto }` (line 183) ĐÃ wrap bảng `admin/products/list.html`; verify giữ nguyên. Form create/edit + login full-width, input ≥ 44px, button không bị cắt (admin-card media line 329-331 đã max-width 100%).
       - `≥ 768`: table/layout render không vỡ.
    4. **Fix nếu phát hiện vi phạm:** nếu một invariant trên bị vỡ, sửa tối thiểu trong CSS (không đổi copy, không thêm hex mới ngoài token set, giữ tổng < 20KB) và ghi fix vào SUMMARY.

    Không thêm JS thư viện, không thêm dependency (Registry Safety 04-UI-SPEC).
  </action>
  <verify>
    <automated>export PYTHONIOENCODING=utf-8 && python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
from wsgi import app
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///' + tempfile.mktemp(suffix='.db')
from app.db import db
from app.models import Product
css = open('app/static/css/style.css', encoding='utf-8').read()
# --- CSS invariants (public) ---
assert 'repeat(2, 1fr)' in css and 'gap: 16px' in css, 'mobile 2-col grid missing'
assert 'repeat(3, 1fr)' in css and 'gap: 24px' in css, 'tablet 3-col grid missing'
assert 'repeat(4, 1fr)' in css, 'desktop 4-col grid missing'
assert 'max-width: 480px' in css and 'margin-left: auto' in css, 'tablet search rule missing'
assert '.product-gallery { width: 360px' in css, 'tablet gallery 360 missing'
assert '.product-gallery { width: 440px' in css, 'desktop gallery 440 missing'
assert 'max-width: 1200px' in css, 'container max 1200 missing'
assert '.contact-strip .btn { min-width: 200px;' in css, 'CTA min-width missing'
# --- CSS invariants (admin) ---
assert '.table-scroll { overflow-x: auto' in css, 'admin table scroll wrapper missing'
# --- CAT-04 de-emphasis HOLD ---
assert '.is-unavailable .product-card-thumb img { opacity: 0.45' in css, 'CAT-04 dim missing'
assert '.badge-overlay' in css, 'badge overlay missing'
assert 'grayscale' not in css.lower(), 'grayscale must not be added (D-05)'
# --- render all public routes 200 with a seeded store ---
with app.app_context():
    db.create_all()
    for i in range(1, 13):
        qty = 0 if i == 3 else 5
        db.session.add(Product(name='Sản phẩm %02d' % i, price=100000*i, quantity=qty, sort_order=i))
    db.session.commit()
    c = app.test_client()
    assert c.get('/').status_code == 200
    assert c.get('/?page=2').status_code == 200
    assert c.get('/search?q=san').status_code == 200
    assert c.get('/search').status_code == 200
    # admin + auth render (unauth admin redirects to login; login page 200)
    assert c.get('/admin/products').status_code == 302, 'admin should redirect to login'
    r = c.get('/auth/login')
    assert r.status_code == 200, 'login page must render'
    assert 'Tên đăng nhập' in r.get_data(as_text=True) or 'Đăng nhập' in r.get_data(as_text=True)
size = len(css.encode('utf-8'))
assert size < 20000, 'CSS over 20KB: %d' % size
print('TASK5_OK size=%d' % size)
PY</automated>
  </verify>
  <acceptance_criteria>
    - CAT-04: `.is-unavailable img` opacity 0.45 + badge overlay giữ nguyên; KHÔNG có `grayscale` trong CSS; grid order không đổi
    - CSS chứa đủ invariant breakpoint: grid 2/3/4, search max-width 480 margin-left auto, gallery 360/440, container 1200, contact-strip min-width 200
    - Admin: `.table-scroll { overflow-x: auto }` bao bảng; form/login không cắt; input ≥ 44px
    - Mọi route public render 200; `/admin/products` chưa đăng nhập → 302 login; `/auth/login` render 200
    - Mọi vi phạm responsive phát hiện được fix; fix ghi trong SUMMARY
    - Verify script exits 0 printing TASK5_OK
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> public API (/, /search, /products/<id>) | Untrusted query params (`page`, `q`), untrusted path; no form POSTs on public pages |
| app -> SQLite | Read-only Product queries; WAL + busy_timeout inherited (PLAT-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `product_detail` main image serving full-size `filename` | mitigate | `filename` là ảnh re-encoded JPEG q85 max 2000px (image_utils) — không EXIF, không payload; kích thước giới hạn MAX_DIMENSION. Không phơi bày dữ liệu mới ngoài upload đã công khai. |
| T-04-02 | Tampering | `search` redirect | mitigate | Redirect chỉ tới `url_for('public.search', ...)` (internal route) — không phản chiếu input người dùng vào Location, không open redirect. |
| T-04-03 | Tampering | `page`/`q` params | mitigate | `request.args.get('page', 1, type=int)` coerce int; `_manual_pagination` clamp; redirect guard `total and page > pages`. Jinja auto-escape mọi interpolation; không dùng `|safe`. |
| T-04-04 | Elevation of Privilege | CSS/template-only changes | accept | Không đổi auth/CSRF; admin surface vẫn guarded bởi Phase 1 `@login_required`. |
</threat_model>

<verification>
- `GET /search?q=ao&page=5` (có kết quả) → 302 `/search?q=ao&page=2` (D-07 #2); `/search` query rỗng → 200 prompt
- `GET /products/<id>` render main image `uploads/<uuid>.jpg` (bản đầy đủ, width 440) + thumbs `_thumb.jpg` 72×72 (D-07 #4)
- `.out-of-stock-note` màu `#B91C1C`, contrast ≥ 4.5:1 (D-07 #3); comment `Declared spec addition` trên `.contact-strip .btn` (D-07 #1); ₫ verdict ghi SUMMARY (D-07 #5)
- CAT-04: `.is-unavailable` opacity 0.45 + badge overlay giữ nguyên, không grayscale (D-05)
- Responsive invariants 480/768/1200 public + admin HOLD; admin table vẫn trong `.table-scroll` (D-06, CAT-06)
- style.css tổng < 20KB
</verification>

<success_criteria>
- Cả 5 deferral UI Phase 3 (D-07) được xử lý; 4/5 là code/comment, 1/5 (₫) là verdict ghi nhận
- CAT-04 de-emphasis được verify giữ nguyên không regression (D-05)
- CAT-06 responsive audit hoàn tất; mọi vi phạm fix; không còn chỗ vỡ tại 480/768/1200 (D-06)
- Không thêm trang/component/thư viện mới; copy tiếng Việt không đổi; CSS < 20KB
</success_criteria>

<output>
Create `.planning/phases/04-polish-deploy/04-01-SUMMARY.md` when done
</output>
