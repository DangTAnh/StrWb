---
context: phase
phase: 06-public-order-form
task: 0
total_tasks: 3
status: in_progress
last_updated: 2026-08-02T06:36:42.413Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Uncommitted file `app/templates/admin/products/form.html` — giữ nguyên, KHÔNG revert, KHÔNG commit.** Pre-existing deletion của 1 help-text (`Trạng thái tự động: Còn hàng...`). Đây là change cố ý từ trước, ngoài scope Phase 5/6. Nếu bị ghi đè hoặc commit, mất change người dùng.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Test không set env → fail vô nghĩa | `SECRET_KEY must be set`, URI trỏ `{BASE_DIR}/data/app.db` cần thư mục `data`, CSRF chặn test client | advisory | Test script Phase 6 phải set `os.environ['SECRET_KEY']`, patch `app_module.BASE_DIR` → temp dir + `os.makedirs(...,'data')`, `WTF_CSRF_ENABLED=False` cho test client |
| Executor tự commit thẳng master | Phase 5 Wave 2 executors không dùng worktree riêng (config `use_worktrees: true` nhưng không áp) | advisory | Không blocking — kết quả OK; nếu thấy cần thì yêu cầu worktree cho 3 executor Phase 6 song song |
| Migration guard chỉ check tồn tại cột | Phase 5 guard check PRAGMA table_info có/không, không check type | advisory | Phase 6 migration giữ cùng pattern idempotent; IN-02 (flask-migrate) deferred |

## Current State

Phase 5 **hoàn tất trọn vẹn**: 3/3 plans (commit 955b5ed/e65129d/24c4ef2), code review resolved (f97784d fix WR-01 FK + WR-02 cost_price 0), verifier 5/5 criteria — human_needed chỉ vì 1 visual check **non-blocking** (05-VERIFICATION.md, commit 3c528a4).

Phase 6 **đổi scope thành Cart + Checkout** (user yêu cầu "thêm giỏ hàng đi", chọn "Cart đầy đủ, thay form đơn"). Các planning docs đã cập nhật (REQUIREMENTS.md 19 reqs, ROADMAP.md Phase 6 redesign, 06-CONTEXT.md rewrite) — commit `313de43`. Chưa có PLAN.md nào.

<current_state>
Milestone v1.1 Buy System. Tại ranh giới giữa Phase 5 (xong) và Phase 6 (chưa plan). Phase 6 = Cart + Checkout, chờ **plan-phase 6** tạo 3 plans từ 06-CONTEXT.md.
</current_state>

<completed_work>

- Task 05-01: Order + Product cost data model — Done (`955b5ed`)
- Task 05-02: Idempotent SQLite migration — Done (`e65129d`)
- Task 05-03: Cost price trên admin product form — Done (`24c4ef2`, `97d540e`)
- Task 05-fixes: WR-01 `PRAGMA foreign_keys=ON` + WR-02 bỏ `or None` — Done (`f97784d`, verified)
- Task 05-review: REVIEW.md resolved, 2 warnings fixed — Done (`5815c7a`)
- Task 05-verify: VERIFICATION.md 5/5, human_needed visual — Done (`3c528a4`)
- Task 06-context: CONTEXT.md rewrite cho cart scope — Done (`313de43`)
</completed_work>

<remaining_work>

- **Task 06-plans**: plan-phase 6 — spawn gsd-planner từ 06-CONTEXT.md → tạo 06-01/06-02/06-03-PLAN.md; verify bằng gsd-plan-checker
- **Task 06-01**: Order → Order + OrderItem refactor + migration idempotent (guard orders + tạo order_items qua create_all, CheckConstraint quantity ≥ 1)
- **Task 06-02**: Cart session `{product_id: quantity}` + add/update/remove + tổng tiền (format_price), badge số món
- **Task 06-03**: Checkout form (name/phone/address bắt buộc, note optional) + submit route + CSRF/honeypot + flash success + xóa giỏ
- **Task 06-review**: code review (standard) + regression v1.0 + verify_phase_goal
- **Task 06-complete**: phase.complete 6 + STATE.md
- **Phases 7-9**: admin order tracking (đọc qua order_items), admin stats (revenue/profit qua items), polish + deploy. Rồi milestone audit → complete → cleanup.
</remaining_work>

<decisions_made>

- **Cart đầy đủ (session-based) thay form đơn 1 SP** — user chốt 2026-08-02, đảo ngược decision cũ. ORD-10/10a/10b active.
- **Order + OrderItem refactor**: orders giữ customer_name/phone/address/note/status; order_items (order_id FK CASCADE, product_id FK SET NULL nullable, snapshot product_name/price/cost_price, quantity ≥ 1 CheckConstraint)
- **Snapshot tại checkout** từ product hiện tại (giá có thể đổi giữa add & checkout)
- **SĐT**: 8-11 chữ số, cho phép space/`-`/`+` prefix, không chặt format VN
- **CSRF** (Flask-WTF toàn app) + **honeypot** field ẩn "website" (bot bị reject im lặng)
- **Success**: redirect về trang chi tiết + flash xanh + xóa giỏ
- **Không trừ tồn kho** khi đặt (ORD-12 → v2)
- **Ẩn add-to-cart block** khi hết hàng/ngừng bán; giữ dải Messenger ở trang chủ/footer
</decisions_made>

<blockers>

- Không có blocker kỹ thuật. Phase 5 all green.
- **Human UAT pending (non-blocking)**: visual check Phase 5 admin form cost_price layout; chưa ai xác nhận.
- **Operator action (defer tới sau Phase 6)**: chạy `flask --app app init-db` trên `data/app.db` thật (vẫn schema v1.0) sau khi migration Phase 6 thêm order_items — cần ADMIN_PASSWORD hợp lệ trong .env.
</blockers>

## Required Reading (in order)

1. `.planning/phases/06-public-order-form/06-CONTEXT.md` — toàn bộ decisions + code_context cho Phase 6 cart
1. `.planning/ROADMAP.md` — Phase 6 definition (5 success criteria, 3 plans)
1. `.planning/REQUIREMENTS.md` — ORD-01/02/03/05/10/10a/10b active
1. `.planning/phases/05-data-model-migration/05-REVIEW.md` — WR-01/WR-02 fixes, IN-01/02/03 deferred (IN-03 CheckConstraint sẽ đưa vào order_items Phase 6)
1. `.planning/HANDOFF.json` — structured state

## Critical Anti-Patterns (do NOT repeat these)

- **Test script env không đủ** → Phase 6 verify phải set SECRET_KEY + patch BASE_DIR + tạo `data/` + WTF_CSRF_ENABLED=False
- **Commit/overwrite `form.html` uncommitted** → giữ nguyên change pre-existing của user
- **Migration không idempotent** → Phase 6 guard theo pattern Phase 5 (`PRAGMA table_info` + `engine.begin()`)

## Infrastructure State

- **Real DB**: `data/app.db` VẪN schema v1.0 (chưa cost_price/orders/order_items) — Phase 5 chỉ test trên temp copy. Refactor Phase 6 an toàn trước go-live, không mất dữ liệu.
- **PRAGMA foreign_keys=ON** đã bật (app/__init__.py listener) → FK CASCADE/SET NULL hoạt động khi migrate
- **MESSENGER_URL** config còn tồn tại (dùng cho dải liên hệ giữ lại)
- **Config**: yolo, code_review standard, verify_phase_goal on, auto_advance true
- **Git**: master tại `313de43`; working tree chỉ còn `app/templates/admin/products/form.html` (uncommitted, giữ nguyên)

<context>

Autonomous chạy milestone v1.1. Tôi vừa hoàn tất đổi scope Phase 6 sang cart (3 docs planning + commit 313de43) rồi dừng tại ranh giới tự nhiên (context ~75%) để tránh cạn giữa chừng. Bước kế: plan-phase 6 — planner cần đọc 06-CONTEXT.md và tạo 3 plans theo đúng decisions cart (đã lock). Sau khi plan verify xong, execute-phase 6 với gates: post-merge build/test, code review, regression, schema drift, verify_phase_goal. Pattern executor: subagent gsd-executor, return `## PLAN COMPLETE`/`## PLAN BLOCKED`, tự commit. Session trước: 2 executor Wave 2 tự commit thẳng master (không worktree) — chấp nhận được.
</context>

<next_action>
Start with: **plan-phase 6** — chạy GSD plan-phase workflow cho phase 6 theo 06-CONTEXT.md (cart scope). Spawn gsd-planner → 3 plans (06-01 Order→Order+OrderItem refactor+migration, 06-02 cart session, 06-03 checkout+CSRF/honeypot) → verify bằng gsd-plan-checker → pass thì execute-phase 6 (3 executor, gates đầy đủ). Không quên: giữ nguyên `form.html` uncommitted.
</next_action>
