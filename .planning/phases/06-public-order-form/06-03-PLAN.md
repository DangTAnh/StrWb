---
phase: 06-public-order-form
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified: [app/forms.py, app/public.py, app/templates/public/_checkout_form.html]
autonomous: true
requirements: [ORD-01, ORD-02, ORD-03, ORD-05]

must_haves:
  truths:
    - "Checkout form yêu cầu bắt buộc Họ và tên / Số điện thoại / Địa chỉ, Ghi chú tùy chọn; SĐT 8-11 chữ số (cho phép space/dấu gạch/dấu cộng) (ORD-01, ORD-02)"
    - "POST /cart/checkout tạo 1 Order + nhiều OrderItem snapshot (product_name/product_price/product_cost_price/quantity tại thời điểm đặt) trong 1 commit (ORD-10a)"
    - "CSRF: POST thiếu csrf_token → 400; honeypot 'website' điền → silent reject (không flash, không tạo đơn) (ORD-05)"
    - "Server re-validate từng món: sản phẩm available + 1 ≤ qty ≤ tồn kho — tamper/hết hàng/ngừng bán → không tạo đơn, flash lỗi (ORD-02)"
    - "Đặt thành công: xóa giỏ, flash success 'Đặt hàng thành công! Chúng tôi sẽ liên hệ xác nhận qua SĐT.', redirect về trang chi tiết (ORD-03)"
    - "Tồn kho KHÔNG giảm khi đặt hàng (ORD-12 deferred v2)"
  artifacts:
    - path: "app/forms.py"
      provides: "CheckoutForm (customer_name/phone/address/note + website honeypot)"
      contains: "class CheckoutForm"
    - path: "app/public.py"
      provides: "checkout route POST /cart/checkout"
      contains: "/cart/checkout"
    - path: "app/templates/public/_checkout_form.html"
      provides: "Checkout form partial (CSRF + honeypot + fields) — được cart.html include"
      contains: "honeypot"
  key_links:
    - from: "checkout route"
      to: "Order + OrderItem"
      via: "db.session.add(Order(...)) → flush → OrderItem(order_id=order.id, ...) → commit"
      pattern: "OrderItem\\("
    - from: "checkout route"
      to: "session['cart']"
      via: "xóa giỏ sau khi đặt thành công"
      pattern: "session\\['cart'\\] = \\{\\}"
    - from: "cart.html include"
      to: "public/_checkout_form.html"
      via: "{% include 'public/_checkout_form.html' ignore missing %} — form render trong trang giỏ (UI-SPEC single-page)"
      pattern: "_checkout_form"
---

<objective>
Checkout tạo đơn nhiều sản phẩm (ORD-01/02/03/05, ORD-10a): form đặt hàng (tên/SĐT/địa chỉ bắt buộc, ghi chú tùy chọn), SĐT 8-11 chữ số, server-side re-validate số lượng ≤ tồn kho, CSRF + honeypot chống spam, tạo 1 Order + nhiều OrderItem snapshot, flash success + xóa giỏ, redirect về trang chi tiết.

Purpose: Hoàn tất luồng khách đặt hàng. Snapshot giá/cost tại thời điểm đặt (ORD-10a) — giá có thể thay đổi giữa lúc thêm giỏ và lúc checkout, quyết định: snapshot tại checkout từ product hiện tại (06-CONTEXT specifics). Không giảm tồn kho (ORD-12 → v2). Form template nằm trong partial `_checkout_form.html` được cart.html (06-02) include — giữ thiết kế single-page cart + checkout (06-UI-SPEC) mà không sửa cart.html.

## Coverage Map

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| ORD-01 | Khách đặt hàng qua form (tên, SĐT, địa chỉ, số lượng, ghi chú) — thay nút Messenger | 06-03 (Task 1, Task 2) |
| ORD-02 | Form bắt buộc tên/SĐT/địa chỉ; số lượng ≥ 1 và ≤ tồn kho (server-side) | 06-03 (Task 1) |
| ORD-03 | Khách thấy thông báo thành công sau khi đặt | 06-03 (Task 1) |
| ORD-05 | CSRF + honeypot chống spam | 06-03 (Task 1, Task 2) |
| ORD-10a | Checkout tạo 1 Order + nhiều OrderItem snapshot | 06-03 (Task 1) |
| 06-CONTEXT | SĐT 8-11 chữ số, không chặt format VN; honeypot 'website' silent reject; flash success xanh; redirect về trang chi tiết | 06-03 (Task 1, Task 2) |
| Deferred | ORD-11 (thông báo admin), ORD-12 (trừ tồn kho) | KHÔNG cài đặt |

Output: CheckoutForm + route checkout + `_checkout_form.html`. Không dependency mới.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-public-order-form/06-CONTEXT.md (decisions checkout: form tên/SĐT/địa chỉ bắt buộc, note tùy chọn; SĐT 8-11 chữ số; server-side validate qty ≤ tồn kho; CSRF + honeypot; success flash + xóa giỏ; redirect về trang chi tiết; snapshot tại checkout)
@.planning/phases/06-public-order-form/06-UI-SPEC.md (Checkout Form markup verbatim; Checkout Flow; Route Map `/cart/checkout`; Flash Messages; Copywriting Contract)
@.planning/phases/06-public-order-form/06-01-PLAN.md (đã execute: Order customer-only + OrderItem snapshot trên disk — checkout route ghi cả hai)
@.planning/phases/06-public-order-form/06-02-PLAN.md (đã execute: cart session + cart.html include `_checkout_form.html` — form partial này được include vào trang giỏ)
@CLAUDE.md (Flask-WTF + CSRFProtect; D-05 Integer VND; tiếng Việt)
</context>

<interfaces>
Consumed from existing code (already on disk):
  - `app/forms.py`: pattern `from flask_wtf import FlaskForm`; `from wtforms import StringField, TextAreaField, SubmitField`; validators đã import: `DataRequired, Optional, Length`. CẦN THÊM imports: `Regexp` từ wtforms.validators, `ValidationError` từ wtforms.
  - `app/public.py`: imports hiện có `from flask import Blueprint, render_template, request, abort, redirect, url_for` + (sau 06-02) `flash, session`; `from .forms import CartForm` (06-02). CẦN THÊM: `from .forms import CheckoutForm`; `from .models import Order, OrderItem` (Order/OrderItem đã tồn tại sau 06-01).
  - `app/models.py`: `Order(customer_name, customer_phone, customer_address, customer_note, status='Chờ xác nhận', created_at, updated_at)` + `items` relationship dynamic; `OrderItem(order_id, product_id, product_name, product_price, product_cost_price, quantity, created_at)`.
  - `app/templates/public/cart.html` (06-02): include `{% include "public/_checkout_form.html" ignore missing %}` trong `.checkout-section` id="checkout" — partial này render vào đó.
  - `app/__init__.py`: `CSRFProtect` app-wide (line 54) — mọi POST cần csrf_token; format_price filter; flash zone trong base.html.
  - Flask session gotcha: ghi `session['cart'] = {}` (gán cả dict) — mark modified.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CheckoutForm (SĐT 8-11 chữ số) + route checkout POST /cart/checkout</name>
  <files>app/forms.py, app/public.py</files>
  <read_first>
    - .planning/phases/06-public-order-form/06-CONTEXT.md (form: tên/SĐT/địa chỉ bắt buộc, note tùy chọn; SĐT 8-11 chữ số cho phép space/-/+; server validate qty ≤ tồn kho; honeypot silent reject; flash success + xóa giỏ; redirect về chi tiết; snapshot tại checkout)
    - .planning/phases/06-public-order-form/06-UI-SPEC.md (Checkout Flow steps 1-9; Field contract: name max 100, phone regex + 8-11 digits, address max 500, note max 1000; Flash Messages)
    - app/forms.py (FlaskForm pattern, validators)
    - app/public.py (routes hiện có + cart routes từ 06-02)
    - app/models.py (Order, OrderItem từ 06-01)
  </read_first>
  <action>
    Hai file — `app/forms.py` và `app/public.py`:

    **forms.py**:
    1. Bổ sung imports: `from wtforms.validators import Regexp` (thêm vào dòng import validators); `from wtforms import ValidationError` (thêm vào dòng import wtforms) nếu chưa có.
    2. Thêm class `CheckoutForm(FlaskForm)` ở cuối file:
       - `customer_name = StringField('Họ và tên', validators=[DataRequired(message='Vui lòng nhập họ và tên.'), Length(max=100, message='Tên không được quá 100 ký tự.')])`
       - `customer_phone = StringField('Số điện thoại', validators=[DataRequired(message='Vui lòng nhập số điện thoại.'), Regexp(r'^\+?[\d\s-]{8,15}$', message='Số điện thoại phải có 8–11 chữ số.')])`
       - `customer_address = TextAreaField('Địa chỉ', validators=[DataRequired(message='Vui lòng nhập địa chỉ.'), Length(max=500, message='Địa chỉ không được quá 500 ký tự.')])`
       - `customer_note = TextAreaField('Ghi chú', validators=[Optional(), Length(max=1000, message='Ghi chú không được quá 1000 ký tự.')])`
       - `website = StringField()` — honeypot ẩn, không validator (silent reject ở route)
       - `submit = SubmitField('Đặt hàng')`
       - Method `def validate_customer_phone(self, field):` — đếm chữ số sau khi strip space/`-`/`+`: `digits = ''.join(ch for ch in field.data if ch.isdigit())`; nếu `not (8 <= len(digits) <= 11)` → `raise ValidationError('Số điện thoại phải có 8–11 chữ số.')`. (Regexp lỏng: chỉ kiểm tra charset + độ dài thô; digit-count là check chính.)

    **public.py**:
    1. Bổ sung imports: `from .forms import CheckoutForm`; `from .models import Order, OrderItem` (thêm vào dòng import Product/ProductImage hiện có).
    2. Thêm route `@public_bp.route('/cart/checkout', methods=['POST'])` `def checkout():` ở cuối file:
       - BƯỚC 1 — Honeypot silent reject (TRƯỚC mọi logic): nếu `request.form.get('website')` truthy → `return redirect(url_for('public.cart'))` — không flash, không ghi DB, không đụng session cart.
       - BƯỚC 2 — Empty cart guard: `cart = session.get('cart', {})`; nếu không có món (`not cart`) → `flash('Giỏ hàng của bạn đang trống.', 'error')`; redirect `/cart`.
       - BƯỚC 3 — Form validation: `form = CheckoutForm()`; nếu `not form.validate()` → `flash('Vui lòng nhập đầy đủ Họ và tên, Số điện thoại, và Địa chỉ.', 'error')`; redirect `/cart` (giữ giỏ nguyên).
       - BƯỚC 4 — Server re-validate từng món trong cart (không tin session/client): `items_to_save = []`; với mỗi `pid_str, qty in cart.items()`: bỏ qua key không phải chữ số; `product = db.session.get(Product, int(pid_str))`; nếu `product is None or product.status != 'available' or not (1 <= qty <= product.quantity)` → `flash('Một số sản phẩm trong giỏ không còn khả dụng. Vui lòng kiểm tra lại giỏ hàng.', 'error')`; redirect `/cart` (KHÔNG tạo đơn).
       - BƯỚC 5 — Tạo đơn trong 1 commit:
         - `order = Order(customer_name=form.customer_name.data.strip(), customer_phone=form.customer_phone.data.strip(), customer_address=form.customer_address.data.strip(), customer_note=form.customer_note.data.strip() or None, status='Chờ xác nhận')`
         - `db.session.add(order)`; `db.session.flush()` (lấy order.id).
         - Với mỗi `(product, qty) in items_to_save`: `db.session.add(OrderItem(order_id=order.id, product_id=product.id, product_name=product.name, product_price=product.price, product_cost_price=product.cost_price, quantity=qty))` — snapshot tại thời điểm đặt từ product hiện tại.
         - `db.session.commit()`.
       - BƯỚC 6 — Xóa giỏ + success: `session['cart'] = {}`; `flash('Đặt hàng thành công! Chúng tôi sẽ liên hệ xác nhận qua SĐT.', 'success')`; `return redirect(url_for('public.product_detail', product_id=items_to_save[0][0].id))` — redirect về trang chi tiết sản phẩm đầu tiên trong giỏ.
       - KHÔNG giảm `product.quantity` (ORD-12 → v2).

    KHÔNG thêm: route GET cho checkout (form nằm trên trang giỏ — UI-SPEC single-page), admin notification (ORD-11), giảm tồn kho (ORD-12).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
with app.app_context():
    from app.db import db
    from app.models import Product
    db.create_all()
    p1 = Product(name='Ao', price=100000, quantity=5, cost_price=60000)
    p2 = Product(name='Quan', price=200000, quantity=3, cost_price=None)
    db.session.add_all([p1, p2]); db.session.commit()
    pid1, pid2 = p1.id, p2.id
client = app.test_client()
def seed_cart(c):
    with client.session_transaction() as s:
        s['cart'] = c
def order_count():
    from app.models import Order
    with app.app_context():
        return Order.query.count()
# 1. happy path: 2 món -> 1 Order + 2 OrderItem snapshot, xóa giỏ, success flash, redirect chi tiết
seed_cart({str(pid1): 2, str(pid2): 1})
r = client.post('/cart/checkout', data={
    'customer_name': 'Nguyen Van A', 'customer_phone': '0912 345 678',
    'customer_address': '12 Le Loi, Q1', 'customer_note': 'Giao sau 18h', 'website': '',
}, follow_redirects=False)
assert r.status_code == 302 and r.headers['Location'].endswith(f'/products/{pid1}')
with client.session_transaction() as s:
    assert s['cart'] == {}  # giỏ đã xóa
    flashes = [(c, m) for c, m in s.get('_flashes', [])]
    assert any(m == 'Đặt hàng thành công! Chúng tôi sẽ liên hệ xác nhận qua SĐT.' for c, m in flashes if c == 'success')
with app.app_context():
    from app.models import Order as O, OrderItem as OI
    assert O.query.count() == 1
    o = O.query.first()
    assert o.customer_name == 'Nguyen Van A' and o.customer_phone == '0912 345 678' and o.customer_note == 'Giao sau 18h' and o.status == 'Chờ xác nhận'
    its = o.items.all(); assert len(its) == 2
    by = {i.product_name: i for i in its}
    assert by['Ao'].product_price == 100000 and by['Ao'].product_cost_price == 60000 and by['Ao'].quantity == 2
    assert by['Quan'].product_price == 200000 and by['Quan'].product_cost_price is None and by['Quan'].quantity == 1
base = order_count()
# 2. honeypot điền -> silent reject, không đơn, giỏ nguyên, không flash
seed_cart({str(pid1): 1})
r = client.post('/cart/checkout', data={'customer_name': 'Bot', 'customer_phone': '0912345678', 'customer_address': 'X', 'website': 'http://spam'})
assert r.status_code == 302 and r.headers['Location'].endswith('/cart')
assert order_count() == base
with client.session_transaction() as s:
    assert s['cart'] == {str(pid1): 1}
# 3. thiếu trường bắt buộc -> flash lỗi, không đơn
seed_cart({str(pid1): 1})
r = client.post('/cart/checkout', data={'customer_name': '', 'customer_phone': '', 'customer_address': ''})
assert r.status_code == 302 and r.headers['Location'].endswith('/cart')
assert order_count() == base
# 4. SĐT quá ngắn (< 8 chữ số) -> reject
r = client.post('/cart/checkout', data={'customer_name': 'A', 'customer_phone': '123', 'customer_address': 'X'})
assert r.status_code == 302
assert order_count() == base
# 5. tamper qty > tồn kho -> không đơn, giỏ nguyên
seed_cart({str(pid1): 99})
r = client.post('/cart/checkout', data={'customer_name': 'A', 'customer_phone': '0912345678', 'customer_address': 'X'})
assert r.status_code == 302 and r.headers['Location'].endswith('/cart')
assert order_count() == base
# 6. sản phẩm ngừng bán trong giỏ -> không đơn
from app.models import Product as P
with app.app_context():
    p3 = P(name='Ngung', price=50000, quantity=5, discontinued=True)
    db.session.add(p3); db.session.commit(); pid3 = p3.id
seed_cart({str(pid3): 1})
r = client.post('/cart/checkout', data={'customer_name': 'A', 'customer_phone': '0912345678', 'customer_address': 'X'})
assert r.status_code == 302
assert order_count() == base
# 7. giỏ trống checkout -> không đơn
seed_cart({})
r = client.post('/cart/checkout', data={'customer_name': 'A', 'customer_phone': '0912345678', 'customer_address': 'X'})
assert r.status_code == 302
assert order_count() == base
# 8. tồn kho không giảm sau đơn
with app.app_context():
    assert db.session.get(P, pid1).quantity == 5 and db.session.get(P, pid2).quantity == 3
# 9. CSRF: instance riêng (CSRF enabled default) -> POST thiếu token -> 400
app2 = app_module.create_app()
r2 = app2.test_client().post('/cart/checkout', data={'customer_name': 'A', 'customer_phone': '0912345678', 'customer_address': 'X'})
assert r2.status_code == 400
print('TASK_OK')
PY</automated>
  </verify>
  <done>Route checkout tạo Order + OrderItem snapshot; honeypot/CSRF/qty-tamper/thiếu field đều chặn; xóa giỏ + flash success + redirect chi tiết; tồn kho không giảm</done>
  <acceptance_criteria>
    - `app/forms.py` có class CheckoutForm: customer_name (DataRequired + Length max 100), customer_phone (DataRequired + Regexp `^\+?[\d\s-]{8,15}$` + validate_customer_phone 8-11 digits), customer_address (DataRequired + Length max 500), customer_note (Optional + Length max 1000), website (honeypot), submit; imports Regexp + ValidationError
    - `app/public.py` có `def checkout():` với route `/cart/checkout` POST; thứ tự: honeypot → empty cart guard → form.validate → re-validate cart → tạo Order + OrderItem 1 commit → xóa giỏ + flash success + redirect detail
    - Honeypot: truthy `website` → redirect /cart không flash/không đơn/không đụng giỏ
    - Re-validate: `product.status != 'available'` hoặc `1 <= qty <= product.quantity` sai → flash lỗi, không tạo đơn
    - Tồn kho sản phẩm KHÔNG đổi sau đặt hàng
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Template _checkout_form.html (CSRF + honeypot + 4 field)</name>
  <files>app/templates/public/_checkout_form.html</files>
  <read_first>
    - .planning/phases/06-public-order-form/06-UI-SPEC.md (Checkout Form section — markup verbatim; Field contract: name required max 100, phone type=tel + help-text, address textarea min-height 120px, note textarea placeholder, honeypot display:none, submit btn-primary; Copywriting Contract)
    - app/templates/admin/products/form.html (pattern `.form-field` + `span.required` + `.field-error` — class CSS tái sử dụng)
    - app/static/css/style.css (`.form-field`, `.form-row-2`, `.btn`, `.btn-primary`, `.help-text`, `.required`, `.honeypot` — honeypot đã có từ 06-02)
  </read_first>
  <action>
    Tạo file MỚI `app/templates/public/_checkout_form.html` — là partial KHÔNG extends base.html (được cart.html include). Dùng markup UI-SPEC "Checkout Form" verbatim, thẻ field name khớp CheckoutForm:

    - `<form class="checkout-form" method="post" action="{{ url_for('public.checkout') }}">`
      - `<input type="hidden" name="csrf_token" value="{{ csrf_token() }}">`
      - `<input type="text" name="website" class="honeypot" autocomplete="off" tabindex="-1" aria-hidden="true">` — honeypot ẩn (CSS `.honeypot { display: none; }` đã có từ 06-02)
      - `.form-field` — label `Họ và tên` + `<span class="required" aria-hidden="true">*</span>`; `<input type="text" id="customer_name" name="customer_name" maxlength="100" required>`
      - `.form-field` — label `Số điện thoại` + span.required; `<input type="tel" id="customer_phone" name="customer_phone" required>` + `<p class="help-text">8–11 chữ số, có thể có dấu cách, gạch ngang hoặc dấu cộng</p>`
      - `.form-field` — label `Địa chỉ` + span.required; `<textarea id="customer_address" name="customer_address" maxlength="500" required></textarea>`
      - `.form-field` — label `Ghi chú (tùy chọn)`; `<textarea id="customer_note" name="customer_note" maxlength="1000" placeholder="Ví dụ: giao sau 18h, gọi trước khi đến..."></textarea>`
      - `.form-field` — `<button type="submit" class="btn btn-primary" style="width: 100%; max-width: 320px;">Đặt hàng</button>`
    - Tất cả input/textarea kế thừa style `.form-field input` / `.form-field textarea` (height 44px, border #D1D5DB, radius 8px, Body 16px — đã có sẵn CSS).

    KHÔNG thêm JS; KHÔNG render per-field errors (luồng approve dùng flash summary — 06-UI-SPEC Checkout Flow step 4 + Copywriting "Vui lòng nhập đầy đủ..."); không thêm `.form-row-2` (giữ markup verbatim: 4 field full-width).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
with app.app_context():
    html = app.jinja_env.get_template('public/_checkout_form.html').render()
    assert 'name="customer_name"' in html and 'name="customer_phone"' in html
    assert 'name="customer_address"' in html and 'name="customer_note"' in html
    assert 'name="website"' in html and 'honeypot' in html
    assert 'csrf_token' in html
    assert '/cart/checkout' in html and 'Đặt hàng' in html
    assert 'type="tel"' in html and 'type="text"' in html
    assert '8–11 chữ số' in html
    # integration: cart.html include partial (cần 06-02 đã execute)
    from app.db import db
    from app.models import Product
    db.create_all()
    p = Product(name='Ao', price=100000, quantity=5)
    db.session.add(p); db.session.commit(); pid = p.id
client = app.test_client()
with client.session_transaction() as s:
    s['cart'] = {str(pid): 1}
html = client.get('/cart').get_data(as_text=True)
assert 'Thông tin đặt hàng' in html and 'Họ và tên' in html and 'name="customer_name"' in html
print('TASK_OK')
PY</automated>
  </verify>
  <done>Partial checkout form render đủ 4 field + honeypot + CSRF + submit; được cart.html include hiển thị trên trang giỏ (integration check)</done>
  <acceptance_criteria>
    - `app/templates/public/_checkout_form.html` tồn tại, không extends base.html; chứa `action="{{ url_for('public.checkout') }}"`, `name="csrf_token"`, `name="website"` class `honeypot`, `name="customer_name"` (maxlength 100 required), `name="customer_phone"` (type="tel" required), `name="customer_address"` (textarea maxlength 500 required), `name="customer_note"` (textarea maxlength 1000 + placeholder), submit `.btn.btn-primary`
    - GET /cart (có items, sau 06-02) render section "Thông tin đặt hàng" + form partial
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /cart/checkout | Toàn bộ payload (customer_name/phone/address/note/website) + session cart cookie — untrusted. |
| public route → orders DB | Route ghi Order + OrderItem — dữ liệu khách hàng (PII). |
| cart.html → _checkout_form.html | Partial render trong trang public — phải không leak cost_price, không có JS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing / Spam | Honeypot `website` field | mitigate | Kiểm tra TRƯỚC mọi logic: field truthy → silent reject (redirect /cart, không flash, không ghi DB, không đụng giỏ). Bot điền sẽ bị trap; form hợp lệ cho người dùng thật (không focusable, CSS display:none). |
| T-06-02 | CSRF | POST /cart/checkout | mitigate | CSRFProtect app-wide (app/__init__.py line 54) + `csrf_token()` trong form. Verify: POST thiếu token → 400 (instance CSRF enabled). |
| T-06-03 | Tampering | qty trong session cart bị sửa (tamper > tồn kho), sản phẩm hết hàng/ngừng bán sau khi add | mitigate | Server re-validate TỪNG món tại checkout: `db.session.get(Product)` mới + `status == 'available'` + `1 <= qty <= product.quantity`; sai → flash lỗi, KHÔNG tạo đơn. Không tin session cart. |
| T-06-04 | Tampering | Injection qua text field (name/address/note) | mitigate | ORM parameterized (không raw SQL); Jinja2 autoescape render. Length bound (100/500/1000). Validate SĐT bằng Regexp + digit-count. |
| T-06-05 | Information Disclosure | `product_cost_price` snapshot trong OrderItem | mitigate | Route chỉ GHI cost_price từ `product.cost_price`; template `_checkout_form.html` và mọi template public không render `cost_price`/`Giá nhập` (grep gate). Admin đọc qua Phase 7 sau `@login_required`. |
| T-06-06 | Information Disclosure | Customer PII (name/phone/address) data-at-rest | accept | SQLite server-local; admin-only access Phase 7 sau `@login_required`. Không expose PII qua route public (không có GET đơn public). |
| T-06-07 | Tampering | Đặt hàng không giảm tồn kho → oversell | accept | ORD-12 deferred v2 (tự trừ tồn kho khi xác nhận). Quy mô hiện tại chấp nhận; admin xử lý qua SĐT. Ghi nhận trong SUMMARY. |
</threat_model>

<verification>
- Task 1 verify exits 0 printing TASK_OK (9 cases: happy path + honeypot + thiếu field + SĐT ngắn + tamper qty + ngừng bán + giỏ trống + tồn kho không đổi + CSRF 400)
- Task 2 verify exits 0 printing TASK_OK (partial render + integration GET /cart)
- Grep gate: `grep -rn "cost_price\|Giá nhập" app/templates/public/` → 0 hits
</verification>

<success_criteria>
- Checkout form bắt buộc tên/SĐT/địa chỉ; SĐT 8-11 chữ số; ghi chú tùy chọn — ORD-01, ORD-02
- POST /cart/checkout tạo 1 Order + nhiều OrderItem snapshot trong 1 commit — ORD-10a
- CSRF (POST thiếu token → 400) + honeypot silent reject — ORD-05
- Server re-validate qty ≤ tồn kho + sản phẩm available — tamper/hết hàng → không tạo đơn — ORD-02
- Khách thấy flash success sau khi đặt; giỏ xóa; redirect về trang chi tiết — ORD-03
- Tồn kho KHÔNG giảm (ORD-12 deferred v2); không cài đặt ORD-11
</success_criteria>

<output>
Create `.planning/phases/06-public-order-form/06-03-SUMMARY.md` when done
</output>
