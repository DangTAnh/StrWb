---
phase: 07-admin-order-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/admin.py, app/templates/admin/orders/list.html, app/templates/admin/dashboard.html, app/static/css/style.css]
autonomous: true
requirements: [ORD-06]

must_haves:
  truths:
    - "Admin xem danh sách đơn phân trang tại GET /admin/orders, đơn mới nhất trước (created_at desc, id desc) — ORD-06"
    - "Admin lọc đơn theo trạng thái qua dropdown GET ?status=<VN label>; status không hợp lệ → bỏ qua filter, không 500"
    - "Mỗi dòng hiện ID (link chi tiết), tên khách, tổng tiền (sum items), badge trạng thái, ngày tạo"
    - "Nav dashboard có nhóm 'Đơn hàng' (count badge) dẫn tới /admin/orders"
    - "Empty states phân biệt: chưa có đơn nào vs không có đơn khớp filter"
  artifacts:
    - path: "app/admin.py"
      provides: "orders() route + _order_total/order_badge_class Jinja globals + ORDER_STATUSES + dashboard pass orders_count"
      contains: "def orders"
    - path: "app/templates/admin/orders/list.html"
      provides: "filter form + paginated data-table + two empty states"
      contains: "badge-order-"
    - path: "app/templates/admin/dashboard.html"
      provides: "nav group 'Đơn hàng'"
      contains: "Đơn hàng"
    - path: "app/static/css/style.css"
      provides: ".badge-order-* x5 + .order-filter + .order-id"
      contains: "badge-order-pending"
  key_links:
    - from: "app/admin.py orders()"
      to: "Order model"
      via: "Order.query.order_by(created_at.desc(), id.desc()).paginate(page, per_page=20, error_out=False)"
      pattern: "Order.query.order_by"
    - from: "list.html"
      to: "admin.order_detail"
      via: "url_for('admin.order_detail', order_id=order.id) — ID cell is the drill-in link"
      pattern: "admin.order_detail"
    - from: "dashboard.html"
      to: "admin.orders"
      via: "url_for('admin.orders') in nav-group"
      pattern: "admin.orders"
    - from: "_order_total"
      to: "OrderItem snapshot"
      via: "sum(item.product_price * item.quantity for item in order.items)"
      pattern: "product_price"
---

<objective>
Order list + status filter (ORD-06): admin xem danh sách đơn phân trang tại `GET /admin/orders`, lọc theo trạng thái VN qua `?status=`, mỗi dòng hiện ID / tên khách / tổng tiền (sum order_items) / badge trạng thái / ngày tạo. Thêm nav "Đơn hàng" trên dashboard.

Purpose: Phase 7 bắt đầu admin order tracking — list là cổng vào chi tiết đơn (07-02) và chuyển trạng thái (07-03). `_order_total` + `order_badge_class` là các Jinja global dùng chung cho list (07-01) VÀ detail (07-02/07-03) — plan này định nghĩa chúng để 07-02/07-03 tái sử dụng (single source of truth).

**Lưu ý file-ownership (khác mô tả ban đầu, có lý do):**
- `_order_total` helper KHÔNG để 07-02 — vì list.html render tổng tiền từng dòng qua `{{ _order_total(order) | format_price }}` (07-UI-SPEC section 1), helper phải tồn tại trước khi list render. Plan này định nghĩa nó như Jinja global; 07-02 chỉ tái sử dụng.
- Cột ngày tạo dùng `order.created_at.strftime('%d/%m/%Y')` (method call, không cần filter) — vì `strftime` Jinja filter thuộc sở hữu 07-02 (`__init__.py`), plan này KHÔNG đụng `__init__.py`.
- Không dùng `OrderStatusForm` (07-03 quyết định CSRF-hidden-form theo UI-SPEC).

## Coverage Map

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| ORD-06 | Danh sách đơn phân trang + lọc theo trạng thái | 07-01 (Task 1 route + helper, Task 2 template, Task 3 nav/CSS) |
| 07-CONTEXT | Sắp xếp created_at desc, id desc; status không hợp lệ → bỏ qua filter; `_order_total` = sum(product_price*quantity); `?status=` trống = tất cả | 07-01 |
| 07-UI-SPEC | `.data-table`/`.pagination`/`.empty-state` reuse; `.badge-order-*` x5; `.order-filter`/`.order-id`; nav-group "Đơn hàng" trên dashboard; filter GET form; option counts | 07-01 |
| Deferred | Export CSV, bulk status, thông báo đơn mới | KHÔNG cài đặt |

Output: route `admin.orders()`, template `admin/orders/list.html`, nav dashboard, CSS badge/filter/id. Không dependency mới.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-order-tracking/07-CONTEXT.md (ORD-06: route GET /admin/orders pattern admin.products; sort created_at desc, id desc; filter ?status= VN label, invalid → bỏ qua; _order_total = sum(item.product_price * item.quantity); per_page=20; count theo status optional; mọi route admin đã @login_required qua _protect_admin)
@.planning/phases/07-admin-order-tracking/07-UI-SPEC.md (section 1 Order List Page: filter GET form name=status, option counts, ID cell = link `.order-id`, `_order_total` | format_price, badge `.badge-order-*`, pagination giữ ?status=, hai empty states; section 0 Admin Nav: nav-group "Đơn hàng" trong dashboard nav-list, count badge)
@.planning/phases/06-public-order-form/06-01-SUMMARY.md (Order = customer+status 8 cột; OrderItem snapshot product_name/product_price/product_cost_price/quantity; items relationship lazy='dynamic')
@app/admin.py (pattern admin.products: `request.args.get('page', 1, type=int)` + `paginate(page, per_page=20, error_out=False)`; `db.session.get(Model, id)`; flash VN; `@admin_bp.before_request @login_required _protect_admin`)
@app/models.py (class Order: id, customer_name/phone/address/note, status default 'Chờ xác nhận', created_at/updated_at utcnow, items = db.relationship lazy='dynamic' cascade delete-orphan)
@app/templates/admin/dashboard.html (nav-list hiện có: a Trang chủ + div.nav-group Sản phẩm + div.nav-group.logout-form)
@app/templates/admin/products/list.html (pattern: .admin-page > .admin-header h1 + .admin-card--wide > .table-scroll > table.data-table + .pagination + .empty-state)
@CLAUDE.md (D-05: tiền = Integer VND; format_price filter đã có ở app/__init__.py)
</context>

<interfaces>
Interfaces plan này TẠO (07-02/07-03 sẽ consume):

- `admin.orders` view (endpoint `admin.orders`): `GET /admin/orders` → render `admin/orders/list.html` với context:
  - `pagination` (SQLAlchemy Pagination), `orders` (= pagination.items), `current_status` (str, '' = tất cả), `status_counts` (dict status→int), `total_orders` (int), `order_statuses` (tuple ORDER_STATUSES)
- `_order_total(order)` — Jinja global (qua `@admin_bp.app_template_global()`): `sum(item.product_price * item.quantity for item in order.items)`. Dùng ở list AND detail. (order.items lazy='dynamic' — iterate triggers query.)
- `order_badge_class(status)` — Jinja global (qua `@admin_bp.app_template_global()`): map VN status → CSS class:
  `{'Chờ xác nhận':'badge-order-pending','Đã gói':'badge-order-packed','Đã gửi':'badge-order-shipped','Đã nhận':'badge-order-delivered','Đã hủy':'badge-order-cancelled'}` — fallback `''`
- `ORDER_STATUSES` module constant: `('Chờ xác nhận', 'Đã gói', 'Đã gửi', 'Đã nhận', 'Đã hủy')`

Trong `app/admin.py` hiện tại (đã có trên disk): import `from .models import Product, ProductImage`; `admin_bp = Blueprint('admin', __name__, url_prefix='/admin')`; route `dashboard()` pass `products_count`; route `products()` pattern pagination.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend — ORDER_STATUSES + _order_total/order_badge_class Jinja globals + orders() route + dashboard orders_count</name>
  <files>app/admin.py</files>
  <read_first>
    - app/admin.py (imports line 1-7; dashboard() line 88-91; products() line 94-100 pattern pagination)
    - app/models.py (class Order lines 73-85; items relationship lazy='dynamic' line 85)
    - .planning/phases/07-admin-order-tracking/07-CONTEXT.md (ORD-06: sort, filter, _order_total)
  </read_first>
  <action>
    Sửa duy nhất `app/admin.py`. Bốn thay đổi:

    1. Thêm import: `from .models import Product, ProductImage, Order` (thay dòng 7).
    2. Thêm module constant (sau `admin_bp` định nghĩa):
       `ORDER_STATUSES = ('Chờ xác nhận', 'Đã gói', 'Đã gửi', 'Đã nhận', 'Đã hủy')` — thứ tự đúng VN.
    3. Thêm HAI Jinja global dùng chung toàn app (07-02/07-03 tái sử dụng — single source of truth):
       - `@admin_bp.app_template_global() def _order_total(order):` return `sum(item.product_price * item.quantity for item in order.items)` — item.product_price/quantity là snapshot Integer VND (D-05). KHÔNG thêm cột total (sum là nguồn truth).
       - `@admin_bp.app_template_global() def order_badge_class(status):` return dict lookup `{'Chờ xác nhận':'badge-order-pending','Đã gói':'badge-order-packed','Đã gửi':'badge-order-shipped','Đã nhận':'badge-order-delivered','Đã hủy':'badge-order-cancelled'}.get(status, '')`.
    4. Thêm route `orders()` (pattern y hệt products(), line 94-100):
       ```python
       @admin_bp.route('/orders', methods=['GET'])
       def orders():
           page = request.args.get('page', 1, type=int)
           status = (request.args.get('status') or '').strip()
           query = Order.query.order_by(Order.created_at.desc(), Order.id.desc())
           if status in ORDER_STATUSES:
               query = query.filter_by(status=status)
           pagination = query.paginate(page=page, per_page=20, error_out=False)
           status_counts = dict(db.session.query(Order.status, db.func.count(Order.id)).group_by(Order.status).all())
           return render_template(
               'admin/orders/list.html',
               pagination=pagination, orders=pagination.items, current_status=status,
               status_counts=status_counts, total_orders=sum(status_counts.values()),
               order_statuses=ORDER_STATUSES,
           )
       ```
       - `status` invalid (không nằm trong ORDER_STATUSES) hoặc rỗng → KHÔNG filter_by → show tất cả (không 500).
       - `db.func` có sẵn qua `from .db import db` (đã import).
    5. Sửa `dashboard()` (line 88-91): thêm `orders_count=Order.query.count()` vào render_context (giữ nguyên products_count). Đây là count tổng, rẻ (SQLite local).

    KHÔNG: thêm filter `strftime` (thuộc 07-02), không đụng `__init__.py`, không thêm dependency, không đổi mọi route product hiện có.
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
assert 'admin.orders' in app.view_functions
assert 'admin.dashboard' in app.view_functions
assert '_order_total' in app.jinja_env.globals
assert 'order_badge_class' in app.jinja_env.globals
from app.admin import ORDER_STATUSES
assert ORDER_STATUSES == ('Chờ xác nhận', 'Đã gói', 'Đã gửi', 'Đã nhận', 'Đã hủy')
print('TASK_OK')
PY</automated>
  </verify>
  <done>View admin.orders + admin.dashboard registered; _order_total + order_badge_class trong jinja_env.globals; ORDER_STATUSES đúng 5 status VN; dashboard pass orders_count</done>
  <acceptance_criteria>
    - `app/admin.py` có: import Order; constant ORDER_STATUSES; `@admin_bp.app_template_global()` `_order_total(order)` trả `sum(item.product_price * item.quantity for item in order.items)`; `@admin_bp.app_template_global()` `order_badge_class(status)` map 5 status; route `orders()` với filter `if status in ORDER_STATUSES` + paginate per_page=20 error_out=False
    - `dashboard()` render_context có `orders_count=Order.query.count()`
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Template admin/orders/list.html — filter form + data-table + pagination (giữ status) + hai empty states</name>
  <files>app/templates/admin/orders/list.html</files>
  <read_first>
    - app/templates/admin/products/list.html (pattern .admin-page/.admin-header/.admin-card--wide/.table-scroll/.data-table/.pagination/.empty-state)
    - .planning/phases/07-admin-order-tracking/07-UI-SPEC.md (section 1: markup filter form, table columns ID/Khách hàng/Tổng tiền/Trạng thái/Ngày tạo, option counts, .order-id link, pagination giữ status, hai empty states)
    - .planning/phases/07-admin-order-tracking/07-CONTEXT.md (per_page=20, sort desc, filter invalid → bỏ qua)
  </read_first>
  <action>
    Tạo MỚI `app/templates/admin/orders/list.html` — `{% extends "base.html" %}` (KHÔNG admin layout riêng, dùng chung base.html như products/list.html).

    Cấu trúc (theo UI-SPEC section 1):
    ```
    <div class="admin-page">
      <div class="admin-header"><h1>Đơn hàng</h1></div>   <!-- KHÔNG CTA — admin đọc, không tạo đơn -->
      <form class="order-filter" method="get" action="{{ url_for('admin.orders') }}">
        <label class="visually-hidden" for="status-filter">Lọc theo trạng thái</label>
        <select id="status-filter" name="status">
          <option value="">Tất cả ({{ total_orders }})</option>
          {% for s in order_statuses %}
          <option value="{{ s }}" {% if current_status == s %}selected{% endif %}>{{ s }} ({{ status_counts.get(s, 0) }})</option>
          {% endfor %}
        </select>
        <button class="btn btn-primary" type="submit">Lọc</button>
      </form>
      <div class="admin-card admin-card--wide">
        {% if orders %}
          <div class="table-scroll">
            <table class="data-table">
              <caption class="visually-hidden">Danh sách đơn hàng</caption>
              <thead><tr>
                <th scope="col">ID</th><th scope="col">Khách hàng</th>
                <th scope="col">Tổng tiền</th><th scope="col">Trạng thái</th><th scope="col">Ngày tạo</th>
              </tr></thead>
              <tbody>
                {% for order in orders %}
                <tr>
                  <td><a class="order-id" href="{{ url_for('admin.order_detail', order_id=order.id) }}">#{{ order.id }}</a></td>
                  <td>{{ order.customer_name }}</td>
                  <td class="price-cell">{{ _order_total(order) | format_price }}</td>
                  <td><span class="badge {{ order_badge_class(order.status) }}">{{ order.status }}</span></td>
                  <td class="qty-muted">{{ order.created_at.strftime('%d/%m/%Y') }}</td>
                </tr>
                {% endfor %}
              </tbody>
            </table>
          </div>
          {% if pagination.pages > 1 %}
          <div class="pagination">
            {% if pagination.has_prev %}
            <a href="{{ url_for('admin.orders', page=pagination.prev_num, status=(current_status or None)) }}">Trước</a>
            {% else %}<span class="disabled" aria-disabled="true">Trước</span>{% endif %}
            <span class="page-indicator">Trang {{ pagination.page }} / {{ pagination.pages }}</span>
            {% if pagination.has_next %}
            <a href="{{ url_for('admin.orders', page=pagination.next_num, status=(current_status or None)) }}">Sau</a>
            {% else %}<span class="disabled" aria-disabled="true">Sau</span>{% endif %}
          </div>
          {% endif %}
        {% else %}
          <div class="empty-state">
            {% if current_status %}
            <h2>Không có đơn nào ở trạng thái “{{ current_status }}”</h2>
            <p>Thử chọn trạng thái khác hoặc chọn “Tất cả”.</p>
            {% else %}
            <h2>Chưa có đơn nào</h2>
            <p>Khách đặt hàng qua web sẽ hiện ở đây.</p>
            {% endif %}
          </div>
        {% endif %}
      </div>
    </div>
    ```

    Yêu cầu bắt buộc:
    - Cột ID là link chi tiết `admin.order_detail` — KHÔNG cột "Hành động" riêng (chỉ 1 drill-in, theo UI-SPEC).
    - Ngày tạo dùng METHOD CALL `order.created_at.strftime('%d/%m/%Y')` — KHÔNG dùng filter `strftime` (thuộc 07-02); tránh 500 khi filter chưa tồn tại.
    - Pagination link giữ filter: `status=(current_status or None)` — url_for bỏ param None khi current_status rỗng → không sinh `?status=`.
    - `current_status` rỗng + không orders → empty state "Chưa có đơn nào"; current_status có giá trị + không orders → empty state "Không có đơn nào ở trạng thái X".
    - KHÔNG hiện product_cost_price (không nằm trong context, cũng không render).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from werkzeug.security import generate_password_hash
from app.db import db
from app.models import AdminUser, Order, OrderItem
with app.app_context():
    db.create_all()
    db.session.add(AdminUser(username='admin', password_hash=generate_password_hash('testpass1234')))
    oA = Order(customer_name='Khách A', customer_phone='0912345678', customer_address='Hà Nội', status='Chờ xác nhận')
    db.session.add(oA); db.session.flush()
    db.session.add(OrderItem(order_id=oA.id, product_name='Áo thun', product_price=100000, quantity=2))
    oB = Order(customer_name='Khách B', customer_phone='0987654321', customer_address='TPHCM', status='Đã gửi')
    db.session.add(oB); db.session.flush()
    db.session.add(OrderItem(order_id=oB.id, product_name='Quần', product_price=200000, quantity=1))
    newest = oB
    for i in range(20):  # 20 bulk pending -> total 22 orders (2 pages), pending 21 (2 pages)
        o = Order(customer_name=f'Khách {i}', customer_phone='0900000000', customer_address='ĐN', status='Chờ xác nhận')
        db.session.add(o); db.session.flush()
        db.session.add(OrderItem(order_id=o.id, product_name='Mũ', product_price=50000, quantity=1))
        newest = o
    db.session.commit()
    oa_id, ob_id, newest_id = oA.id, oB.id, newest.id
with app.test_client() as c:
    assert c.post('/login', data={'username':'admin','password':'testpass1234'}).status_code == 302
    # page 1 unfiltered (22 orders -> 2 pages): newest first
    b = c.get('/admin/orders').get_data(as_text=True)
    assert 'Đơn hàng' in b and 'Trang 1 / 2' in b and f'href="/admin/orders/{newest_id}"' in b
    # page 2: order A (pending, total 200.000) + order B
    b2 = c.get('/admin/orders', query_string={'page': 2}).get_data(as_text=True)
    assert 'Trang 2 / 2' in b2 and f'href="/admin/orders/{oa_id}"' in b2 and f'href="/admin/orders/{ob_id}"' in b2
    assert '200.000₫' in b2 and 'badge-order-pending' in b2
    # filter pending: Khách B (shipped) absent; pagination link preserves status
    bp = c.get('/admin/orders', query_string={'status': 'Chờ xác nhận'}).get_data(as_text=True)
    assert 'Khách B' not in bp and 'Trang 1 / 2' in bp and 'status=' in bp
    bp2 = c.get('/admin/orders', query_string={'status': 'Chờ xác nhận', 'page': 2}).get_data(as_text=True)
    assert 'Khách A' in bp2
    # filter shipped: only Khách B + shipped badge
    bs = c.get('/admin/orders', query_string={'status': 'Đã gửi'}).get_data(as_text=True)
    assert 'Khách B' in bs and 'Khách A' not in bs and 'badge-order-shipped' in bs
    # invalid filter -> ignored (22 orders, 2 pages), no 500
    bi = c.get('/admin/orders', query_string={'status': 'INVALID'}).get_data(as_text=True)
    assert 'Trang 1 / 2' in bi
    # filtered empty state
    be = c.get('/admin/orders', query_string={'status': 'Đã nhận'}).get_data(as_text=True)
    assert 'Không có đơn nào ở trạng thái' in be and 'Đã nhận' in be
    # out-of-range page -> 200 (error_out=False), no 500
    assert c.get('/admin/orders', query_string={'page': 999}).status_code == 200
print('TASK_OK')
PY</automated>
  </verify>
  <done>GET /admin/orders render list: phân trang 2 page, filter pending/shipped/invalid/empty đúng, tổng tiền 200.000₫ từ sum items, pagination giữ ?status=, empty state filter; page 999 → 200</done>
  <acceptance_criteria>
    - `app/templates/admin/orders/list.html` extends base.html; chứa `.order-filter` form GET (select name=status + nút Lọc), `.data-table` 5 cột, `.order-id` link detail, `.badge {{ order_badge_class(order.status) }}`, `.pagination` giữ status, hai `.empty-state`
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Nav "Đơn hàng" trên dashboard.html + CSS .badge-order-*/.order-filter/.order-id</name>
  <files>app/templates/admin/dashboard.html, app/static/css/style.css</files>
  <read_first>
    - app/templates/admin/dashboard.html (nav-list hiện có: Trang chủ + nav-group Sản phẩm + nav-group.logout-form)
    - app/static/css/style.css (lines 213-224 badge trio; lines 147-152 .admin-page/.admin-header/.admin-card--wide; lines 184-198 .data-table/.price-cell/.qty-muted; media query lines 331-335)
    - .planning/phases/07-admin-order-tracking/07-UI-SPEC.md (section 0: nav-group "Đơn hàng" + count badge markup; section 1: .order-filter/.order-id; badge bảng màu 5 status)
  </read_first>
  <action>
    Hai file:

    1. `app/templates/admin/dashboard.html` — thêm nav-group "Đơn hàng" SAU nav-group "Sản phẩm" (trước logout-form), mirror y hệt pattern Sản phẩm:
    ```html
    <div class="nav-group">
      <a href="{{ url_for('admin.orders') }}">Đơn hàng</a>
      {% if orders_count > 0 %}<span class="badge">{{ orders_count }}</span>{% else %}<span class="badge">Chưa có đơn</span>{% endif %}
    </div>
    ```
    Không CSS mới — reuse `.nav-group`/`.badge`. (orders_count được dashboard() pass từ Task 1.)

    2. `app/static/css/style.css` — APPEND (cuối file, trước hoặc sau media queries hợp lý) CSS Phase 7 list:
    ```css
    /* Order list (Phase 7) */
    .badge-order-pending { color: #1D4ED8; background: #DBEAFE; border-color: #BFDBFE; }
    .badge-order-packed { color: #D97706; background: #FFFBEB; border-color: #FDE68A; }
    .badge-order-shipped { color: #1D4ED8; background: #DBEAFE; border-color: #BFDBFE; }
    .badge-order-delivered { color: #059669; background: #ECFDF5; border-color: #A7F3D0; }
    .badge-order-cancelled { color: #6B7280; background: #F3F4F6; border-color: #E5E7EB; }
    .order-filter { display: flex; align-items: center; gap: 16px; margin-bottom: 16px; }
    .order-id { color: var(--accent); font-size: 14px; font-weight: 600; display: inline-block; padding: 12px 8px; }
    ```
    - 5 badge đúng bộ màu UI-SPEC section 3 (xanh mới #DBEAFE/#BFDBFE cho pending+shipped; packed/delivered/cancelled tái sử dụng trio Phase 2).
    - `.order-id` padding 12px 8px → touch target ≥44px (14px font × 1.5 line-height + 24px padding ≈ 45px).
    - Nếu mobile media query cần `.order-filter` xếp dọc: chỉ THÊM rule trong block `@media (max-width: 480px)` nếu cần để select full-width + nút dưới (UI-SPEC responsive); không bắt buộc nếu flex wrap đủ.

    KHÔNG đụng `.cart-total`/`.line-total` (đã có từ Phase 6), không sửa badge product hiện có.
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
orig_base = app_module.BASE_DIR
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from werkzeug.security import generate_password_hash
from app.db import db
from app.models import AdminUser, Order
with app.app_context():
    db.create_all()
    db.session.add(AdminUser(username='admin', password_hash=generate_password_hash('testpass1234')))
    db.session.add(Order(customer_name='A', customer_phone='1', customer_address='2'))
    db.session.commit()
css = open(os.path.join(orig_base, 'app', 'static', 'css', 'style.css'), encoding='utf-8').read()
assert '.badge-order-pending' in css and '.badge-order-packed' in css and '.badge-order-shipped' in css
assert '.badge-order-delivered' in css and '.badge-order-cancelled' in css
assert '.order-filter' in css and '.order-id' in css
with app.test_client() as c:
    assert c.post('/login', data={'username':'admin','password':'testpass1234'}).status_code == 302
    b = c.get('/admin/').get_data(as_text=True)
    assert 'Đơn hàng' in b and 'href="/admin/orders"' in b
    assert 'Chưa có đơn' not in b  # 1 order -> count badge số, không phải 'Chưa có đơn'
    with app.app_context():
        Order.query.delete(); db.session.commit()
    b0 = c.get('/admin/').get_data(as_text=True)
    assert 'Chưa có đơn' in b0  # 0 orders -> badge 'Chưa có đơn'
print('TASK_OK')
PY</automated>
  </verify>
  <done>Dashboard nav hiện "Đơn hàng" + count badge (số khi có đơn, "Chưa có đơn" khi 0); style.css chứa 5 class .badge-order-* + .order-filter + .order-id</done>
  <acceptance_criteria>
    - `app/templates/admin/dashboard.html` có `.nav-group` "Đơn hàng" với `href="{{ url_for('admin.orders') }}"` + badge count
    - `app/static/css/style.css` chứa `.badge-order-pending/shipped` (màu xanh #1D4ED8/#DBEAFE/#BFDBFE), `.badge-order-packed/delivered/cancelled`, `.order-filter`, `.order-id`
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → admin routes | Route admin đã bảo vệ `@login_required` qua `_protect_admin` before_request (Phase 1). |
| Admin browser → GET /admin/orders?status= | Query param từ URL — untrusted input vào filter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Order list (customer_name/phone/address PII) | mitigate | Route nằm trong admin_bp + `@login_required` (`_protect_admin` before_request) — chỉ admin đăng nhập đọc. Không route public nào render Order. |
| T-07-02 | Tampering | `?status=` query param | mitigate | Whitelist `if status in ORDER_STATUSES`; status không hợp lệ → bỏ qua filter, show tất cả, không 500. `filter_by` dùng bound param — không SQL injection. |
| T-07-03 | Information Disclosure | Trang ngoài phạm vi (`?page=999`) | mitigate | `error_out=False` → trả page rỗng, không 500, không leak dữ liệu. (Không redirect — khớp pattern admin.products.) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Không có package install trong plan này (0 dependency mới). |
</threat_model>

<verification>
- Task 1 verify exits 0 printing TASK_OK: view admin.orders/admin.dashboard + 2 Jinja global + ORDER_STATUSES
- Task 2 verify exits 0 printing TASK_OK: list render phân trang, filter, tổng tiền sum, empty states, page 999 → 200
- Task 3 verify exits 0 printing TASK_OK: dashboard nav "Đơn hàng" + count badge (có/không đơn), CSS badge/filter/id
- Toàn bộ verify chạy trên temp DB (patch app_module.BASE_DIR → temp, create_all + seed) — KHÔNG chạm `data/app.db` thật
</verification>

<success_criteria>
- GET /admin/orders trả danh sách phân trang (per_page=20, created_at desc, id desc), lọc ?status= VN label (invalid → bỏ qua) — ORD-06
- Mỗi dòng: ID link chi tiết, tên khách, tổng tiền = `_order_total(order)` sum items (format_price), badge status, ngày tạo %d/%m/%Y
- Dashboard nav "Đơn hàng" + count badge; hai empty states đúng copy UI-SPEC
- `_order_total`/`order_badge_class`/`ORDER_STATUSES` là interfaces dùng chung cho 07-02/07-03
- Không dependency mới; DB thật không bị đổi trong verify
</success_criteria>

<output>
Create `.planning/phases/07-admin-order-tracking/07-01-SUMMARY.md` when done
</output>
