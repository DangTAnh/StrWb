---
phase: 07-admin-order-tracking
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [app/admin.py, app/__init__.py, app/templates/admin/orders/detail.html, app/static/css/style.css]
autonomous: true
requirements: [ORD-07]

must_haves:
  truths:
    - "Admin xem chi tiết đơn tại GET /admin/orders/<id>: thông tin khách (tên/SĐT/địa chỉ/ghi chú), từng sản phẩm snapshot (tên/số lượng/đơn giá/thành tiền), tổng tiền (sum items), ngày tạo/cập nhật — ORD-07"
    - "Đơn không tồn tại → flash 'Không tìm thấy đơn.' + redirect /admin/orders (không 404, không 500)"
    - "strftime Jinja filter tồn tại để render %d/%m/%Y %H:%M"
    - "KHÔNG hiện product_cost_price trên trang detail"
  artifacts:
    - path: "app/__init__.py"
      provides: "strftime Jinja filter"
      contains: "template_filter('strftime')"
    - path: "app/admin.py"
      provides: "order_detail route (db.session.get(Order, order_id))"
      contains: "def order_detail"
    - path: "app/templates/admin/orders/detail.html"
      provides: "back-link + header badge + customer info + items table + total + timestamps"
      contains: "Thông tin khách"
    - path: "app/static/css/style.css"
      provides: ".order-detail + .order-section + .order-meta + .unit-price"
      contains: "order-section"
  key_links:
    - from: "order_detail"
      to: "Order"
      via: "db.session.get(Order, order_id) — missing -> flash + redirect"
      pattern: "db.session.get(Order"
    - from: "detail.html"
      to: "admin.orders"
      via: "url_for('admin.orders') — back-link"
      pattern: "admin.orders"
    - from: "detail.html"
      to: "_order_total"
      via: "Jinja global từ 07-01 — tổng tiền"
      pattern: "_order_total(order)"
    - from: "detail.html"
      to: "order.items"
      via: "{% for item in order.items %} (lazy='dynamic' iterate)"
      pattern: "for item in order.items"
---

<objective>
Order detail view (ORD-07): `GET /admin/orders/<int:order_id>` render thông tin khách (customer_name/phone/address/note), danh sách OrderItem snapshot (product_name, quantity, product_price, thành tiền = price*qty), tổng tiền qua `_order_total(order)` (Jinja global từ 07-01), timestamps `%d/%m/%Y %H:%M`. Thêm `strftime` Jinja filter vào `app/__init__.py` (chưa có — đã verify grep).

Purpose: chi tiết đơn là nơi admin đọc toàn bộ thông tin đơn; 07-03 thêm section "Cập nhật trạng thái" (stepper + buttons) vào detail.html này.

**File-ownership:**
- `app/__init__.py` — CHỈ plan này khai (strftime filter). 07-01 dùng method-call `.strftime()` nên không đụng file này.
- `_order_total` + `order_badge_class` — tái sử dụng từ 07-01 (không định nghĩa lại).
- `app/templates/admin/orders/detail.html` — plan này TẠO bản nền (customer + items + total + timestamps + badge header). 07-03 sẽ SỬA thêm section status.
- Không render `product_cost_price` (07-CONTEXT decision — cost_price để Phase 8 stats; giữ tối giản).

## Coverage Map

| ID | Yêu cầu / Decision | Plan |
|----|--------------------|------|
| ORD-07 | Chi tiết đơn: khách, sản phẩm snapshot, số lượng, giá, ghi chú, thời gian | 07-02 (Task 1 route + filter, Task 2 template, Task 3 CSS) |
| 07-CONTEXT | Missing order → flash 'Không tìm thấy đơn.' + redirect; product_id NULL → hiện snapshot product_name (plain text, không link); KHÔNG hiện product_cost_price; timestamps %d/%m/%Y %H:%M | 07-02 |
| 07-UI-SPEC | Detail page: .back-link, header h1 "Đơn #N" + badge, .order-detail card 720px, .order-section, .order-meta, items table, .cart-total reuse, strftime filter | 07-02 |
| Deferred | Hiện cost_price trên detail admin, link product vào admin edit | KHÔNG cài đặt |

Output: route `admin.order_detail()`, `strftime` filter, template `admin/orders/detail.html` (bản nền), CSS detail. Không dependency mới.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-order-tracking/07-CONTEXT.md (ORD-07: route GET /admin/orders/<int:order_id>; missing → flash 'Không tìm thấy đơn.' + redirect /admin/orders; hiện customer/status/created/updated/từng item (product_name, quantity, product_price, thành tiền), tổng tiền; product_id NULL → snapshot product_name là đủ, không link; KHÔNG hiện product_cost_price; timestamps %d/%m/%Y %H:%M)
@.planning/phases/07-admin-order-tracking/07-01-SUMMARY.md (07-01 đã tạo: _order_total(order) + order_badge_class(status) Jinja global, ORDER_STATUSES, admin.orders route — tái sử dụng, không định nghĩa lại)
@.planning/phases/07-admin-order-tracking/07-UI-SPEC.md (section 2 Order Detail Page: .back-link, h1 "Đơn #N", badge header, .admin-card.order-detail padding 24px, dl.order-meta, items table data-table 4 cột, .cart-total reuse, strftime '%d/%m/%Y %H:%M' cho created+updated; KHÔNG render product_cost_price)
@app/admin.py (sau 07-01: có import Order, ORDER_STATUSES, _order_total/order_badge_class globals, orders() route, dashboard pass orders_count)
@app/__init__.py (lines 64-66: @app.template_filter('format_price') — chèn strftime bên cạnh; KHÔNG có strftime hiện tại)
@app/models.py (class Order items = db.relationship('OrderItem', lazy='dynamic'); OrderItem: product_name/product_price/product_cost_price/quantity, product_id FK SET NULL nullable)
@app/static/css/style.css (.cart-total/.cart-total-label/.cart-total-value/.data-table .line-total đã có từ Phase 6 — reuse)
@CLAUDE.md (D-05: tiền = Integer VND; ngôn ngữ tiếng Việt)
</context>

<interfaces>
Consumed từ 07-01 (đã trên disk): `_order_total(order)` Jinja global, `order_badge_class(status)` Jinja global, `admin.orders` endpoint, `ORDER_STATUSES`.

Plan này TẠO:
- `strftime` Jinja filter trong `app/__init__.py`: `@app.template_filter('strftime')` nhận `(value, fmt)`, trả `''` nếu value None, else `value.strftime(fmt)`. Đăng ký trong `create_app()` bên cạnh `format_price`.
- `admin.order_detail(order_id)` endpoint: `GET /admin/orders/<int:order_id>` → render `admin/orders/detail.html` với context `order`. Missing → flash 'Không tìm thấy đơn.' (error) + redirect `admin.orders`.

Đối tượng Order (models.py): `.id`, `.customer_name`, `.customer_phone`, `.customer_address`, `.customer_note` (nullable), `.status`, `.created_at`/`.updated_at` (datetime aware utc), `.items` (lazy='dynamic'). OrderItem: `.product_name`, `.product_price` (Integer VND), `.quantity`, `.product_id` (nullable — product bị xóa).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: strftime filter trong app/__init__.py + order_detail route trong app/admin.py</name>
  <files>app/__init__.py, app/admin.py</files>
  <read_first>
    - app/__init__.py (lines 64-66 @app.template_filter('format_price') — chèn strftime ngay sau; create_app structure)
    - app/admin.py (sau 07-01: import Order đã có; _order_total/order_badge_class globals; orders() route — thêm order_detail route cạnh nó)
    - .planning/phases/07-admin-order-tracking/07-CONTEXT.md (ORD-07: missing order flash + redirect)
  </read_first>
  <action>
    Hai file:

    1. `app/__init__.py` — trong `create_app()`, NGAY SAU block `@app.template_filter('format_price')` (lines 64-66), thêm:
    ```python
    @app.template_filter('strftime')
    def _strftime(value, fmt):
        if value is None:
            return ''
        return value.strftime(fmt)
    ```
    - `datetime` đã import sẵn (line 3) — không cần import mới. KHÔNG thêm dependency.
    - Filter name là `strftime` — template gọi `{{ value | strftime('%d/%m/%Y %H:%M') }}`.

    2. `app/admin.py` — thêm route (cạnh `orders()`):
    ```python
    @admin_bp.route('/orders/<int:order_id>', methods=['GET'])
    def order_detail(order_id):
        order = db.session.get(Order, order_id)
        if order is None:
            flash('Không tìm thấy đơn.', 'error')
            return redirect(url_for('admin.orders'))
        return render_template('admin/orders/detail.html', order=order)
    ```
    - Pattern `db.session.get(Order, order_id)` y hệt `db.session.get(Product, product_id)` trong admin.py.
    - Missing → flash + redirect (UI-SPEC: không 404, không 500).

    KHÔNG: render product_cost_price ở đây (không nằm trong template tới Task 2); không thêm query eager-load (order.items lazy dynamic — đủ cho 1 đơn ít items).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
assert 'strftime' in app.jinja_env.filters
assert 'admin.order_detail' in app.view_functions
from datetime import datetime, timezone
f = app.jinja_env.filters['strftime']
dt = datetime(2026, 8, 2, 9, 30, tzinfo=timezone.utc)
assert f(dt, '%d/%m/%Y %H:%M') == '02/08/2026 09:30'
assert f(None, '%d/%m/%Y') == ''
print('TASK_OK')
PY</automated>
  </verify>
  <done>strftime filter trong jinja_env.filters render đúng %d/%m/%Y %H:%M và None-safe; view admin.order_detail registered</done>
  <acceptance_criteria>
    - `app/__init__.py` có `@app.template_filter('strftime')` (None → '')
    - `app/admin.py` có `@admin_bp.route('/orders/<int:order_id>')` `def order_detail(order_id)` với missing → flash('Không tìm thấy đơn.', 'error') + redirect url_for('admin.orders')
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Template admin/orders/detail.html — back-link + header badge + customer + items + total + timestamps</name>
  <files>app/templates/admin/orders/detail.html</files>
  <read_first>
    - .planning/phases/07-admin-order-tracking/07-UI-SPEC.md (section 2 markup: .back-link, .admin-header h1 "Đơn #N" + .badge, .admin-card.order-detail, section.order-section h2, dl.order-meta, items table data-table 4 cột, .cart-total/.cart-total-label/.cart-total-value, strftime '%d/%m/%Y %H:%M')
    - .planning/phases/07-admin-order-tracking/07-CONTEXT.md (item snapshot product_name plain text, KHÔNG link product; KHÔNG hiện product_cost_price)
    - app/templates/admin/products/list.html (pattern .admin-page/.admin-header/.admin-card/.data-table/.visually-hidden caption)
  </read_first>
  <action>
    Tạo MỚI `app/templates/admin/orders/detail.html` — `{% extends "base.html" %}`. Bản nền: customer + items + total + timestamps + badge header. Section "Cập nhật trạng thái" (stepper/buttons) do 07-03 thêm.

    Cấu trúc (theo UI-SPEC section 2):
    ```
    <div class="admin-page">
      <a class="back-link" href="{{ url_for('admin.orders') }}">Quay lại</a>
      <div class="admin-header">
        <h1>Đơn #{{ order.id }}</h1>
        <span class="badge {{ order_badge_class(order.status) }}">{{ order.status }}</span>
      </div>
      <div class="admin-card order-detail">
        <section class="order-section">
          <h2>Thông tin khách</h2>
          <dl class="order-meta">
            <dt>Họ và tên</dt><dd>{{ order.customer_name }}</dd>
            <dt>Số điện thoại</dt><dd>{{ order.customer_phone }}</dd>
            <dt>Địa chỉ</dt><dd>{{ order.customer_address }}</dd>
            {% if order.customer_note %}<dt>Ghi chú</dt><dd>{{ order.customer_note }}</dd>{% endif %}
          </dl>
        </section>
        <section class="order-section">
          <h2>Sản phẩm</h2>
          <table class="data-table">
            <caption class="visually-hidden">Sản phẩm trong đơn</caption>
            <thead><tr>
              <th scope="col">Sản phẩm</th><th scope="col">Số lượng</th><th scope="col">Đơn giá</th><th scope="col">Thành tiền</th>
            </tr></thead>
            <tbody>
              {% for item in order.items %}
              <tr>
                <td>{{ item.product_name }}</td>
                <td>{{ item.quantity }}</td>
                <td class="unit-price">{{ item.product_price | format_price }}</td>
                <td class="line-total">{{ (item.product_price * item.quantity) | format_price }}</td>
              </tr>
              {% endfor %}
            </tbody>
          </table>
          <div class="cart-total">
            <span class="cart-total-label">Tổng cộng</span>
            <span class="cart-total-value">{{ _order_total(order) | format_price }}</span>
          </div>
        </section>
        <section class="order-section">
          <h2>Thời gian</h2>
          <dl class="order-meta">
            <dt>Ngày tạo</dt><dd>{{ order.created_at | strftime('%d/%m/%Y %H:%M') }}</dd>
            <dt>Cập nhật</dt><dd>{{ order.updated_at | strftime('%d/%m/%Y %H:%M') }}</dd>
          </dl>
        </section>
      </div>
    </div>
    ```

    Bắt buộc:
    - `item.product_name` là plain text — KHÔNG link product (07-CONTEXT: product_id có thể NULL, snapshot đủ).
    - KHÔNG render `item.product_cost_price` ở bất kỳ đâu.
    - Dùng `strftime` filter (đã có từ Task 1) cho created_at/updated_at.
    - `_order_total(order)` từ 07-01 — single source of truth tổng tiền.
    - `{{ (item.product_price * item.quantity) | format_price }}` — Jinja multiplication, format_price đã có.
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile, re
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from werkzeug.security import generate_password_hash
from app.db import db
from app.models import AdminUser, Order, OrderItem
with app.app_context():
    db.create_all()
    db.session.add(AdminUser(username='admin', password_hash=generate_password_hash('testpass1234')))
    o = Order(customer_name='Khách A', customer_phone='0912345678', customer_address='Hà Nội',
              customer_note='Gọi trước khi giao', status='Chờ xác nhận')
    db.session.add(o); db.session.flush()
    db.session.add(OrderItem(order_id=o.id, product_name='Áo thun', product_price=100000, quantity=2))
    db.session.add(OrderItem(order_id=o.id, product_name='Quần', product_price=200000, quantity=1))
    db.session.commit(); oid = o.id
with app.test_client() as c:
    assert c.post('/login', data={'username':'admin','password':'testpass1234'}).status_code == 302
    b = c.get(f'/admin/orders/{oid}').get_data(as_text=True)
    assert f'Đơn #{oid}' in b and 'badge-order-pending' in b
    assert 'Họ và tên' in b and 'Khách A' in b
    assert 'Số điện thoại' in b and '0912345678' in b
    assert 'Địa chỉ' in b and 'Hà Nội' in b
    assert 'Ghi chú' in b and 'Gọi trước khi giao' in b
    assert 'Áo thun' in b and 'Quần' in b
    assert '200.000₫' in b   # line total 100000*2
    assert '400.000₫' in b   # grand total 100000*2 + 200000*1
    assert 'Thời gian' in b and 'Ngày tạo' in b and 'Cập nhật' in b
    assert re.search(r'\d{2}/\d{2}/\d{4} \d{2}:\d{2}', b)  # strftime %d/%m/%Y %H:%M
    assert 'product_cost_price' not in b  # COST-02 admin detail
    assert 'href="/admin/orders"' in b    # back-link
    # missing order -> redirect to list + flash, no 500
    rb = c.get('/admin/orders/99999', follow_redirects=True)
    assert rb.status_code == 200 and 'Không tìm thấy đơn.' in rb.get_data(as_text=True)
print('TASK_OK')
PY</automated>
  </verify>
  <done>GET /admin/orders/<id> render: badge header, customer, items snapshot, line total + grand total 400.000₫, timestamps %d/%m/%Y %H:%M, back-link; missing → flash + redirect; không có product_cost_price</done>
  <acceptance_criteria>
    - `app/templates/admin/orders/detail.html` chứa: `.back-link` → admin.orders; header h1 "Đơn #{{order.id}}" + `.badge` via `order_badge_class`; 3 `section.order-section` (Thông tin khách / Sản phẩm / Thời gian); `dl.order-meta`; items `.data-table` với `.unit-price`/`.line-total`; `.cart-total` reuse; `| strftime('%d/%m/%Y %H:%M')`
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CSS detail — .order-detail + .order-section + .order-meta + .unit-price</name>
  <files>app/static/css/style.css</files>
  <read_first>
    - app/static/css/style.css (.cart-total/.cart-total-label/.cart-total-value/.data-table .line-total đã có — reuse; .admin-card line 87-93; .data-table line 184-198)
    - .planning/phases/07-admin-order-tracking/07-UI-SPEC.md (section 2: .order-detail padding 24px; .order-section border-top #E5E7EB padding-top/margin-top 24px; section h2 16px/600; .order-meta dt #6B7280 14px, dd #1F2937 16px; .unit-price #6B7280 14px tabular-nums)
  </read_first>
  <action>
    APPEND vào `app/static/css/style.css` (cuối file, trước/sau media queries hợp lý):
    ```css
    /* Order detail (Phase 7) */
    .order-detail { padding: 24px; }
    .order-section { border-top: 1px solid #E5E7EB; padding-top: 24px; margin-top: 24px; }
    .order-section h2 { font-size: 16px; font-weight: 600; margin-bottom: 8px; }
    .order-meta dt { color: #6B7280; font-size: 14px; }
    .order-meta dd { color: #1F2937; margin: 0 0 8px 0; }
    .data-table .unit-price { color: #6B7280; font-size: 14px; font-variant-numeric: tabular-nums; }
    ```
    - `.order-detail` 720px tự nhiên (`.admin-card` max-width sẵn có) — KHÔNG dùng `.admin-card--wide` (nó padding:0 cho full-bleed table).
    - `.cart-total`/`.cart-total-value` đã có từ Phase 6 — KHÔNG định nghĩa lại.
    - Mobile: nếu cần `.order-detail` max-width:100% ở `@media (max-width: 768px)` — chỉ thêm nếu chưa có rule cho `.admin-card` ở breakpoint đó; kiểm tra block media hiện có trước.

    KHÔNG đụng class product/badge hiện có, không thêm class stepper (07-03).
  </action>
  <verify>
    <automated>python - <<'PY'
import os
os.environ['SECRET_KEY'] = 'x'*32
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
css = open(os.path.join(app_module.BASE_DIR, 'app', 'static', 'css', 'style.css'), encoding='utf-8').read()
assert '.order-detail' in css and '.order-section' in css and '.order-meta' in css
assert '.data-table .unit-price' in css
assert '.cart-total-value' in css  # reuse từ Phase 6, không được xóa
print('TASK_OK')
PY</automated>
  </verify>
  <done>style.css chứa .order-detail/.order-section/.order-meta/.data-table .unit-price; .cart-total-value còn nguyên</done>
  <acceptance_criteria>
    - `app/static/css/style.css` chứa `.order-detail` (padding 24px), `.order-section` (border-top + padding/margin 24px + h2 16px/600), `.order-meta dt/dd`, `.data-table .unit-price`
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → GET /admin/orders/<id> | Route admin `@login_required` qua `_protect_admin`; id từ URL — untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Detail page PII (name/phone/address/note) | mitigate | Route trong admin_bp + `@login_required` (`_protect_admin`). Missing order → flash + redirect (không lộ tồn tại qua 404, không 500). |
| T-07-02 | Information Disclosure | `product_cost_price` (giá nhập) | mitigate | Template detail KHÔNG render `item.product_cost_price` (07-CONTEXT decision); verify grep `product_cost_price not in b`. Cost_price giữ trong vùng `@login_required` cho Phase 8 stats. |
| T-07-03 | Tampering | `order_id` path param | mitigate | `db.session.get(Order, order_id)` + missing → flash + redirect; không privilege escalation (mọi route admin đồng quyền). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Không có package install trong plan này. |
</threat_model>

<verification>
- Task 1 verify exits 0 printing TASK_OK: strftime filter render + None-safe; view order_detail registered
- Task 2 verify exits 0 printing TASK_OK: detail render đủ section, total 400.000₫, timestamps format, missing → flash+redirect, không cost_price
- Task 3 verify exits 0 printing TASK_OK: CSS detail classes + .cart-total-value còn
- Verify chạy trên temp DB (patch BASE_DIR) — KHÔNG chạm data/app.db thật
</verification>

<success_criteria>
- GET /admin/orders/<id> hiện: header "Đơn #N" + badge status, Thông tin khách (Họ và tên/SĐT/Địa chỉ/Ghi chú nếu có), Sản phẩm snapshot (tên/số lượng/đơn giá/thành tiền), Tổng cộng = `_order_total` sum, Thời gian (Ngày tạo/Cập nhật) %d/%m/%Y %H:%M — ORD-07
- Missing order → flash 'Không tìm thấy đơn.' + redirect /admin/orders (không 404/500)
- `strftime` filter tồn tại (07-02 sở hữu `__init__.py`); `_order_total`/`order_badge_class` tái sử dụng từ 07-01
- KHÔNG render product_cost_price; không dependency mới; DB thật không bị đổi
</success_criteria>

<output>
Create `.planning/phases/07-admin-order-tracking/07-02-SUMMARY.md` when done
</output>
