---
phase: 08-admin-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/admin.py, app/templates/admin/stats.html, app/templates/admin/dashboard.html, app/static/css/style.css]
autonomous: true
requirements: [STAT-01, STAT-02]

must_haves:
  truths:
    - "Admin truy cập GET /admin/stats thấy thẻ 'Tổng doanh thu' hiển thị tổng tiền qua format_price (0₫ khi không có đơn) — STAT-01"
    - "Doanh thu chỉ tính item của đơn Đã gửi + Đã nhận; Đã hủy/Chờ xác nhận/Đã gói bị loại — STAT-01"
    - "Thẻ 'Lợi nhuận' hiển thị doanh thu trừ giá nhập trên item có product_cost_price IS NOT NULL; item NULL bị loại, không coi là 0 — STAT-02"
    - "Note 'Lợi nhuận tính trên N sản phẩm có giá nhập.' chỉ hiện khi có item bị loại khỏi lợi nhuận (excluded > 0) — STAT-02"
    - "Empty DB: GET /admin/stats trả 200 với 0₫, không 500 (db.func.coalesce trên mọi SUM) — STAT-01/02"
    - "Nav dashboard có mục 'Thống kê' (không badge) dẫn tới /admin/stats"
  artifacts:
    - path: "app/admin.py"
      provides: "REVENUE_STATUSES const + admin.stats() route + 3 aggregate queries (Q1 revenue+units, Q2 profit+count, Q3 total-qualifying -> profit_note)"
      contains: "def stats"
    - path: "app/templates/admin/stats.html"
      provides: "Doanh thu & Lợi nhuận section (revenue card + profit card + conditional note)"
      contains: "Tổng doanh thu"
    - path: "app/templates/admin/dashboard.html"
      provides: "nav-group 'Thống kê' (không badge)"
      contains: "Thống kê"
    - path: "app/static/css/style.css"
      provides: ".stats-group/.stats-group-title/.stats-grid/.stat-card/.stat-label/.stat-value/.stat-value--display/.stat-value--accent/.stat-hint/.stat-note"
      contains: "stats-group"
  key_links:
    - from: "admin.stats()"
      to: "OrderItem ⋈ Order"
      via: "db.session.query(...).join(Order, OrderItem.order_id == Order.id).filter(Order.status.in_(REVENUE_STATUSES))"
      pattern: "REVENUE_STATUSES"
    - from: "admin.stats()"
      to: "OrderItem.product_cost_price"
      via: "filter(OrderItem.product_cost_price.isnot(None)) on the profit query"
      pattern: "product_cost_price.isnot"
    - from: "stats.html"
      to: "format_price filter"
      via: "{{ revenue | format_price }}"
      pattern: "format_price"
    - from: "dashboard.html"
      to: "admin.stats"
      via: "url_for('admin.stats') in nav-group"
      pattern: "admin.stats"
---

<objective>
Admin stats dashboard — revenue + profit (NULL-safe), phục vụ STAT-01 + STAT-02. Plan 01 của Phase 8 theo split ROADMAP (08-01 Revenue + profit).

Purpose: Xây dựng route `admin.stats()` (GET /admin/stats) với 3 aggregate queries trên OrderItem ⋈ Order: Q1 revenue + units_sold (chung 1 query tuple — research verified), Q2 profit + count item có cost, Q3 count tổng item đủ điều kiện → derive `profit_note` có điều kiện. Kèm template `admin/stats.html` với section "Doanh thu & Lợi nhuận" (2 thẻ: Tổng doanh thu, Lợi nhuận), nav "Thống kê" trên dashboard, và CSS nền Phase 8.

**Giới hạn plan (self-consistent — research Pitfall 4):** plan này chỉ render section "Doanh thu & Lợi nhuận". Card "Sản phẩm đã bán" (units_sold) + section "Đơn hàng" + section "Kho" thuộc 08-02/08-03 — template ở đây KHÔNG được tham chiếu `status_counts`/`total_orders`/`total_products`/`in_stock`/`out_of_stock`/`discontinued` (route chưa pass → UndefinedError → 500).

**Lưu ý units_sold:** Q1 trả (revenue, units_sold) trong 1 tuple (research verified) nên route 08-01 đã tính units_sold nhưng template chưa render card đó — units_sold chỉ được render trong 08-02 theo split ROADMAP.

**Reachability:** route mới `admin.stats()` có thể truy cập qua nav "Thống kê" trên dashboard (Task 3 plan này) — không phải URL mồ côi.

Output: `REVENUE_STATUSES` + `admin.stats()` + `admin/stats.html` (money section) + nav dashboard + CSS stats. Không dependency mới, GET-only, zero JS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-admin-stats/08-CONTEXT.md (decisions locked: chỉ đơn Đã gửi+Đã nhận tính doanh thu; profit NULL-safe loại item cost IS NULL; note chỉ hiện khi có item bị loại; toàn thời gian; route GET /admin/stats KHÔNG nhúng dashboard; server-rendered, 0 dependency, empty state hiện 0 rõ ràng; dùng format_price có sẵn)
@.planning/phases/08-admin-stats/08-RESEARCH.md (Pattern 1 coalesce load-bearing — mọi SUM phải bọc db.func.coalesce(..., 0); Code Examples admin.stats() route đầy đủ đã verified; New CSS block; Pitfall 4 self-consistent)
@.planning/phases/08-admin-stats/08-UI-SPEC.md (section 1 markup money section: 3 stat-card trong .stats-grid; copy contract Tổng doanh thu/Lợi nhuận/Sản phẩm đã bán + hints + note; section 0 nav 'Thống kê' không badge; New CSS classes list; .stat-value--accent #2563EB chỉ cho revenue)
@app/admin.py (import line 7 hiện chỉ có Product, ProductImage, Order — cần thêm OrderItem; ORDER_STATUSES line 11; dashboard() line 118-122; orders() line 125-144 pattern group_by status; _protect_admin line 42-46 tự bảo vệ mọi route)
@app/models.py (Order.status line 81; OrderItem.product_price/product_cost_price/quantity lines 96-98)
@app/__init__.py (format_price filter line 64-66: int(value) — None sẽ TypeError → coalesce bắt buộc)
</context>

<interfaces>
Context contract plan này TẠO cho `admin.stats()` (08-02/08-03 sẽ mở rộng):

- `REVENUE_STATUSES` — module constant trong `app/admin.py`, cạnh ORDER_STATUSES (line 11): `('Đã gửi', 'Đã nhận')`. Tuple (không set) để giữ thứ tự deterministic.
- `admin.stats` view — `@admin_bp.route('/stats', methods=['GET'])` → render `admin/stats.html` với context:
  - `revenue` (int VND, đã coalesce — không bao giờ None)
  - `profit` (int VND, đã coalesce)
  - `profit_note` (str | None — None khi không có item bị loại)
  - `units_sold` (int, đã coalesce — tính sẵn nhưng chưa render trong plan này)
- Template `app/templates/admin/stats.html` — extends `base.html`; `.admin-page` > `.admin-header h1 "Thống kê"` > `.admin-card.admin-card--wide` > `section.stats-group`.

Reused assets (đã có trên disk, KHÔNG tạo mới):
- `format_price` filter (`app/__init__.py:64-66`), `order_badge_class`/`ORDER_STATUSES` Jinja globals (`app/admin.py:11,31-39`), `.admin-page`/`.admin-header`/`.admin-card--wide` (`style.css:147-150`), `.nav-group` (`style.css:96`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend — REVENUE_STATUSES + admin.stats() route (Q1 revenue+units, Q2 profit+count, Q3 total-qualifying → profit_note)</name>
  <files>app/admin.py</files>
  <read_first>
    - app/admin.py (import line 7: `from .models import Product, ProductImage, Order` — cần thêm `OrderItem`; ORDER_STATUSES line 11; orders() line 125-144 pattern group_by + db.func; dashboard() line 118-122)
    - .planning/phases/08-admin-stats/08-RESEARCH.md (Code Examples `admin.stats()` route — query patterns đã executed/verified; Q1/Q2/Q3; `REVENUE_STATUSES` đặt cạnh ORDER_STATUSES)
    - .planning/phases/08-admin-stats/08-CONTEXT.md (STAT-01/02: revenue = sum(product_price*quantity) trên đơn Đã gửi+Đã nhận; profit = sum((price-cost)*qty) item có cost; note `Lợi nhuận tính trên {N} sản phẩm có giá nhập.` khi excluded > 0)
    - app/models.py (OrderItem product_price/product_cost_price/quantity lines 96-98; Order.status line 81)
  </read_first>
  <action>
    Sửa duy nhất `app/admin.py`. Ba thay đổi:

    1. Sửa dòng import 7 thành: `from .models import Product, ProductImage, Order, OrderItem` (OrderItem chưa được import — bắt buộc cho join/aggregate).
    2. Thêm module constant SAU ORDER_STATUSES (line 11): `REVENUE_STATUSES = ('Đã gửi', 'Đã nhận')`. Tuple để thứ tự deterministic (research); không dùng set.
    3. Thêm route `stats()` — đặt SAU `orders()` (line 144) và TRƯỚC `order_detail()`:
       `@admin_bp.route('/stats', methods=['GET'])` + `def stats():`
       Chạy ĐÚNG 3 query (thứ tự Q1→Q3), mỗi query join `OrderItem.order_id == Order.id` và filter `Order.status.in_(REVENUE_STATUSES)`:
       - Q1 (revenue + units_sold chung 1 tuple row, research verified): query SELECT `db.func.coalesce(db.func.sum(OrderItem.product_price * OrderItem.quantity), 0)` và `db.func.coalesce(db.func.sum(OrderItem.quantity), 0)`, `.join(Order, OrderItem.order_id == Order.id)`, `.filter(Order.status.in_(REVENUE_STATUSES))`, `.one()` → unpack `revenue, units_sold`.
       - Q2 (profit + profit_items): query SELECT `db.func.coalesce(db.func.sum((OrderItem.product_price - OrderItem.product_cost_price) * OrderItem.quantity), 0)` và `db.func.count(OrderItem.id)`, cùng join, filter `Order.status.in_(REVENUE_STATUSES), OrderItem.product_cost_price.isnot(None)`, `.one()` → unpack `profit, profit_items`.
       - Q3 (total_qualifying_items): query SELECT `db.func.count(OrderItem.id)`, cùng join, filter `Order.status.in_(REVENUE_STATUSES)` (KHÔNG có predicate cost), `.scalar()` → `total_qual_items`.
       - Derive note: `profit_note = None`; nếu `total_qual_items - profit_items > 0` thì `profit_note = f'Lợi nhuận tính trên {profit_items} sản phẩm có giá nhập.'`.
       - Return `render_template('admin/stats.html', revenue=revenue, profit=profit, profit_note=profit_note, units_sold=units_sold)`.

    BẮT BUỘC: mọi `db.func.sum(...)` phải bọc trong `db.func.coalesce(..., 0)` — research verified: qualifying set rỗng → SQLite NULL → `format_price` gọi `int(None)` → TypeError → 500. Dùng `.one()` cho Q1/Q2 (luôn trả 1 row nhờ coalesce), `.scalar()` cho Q3.

    KHÔNG: đọc `request.args` (route thuần, không param — V5 ASVS no new input), không mutation DB, không thêm dependency, không đụng `__init__.py`, không render `status_counts`/`total_orders`/inventory (thuộc 08-02/08-03).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'test'
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from app.admin import REVENUE_STATUSES
assert REVENUE_STATUSES == ('Đã gửi', 'Đã nhận'), REVENUE_STATUSES
assert 'admin.stats' in app.view_functions
from app.db import db
from app.models import Product, Order, OrderItem
with app.app_context():
    db.create_all()
    p1 = Product(name='Áo', price=100000, cost_price=60000, quantity=5)
    p2 = Product(name='Quần', price=200000, cost_price=None, quantity=0)
    p3 = Product(name='Ví', price=300000, cost_price=200000, quantity=3, discontinued=True)
    db.session.add_all([p1, p2, p3]); db.session.flush()
    a = Order(customer_name='A', customer_phone='1', customer_address='x', status='Đã nhận')
    b = Order(customer_name='B', customer_phone='1', customer_address='x', status='Đã gửi')
    c = Order(customer_name='C', customer_phone='1', customer_address='x', status='Đã hủy')
    d = Order(customer_name='D', customer_phone='1', customer_address='x', status='Chờ xác nhận')
    db.session.add_all([a, b, c, d]); db.session.flush()
    db.session.add_all([
        OrderItem(order_id=a.id, product_id=p1.id, product_name='Áo', product_price=100000, product_cost_price=60000, quantity=2),
        OrderItem(order_id=a.id, product_id=p2.id, product_name='Quần', product_price=200000, product_cost_price=None, quantity=1),
        OrderItem(order_id=b.id, product_id=p3.id, product_name='Ví', product_price=300000, product_cost_price=200000, quantity=1),
        OrderItem(order_id=c.id, product_id=p1.id, product_name='Áo', product_price=100000, product_cost_price=60000, quantity=5),
        OrderItem(order_id=d.id, product_id=p2.id, product_name='Quần', product_price=200000, product_cost_price=None, quantity=1),
    ])
    db.session.commit()
    # Q1: revenue + units (chỉ Đã nhận a + Đã gửi b; Đã hủy c + Chờ xác nhận d bị loại)
    row = db.session.query(
        db.func.coalesce(db.func.sum(OrderItem.product_price * OrderItem.quantity), 0),
        db.func.coalesce(db.func.sum(OrderItem.quantity), 0),
    ).join(Order, OrderItem.order_id == Order.id).filter(Order.status.in_(REVENUE_STATUSES)).one()
    assert row == (700000, 4), row
    # Q2: profit + count item có cost
    profit, profit_items = db.session.query(
        db.func.coalesce(db.func.sum((OrderItem.product_price - OrderItem.product_cost_price) * OrderItem.quantity), 0),
        db.func.count(OrderItem.id),
    ).join(Order, OrderItem.order_id == Order.id).filter(
        Order.status.in_(REVENUE_STATUSES), OrderItem.product_cost_price.isnot(None)
    ).one()
    assert (profit, profit_items) == (180000, 2), (profit, profit_items)
    # Q3: total qualifying + note derive
    total_qual = db.session.query(db.func.count(OrderItem.id)).join(
        Order, OrderItem.order_id == Order.id
    ).filter(Order.status.in_(REVENUE_STATUSES)).scalar()
    note = None if total_qual - profit_items <= 0 else f'Lợi nhuận tính trên {profit_items} sản phẩm có giá nhập.'
    assert total_qual == 3 and note == 'Lợi nhuận tính trên 2 sản phẩm có giá nhập.', (total_qual, note)
    # Empty-set coalesce: không có đơn Đã gói -> 0 (không None)
    empty = db.session.query(
        db.func.coalesce(db.func.sum(OrderItem.product_price * OrderItem.quantity), 0)
    ).join(Order, OrderItem.order_id == Order.id).filter(Order.status.in_(('Đã gói',))).scalar()
    assert empty == 0, empty
print('TASK_OK')
PY</automated>
  </verify>
  <done>View admin.stats + REVENUE_STATUSES = ('Đã gửi','Đã nhận') registered; Q1→Q3 đúng aggregate (700000/4, 180000/2, note text); empty-set coalesce trả 0 không None</done>
  <acceptance_criteria>
    - `app/admin.py` import line 7 chứa `OrderItem`; có `REVENUE_STATUSES = ('Đã gửi', 'Đã nhận')` cạnh ORDER_STATUSES; có `def stats()` với 3 query join Order.filter(Order.status.in_(REVENUE_STATUSES)), mọi `db.func.sum` bọc `db.func.coalesce(..., 0)`
    - `render_template('admin/stats.html', revenue=..., profit=..., profit_note=..., units_sold=...)` — đúng 4 context var
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Template admin/stats.html — section "Doanh thu & Lợi nhuận" (revenue card + profit card + conditional note)</name>
  <files>app/templates/admin/stats.html</files>
  <read_first>
    - .planning/phases/08-admin-stats/08-UI-SPEC.md (section 1 markup money section: `section.stats-group` > `h2.stats-group-title` "Doanh thu &amp; Lợi nhuận" > `div.stats-grid` > 2 `article.stat-card`; copy contract: label Tổng doanh thu/Lợi nhuận, hint, note; `.stat-value--accent` chỉ cho revenue; zero renders 0₫ không dimming)
    - .planning/phases/08-admin-stats/08-RESEARCH.md (stats.html snippets — revenue/profit cards, note conditional `{% if profit_note %}`; `{{ revenue | format_price }}`)
    - app/templates/admin/orders/list.html (pattern .admin-page > .admin-header h1 + .admin-card.admin-card--wide; extends base.html — KHÔNG admin layout riêng)
    - app/templates/base.html (block content; lang=vi; header block rỗng cho admin — admin header nằm trong content)
  </read_first>
  <action>
    Tạo MỚI `app/templates/admin/stats.html` — `{% extends "base.html" %}`, block content.

    Cấu trúc (chỉ section money, theo UI-SPEC section 1):
    ```
    <div class="admin-page">
      <div class="admin-header"><h1>Thống kê</h1></div>
      <div class="admin-card admin-card--wide">
        <section class="stats-group">
          <h2 class="stats-group-title">Doanh thu &amp; Lợi nhuận</h2>
          <div class="stats-grid">
            <article class="stat-card">
              <p class="stat-label">Tổng doanh thu</p>
              <p class="stat-value stat-value--display stat-value--accent">{{ revenue | format_price }}</p>
              <p class="stat-hint">Chỉ tính đơn Đã gửi và Đã nhận.</p>
            </article>
            <article class="stat-card">
              <p class="stat-label">Lợi nhuận</p>
              <p class="stat-value stat-value--display">{{ profit | format_price }}</p>
              {% if profit_note %}<p class="stat-note">{{ profit_note }}</p>{% endif %}
              <p class="stat-hint">Doanh thu trừ giá nhập.</p>
            </article>
          </div>
        </section>
      </div>
    </div>
    ```

    BẮT BUỘC:
    - `stat-value--accent` CHỈ trên revenue card (research Open Question 1 — micro-deviation 1 dòng so với class list UI-SPEC, profit #1F2937 để không đọc nhầm khi âm).
    - `{% if profit_note %}` — note chỉ render khi profit_note khác None (context trả str khi excluded > 0, None khi không).
    - `{{ revenue | format_price }}` / `{{ profit | format_price }}` — dùng format_price filter có sẵn; 0 render `0₫` (KHÔNG component empty-state, KHÔNG dimming — CONTEXT.md decision).
    - KHÔNG render units_sold card, KHÔNG section "Đơn hàng", KHÔNG section "Kho" — các var tương ứng chưa được route pass trong plan này (Pitfall 4: UndefinedError → 500).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'test'
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from werkzeug.security import generate_password_hash
from app.db import db
from app.models import AdminUser, Product, Order, OrderItem
with app.app_context():
    db.create_all()
    db.session.add(AdminUser(username='admin', password_hash=generate_password_hash('testpass1234')))
    p1 = Product(name='Áo', price=100000, cost_price=60000, quantity=5)
    p2 = Product(name='Quần', price=200000, cost_price=None, quantity=0)
    db.session.add_all([p1, p2]); db.session.flush()
    a = Order(customer_name='A', customer_phone='1', customer_address='x', status='Đã nhận')
    b = Order(customer_name='B', customer_phone='1', customer_address='x', status='Đã gửi')
    db.session.add_all([a, b]); db.session.flush()
    db.session.add_all([
        OrderItem(order_id=a.id, product_id=p1.id, product_name='Áo', product_price=100000, product_cost_price=60000, quantity=2),
        OrderItem(order_id=a.id, product_id=p2.id, product_name='Quần', product_price=200000, product_cost_price=None, quantity=1),
        OrderItem(order_id=b.id, product_id=p1.id, product_name='Áo', product_price=100000, product_cost_price=60000, quantity=1),
    ])
    db.session.commit()
with app.test_client() as tc:
    assert tc.post('/login', data={'username':'admin','password':'testpass1234'}).status_code == 302
    resp = tc.get('/admin/stats')
    assert resp.status_code == 200
    html = resp.get_data(as_text=True)
    assert 'Thống kê' in html
    # revenue = a(Áo 100000*2 + Quần 200000*1) + b(Áo 100000*1) = 200000+200000+100000 = 500000
    assert 'Tổng doanh thu' in html and '500.000₫' in html
    # profit (cost-bearing only) = a Áo (100000-60000)*2 + b Áo (100000-60000)*1 = 80000+40000 = 120000
    assert 'Lợi nhuận' in html and '120.000₫' in html
    # excluded = total_qual(3) - profit_items(2) = 1 > 0 -> note present
    assert 'Lợi nhuận tính trên 2 sản phẩm có giá nhập.' in html
    assert 'Doanh thu trừ giá nhập.' in html
    assert 'Sản phẩm đã bán' not in html      # units card deferred to 08-02
    assert 'Đơn hàng' not in html             # orders section deferred
print('TASK_OK')
PY</automated>
  </verify>
  <done>GET /admin/stats render section "Doanh thu & Lợi nhuận" với 2 thẻ, format_price đúng VND, note có điều kiện; không render section chưa có var</done>
  <acceptance_criteria>
    - `app/templates/admin/stats.html` extends base.html; chứa `h1>Thống kê`, `section.stats-group`, `h2.stats-group-title>Doanh thu &amp; Lợi nhuận`, 2 `article.stat-card` (Tổng doanh thu `stat-value--display stat-value--accent`; Lợi nhuận `stat-value--display`), `{% if profit_note %}`, `{{ revenue | format_price }}`, `{{ profit | format_price }}`
    - KHÔNG chứa `status_counts`/`total_orders`/`total_products`/`in_stock`/`out_of_stock`/`discontinued` (var chưa pass)
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Nav "Thống kê" trên dashboard.html + CSS stats classes trong style.css</name>
  <files>app/templates/admin/dashboard.html, app/static/css/style.css</files>
  <read_first>
    - app/templates/admin/dashboard.html (nav-list lines 5-21: nav-group Sản phẩm line 7-10, nav-group Đơn hàng line 11-14, logout-form line 15-20)
    - app/static/css/style.css (lines 147-150 .admin-page/.admin-header/.admin-card--wide; line 214 .badge; lines 461-465 badge-order-*; cuối file Phase 7 section line 455+ để APPEND Phase 8 sau)
    - .planning/phases/08-admin-stats/08-UI-SPEC.md (section 0: nav 'Thống kê' không badge, sau nav-group Đơn hàng trước logout-form; New CSS classes + color: .stat-card bg #F9FAFB border #E5E7EB, .stat-note #B45309 AA 4.7:1, .stat-value--accent #2563EB)
    - .planning/phases/08-admin-stats/08-RESEARCH.md (New CSS block đầy đủ — sao chép giá trị chính xác)
  </read_first>
  <action>
    Hai file:

    1. `app/templates/admin/dashboard.html` — thêm nav-group "Thống kê" SAU nav-group "Đơn hàng" (line 14) và TRƯỚC logout-form (line 15):
       `<div class="nav-group"><a href="{{ url_for('admin.stats') }}">Thống kê</a></div>`
       KHÔNG badge (UI-SPEC decision 5: không có 1 scalar count đại diện cho stats page). Không CSS mới — reuse `.nav-group`.

    2. `app/static/css/style.css` — APPEND cuối file (sau Phase 7 section) dưới banner `/* ============ Phase 8: Admin stats ============ */` các class sau (giá trị chính xác từ RESEARCH New CSS block):
       - `.stats-group { padding: 24px; }`
       - `.stats-group + .stats-group { border-top: 1px solid #E5E7EB; }` (plan này chỉ có 1 group — rule để dành cho 08-02)
       - `.stats-group-title { font-size: 16px; font-weight: 600; margin-bottom: 16px; }`
       - `.stats-grid { display: grid; grid-template-columns: 1fr; gap: 16px; }`
       - `@media (min-width: 768px) { .stats-grid { grid-template-columns: repeat(2, 1fr); } }`
       - `@media (min-width: 1200px) { .stats-grid { grid-template-columns: repeat(3, 1fr); } }`
       - `.stat-card { background: #F9FAFB; border: 1px solid #E5E7EB; border-radius: 8px; padding: 16px; }`
       - `.stat-label { font-size: 14px; color: #6B7280; }`
       - `.stat-value { font-size: 24px; font-weight: 600; line-height: 1.2; color: #1F2937; font-variant-numeric: tabular-nums; margin-top: 4px; }`
       - `.stat-value--display { font-size: 32px; line-height: 1.1; }`
       - `.stat-value--accent { color: #2563EB; }` (micro-deviation: ngoài class list UI-SPEC — research Open Question 1, áp chỉ cho revenue)
       - `.stat-hint { font-size: 14px; color: #6B7280; margin-top: 8px; }`
       - `.stat-note { font-size: 14px; color: #B45309; margin-top: 8px; }` (#B45309 là hex MỚI duy nhất Phase 8, AA 4.7:1 trên #F9FAFB — UI-SPEC)
       - `@media (max-width: 480px) { .stats-group { padding: 16px; } }`

       KHÔNG thêm `.status-breakdown`/`.badge-neutral` (thuộc 08-02). Không sửa `.badge`/`.badge-order-*` hiện có.
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'test'
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
orig_base = app_module.BASE_DIR
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
css = open(os.path.join(orig_base, 'app', 'static', 'css', 'style.css'), encoding='utf-8').read()
assert '.stats-group' in css and '.stats-group-title' in css and '.stats-grid' in css
assert '.stat-card' in css and '.stat-label' in css and '.stat-value' in css
assert '.stat-value--display' in css and '.stat-value--accent' in css
assert '.stat-hint' in css and '.stat-note' in css
assert '.status-breakdown' not in css and '.badge-neutral' not in css   # 08-02 chưa tới
from werkzeug.security import generate_password_hash
from app.db import db
from app.models import AdminUser
with app.app_context():
    db.create_all()
    db.session.add(AdminUser(username='admin', password_hash=generate_password_hash('testpass1234')))
    db.session.commit()
with app.test_client() as tc:
    tc.post('/login', data={'username':'admin','password':'testpass1234'})
    b = tc.get('/admin/').get_data(as_text=True)
    assert 'Thống kê' in b and 'href="/admin/stats"' in b
print('TASK_OK')
PY</automated>
  </verify>
  <done>Dashboard nav hiện "Thống kê" (không badge) → /admin/stats; style.css chứa 11 class stats Phase 8, chưa có status-breakdown/badge-neutral</done>
  <acceptance_criteria>
    - `app/templates/admin/dashboard.html` có `.nav-group` "Thống kê" với `href="{{ url_for('admin.stats') }}"`, KHÔNG span.badge
    - `app/static/css/style.css` chứa `.stats-group`, `.stats-group-title`, `.stats-grid`, `.stat-card`, `.stat-label`, `.stat-value`, `.stat-value--display`, `.stat-value--accent` (color #2563EB), `.stat-hint`, `.stat-note` (color #B45309), media queries 768/1200/480
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → /admin/stats | Route admin bảo vệ `@login_required` qua `_protect_admin` before_request (inherited từ Phase 1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `admin.stats()` — lộ doanh thu/lợi nhuận (dữ liệu kinh doanh nhạy cảm) | mitigate | Route nằm trong `admin_bp` (`url_prefix='/admin'`) + `_protect_admin` `@login_required` — chỉ admin đã đăng nhập đọc. Không đăng ký route ngoài admin_bp. |
| T-08-02 | Tampering | Query aggregate | accept | Route KHÔNG đọc `request.args`/`request.form` (không input người dùng); status dùng `REVENUE_STATUSES` tuple hardcoded, tham số hóa bởi SQLAlchemy — không injection surface. |
| T-08-03 | Information Disclosure | `product_cost_price` (COST-02) | mitigate | Stats page admin-only; template chỉ render aggregate (revenue/profit), KHÔNG render cột cost thô; mirrors COST-02 (không bao giờ hiện cost cho khách). |
| T-08-04 | DoS | Query nặng trên SQLite | accept | 3 query nhỏ có index FK; dataset single-admin/low-volume; không cache phức tạp. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Không có package install trong plan này (0 dependency mới). |
</threat_model>

<verification>
- Task 1 verify exits 0 printing TASK_OK: REVENUE_STATUSES + admin.stats view + Q1/Q2/Q3 aggregates (700000/4, 180000/2, note text) + empty-set coalesce 0
- Task 2 verify exits 0 printing TASK_OK: GET /admin/stats render 200, money format_price, note conditional, không render section chưa có var
- Task 3 verify exits 0 printing TASK_OK: nav 'Thống kê' + CSS 11 class stats (chưa có status-breakdown/badge-neutral)
- Toàn bộ verify chạy trên temp DB (patch app_module.BASE_DIR → temp, create_all + seed, SECRET_KEY=test) — KHÔNG chạm `data/app.db` thật
</verification>

<success_criteria>
- GET /admin/stats trả 200, section "Doanh thu & Lợi nhuận" với 2 thẻ format_price (revenue 0₫ khi empty — coalesce) — STAT-01, STAT-02
- Revenue = sum(product_price*quantity) chỉ trên đơn Đã gửi + Đã nhận; profit = sum((price-cost)*qty) chỉ item có cost; note có điều kiện excluded > 0
- Nav dashboard "Thống kê" không badge; CSS stats nền Phase 8
- Không dependency mới; DB thật không bị đổi trong verify; GET-only không CSRF
</success_criteria>

<output>
Create `.planning/phases/08-admin-stats/08-01-SUMMARY.md` when done
</output>
