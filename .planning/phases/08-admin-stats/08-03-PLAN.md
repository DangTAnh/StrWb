---
phase: 08-admin-stats
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified: [app/admin.py, app/templates/admin/stats.html, .planning/tmp/verify_08_stats_full.py]
autonomous: true
requirements: [STAT-04]

must_haves:
  truths:
    - "Admin thấy section 'Kho' với 4 thẻ: Tổng sản phẩm, Còn hàng, Hết hàng, Ngừng bán — STAT-04"
    - "Tổng sản phẩm = đếm tất cả Product (kể cả discontinued) — STAT-04"
    - "Hết hàng = quantity == 0 AND NOT discontinued; tách riêng khỏi ngừng bán — STAT-04"
    - "Ngừng bán = discontinued == True — STAT-04"
    - "Còn hàng = quantity > 0 AND NOT discontinued (thêm ngoài SC — 3 phân khúc rõ ràng) — STAT-04"
    - "Empty DB: 4 thẻ kho đều 0, GET /admin/stats trả 200 — STAT-04"
  artifacts:
    - path: "app/admin.py"
      provides: "Q5 inventory: total_products/in_stock/out_of_stock/discontinued (Product.discontinued.is_(True)/is_(False))"
      contains: "total_products"
    - path: "app/templates/admin/stats.html"
      provides: "section 'Kho' với 4 stat-card (Tổng sản phẩm, Còn hàng, Hết hàng, Ngừng bán)"
      contains: "Ngừng bán"
    - path: ".planning/tmp/verify_08_stats_full.py"
      provides: "Full E2E self-check: temp DB seed (NULL-cost item, Đã hủy order, zero-stock, discontinued) → GET /admin/stats → assert revenue/profit/note/status/inventory/empty-set"
      contains: "TASK_OK"
  key_links:
    - from: "admin.stats() Q5"
      to: "Product model"
      via: "Product.query.filter(Product.quantity == 0, Product.discontinued.is_(False)).count() — .is_(True)/.is_(False), không == True/False (research anti-pattern)"
      pattern: "discontinued.is_"
    - from: "stats.html Kho section"
      to: "context inventory vars"
      via: "{{ total_products }} {{ in_stock }} {{ out_of_stock }} {{ discontinued }}"
      pattern: "out_of_stock"
---

<objective>
Hoàn thiện stats dashboard — inventory counts, phục vụ STAT-04. Plan 03 (cuối) của Phase 8 theo split ROADMAP (08-03 Inventory counts dashboard).

Purpose: Thêm Q5 (inventory) vào `admin.stats()` — 4 count query đơn giản trên `Product`: total_products (đếm tất cả, kể cả discontinued), in_stock (qty>0 AND NOT discontinued), out_of_stock (qty==0 AND NOT discontinued), discontinued (discontinued is True). Render section "Kho" với 4 stat-card. Kèm full E2E self-check script `.planning/tmp/verify_08_stats_full.py` — mirror Phase 7 `verify_0701_full.py` pattern — seed DB đầy đủ (gồm item cost NULL, đơn Đã hủy, sản phẩm hết hàng, sản phẩm ngừng bán) và assert aggregate chính xác (revenue 700000, profit 180000, note, status dict, inventory 3/1/1/1, empty-set 0).

**Self-consistent (Pitfall 4):** plan này thêm var `total_products`/`in_stock`/`out_of_stock`/`discontinued` vào route VÀ template cùng lúc — đây là plan cuối, template render đủ 3 section.

**BẮT BUỘC (research anti-pattern):** filter boolean bằng `Product.discontinued.is_(True)` / `.is_(False)` — KHÔNG dùng `== True`/`== False` (SQLAlchemy boolean predicate, verified trên SQLite).

Output: Q5 + section "Kho" + verify_08_stats_full.py. Không dependency mới, GET-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-admin-stats/08-CONTEXT.md (STAT-04: Tổng sản phẩm = count tất cả (kể cả discontinued); Hết hàng = quantity=0 AND NOT discontinued; Ngừng bán = discontinued=True; Còn hàng = quantity>0 AND NOT discontinued)
@.planning/phases/08-admin-stats/08-RESEARCH.md (Q5 code verified: total_products Product.query.count(); in_stock filter(qty>0, discontinued.is_(False)); out_of_stock filter(qty==0, is_(False)); discontinued filter(is_(True)); anti-pattern == True/False; Validation Architecture note: self-check script mirror Phase 7)
@.planning/phases/08-admin-stats/08-UI-SPEC.md (section 1 markup Kho: section.stats-group "Kho" > div.stats-grid > 4 article.stat-card; copy: Tổng sản phẩm hint "Gồm cả sản phẩm ngừng bán.", Còn hàng, Hết hàng, Ngừng bán)
@.planning/phases/08-admin-stats/08-01-SUMMARY.md và 08-02-SUMMARY.md (nếu có: xác nhận stats() đã pass revenue/profit/profit_note/units_sold + status_counts/total_orders)
@app/admin.py (sau 08-02: stats() có Q1..Q4; cần import Product — đã có line 7 sau 08-01)
@app/models.py (Product.quantity line 32, Product.discontinued line 33)
@app/templates/admin/stats.html (sau 08-02: 2 sections — thêm section "Kho" SAU section "Đơn hàng")
@.planning/tmp/verify_08_stats_queries.py (research throwaway — working starting point cho verify_08_stats_full.py)
</context>

<interfaces>
Context contract plan này HOÀN TẤT cho `admin.stats()` — render_template cuối cùng pass 10 var:

- Từ 08-01: `revenue`, `profit`, `profit_note`, `units_sold`
- Từ 08-02: `status_counts`, `total_orders`
- Thêm 08-03: `total_products`, `in_stock`, `out_of_stock`, `discontinued`

`admin/stats.html` sau plan này render đủ 3 section: "Doanh thu & Lợi nhuận" (3 cards), "Đơn hàng" (total + breakdown), "Kho" (4 cards).

Full self-check script `.planning/tmp/verify_08_stats_full.py` — standalone, chạy `python .planning/tmp/verify_08_stats_full.py`, in `TASK_OK` khi pass. Pattern từ `verify_0701_full.py` (Phase 7) + `verify_08_stats_queries.py` (research): patch `app_module.BASE_DIR` → temp dir trước `create_app()`, create_all + seed, test_client login admin, GET /admin/stats, assert HTML/aggregates.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend — Q5 inventory counts trong admin.stats()</name>
  <files>app/admin.py</files>
  <read_first>
    - app/admin.py (sau 08-02: stats() Q1..Q4 + render_template; import line 7 đã có Product sau 08-01)
    - .planning/phases/08-admin-stats/08-RESEARCH.md (Q5 code + anti-pattern == True/False; inventory predicates verified: 3/1/1/1)
    - .planning/phases/08-admin-stats/08-CONTEXT.md (STAT-04 định nghĩa 4 count)
    - app/models.py (Product.quantity line 32, Product.discontinued line 33)
  </read_first>
  <action>
    Sửa duy nhất `app/admin.py` — trong `stats()`, SAU Q4 (status_counts/total_orders) và TRƯỚC `return render_template(...)`:

    1. Thêm Q5 — 4 count query (thứ tự: total, in_stock, out_of_stock, discontinued):
       - `total_products = Product.query.count()` (đếm tất cả, kể cả discontinued)
       - `in_stock = Product.query.filter(Product.quantity > 0, Product.discontinued.is_(False)).count()`
       - `out_of_stock = Product.query.filter(Product.quantity == 0, Product.discontinued.is_(False)).count()`
       - `discontinued = Product.query.filter(Product.discontinued.is_(True)).count()`
    2. Thêm vào `render_template('admin/stats.html', ...)` 4 var mới: `total_products=total_products, in_stock=in_stock, out_of_stock=out_of_stock, discontinued=discontinued`. GIỮ NGUYÊN 6 var 08-01+08-02.

    BẮT BUỘC: dùng `Product.discontinued.is_(True)` / `.is_(False)` — KHÔNG `== True`/`== False` (research anti-pattern: SQLAlchemy boolean predicate trên SQLite). KHÔNG thêm query khác, không mutation DB, không đọc request.args.
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile
os.environ['SECRET_KEY'] = 'test'
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from app.db import db
from app.models import Product
with app.app_context():
    db.create_all()
    p1 = Product(name='Áo', price=100000, cost_price=60000, quantity=5)   # in stock
    p2 = Product(name='Quần', price=200000, cost_price=None, quantity=0)  # out of stock
    p3 = Product(name='Ví', price=300000, cost_price=200000, quantity=3, discontinued=True)  # discontinued
    db.session.add_all([p1, p2, p3]); db.session.commit()
    total_products = Product.query.count()
    in_stock = Product.query.filter(Product.quantity > 0, Product.discontinued.is_(False)).count()
    out_of_stock = Product.query.filter(Product.quantity == 0, Product.discontinued.is_(False)).count()
    discontinued = Product.query.filter(Product.discontinued.is_(True)).count()
    assert (total_products, in_stock, out_of_stock, discontinued) == (3, 1, 1, 1), (total_products, in_stock, out_of_stock, discontinued)
print('TASK_OK')
PY</automated>
  </verify>
  <done>Q5 4 inventory count đúng (3/1/1/1), render_template pass total_products/in_stock/out_of_stock/discontinued, dùng .is_(True)/.is_(False)</done>
  <acceptance_criteria>
    - `app/admin.py` `stats()` chứa `total_products = Product.query.count()`, `in_stock = Product.query.filter(Product.quantity > 0, Product.discontinued.is_(False)).count()`, `out_of_stock = Product.query.filter(Product.quantity == 0, Product.discontinued.is_(False)).count()`, `discontinued = Product.query.filter(Product.discontinued.is_(True)).count()`
    - `render_template('admin/stats.html', ...)` chứa `total_products=total_products, in_stock=in_stock, out_of_stock=out_of_stock, discontinued=discontinued` và giữ 6 var cũ
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Template stats.html — section "Kho" (4 stat-card)</name>
  <files>app/templates/admin/stats.html</files>
  <read_first>
    - app/templates/admin/stats.html (sau 08-02: 2 sections — thêm section "Kho" SAU section "Đơn hàng", TRONG cùng .admin-card--wide; đóng section Đơn hàng trước)
    - .planning/phases/08-admin-stats/08-UI-SPEC.md (section 1 markup Kho: section.stats-group "Kho" > div.stats-grid > 4 article.stat-card; copy contract: Tổng sản phẩm + hint "Gồm cả sản phẩm ngừng bán.", Còn hàng, Hết hàng, Ngừng bán)
  </read_first>
  <action>
    Sửa `app/templates/admin/stats.html` — THÊM section mới "Kho" SAU section "Đơn hàng" (đóng `</section>` Đơn hàng trước), TRONG cùng `.admin-card--wide`:

    ```
    <section class="stats-group">
      <h2 class="stats-group-title">Kho</h2>
      <div class="stats-grid">
        <article class="stat-card">
          <p class="stat-label">Tổng sản phẩm</p>
          <p class="stat-value">{{ total_products }}</p>
          <p class="stat-hint">Gồm cả sản phẩm ngừng bán.</p>
        </article>
        <article class="stat-card">
          <p class="stat-label">Còn hàng</p>
          <p class="stat-value">{{ in_stock }}</p>
        </article>
        <article class="stat-card">
          <p class="stat-label">Hết hàng</p>
          <p class="stat-value">{{ out_of_stock }}</p>
        </article>
        <article class="stat-card">
          <p class="stat-label">Ngừng bán</p>
          <p class="stat-value">{{ discontinued }}</p>
        </article>
      </div>
    </section>
    ```

    BẮT BUỘC: 4 card đúng copy UI-SPEC (Tổng sản phẩm/Còn hàng/Hết hàng/Ngừng bán); values là số nguyên (không format_price); hint "Gồm cả sản phẩm ngừng bán." CHỈ trên card Tổng sản phẩm; không thêm CSS mới (reuse .stats-grid/.stat-card/.stat-label/.stat-value/.stat-hint từ 08-01).
  </action>
  <verify>
    <automated>python - <<'PY'
import os, tempfile, re
os.environ['SECRET_KEY'] = 'test'
os.environ['FLASK_DEBUG'] = '0'
import app as app_module
tmpdir = tempfile.mkdtemp(); os.makedirs(os.path.join(tmpdir, 'data'))
app_module.BASE_DIR = tmpdir
app = app_module.create_app()
app.config['WTF_CSRF_ENABLED'] = False
from werkzeug.security import generate_password_hash
from app.db import db
from app.models import AdminUser, Product
with app.app_context():
    db.create_all()
    db.session.add(AdminUser(username='admin', password_hash=generate_password_hash('testpass1234')))
    p1 = Product(name='Áo', price=100000, cost_price=60000, quantity=5)
    p2 = Product(name='Quần', price=200000, cost_price=None, quantity=0)
    p3 = Product(name='Ví', price=300000, cost_price=200000, quantity=3, discontinued=True)
    db.session.add_all([p1, p2, p3]); db.session.commit()
with app.test_client() as tc:
    tc.post('/login', data={'username':'admin','password':'testpass1234'})
    resp = tc.get('/admin/stats')
    assert resp.status_code == 200
    html = resp.get_data(as_text=True)
    assert 'Kho' in html and 'Gồm cả sản phẩm ngừng bán.' in html
    for label, val in [('Tổng sản phẩm', 3), ('Còn hàng', 1), ('Hết hàng', 1), ('Ngừng bán', 1)]:
        m = re.search(rf'<p class="stat-label">{label}</p>\s*<p class="stat-value">(\d+)</p>', html)
        assert m and int(m.group(1)) == val, (label, m.group(1) if m else None)
print('TASK_OK')
PY</automated>
  </verify>
  <done>GET /admin/stats render section "Kho" với 4 card đúng value (3/1/1/1) + hint, không 500</done>
  <acceptance_criteria>
    - `app/templates/admin/stats.html` chứa section `Kho` với `h2.stats-group-title>Kho`, 4 `article.stat-card` label Tổng sản phẩm (`{{ total_products }}` + hint), Còn hàng (`{{ in_stock }}`), Hết hàng (`{{ out_of_stock }}`), Ngừng bán (`{{ discontinued }}`)
    - Verify script exits 0 printing TASK_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full E2E self-check .planning/tmp/verify_08_stats_full.py (temp DB, SECRET_KEY=test)</name>
  <files>.planning/tmp/verify_08_stats_full.py</files>
  <read_first>
    - .planning/tmp/verify_08_stats_queries.py (research throwaway — working starting point, query patterns + expected values)
    - .planning/tmp/verify_0701_full.py nếu có (Phase 7 full self-check pattern — standalone script, patch BASE_DIR, test_client login, assert rendered HTML)
    - app/admin.py (stats() sau 08-03 — đầy đủ Q1..Q5, 10 context var)
    - app/models.py (Product/Order/OrderItem constructors — cost_price/quantity/discontinued keyword args)
  </read_first>
  <action>
    Tạo MỚI `.planning/tmp/verify_08_stats_full.py` — standalone E2E self-check toàn Phase 8. Dựa trên `.planning/tmp/verify_08_stats_queries.py` (research), nâng cấp thành full HTTP render test. Cấu trúc:

    1. Header + setup: set `os.environ['SECRET_KEY'] = 'test'`, `os.environ['FLASK_DEBUG'] = '0'`, `sys.path.insert(0, repo_root)` (3 cấp dirname từ file tmp), `tmpdir = tempfile.mkdtemp(prefix='gsd_verify_08_')`, `app_module.BASE_DIR = tmpdir`, `os.makedirs(os.path.join(tmpdir, 'data'), exist_ok=True)`, `app = app_module.create_app()`, `app.config['WTF_CSRF_ENABLED'] = False`.
    2. Seed trong `app.app_context()`:
       - 3 Product: p1 Áo price100000 cost60000 qty5 (in stock); p2 Quần price200000 cost None qty0 (out of stock, NULL cost); p3 Ví price300000 cost200000 qty3 discontinued=True.
       - 4 Order: a Đã nhận, b Đã gửi, c Đã hủy, d Chờ xác nhận.
       - 5 OrderItem: a→(p1 qty2), a→(p2 qty1 cost None), b→(p3 qty1), c→(p1 qty5 — excluded vì Đã hủy), d→(p2 qty1 — excluded).
       - 1 AdminUser username='admin' password_hash=generate_password_hash('testpass1234'). commit.
    3. Test client login admin, GET /admin/stats, assert status 200 + HTML chứa:
       - revenue `700.000₫` (a: 100000*2 + 200000*1 = 400000; b: 300000*1 = 300000 → 700000)
       - profit `180.000₫` (a p1 (100000-60000)*2=80000; b p3 (300000-200000)*1=100000 → 180000)
       - note `Lợi nhuận tính trên 2 sản phẩm có giá nhập.` (profit_items=2, total_qual=3, excluded=1)
       - `Sản phẩm đã bán` + units value `4` (2+1+1)
       - `Tổng số đơn` + total_orders `4`; breakdown status đủ, Đã gói hiển thị `0`
       - Kho: Tổng sản phẩm `3`, Còn hàng `1`, Hết hàng `1`, Ngừng bán `1`
    4. Empty-DB check: reset engine/dispose (hoặc tạo app mới với BASE_DIR mới) — DB không có order → GET /admin/stats trả 200, revenue `0₫`, profit `0₫`, total_orders `0`, các badge breakdown `0`, inventory `0`.
    5. In `TASK_OK` khi tất cả pass.

    KHÔNG chạm `data/app.db` thật (luôn dùng temp BASE_DIR). Script chạy được độc lập: `python .planning/tmp/verify_08_stats_full.py`.
  </action>
  <verify>
    <automated>SECRET_KEY=test python .planning/tmp/verify_08_stats_full.py</automated>
  </verify>
  <done>verify_08_stats_full.py chạy độc lập trên temp DB, in TASK_OK; assert đủ revenue/profit/note/units/status/inventory + empty-DB 0₫</done>
  <acceptance_criteria>
    - `.planning/tmp/verify_08_stats_full.py` tồn tại, chạy `python .planning/tmp/verify_08_stats_full.py` (với SECRET_KEY=test) exits 0 in TASK_OK
    - Script chứa assert revenue 700000/profit 180000/note "Lợi nhuận tính trên 2 sản phẩm có giá nhập."/units 4/status dict/total 4/inventory 3-1-1-1/empty-set 0
    - Script dùng temp BASE_DIR (không đụng data/app.db thật)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → /admin/stats | Route admin bảo vệ `@login_required` qua `_protect_admin` before_request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | inventory counts (số lượng tồn kho) | mitigate | Route trong admin_bp + `_protect_admin` @login_required; stats page admin-only. |
| T-08-08 | Tampering | Product boolean filter | accept | Không input user; `.is_(True)`/`.is_(False)` là SQLAlchemy parameterized predicate — không injection. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Không có package install trong plan này (0 dependency mới; verify script dùng stdlib + app sẵn có). |
</threat_model>

<verification>
- Task 1 verify exits 0 printing TASK_OK: Q5 4 inventory count 3/1/1/1, dùng .is_(True)/.is_(False)
- Task 2 verify exits 0 printing TASK_OK: GET /admin/stats render 200, section Kho 4 card 3/1/1/1 + hint
- Task 3 verify exits 0 printing TASK_OK: `python .planning/tmp/verify_08_stats_full.py` full E2E (revenue/profit/note/units/status/inventory + empty-DB)
- Toàn bộ verify chạy trên temp DB (patch BASE_DIR, create_all + seed, SECRET_KEY=test) — không chạm data/app.db
</verification>

<success_criteria>
- Admin thấy section "Kho": Tổng sản phẩm (gồm discontinued), Còn hàng, Hết hàng, Ngừng bán — STAT-04
- Count đúng predicate: hết hàng = qty==0 AND NOT discontinued; ngừng bán = discontinued — STAT-04
- Full self-check script pass, chứng minh 4 requirement STAT-01..04 aggregate chính xác trên temp DB
- Không dependency mới; DB thật không đổi; GET-only
</success_criteria>

<output>
Create `.planning/phases/08-admin-stats/08-03-SUMMARY.md` when done
</output>
