---
phase: 09-polish-deploy
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - .planning/tmp/verify_11_full.py
  - .planning/tmp/seed_serve_11.py
  - .planning/phases/09-polish-deploy/09-VERIFICATION.md
  - .planning/ui-reviews/*.png
autonomous: true
requirements: [ORD-01, ORD-02, ORD-03, ORD-04, ORD-05, ORD-06, ORD-07, ORD-08, ORD-09, COST-01, COST-02, STAT-01, STAT-02, STAT-03, STAT-04, PLAT-05]
user_setup: []
must_haves:
  truths:
    - "All 16 phase requirements (ORD-01..09, COST-01/02, STAT-01..04, PLAT-05) pass scripted assertions"
    - "Cart/checkout surfaces (ORD-10, ORD-10a, ORD-10b — Phase 6) pass re-verification — no v1.1 regression"
    - "v1.0 surfaces (catalog list/detail, diacritic-free search, Messenger contact strip, admin login + product CRUD) pass smoke — no v1.0 regression"
    - "Five v1.1 surfaces render correctly at 375 / 768 / 1440 (V-02)"
    - "Verification evidence recorded in 09-VERIFICATION.md with full requirement traceability"
  artifacts:
    - path: ".planning/tmp/verify_11_full.py"
      provides: "Full v1.1 verification harness (temp DB, seed, assert, TASK_OK)"
      exports: ["_setup_app", "_seed_full", "main"]
    - path: ".planning/tmp/seed_serve_11.py"
      provides: "Seeded temp-DB server for V-02 Chrome screenshots (reuses seed helpers)"
    - path: ".planning/phases/09-polish-deploy/09-VERIFICATION.md"
      provides: "Verification report with requirement traceability + V-02 matrix"
      contains: "ORD-01..PLAT-05 traceability, v1.0 smoke, V-02 matrix"
    - path: ".planning/ui-reviews/*.png"
      provides: "3-breakpoint screenshots of the 5 v1.1 surfaces (V-02 evidence)"
  key_links:
    - from: "verify_11_full.py"
      to: "app.create_app"
      via: "BASE_DIR patched to tempfile.mkdtemp() before create_app()"
    - from: "verify_11_full.py"
      to: "public/admin/auth routes"
      via: "app.test_client() POST/GET"
    - from: "verify_11_full.py"
      to: "init-db CLI (PLAT-05)"
      via: "app.test_cli_runner().invoke(args=['init-db'])"
    - from: "seed_serve_11.py"
      to: "verify_11_full.py seed helpers"
      via: "import"
    - from: "Chrome headless"
      to: "seed_serve_11.py served app"
      via: "http://127.0.0.1:8011/<path>"
---

<objective>
Build and run the full v1.1 verification harness and the 3-breakpoint responsive evidence pass, then write the phase verification report.

Purpose: Prove every v1.1 requirement (16 phase reqs + the Phase 6 cart/checkout reqs ORD-10/10a/10b) and prove zero v1.0 regression, all against isolated temp DBs — never the real `data/app.db`. Capture V-02 screenshots of the polished v1.1 surfaces at 375/768/1440 and record the pass/fail matrix, following the pattern established by `verify_08_stats_full.py` and the existing `.planning/ui-reviews/` evidence.

Output: `.planning/tmp/verify_11_full.py` (prints `TASK_OK`), `.planning/tmp/seed_serve_11.py`, 15 evidence screenshots, and `.planning/phases/09-polish-deploy/09-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-deploy/09-CONTEXT.md
@.planning/phases/09-polish-deploy/09-UI-SPEC.md
@.planning/tmp/verify_08_stats_full.py
@.planning/phases/08-admin-stats/08-VERIFICATION.md

<interfaces>
<!-- Contracts the harness must use. Extract from these files; no exploration needed. -->

From app/models.py (SQLAlchemy models — seed against these):
```python
class Product(db.Model):  # __tablename__ = 'products'
    id, name, price, cost_price  # cost_price nullable, Integer VND (COST-01)
    quantity, discontinued, sort_order, sku
    @property status  # 'available' if quantity>0 and not discontinued, else 'out_of_stock'/'discontinued'
    images  # dynamic relationship, ordered by sort_order

class Order(db.Model):  # __tablename__ = 'orders'
    id, customer_name, customer_phone, customer_address, customer_note
    status  # 'Chờ xác nhận' | 'Đã gói' | 'Đã gửi' | 'Đã nhận' | 'Đã hủy'
    created_at, updated_at
    items  # dynamic OrderItem relationship

class OrderItem(db.Model):  # __tablename__ = 'order_items', CheckConstraint quantity >= 1
    order_id, product_id, product_name, product_price, product_cost_price, quantity
```

From app/public.py (routes to drive via test_client):
- GET `/` home; GET `/products/<int:id>` detail; GET `/search?q=` (diacritic-insensitive via normalize_search_text)
- POST `/cart/add/<int:id>`; GET `/cart` (stale-item filter pops out-of-stock/discontinued rows + info flash, clamps qty to stock); POST `/cart/update/<int:id>` (no upsert); POST `/cart/remove/<int:id>`
- POST `/cart/checkout` (honeypot 'website' silent-reject -> empty-cart guard -> form validate -> server re-validate -> 1 Order + N OrderItem snapshots in 1 commit -> clears cart -> redirect to first item's detail)

From app/admin.py:
- ORDER_STATUSES = ('Chờ xác nhận', 'Đã gói', 'Đã gửi', 'Đã nhận', 'Đã hủy'); REVENUE_STATUSES = ('Đã gửi', 'Đã nhận')
- TRANSITION_MAP: 'Chờ xác nhận'->{'Đã gói','Đã hủy'}, 'Đã gói'->{'Đã gửi','Đã hủy'}, 'Đã gửi'->{'Đã nhận'}, 'Đã nhận'/'Đã hủy' -> set()
- Routes: GET /admin/orders (page + status filter, paginate per_page=20, page indicator `Trang {{pagination.page}} / {{pagination.pages}}` only when pages>1), GET /admin/orders/<id>, POST /admin/orders/<id>/status (next_status), GET /admin/stats, GET /admin/products, POST /admin/products/new, POST /admin/products/<id>/edit, POST /admin/products/<id>/delete
- admin.py:14 `REVENUE_STATUSES` = ('Đã gửi', 'Đã nhận'); revenue/profit queries NULL-safe with coalesce; profit excludes NULL cost items; profit_note text `Lợi nhuận tính trên {n} sản phẩm có giá nhập.`

From app/db.py — init-db migration (PLAT-05):
- `init_db_command` (registered as `init-db`): requires ADMIN_PASSWORD env (>=8 chars, not 'change-me'); db.create_all(); PRAGMA guard adds products.cost_price if missing; legacy orders table (has product_name column) is DROPPED only when empty (raises ClickException 'Manual migration required...' when it has rows) then rebuilt to the new schema; upserts admin.

From app/__init__.py + verify_08 pattern:
- Temp-DB isolation: patch `app_module.BASE_DIR = tmpdir` (mkdir data/) BEFORE `create_app()`. Set WTF_CSRF_ENABLED=False in app.config. Run: `SECRET_KEY=test python .planning/tmp/verify_11_full.py` -> `TASK_OK`.
- format_price filter: `f'{int(value):,}'.replace(',', '.') + '₫'` — e.g. 700000 -> `700.000₫` (the stats assertions in verify_08 rely on this).

Existing evidence naming: `.planning/ui-reviews/<surface>-<breakpoint>.png` (cart-empty.png / cart-empty-tablet.png / cart-empty-mobile.png already exist; git-ignored).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run verify_11_full.py — all 16 reqs + v1.0 smoke + V-01 + PLAT-05 migration</name>
  <files>.planning/tmp/verify_11_full.py</files>
  <read_first>
    - .planning/tmp/verify_08_stats_full.py (pattern: temp DB, seed, assert, TASK_OK)
    - app/public.py (cart/checkout/search route behavior and exact flash strings)
    - app/admin.py (order routes, TRANSITION_MAP, stats queries, exact strings)
    - app/models.py (model fields)
    - app/db.py (init-db migration guards — PLAT-05)
  </read_first>
  <action>
    Create `.planning/tmp/verify_11_full.py`, a single-file self-check following verify_08_stats_full.py's structure: set `os.environ['SECRET_KEY']='test'` / `FLASK_DEBUG='0'`, patch `app_module.BASE_DIR` to a fresh `tempfile.mkdtemp()` before `create_app()`, set `WTF_CSRF_ENABLED=False`, drive everything via `app.test_client()` / `app.test_cli_runner()`. Add a guard that resolves the DB path and asserts it lives inside the temp dir (never the repo `data/`).

    Implement `_setup_app(tmpdir)` and `_seed_full(tmpdir)` (reuse the verify_08 pattern; extend the seed to cover all five order statuses — including a `Đã hủy` order — a NULL-cost item, a discontinued product, an in-stock product with cost, and 25 orders total so they exceed the `/admin/orders` route's `per_page=20` and pagination engages). Then these verification groups, each printing a labeled line and failing loudly on assert:
    1. Cart + checkout (ORD-10, ORD-10a, ORD-10b, ORD-01, ORD-02, ORD-03, ORD-05): POST `/cart/add/<id>` sets the session cart; GET `/cart` renders the item + total via format_price (e.g. `100.000₫`); `/cart/update/<id>` and `/cart/remove/<id>` work; POST `/cart/checkout` with name/phone/address creates exactly 1 Order + N OrderItem rows with snapshot `product_name`/`product_price`/`product_cost_price`, clears the session cart, redirects 302, success flash. Honeypot: checkout with `website` filled creates 0 orders. Validation: missing name -> no order + error flash; qty above stock -> rejected. Discontinued/out-of-stock product: detail page contains no add-to-cart form and POST `/cart/add` is rejected. ORD-04 snapshot: change the product price AFTER the order and assert the OrderItem keeps the ordered price. ORD-05 CSRF facet (run on a SECOND `_setup_app` instance so the main harness can keep `WTF_CSRF_ENABLED=False`): build a fresh temp app with `WTF_CSRF_ENABLED=True`, add an item to the cart, then POST `/cart/checkout` with a valid name/phone/address form but NO `csrf_token` field; assert the response is 400 (Flask-WTF CSRFError) and that 0 Order rows are created — proving ORD-05's CSRF protection independently of the honeypot facet.
    2. Admin order tracking (ORD-06, ORD-07, ORD-08, ORD-09): login as the seeded admin (`POST /login`); GET `/admin/orders` lists orders. ORD-06 pagination facet: against the 25 seeded orders (assert this BEFORE the checkout group's extra order is created, or scope the row count to the seeded set), GET `/admin/orders` returns page 1 with exactly 20 tbody order rows and the `Trang 1 / 2` page indicator (`pagination.pages == 2`, `has_next` true); GET `/admin/orders?page=2` returns the remaining 5 rows (`has_next` false). Keep the status-filter assertion: `?status=Đã gửi` returns only the seeded `Đã gửi` orders. GET `/admin/orders/<id>` shows customer info, item snapshots, timestamps; POST `/admin/orders/<id>/status` with `next_status=Đã gói` advances; ORD-08 `Đã hủy` forward transition: POST `next_status=Đã hủy` on a `Chờ xác nhận` order advances it to `Đã hủy` with the success flash `Đã hủy đơn #<id>.`, and a follow-up transition from the now-terminal `Đã hủy` order is rejected with a flash and no DB change; a backward transition (e.g. from `Đã gói` back to `Chờ xác nhận`) is rejected with a flash and no DB change; a terminal order (`Đã nhận`) accepts no further transition.
    3. Cost price (COST-01, COST-02): the admin product form contains the `Giá nhập` field; public pages (home, product_detail, search) never render a seeded product's cost_price value.
    4. Stats (STAT-01..04): port the verify_08_stats_full.py assertions — revenue 700000 / profit 180000 over Đã gửi + Đã nhận only, NULL-safe profit_note, units 4, status counts, inventory 3/1/1/1, and an empty-DB run asserting 0 VND / 0 đơn / 0 inventory.
    5. PLAT-05 migration: build a v1.0-shaped temp DB (raw sqlite3: `products` WITHOUT a cost_price column, plus an EMPTY legacy `orders` table that HAS a `product_name` column) at `tmpdir/data/app.db`, then set BASE_DIR, create_app, set `ADMIN_PASSWORD` env, and invoke `app.test_cli_runner().invoke(args=['init-db'])`; assert PRAGMA shows products.cost_price added, orders rebuilt to the new schema (no `product_name` column), and admin upserted. Also assert the guard branch: a legacy `orders` table WITH rows makes init-db abort with the 'Manual migration required' message (no data loss).
    6. V-01 qty-0 edge: seed a product at stock 1, add qty 1 to the cart, set `quantity=0`, GET `/cart` -> assert no qty-0 row for that product renders and an info flash appears.
    7. v1.0 regression smoke: GET `/` -> 200 with product cards; GET `/products/<id>` -> 200; search `ao` (no diacritics) returns the `Áo` product; the base template contains the MESSENGER_URL contact strip; admin product create/edit/delete via test_client (POST `/admin/products/new`, POST `/admin/products/<id>/edit`, POST `/admin/products/<id>/delete`).
    End `main()` with `print('TASK_OK')`. Run it; every assert must pass before moving on.
  </action>
  <acceptance_criteria>
    - `.planning/tmp/verify_11_full.py` exists, contains `_setup_app`, `_seed_full`, `main`, and `TASK_OK`.
    - Script contains assertions naming each of the 16 phase reqs (grep-able: ORD-01..09, COST-01/02, STAT-01..04, PLAT-05) plus the v1.0 smoke items and V-01.
    - Script asserts ORD-05's CSRF facet on a second app instance (`WTF_CSRF_ENABLED=True`): a token-less POST /cart/checkout is rejected with 400 (CSRFError) and creates no order.
    - Script asserts ORD-06's pagination facet: the 25 seeded orders page at per_page=20 — page 1 renders 20 rows + the `Trang 1 / 2` indicator, `?page=2` renders the remaining 5 — and the `?status=Đã gửi` filter still holds.
    - Script asserts ORD-08's `Đã hủy` forward transition (`Chờ xác nhận` → `Đã hủy`, flash `Đã hủy đơn #<id>.`) and that the terminal `Đã hủy` order accepts no further transition.
    - `SECRET_KEY=test python .planning/tmp/verify_11_full.py` exits 0 and prints `TASK_OK`.
    - The script's temp-DB guard is present (assert DB path is inside the temp dir).
  </acceptance_criteria>
  <verify>
    <automated>SECRET_KEY=test python .planning/tmp/verify_11_full.py</automated>
  </verify>
  <done>
    Harness passes end-to-end: 16 phase reqs + ORD-10/10a/10b + v1.0 smoke + V-01 + PLAT-05 migration all assert true, printed TASK_OK, real data/app.db untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: V-02 responsive screenshots — 5 surfaces x 3 breakpoints via Chrome headless</name>
  <files>.planning/tmp/seed_serve_11.py, .planning/ui-reviews/*.png</files>
  <read_first>
    - .planning/tmp/verify_11_full.py (seed helpers to reuse)
    - .planning/ui-reviews/ (existing cart-empty evidence + naming pattern; .gitignore)
    - .planning/phases/09-polish-deploy/09-UI-SPEC.md (V-02 section — the pass-condition table)
  </read_first>
  <action>
    Create `.planning/tmp/seed_serve_11.py`: set BASE_DIR to a fresh temp dir, seed the full data set by importing the seed helpers from verify_11_full (or duplicating the seed), then serve the app with waitress on `127.0.0.1:8011` (temp DB only — never the real data/app.db). To render admin pages without interactive login, add a small WSGI middleware that injects a valid admin session cookie into every request lacking one — obtain the cookie once via `app.test_client().post('/login', ...)` with the seeded admin credentials (shared SECRET_KEY makes it valid).

    Run Chrome headless at viewports 375 / 768 / 1440 against these URLs, saving to `.planning/ui-reviews/` as `<surface>-<breakpoint>.png` (existing naming pattern, e.g. `orders-list-mobile.png`):
    - Cart populated (2 items): `/cart` — pass condition: `.table-scroll` horizontal scroll on mobile, `.cart-actions` wraps (F-01), `.cart-thumb` renders (F-02), no horizontal overflow at 375.
    - Cart empty: reuse the existing `cart-empty.png` / `cart-empty-tablet.png` / `cart-empty-mobile.png` unchanged — F-02/F-05 only touch the items branch of cart.html, so the empty-state markup is byte-identical. Re-capture only if you suspect a regression.
    - Checkout form section: `/cart` populated, scroll to `#checkout` — fields full-width on mobile, no inline-style residue (F-06), phone help-text wraps cleanly.
    - Admin orders list: `/admin/orders` — `.table-scroll` on mobile, filter stacks on mobile (style.css:468), pagination fits.
    - Admin order detail: `/admin/orders/<id>` — stepper scrolls horizontally (`overflow-x: auto`), action-row buttons stack full-width.
    - Admin stats: `/admin/stats` — `.stats-grid` 1 -> 2 -> 3 columns, stat cards fit, breakdown rows >= 44px.

    After capture, Read each PNG with the Read tool and confirm the pass conditions in the UI-SPEC V-02 table, noting any failure. Keep the server process alive only long enough for the captures, then kill it.
  </action>
  <acceptance_criteria>
    - 15 new PNG files exist in `.planning/ui-reviews/` matching `<surface>-<breakpoint>.png` (5 surfaces x 3 breakpoints; empty-cart reused, not re-captured unless a regression appeared).
    - `populated-cart-mobile.png` shows no horizontal overflow, wrapped `.cart-actions`, and `.cart-thumb` images.
    - `orders-detail-mobile.png` shows the horizontally scrollable stepper and stacked action buttons.
    - `stats-mobile.png` shows the 1-column `.stats-grid`.
    - `ls .planning/ui-reviews/*.png` count >= 18 (15 new + 3 reused cart-empty).
  </acceptance_criteria>
  <verify>
    <automated>ls .planning/ui-reviews/*.png | wc -l</automated>
  </verify>
  <done>
    All 5 v1.1 surfaces captured at 375/768/1440 against the seeded temp DB, pass conditions visually confirmed per the V-02 table, evidence named per the existing pattern, server stopped.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 09-VERIFICATION.md — traceability + v1.0 smoke + V-02 matrix + human UAT</name>
  <files>.planning/phases/09-polish-deploy/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-admin-stats/08-VERIFICATION.md (exact report structure to mirror)
    - .planning/tmp/verify_11_full.py (actual assert results to transcribe)
    - .planning/phases/09-polish-deploy/09-CONTEXT.md (deferred human-UAT list)
  </read_first>
  <action>
    Write `.planning/phases/09-polish-deploy/09-VERIFICATION.md` mirroring 08-VERIFICATION.md's structure. Frontmatter: `phase: 09-polish-deploy`, `verifier: gsd-verifier`, `status: verified`, `date: 2026-08-03`, `requirements_checked: [ORD-01..ORD-09, COST-01, COST-02, STAT-01..STAT-04, PLAT-05]` (all 16 IDs). Body:
    1. Header + the script run line `SECRET_KEY=test python .planning/tmp/verify_11_full.py` -> `TASK_OK`.
    2. Requirement Traceability table: one row per phase req ID (ORD-01..09, COST-01/02, STAT-01..04, PLAT-05), mapped to its originating plan (Phase 5/6/7/8 plan IDs) with a source-of-truth note from REQUIREMENTS.md, status Verified. Add a note row that ORD-10 / ORD-10a / ORD-10b (Phase 6) were re-verified by the harness for no-regression.
    3. v1.0 regression smoke results: catalog list/detail, diacritic-free search, Messenger contact strip, admin login + product CRUD — pass/fail per line.
    4. V-02 responsive matrix: a table of 5 surfaces x 3 breakpoints (375/768/1440) with pass/fail and the evidence file name for each cell.
    5. Non-blocking human UAT items from 09-CONTEXT.md deferred list (the remaining Phase 2/3/6 visual items), explicitly labeled non-blocking for close-out.
  </action>
  <acceptance_criteria>
    - 09-VERIFICATION.md exists with frontmatter listing all 16 requirement IDs.
    - The traceability table contains every one of ORD-01..09, COST-01, COST-02, STAT-01..04, PLAT-05 with status Verified.
    - The report contains the v1.0 smoke results, the V-02 matrix (with evidence file names), and the non-blocking human UAT list.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import re; s=open('.planning/phases/09-polish-deploy/09-VERIFICATION.md',encoding='utf-8').read(); ids=['ORD-01','ORD-02','ORD-03','ORD-04','ORD-05','ORD-06','ORD-07','ORD-08','ORD-09','COST-01','COST-02','STAT-01','STAT-02','STAT-03','STAT-04','PLAT-05']; missing=[i for i in ids if i not in s]; assert not missing, missing; print('TRACEABILITY_OK')"</automated>
  </verify>
  <done>
    The verification report records all 16 reqs as Verified, documents v1.0 smoke + V-02 matrix + human-UAT items, and is internally consistent with the harness output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify harness → DB | Script runs real model + route code; must never touch the production `data/app.db` |
| screenshot server → DB | seed_serve_11.py serves the app for Chrome; must render only temp-DB data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | data/app.db (real DB) | mitigate | Harness patches `app_module.BASE_DIR` to `tempfile.mkdtemp()` before `create_app()`; add an assert that the resolved SQLite path lives inside the temp dir — fail fast if it resolves to the repo `data/` dir |
| T-09-02-02 | Information Disclosure | screenshots | mitigate | seed_serve_11.py uses a fresh temp DB and its own temp dir; Chrome is never pointed at the real `data/app.db` |
| T-09-02-03 | Spoofing | admin session injection middleware | accept | Local-only dev server bound to 127.0.0.1:8011 for the screenshot run; killed immediately after captures; no production exposure, no code change to the app |

No package installs in this plan — package-legitimacy gate not applicable (T-{phase}-SC not required).
</threat_model>

<verification>
1. `SECRET_KEY=test python .planning/tmp/verify_11_full.py` exits 0, prints `TASK_OK`, and each group's labeled line prints.
2. `data/app.db` mtime unchanged across all runs (real DB untouched).
3. V-02 evidence: >= 18 PNGs in `.planning/ui-reviews/`; each Read confirms its pass conditions.
4. 09-VERIFICATION.md passes the 16-ID traceability grep (verify in Task 3).
</verification>

<success_criteria>
- Harness verifies all 16 phase requirements + ORD-10/10a/10b + v1.0 regression smoke + V-01, all green on temp DBs.
- PLAT-05 migration verified against a v1.0-shaped temp DB (idempotent upgrade, legacy-orders rebuild, no-data-loss guard).
- V-02 responsive evidence captured for all 5 v1.1 surfaces at 375/768/1440 and recorded.
- 09-VERIFICATION.md written with full traceability; real data/app.db untouched throughout.
</success_criteria>

<output>
Create `.planning/phases/09-polish-deploy/09-02-SUMMARY.md` when done
</output>
