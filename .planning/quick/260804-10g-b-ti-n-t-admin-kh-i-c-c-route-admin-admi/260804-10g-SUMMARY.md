---
phase: quick-260804-10g
plan: 1
type: execute
status: complete
---

# Quick 260804-10g: Remove /admin prefix + nav/logout wiring — Summary

## What was done

**Pre-existing work (not done by this run, verified only):**
- `app/admin.py`: `admin_bp = Blueprint('admin', __name__, url_prefix='')` — routes at `/products`, `/orders`, `/stats` (no `/admin` prefix). Verified via route registration check.
- `app/auth.py`: login redirects to `url_for('public.home')`. Old `/admin` dashboard route + `dashboard.html` deleted.
- All templates reference admin pages via `url_for('admin.*')` — no link edits needed for the prefix change.

**Task 1 (committed `3fa4b7a`):**
- Created `app/templates/_admin_nav.html` — `<nav class="site-nav">` with Trang chủ / Sản phẩm / Đơn hàng / Thống kê links + a POST logout form using the exact `csrf_token()` hidden-input pattern from `admin/orders/detail.html`.
- Created `app/templates/admin/base.html` — extends `base.html`, overrides `{% block header %}` with a `.site-header` containing the brand link (`url_for('public.home')`) + `{% include "_admin_nav.html" %}`.
- Added `.site-nav` + `.site-nav a` + `.logout-form button` CSS after the existing Phase 3 `.site-header` block in `app/static/css/style.css` (reusing `#1F2937`/`#2563EB` tokens). Did NOT touch `login` route, login template, or any `url_for` endpoint names.

**Task 2 (committed `eeefb1a`):**
- Changed `{% extends "base.html" %}` → `{% extends "admin/base.html" %}` in all 6 admin templates: `admin/orders/list.html`, `admin/orders/detail.html`, `admin/products/list.html`, `admin/products/form.html`, `admin/products/delete.html`, `admin/stats.html`. Inner `{% block content %}` bodies and existing `url_for`/`csrf_token` markup untouched.
- Added `{% include "_admin_nav.html" %}` to `app/templates/public/_nav.html` after the cart `<a>` — home page header now shows section links + logout while keeping brand, search form, and cart link.

**Task 3 (browser checkpoint — skipped per plan):** Not run interactively. Manual browser verification pending — see "Manual Verify (Pending)" below.

## Verification

- Route registration: `SECRET_KEY=x python -c "..."` → `routes-ok` (`/products`, `/orders`, `/stats` in url_map; no `/admin*` rules).
- Template render smoke test: `_admin_nav.html` and `admin/base.html` render without Jinja errors.
- Task 1 automated verify: all assertions passed (non-empty files, `.site-nav` in CSS, `auth.logout` + `csrf_token()` in nav partial).
- Task 2 automated verify: 6/6 admin templates extend `admin/base.html`; `_admin_nav.html` present in `public/_nav.html`.

## Manual Verify (Pending)

Per plan constraint, the browser checkpoint was not executed. Pending manual UAT:
1. Log in as admin → visit `/products`, `/orders`, `/stats` → each renders the header with Trang chủ / Sản phẩm / Đơn hàng / Thống kê and a Đăng xuất button.
2. On each admin page, click Trang chủ → lands on `/`.
3. At `/`, header shows the three section links + Đăng xuất.
4. In `/orders/1`, click Đăng xuất → session ends, redirects to login page (login page must show NO admin nav header).

## Commits

| Task | Commit | Files |
|------|--------|-------|
| Task 1 | `3fa4b7a` | `app/templates/_admin_nav.html`, `app/templates/admin/base.html`, `app/static/css/style.css` |
| Task 2 | `eeefb1a` | `app/templates/admin/orders/list.html`, `app/templates/admin/orders/detail.html`, `app/templates/admin/products/list.html`, `app/templates/admin/products/form.html`, `app/templates/admin/products/delete.html`, `app/templates/admin/stats.html`, `app/templates/public/_nav.html` |

## Deviations

None. Route-prefix work in `admin.py` was already completed in the working tree (per plan context); this run executed only Tasks 1 and 2, with Task 3 deferred as instructed.

## Notes

- Logout is POST-only with global `CSRFProtect`; the `csrf_token()` hidden input in `_admin_nav.html` is the proven pattern from `admin/orders/detail.html` and renders correctly.
- `auth/login.html` extends `base.html` directly (not `admin/base.html`), so the login page renders no admin nav header — confirmed by template inspection.

## Self-Check: PASSED

All artifacts exist, both commits present, 6/6 admin templates extend `admin/base.html`, nav partial referenced in `public/_nav.html`, `.site-nav` + `.logout-form` in CSS, `csrf_token()` + `auth.logout` in nav partial, route registration check passed (`routes-ok`), template render smoke test passed.
