---
phase: quick-260804-301
plan: 1
type: execute
status: complete
---

# Quick 260804-301: Cho phép Ctrl+V paste ảnh vào form Thêm/Sửa sản phẩm

## Done
- Extracted inline gallery JS from `admin/products/form.html` into `app/static/js/form-gallery.js` (reorder/delete/upload flow preserved unchanged).
- Added paste handler: clipboard image/* blobs are appended to the `gallery-file` input and dispatched as `change` → they enter the same `renderNew()`/`syncFileInput()`/`syncOrder()` flow as file-picker selections, so they upload with the form via the existing server `images` endpoint.
- Server-side `image_utils.validate_image_upload()` magic-byte gate still validates every pasted blob (safe).
- form.html loads the module via `<script src>`; inline `<script>` removed.
- CSS paste cue (`.gallery-grid.paste-received` flash).

## Verification
- `routes-ok` (no /admin prefix).
- `/products` → 302 `/login?next=/products`.
- form.html render smoke test (with ProductForm + session context): script src present, inline script removed.

## Manual (pending)
Open `/products/new`, focus the form, Ctrl+V an image from clipboard (e.g. PrintScreen of a photo) → image appears in the gallery grid, then submit → image uploads via existing endpoint.

## Commit
`ee5bed2` — feat: paste images (Ctrl+V) into product form gallery

## Self-Check: PASSED
