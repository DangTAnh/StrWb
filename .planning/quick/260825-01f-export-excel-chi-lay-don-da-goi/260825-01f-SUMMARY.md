---
phase: quick-260825-01f
plan: 01
subsystem: admin-export
tags: [feature-change, xlsx, export, viettel-post]
requires: [EXPORT-01 route export_orders_xlsx, EXPORT-02 build_orders_xlsx]
provides: [export xlsx chi chua don trang thai "Da goi", nut export khong con truyen ?status=]
affects: [app/admin.py::export_orders_xlsx (bo doc param ?status=), app/templates/admin/orders/list.html (href export)]
key-files:
  created: []
  modified: [app/admin.py, app/templates/admin/orders/list.html]
decisions:
  - "Hard-code Order.status == 'Đã gói' thay vi list allowed — bo hoan toan nhánh ?status= theo yeu cau user"
  - "Nut export mat boi canh loc hien tai (current_status) — file gui hang luon la 'don chua tao tren he thong VC'"
metrics:
  duration: "~5 phut"
  completed: 2026-08-25
---

# Quick Task 260825-01f: Export Excel chỉ lấy đơn "Đã gói" — Summary

**One-liner:** Route `/orders/export.xlsx` hard-code lọc `Order.status == 'Đã gói'` (bỏ `?status=`), nút "Xuất Excel" trên danh sách đơn không còn gắn query param — file gửi hàng chỉ chứa đơn chưa tạo trên hệ thống vận chuyển, hết trùng đơn khi upload Viettel Post.

## What Was Done

### Task 1 — Route hard-code 'Đã gói' (`app/admin.py`)

- Xóa block đọc `request.args.get('status')` + tuple `allowed = ('Đã gói', 'Đã gửi', 'Đã nhận')` và filter `Order.status.in_(statuses)`.
- Thay bằng filter trực tiếp `Order.status == 'Đã gói'`, kèm comment `EXPORT-02` giải thích lý do (chưa tạo đơn trên hệ thống vận chuyển → tránh trùng lặp khi upload lên Viettel Post).
- Docstring route cập nhật thành `EXPORT-01/02 ... Chỉ đơn trạng thái "Đã gói"`.

### Task 2 — Bỏ query param ở nút export (`app/templates/admin/orders/list.html`, dòng 41)

- `{{ url_for('admin.export_orders_xlsx') }}?status={{ current_status or '' }}` → `{{ url_for('admin.export_orders_xlsx') }}`.

## Verification

test_client session admin #1 (`load_dotenv()` trước `create_app()`, `PYTHONIOENCODING=utf-8`, python hệ thống):

```
1) export status: 200
1) zip OK, entries: 6
2) count Đã gói=0, tổng 3 trạng thái=0, rows trong sheet=1, data_rows=0
3) /orders status: 200
3) href OK: /orders/export.xlsx không kèm ?status=
ALL CHECKS PASSED
```

- (1) GET `/orders/export.xlsx` → 200, unzip được (`xl/worksheets/sheet1.xml` có mặt).
- (2) DB dev đang có **0** đơn ở cả 3 trạng thái → theo nhánh dự phòng của plan: sheet chỉ còn đúng 1 `<row>` (header), `data_rows (0) == count (0)`.
- (3) GET `/orders` → 200, HTML chứa `href="/orders/export.xlsx"` không kèm `?status=`.

Commit chứa đúng 2 file scope (`2 files changed, 5 insertions(+), 12 deletions(-)`); `app/image_utils.py` đang modified không bị stage.

## Deviations from Plan

None — plan executed exactly as written. (Script verify đặt tạm trong thư mục quick task, đã xóa sau khi chạy; không commit.)

## Self-Check: PASSED

- FOUND: commit `dadce81 feat(export): xuat excel chi lay don Da goi` (2 files: app/admin.py, app/templates/admin/orders/list.html)
- FOUND: verify script chạy pass toàn bộ assert (đã dọn dẹp)
